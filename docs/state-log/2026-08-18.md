# STATE.md session log — 2026-08-18

_Moved verbatim from STATE.md on 2026-08-18. Entries newest-first. Later sessions append this date's entries here when STATE.md rotates them out._

- 2026-08-18 — **BATCH 9 (JP, live-site report) built — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., **T160**; live pass owed as **T161**; full mechanism in `gantt-frame-notes.md` under *Batch 9 — the drag ghost is the coloured bars*). **ONE frontend file (`frontend/styles/35-gantt.css`, two rules) and ONE test file.** No template change, no JavaScript change, no server, no wire, no schema, no migration; `lib/**` and `src/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency, no token invented. **JP, dragging on the deployed build:** *"the ghost that follows the pointer shows a background with the week grid lines and a drop shadow. I want only the coloured bars."* **Cause, measured on the live page**: `.gantt .grun` — the drag source since batch 8 — was `top: 0; bottom: 0`, i.e. the **full row** (99px measured) by the run's width (184px measured), and transparent. The browser's default drag image is **a snapshot of the source element's box**, and Blink paints that snapshot from the nearest stacking context clipped to the source's bounds — its own source comment says it "will also paint the contents behind the object if the object contains transparency" — so the `.gweek` gridlines crossing that 99×184 rectangle came with it and the shadow wrapped the tall silhouette. Nothing was wrong with the bars; the wrong thing was the **box**. **The fix**: `.gantt .grun` becomes the **bar strip** — `position: absolute; top: 50%; transform: translateY(-50%); height: var(--gbar-h)`, the same 26px band the segments already occupied — and `.gantt .gtrack .gseg` fills it with `top: 0; bottom: 0` instead of centring itself against the row. **The centring did not disappear, it moved up one level**: the identical triple came off the segment and went onto the strip, and its `50%` resolves against the **same containing block it always did** (`.gbar`, `inset: 0` of `.gtrack`, untouched), so the strip's top edge lands on the pixel the segment's top edge landed on and the segment then fills it. Same place on screen, in **exact reals** — no rounding on this axis at all, unlike the horizontal one. **Horizontal geometry is byte-for-byte untouched**: `phaseRun`'s arithmetic, the percent `left`/`width`, the 24px minimum grab clamp; `01-app.js` was not edited. **The one rule above all held**: `pointer-events: auto` stays on `.grun` and on `.grun .gseg` in every state — Chrome cancels a drag whose source cannot be hit, proven with real mouse input in batch 7 — and **no `pointer-events` declaration anywhere in the app changed**; the inventory is still 8. **The corollary is the ruling, not a regression**: the vertical grab area shrinks from the whole 99px row to the 26px band. That is exactly what JP ruled — only the coloured bars are the handle — and it is T157's cursor ruling finally applied to the hit area too, so affordance and target agree on both axes. Nothing was added to widen it back. Horizontally the ~6px invisible minimum-grab extension survives, so a one-day phase is still catchable. `transform` makes `.grun` a stacking context (paint order unchanged — `.gdl` and `.gghost` are later siblings of `.gbar` at `z-index: auto` and still paint on top) and a containing block it already was. `--gbar-h` now has exactly **one owner inside the track**; `.gseg` deliberately does not restate it, because a second copy would over-constrain the box and could then be re-tuned alone. **Checked before editing, as the brief required**: `.gantt .gtrack .gseg` (changed) · `.glegend .gseg` (the 20×10 legend swatch — `.glegend` is a sibling of `.gwrap` and sits **outside** every `.gtrack`, so the changed rule cannot select it) · the four phase colour classes (colour only, no box, shared by both) · `.gantt .grun .gseg` + `:active` (T157 cursor) · `.gantt .growr.pinned .grun .gseg` + `:active` (the pinned refusal). Only the first was touched; `test/gantt-legend.test.ts` is untouched and green, which is the check. **Verification (T160)**: `test/gantt-run-geometry.test.ts` grows **SUITE 6 — the vertical position invariant**, built the way SUITE 1 was built for the horizontal one — a **frozen** transcription of the `79c1ad3` declarations as the oracle, the **shipped** declarations sliced out of the stylesheet on the other side, and a small **total** CSS resolver between them that throws rather than defaulting on anything it does not model (it encodes the one subtlety the batch turns on: a `translateY` percentage resolves against the element's **own** height). Both sides are exact reals, so the assertion is **equality, not a tolerance**, across four row heights (84 frame nominal, 99 measured live, 120, and the degenerate 26). SUITE 5's two `.grun` shape tests were **repointed, not weakened**: the box must now name `top: 50%` / `translateY(-50%)` / `height: var(--gbar-h)` and must **not** name `top: 0` or any `bottom` — restoring either restores the ghost silently with the bars still perfectly correct, which is precisely the failure a test has to catch — and the `.gseg` rule must restate neither a height nor a translate. **Mutation-probed on the real sheet**: putting `top: 0; bottom: 0` back on `.grun` fails **7 named tests**; a `not vacuous` case pins the two shapes that could have shipped instead (a top-aligned strip, 36.5px wrong on a 99px row; segments left double-centring inside the strip). **Gates**: `npx tsc --noEmit` clean · `npx eslint .` clean · `npx vitest run` **58 files / 794 tests** green **first run**, and the same under `TZ=UTC` (783 → 794, the 11 new tests; no T149 flake seen this pass) · `node frontend/build.js` 335,370 bytes. **One defect found at build, by an unrelated guard**: a new CSS comment naming the four phase colour classes turned `test/gantt-legend.test.ts` red on *"declares each phase colour exactly once in the whole stylesheet"* — that guard counts occurrences in the **raw stylesheet text, comments included**. The guard was right and the comment was reworded, not the guard. Second time in two batches that a comment collided with a drift guard reading it as code (batch 8's was `0.083` in `01-app.js`); worth stating as a standing constraint. **What is ours and what is Chrome's, honestly**: CSS controls the **size and content of the snapshot** — now a 26px band containing the coloured segments instead of a 99px box containing the gridlines behind them. CSS does **not** control the translucency the drag image is composited at, the drop shadow macOS/Chrome draws around it, the pointer's grab offset within it, or the fact that the source stays painted in place. The only lever over any of that is `setDragImage()` in `dragRow` — a JavaScript change the brief put out of scope, and the right next step **only if** the live check still shows something unwanted. **Residual risk [likely]**: the snapshot still paints what is behind a transparent source *within* the source's bounds, and the gridlines run the full row height, so they still cross the band — the opaque segments cover them across the run's extent, leaving two possible slivers, the `--radius-xs` corners and the up-to-6px invisible grab extension. Expect bars; do not be shocked by a hairline at a corner. **Still owed, and stated in the test file**: nothing here proves a drag works and **nothing here can see a repaint** — no jsdom, no browser runner, **no synthetic `DragEvent` was added and none may be**. **T161** is the real-mouse pass: the ghost is the coloured bars only; a before/after screenshot of the resting planner identical to batch 8's to the pixel; the grab still starts anywhere on the band; the 26px vertical target comfortable in practice (JP's call); and whatever Chrome still adds of its own recorded rather than fought

- 2026-08-18 — **BATCH 8 (JP's structural ruling of 2026-08-18) built via workflow — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., **T158–T159**; full mechanism in `gantt-frame-notes.md` under *Batch 8 — the handle becomes the coloured run*). **Numbering note:** the entry below labels **T157** "BATCH 8" as well — the frame notes are authoritative and read T153–T157 = batch 7 + 7b, T158–T159 = batch 8. Three frontend files, three test files + one new test file. No server, no wire, no schema, no migration, `lib/**` and `src/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency, no token invented. **The drag SOURCE moves off the track-wide wrapper onto the coloured run's own box.** JP read the live DOM himself: *"gbar spans across the whole of the timeline so dragging it makes the whole thing draggable… those colored bars should be wrapped into a draggable container instead. Gbar stays."* Batch 7 made the drag work by making the source hit-testable; T157 moved the *cursor* onto the segments; this moves the source. **Structure**: `.gtrack > .gbar > .grun > .gseg × N`, `.gdl` and `.gghost` still direct children of `.gtrack` and untouched. `.grun` (new; grep-clean at `1c571f1`) carries all five directives (`draggable`, `on-dragstart`, `on-dragend`, `on-dragover`, `on-drop`) plus its own inline `left`/`width`; `.gbar` keeps its element and class and carries none of them; neither box carries a `title`. **`.gantt .gbar` goes back to `pointer-events: none` ON PURPOSE, and it is not the 13g/13j bug returning** — the bug was a *drag source* Chrome could not hit, and `.gbar` is now the source's parent, not a source. At 1104px it swallows every drop landing outside the coloured run and the `.gweek` columns never see them; transparent, it hands those columns their drops back while `.grun` keeps its own, both ending in the **same** `moveRows`. Legal only because `pointer-events` **inherits and a child's explicit value overrides an inherited one**, so `.gantt .grun { pointer-events: auto }` is written out rather than omitted — **the two rules ship together or not at all**, and the guard now asserts exactly that, plus a test that strips the `auto` out of the *real* rule set and proves the sweep fires. **The geometry had to move, in one place.** `phaseBars` emitted each segment as a percentage of the 60-workday window; inside a narrower box every one re-bases and the bars move. `phaseBars` is **DELETED**, replaced by a column-0 `const phaseRun = (row) => …` returning the box **and** its re-based segments in one shape — no second helper, and the template does no arithmetic (no `*`, `+`, `calc(`, no `{{#if}}` on geometry; every value arrives as a 2dp string). `L = max(0, min(R0, TOTAL_UNITS − W))` is the whole direction rule: grow the invisible part right, slide left only if it would pass the end of the track, never go negative — one expression, no branch. The invariant `left + left'ᵢ·width/100 = 100·sLᵢ/T` holds because **W cancels**, which is why the minimum-width extension is free of visual consequence. `unitPct` redefined as `pctOf(u, TOTAL_UNITS)`, output byte-identical, so the file holds one rounding rule. **No forecast math changed** — same server dates, same order, same drop rule; only the denominator moved. **The 24px minimum is arithmetic, never CSS**: `24 / (92/5) = 1.3043478…` units = 2.17% of track, exact because `--gw` is declared once and `.gtrack` is content-sized at 12 columns. A CSS `min-width` would widen the *rendered* box after the arithmetic and visibly stretch every short bar, because the `.gseg` children resolve their percentages against it — the trap, and it is banned by name in the tests along with `padding`, `border`, `min-inline-size` and `box-sizing`. `.grun` is `top: 0; bottom: 0`, so `.gantt .gtrack .gseg`'s `top: 50%` centring resolves against a containing block of exactly today's height and that rule stays **byte-identical**; the grab target is the full 84px row instead of 26px, free because the box paints nothing. A row whose phases the window clips away renders `<div class="gbar"></div>` — `phaseRun` returns `[]`, the `ghostBar` idiom — so no box, no handle, no `draggable`. **NO VISUAL CHANGE, MEASURED not asserted** (integrate): the same fixture row rendered through the OLD template + `phaseBars` at `1c571f1` and the NEW template + `phaseRun`, real Ractive, absolute positions read back out of both HTML strings, every fixture pinned **and** unpinned — worst composed drift **0.0075 pp = 0.083px** on a 1104px track (F2, three touching segments); F6 the final column 0.0063 pp = 0.069px with its right edge at exactly 100.00%; a sweep of **every** one-day run at units 0…59 rendered both ways at 0.0063 pp with **0** boxes off the track and **0** under 2.17%; segment count, `cls` and `title` identical every time. **The build brief predicted F6 as the worst and was wrong** — three roundings feed the composed error and the omitted one is the largest, the `1c571f1` value being compared against was rounded too and does not cancel; the 0.02 pp tolerance is therefore the analytic bound, not a round number, and both the helper's comment and the test's were corrected at integrate from "≈0.006 pp, F6" to the measured numbers. **The guard moved with the source and did not shrink**: source count still **3** (`['entry','growr','grun']` — it moved, it did not multiply), and the ancestor sweep **grew a rule it did not have**, per source, that an inherited `none` is cured by the source's own explicit `auto` (`.gbrows { none } + .grun { auto }` is still caught for `.growr` — an exemption, not a hole); the SOURCE sweep and the `.gdragging` sweep take no exemption at all. Legitimate transparencies 4 → **5**. **Unchanged and green, which is itself the check**: every `dropOnBar` body assertion, both `weekAtX` suites, `draggingSubjects === ['gdl']`, and `test/gantt-legend.test.ts` + `test/planner-weeks.test.ts` untouched — the colour map did not fork and `TOTAL_UNITS`/`dayIndex` survived verbatim. **Three contract deviations at build, all forced, none weakening** (sorted names are `['entry','growr','grun']` because `'o' < 'u'`; `draggable` adjacency is not assertable because Ractive 1.4.4 emits `style` first whatever the template order — verified across three variants — so three render assertions read the extracted open tag instead; the ancestor sweep is a de-duplicated union, which the contract's own fixture table requires and which the pre-batch guard already did). **Gates**: `node frontend/build.js` 333,049 bytes · `npx tsc --noEmit` clean · `npx eslint .` clean · `npx vitest run` **58 files / 783 tests** green, and the same under `TZ=UTC` (baseline 57 / 735; 780 at integrate, +3 from the fix pass). **Fix pass (post-integrate review), test-only — no frontend file changed, so the build bytes are unchanged**: the `.gweek` cells got their own derived sweep (`WEEK_CELLS` + `weekCellOffenders()`), because T158's `auto` exemption correctly drops `.grun`'s chain — and `.gtrack` is an ancestor of `.grun` and of nothing else that drags, so the chain carrying the week cells stopped being swept exactly when T158 made those cells the only drop path outside the coloured run; mutation-probed on the real sheet, `.gantt .gtrack { pointer-events: none }` now fails one named test and nothing else. And `declaresAuto()` no longer takes a `:hover`- or `@media`-scoped `auto` as a cure — a ban may bite sometimes, a cure may not. **One real defect found at integrate, by an unrelated guard**: correcting the helper's comment in `01-app.js` to the measured drift turned `test/suggest-counts.test.ts` red — its drift guard strips lines beginning `/*`, `*` or `//` and then requires the `HARD_IDEAL` share to appear on exactly one line of the client, and this file's block comments are indented continuation lines with no leading `*`, so a prose pixel measurement survived the strip and read as a second copy of the constant. **The guard was right and the comment was changed**, not the guard; the pixel figure is spelled in words now, with a note saying why a bare decimal must not go back. **Flake honesty (T149)**: the first run went red on ONE test in `test/dayplan-api.test.ts` (`/__test/login` → 404), a file this batch does not touch — the same parallel-startup contention signature; rerun clean, nothing retried, capped or adjusted. **Still owed, and stated in two test-file headers**: none of this proves a drag works — no jsdom, no browser runner, and a synthetic `DragEvent` never enters Chrome's drag machinery, which is exactly why every automated check from 13g/13j to batch 6 passed while the live bar was un-draggable. **No synthetic DragEvents were added and none may be.** T155 is amended: (a)–(g) must be re-run against this build, and five checks join them — (i) the drag starts only from the coloured run, (j) a drop on empty track outside the run still lands through the `.gweek` cell, (k) a one-day phase is catchable at the 24px minimum including one in the final column, (l) a pinned row still refuses the grab on its colour and still accepts someone else's drop, (m) a before/after screenshot identical apart from the cursor

- 2026-08-18 — **BATCH 8 (JP's ruling of 2026-08-18) built via workflow — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., **T157**; closes **T155(h)**; ruling recorded in `gantt-frame-notes.md` under *The affordance is the coloured bars only*). Two frontend files, two test files. No server, no wire, no schema, no migration, `lib/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency. **The pointer affordance moves onto the coloured bars; the drag mechanics do not move at all.** Batch 7 correctly made `.gbar` span the whole 1104px track and stay `pointer-events: auto` — and `cursor: grab` plus `title="Drag along the timeline to reslot"` spread with it, so blank track to the right of a two-week run advertised a handle that is not drawn there. T153's consequence 3 argued that was honest because the wrapper *is* the drag source; it is honest about the mechanism and wrong about the user, and JP overruled it. **`pointer-events` was not touched anywhere** — `.gbar` and `.gseg` stay `auto`, the five directives on the open tag, `weekAtX`, `dropOnBar`, `moveRows`, geometry, colours, z-order and `.gantt.gdragging .gdl` are byte-identical, and `test/drag-hittest.test.ts` still bans a transparent drag source in any state. **Cursor**: `.gantt .gbar` `grab` → `default` (`default`, not `auto`, because `.growr` declares no cursor at all — the track now reads exactly like the rest of the row); `.gantt .gbar .gseg` `inherit` → `grab` (`inherit` is *how* it picked the wrapper's `grab` up; naming its own value stops the two silently re-merging); `:active` `grabbing` unchanged; and the pinned rule's subject moves off the wrapper — `.gantt .growr.pinned .gbar,` becomes `.gantt .growr.pinned .gbar .gseg,`, keeping the `:active` clause — only the RESTING clause is load-bearing (at (0,5,0) it outranks both `grab` and `grabbing`), the second is defensive. Segment-scoped for the same reason `grab` was: a refusal is an affordance too, and a 1104px `not-allowed` would withhold a track that never offered anything. **Title**: deleted from `.gbar` in BOTH branches — no new tooltip mechanism invented, and nothing added to the segment titles, which name the phase and its end date and would go noisy restating the mechanic on every run. The reslot sentence survives as **standing text** in the `.fnnote` hint above the Gantt (*"Drag a bar along its row to reslot it — an unscheduled row drags onto a week. Pinned rows never move."*): already there, not hover-gated, and strictly the better home, since a tooltip that appears only once the pointer is on the bar teaches nobody how to find the bar. The pinned sentence survives twice — `.growr` carries `title="Pinned — unpin to move"` and `title` is **inherited by a title-less descendant**, so the row's attribute covers the `.gtrack` and `.gbar` inside it (deleting the bar's copy removed a duplicate, not the message), and each `.gseg` still appends `· Pinned — unpin to move` to its phase title. **What a pinned row hovers as, verified against the render**: empty track = `default` cursor + "Pinned — unpin to move"; a coloured segment = `not-allowed` at rest **and** while pressed + "Sketch · Pinned — unpin to move"; `draggable="false"` unchanged, and the bar keeps its drop directives so it stays a valid landing strip for someone else's drag. An unpinned scheduled row, for contrast: empty track `default` and **silent**, segment `grab`/`grabbing` with its phase title only. **Tests moved, not weakened**: the old `expect(r).toContain('title="Drag along the timeline to reslot"')` is not softened — it is wrong now — and four assertions took its place (the wrapper emits no `title=` and the string is absent from the row; `.gbar` is `cursor: default` and **not** `grab` while `.gseg` is `grab`/`grabbing`; the instruction is present as standing template text; the segment titles still carry a title with no `reslot` in it), plus a segment-title assertion and a **negative subject guard** on the pinned block — no rule may name `.gantt .growr.pinned .gbar` as a subject again. Five of the six new CSS assertions flip against the pre-ruling stylesheet; the sixth (`.gseg:active` = `grabbing`) is byte-identical before and after and is labelled in the file as a preservation guard, not a regression proof. Gates: `tsc --noEmit` ✅ · `eslint .` ✅ · `vitest run` **735/735** (57 files) ✅ · `TZ=UTC vitest run` **735/735** ✅ (both first-run green — no T149 flake seen this pass) · `node frontend/build.js` ✅ (325,694 bytes). Suite **731 → 735**, the four net new tests. **Still owed**: T155 (a)–(g), the live-browser pass with a real mouse — this change cannot break the drag because it never touches hit-testing, but the batch-7 drag itself is still unproven outside a browser

- 2026-08-18 — **BATCH 7 (JP, live-site report) built via workflow — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., T153–T155; full ruling R-g-1 in `gantt-frame-notes.md` under *Batch 7*). Three frontend files, two test files, one test helper. No server, no wire, no schema, no migration, `lib/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency. **The Gantt phase bar could not be dragged with a real mouse — and had not been draggable since batch 4.** The app's handlers were never the problem; they were never reached. `.gbar` carries `draggable="true"` while its `.gseg` children carry the pixels, but **Chrome begins a drag from the draggable ancestor and hit-tests *that*** — so `.gantt .gbar { pointer-events: none }`, shipped since 13g/13j, made the drag get created and cancelled in the same tick. Root-caused with **real mouse input (CDP) before any code was written**: the shipped build produced exactly `mousedown:.gseg → mousemove → dragstart:.gbar → dragend:.gbar` with `dropEffect: "none"` — no `drag`, no `dragenter`, no `dragover`, no `drop` — while at `dragstart` everything looked correct (`text/plain` set, `effectAllowed: "move"`, `.gantt.gdragging` applied, source connected, nothing calling `preventDefault`). Three controls closed it: the unscheduled ROW drags fine with the same tool (`.growr` is `pointer-events: auto`), so tooling and app are both sound; forcing `auto` onto `.gbar` makes the sequence live; and forcing `.gantt.gdragging .gbar { pointer-events: none }` brings the instant-cancel back, proving **"hit-testable at mousedown only" is not enough** — which also rules out moving `draggable` onto `.gseg`, since the shipped `.gantt.gdragging .gbar .gseg` rule would then blank the SOURCE mid-drag. **The fix inverts batch 4's reasoning: the bar owns its own drop.** `pointer-events: auto` on `.gbar`, written out explicitly rather than merely omitted (the property **inherits**, so the stated value defends the source against a future ancestor rule); `.gseg` dropped from the `.gdragging` rule; and `on-dragover="['dragOver']" on-drop="['dropOnBar']"` added on a **third** attribute line so the two lines above it stay byte-identical for the tests that pin them. **`ganttDragging` survives** — the brief allowed deleting it, recon proved otherwise: `.gdl` is a later sibling of `.gbar` in the same positioned `.gtrack`, both absolute at `z-index: auto`, so the deadline tick paints **over** the bar and wins hit-testing at its 2px column; left solid it would refuse the drop at exactly the deadline, and it cannot be permanently transparent because at rest it owns a real `title`. With the bar solid, the `.gweek` cells no longer see the drag on a scheduled row, so a new **named pure** `weekAtX(clientX, rect, weeks)` does the mapping they used to do by being hit — no `document`/`window`/`app.get`/`WEEK_PX`/`WEEK_COUNT`, dividing the **measured** width by the count rather than the hard-coded 92px, half-open, clamped both ends — and `dropOnBar` runs the **same `moveRows`** `dropOnWeek` runs: still exactly the two pre-existing `POST …/replot` call sites, one audit path, BR-8 group resolution byte-unchanged. The card id comes off the dataTransfer, never off the bar's own row, so an unscheduled row dropped on a scheduled row's bar moves the right card; the handler measures `ctx.node.closest('.gtrack')`, never `event.target`, because the event now fires on a 26px segment and bubbles. `.gweek`, `.gblockhead` unslot, the unscheduled row-drag, `rowKey`'s keyboard reslot, the phase→colour map, segment geometry and the legend are all untouched. A **pinned** row keeps `draggable="false"` and its refusal title but deliberately **keeps** the drop handlers — the pin freezes the pinned ROW, not the column, and suppressing them would carve a dead 1104px strip that silently refuses every drop. **Why every automated check missed this for three batches**: there is no jsdom and no browser runner in this repo, so every planner test is `toHTML()` or a source regex, and a synthetic `DragEvent` calls the app's handlers **directly** without ever entering Chrome's drag machinery. The standing answer is `test/drag-hittest.test.ts` (**44** tests): drag sources enumerated **from the shipped template** (count pinned at 3, so a fourth joins automatically), every `pointer-events: none` rule across **all seven stylesheets** read off disk and matched on its **rightmost compound**, plus an ancestor sweep (the property inherits), a class-less-subject check (`*` would slip a subset test), and parser self-tests. **Proved non-vacuous twice** — by permanent in-file fixtures covering the shipped rule and all three controls, and at integrate by reintroducing the bug in the real tree and watching the guard go red with ``35-gantt.css: `.gantt .gbar` makes the .gbar drag source un-hit-testable`` before restoring the line. Gates: `tsc --noEmit` ✅ · `eslint .` ✅ · `vitest run` **726/726** (57 files) ✅ · `TZ=UTC vitest run` **726/726** ✅ · `node frontend/build.js` ✅ (322,831 bytes). Suite **682 → 726**, exactly the 44 new tests; no test weakened or deleted without a replacement asserting more. **Flake honesty (T149, unchanged and not this batch's)**: eight full runs at integrate — 2 default (both green) and 6 `TZ=UTC` (3 green, 3 red). Every red was a **single** test in a long-standing server file this batch does not touch (`admin.test.ts`, `acks.test.ts`, one not captured), never an assertion in a file this batch edits; both are 3/3 green in isolation, and the five suites this batch touches or neighbours are **124/124 across five isolated `TZ=UTC` runs**. Nothing was retried, capped or otherwise adjusted to hide it. Ractive `{{! }}` hazard cleared: the rewritten template comment quotes no `}}` before its terminator and `Ractive.parse()` on the built `public/index.html` shows it fully stripped (an HTML comment there would have broken `expect(renderGantt()).not.toContain('gdragging')`). **Not deployed and not committed — and nothing here proves the drag works.** T155 is the live-browser pass that closes T153, and it is non-negotiable: this batch's whole subject is a defect class the suite is structurally blind to. **Review fix pass (T156)** then corrected two things and changed no behaviour: a comment in all four touched files claimed the segments left the `.gdragging` rule because blanking them would blank the drag source — **not true as shipped** (`draggable` is on `.gbar`, which stays `auto`, so a transparent `.gseg` falls through to it), and the honest reason is now written down: nothing needs them transparent once the bar owns the drop, and the ban is the deliberately stricter line that stops `draggable` ever moving down onto `.gseg`; and the guard itself had three ways past it — a case-/whitespace-sensitive matcher (`POINTER-EVENTS : NONE` was invisible), an eleven-entry **hand-kept** ancestor list (the same staleness vector the sources are protected from — the template is now parsed as a balanced tree and each source's ancestors come from its real nesting, which immediately picked up `view`, `pscrollwrap`, `pscroll` and `gscroll`, four wrappers the list had missed), and stylesheets-only reach (template inline styles and all four shipped scripts are swept now, plus a mid-drag sweep for `visibility: hidden`/`display: none` on `.gdragging` rules, since control 3 proved hittable-at-mousedown-only is not enough). Non-vacuity re-proved against the **real pre-fix file** (`git show 1e13088:…/35-gantt.css` dropped into the tree → 3 red, then restored). `drag-hittest` **44 → 49**; gates re-run: `tsc` ✅ · `eslint` ✅ · `vitest run` **731/731** ✅ · `TZ=UTC vitest run` **731/731** ✅ (rerun — one red at first pass was `test/webhook.test.ts` failing at SETUP with `Port "54463" already in use` from `mongodb-memory-server`, untouched by this batch, 9/9 in isolation; a sharper T149 signature than the `socket hang up` face, pointing at mongod port allocation racing across 57 parallel suites) · `node frontend/build.js` ✅ (323,355 bytes). The `cursor: grab` / `title` spread across the full track was left alone on purpose — it is JP's call at T155(h), and restyling here would pre-empt it
