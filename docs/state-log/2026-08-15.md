# STATE.md session log — 2026-08-15

_Moved verbatim from STATE.md on 2026-08-18. Entries newest-first. Later sessions append this date's entries here when STATE.md rotates them out._

- 2026-08-15 — **URL ROUTING FOR MAIN TABS built via workflow + DEPLOYED** (phase 13h, T127–T129; JP-approved scheme: `/sirius/<project-code>/<tab>`, plural `/requests`, Pipeline default, silent fallbacks, admin enforcement stays server-side). Server: whitelist catch-all (GET/HEAD, registered last) serves the single self-contained shell with `window.SIRIUS_BASE` stamped at serve time; `resolvesToShellFile` guard closes **13 encoded/dot-segment spellings** that reached the unstamped static file (verify finding, reproduced then fixed — `/index%2Ehtml`, `//index.html`, `%2e%2f` tricks etc. all 302 now); `/api/*` JSON 404 untouched. **`00-api.js` BASE landmine fixed** (was `location.pathname`-derived — every API call would have 404'd under a deep link). Auth deep-link return: `returnTo` whitelist-validated on write AND read, consumed **before** `req.logIn` (passport regenerates the session on login — the workflow's builder caught the contract specifying the wrong side and proved it; fixation guard kept). Client: pure `parseRoute`/`buildPath` drift-pinned to the server whitelist, initial parse applies after projects load, one observer pushes, popstate restores under a guard, replaceState normalizes; `resetForProjectSwitch` extracted verbatim so back/forward across projects matches the switcher. 5-agent workflow; **375/375 dual-TZ** (+70 from 305). Deployed + live-verified in the real browser: deep-link cold load, tab click URL, project switch keeps tab, back×2/forward incl. project boundary, refresh persistence, bare-URL normalization, encoded spellings 302, console clean. Signed-out deep link → Google → return-to **confirmed working by JP 2026-08-15** (manual pass; 13 supertest cases also cover it). Known accepted risks recorded in tasks 13h notes: admin-tab fallback couples to the tabs array (data still 403s); pre-login `returnTo` session writes bounded by cap+TTL. deploy.sh test-gate flaked once on the known mongodb-memory-server race (green on rerun). Tasks 13g backfilled (T125 Gantt, T126 calendar amendment).

- 2026-08-15 — **CALENDAR AMENDMENT (constitution v4.2.0) + ARES-CANONICAL WORKING-DAY CALENDAR built + DEPLOYED** (JP: "don't count sunday and weekends on forecast" → investigated: weekends were NEVER counted — 4,380-output sweep, 0 weekend landings; the real defects were (1) `buildWeeks` keys = toISOString of local Monday = the SUNDAY before on Manila hosts, and (2) `isHoliday` UTC/local mix — which does NOT drop holidays east of UTC, it excludes the day AFTER each one. JP chose option (a) + "ares data becomes canonical reference"). Changes (commits `ba94db4`/`ea07203`/`fa0041a`): `lib/calendar.ts` amended — `localIso()` for week keys + holiday match, `setHolidays()/getHolidays()` injectable set, seed list unchanged, toFriday quirk preserved; golden tests rebuilt on a TZ-true reference with oracle parity kept where the oracle is correct (UTC) — the forecast 3,600-matrix mirrors the oracle's effective shifted set east of UTC so composition parity still proves the port; **305/305 green under Asia/Manila, UTC, AND America/New_York**. ARES feed: `/api/workload?mode=daily` columns contain ONLY working days (probed live — Aug 25 absent from a Mon–Fri window) → worker `calendarTick` derives non-working weekdays, cross-checks `/api/portfolio/capacity.workingDays[]`, persists `calendar_days` (system-reference, migrations-class — documented invariant-1 exception), server loads at boot + 15-min; adapter methods behind ares.ts per the steering pattern; contract table updated. Migration `005-monday-slotted-week` normalized 8 live rt-test rows with 8 audit rows (invariant 10). **Deployed + live-verified**: worker log "calendar sync: 10 non-working weekdays", ZERO cross-check mismatches; ARES 2026–27 calendar = Jul 27 · Aug 21 · Aug 25 · Nov 2 · Nov 30 · Dec 8 · Dec 24 · Dec 25 · Dec 30 · Dec 31 — **the prototype's static list was materially wrong** (had Aug 31; missed Aug 21, Nov 2, Dec 8, Dec 24, Dec 31); host buildWeeks now yields Monday keys; rt-test footer totals populate (was 11/12 dashes), perWeek all Mondays, bars stretch across the new holidays, console clean; "Outside any sprint" for all 8 rows is CORRECT (prod rt-test defines zero sprints). The 13f Accept guard stays as an inert tripwire. Suggest is now usable on prod.

- 2026-08-15 — **DELIVERABLES TABLE + GANTT PLANNER built via workflow + DEPLOYED** (owl #22; Rex-verified 8 annotations on `95:5795`; commits `9977f07`–`455a230`). REPLACES the legacy schedules week-board as the planner body; 13f toolbar + Accept/Discard untouched. Extraction discovery: the Gantt bars are **phase-segmented** (Phase Bar component set: Sketch #f59e0b · Review #bfdbfe · Render #2563eb · RenderOverdue #dc2626, day-resolution) — server emits `phases[]` per row from the verbatim forecast dates (half-open, buildPhases in pipeline.ts); `perWeek` totals with BR-6c weights + `hardShare` vs `HARD_MIX` (12.9% never retyped); `loadPipeline` now takes `weeklyCapacity` (3rd arg, all call sites). Frontend: pinned 999px column block (sticky) inside one scroller, months/wkN derived from Monday-owned weeks (frame's OCTOBER mislabel + bogus Sep numbering fixed by construction), derived sprint blocks + **Outside any sprint** + Unscheduled (R5 — cross-sprint drag IS a reslot), capacity footer w/ red over-capacity + amber over-hard-mix, whole-row DnD onto week columns via the existing audited `/replot`, violet ghost proposals. 8-agent workflow (recon+contract → 2 Opus builders ∥ → Opus integrate → 3 Fable lenses → Opus fix); 13 findings all fixed; **293/293 dual-TZ ×3** (baseline was 265 not 263; +16 payload +12 client-week-math tests, the latter a NEW technique: extracts the shipped frontend function text); 74/74 seeded isolated-DB E2E probe incl. a real browser drag. Deployed + live-verified: rt-837 218 rows all Unscheduled (correct — nothing slotted in observation mode), footer `capacity 120 · typical 92 · hard ceiling 13%` (pin renders right); rt-test 8 rows w/ 23 phase segments. **⚠ TWO PRE-EXISTING `lib/calendar.ts` DEFECTS SURFACED, JP DECISION NEEDED**: (1) `buildWeeks().key` (:93) yields SUNDAYS on a Manila host — reproduced live: `/suggest` proposes Sunday keys; Accept would persist them (rows fall out of sprint blocks, footer goes blank). Shipped a GUARD (Accept disabled + "Suggestion blocked" strip when non-Monday keys arrive) so nothing corrupts, but **Suggest is unusable on prod until fixed** — options tabled in gantt-frame-notes.md: (a) fix lib/calendar.ts:93 (constitution amendment, golden test), (b) normalize to Mondays in the /suggest route, (c) client snap. (2) `isHoliday` (:36) mixes local-midnight vs UTC ISO — prod (Manila) silently ignores every PH holiday in every forecast date. Also live: **rt-test already has 8 Sunday `slotted_week` rows in prod** (they render honestly under Outside-any-sprint w/ dashed footer; data repair = migration decision). Product flags in the deploy owl: R4 Review-palette stale copy, sprint-header checkbox + collapse/expand not built, hover-revealed pin/duplicate/note cluster beyond the frame, MC-825's 99 identical MC-labels, WEEK_COUNT 8→12 widens the Suggest horizon, 999px pinned pane leaves 5.1/12 weeks visible at 1600px (needs a product call). Same session: owl #14 sent to Miles — capacity-slider governance, per-project constraint, recommendation B (per-project lock flag).
