# STATE.md session log — 2026-08-17

_Moved verbatim from STATE.md on 2026-08-18. Entries newest-first. Later sessions append this date's entries here when STATE.md rotates them out._

- 2026-08-17 — **BATCH 6 (owl #39 as corrected by #40) built via workflow — INTEGRATED, NOT DEPLOYED** (phase 13k cont., T150–T152; ruling and the full mechanism in `gantt-frame-notes.md` under *Batch 6*). One cell, four frontend files, no server / wire / schema / migration and nothing near the write registry. **The Requestor cell was cutting long values mid-character** — and, because the clip belonged to `.gcell` rather than to the badge, ~8px of that text painted **outside the badge's own 1px stroke**. Two defects, one cause: `text-overflow: ellipsis` can only act on the box that owns the overflow. **The clip moved inward.** A reusable `.clipbadge` / `.cliptext` recipe now lives beside `.pbadge` in `20-pipeline.css` (it names no column and no pixel), and the single application is `.gantt .gdetails .c-req { width: 136px; overflow: visible; }` — same specificity as the `.gcell` rule (0,3,0), declared later, so it wins on **source order with no new selector**. This does **not** weaken the fixed-box rule: what holds the pinned block to `--gleft` is `flex: none` + the column width, both unchanged; the cell's clip only ever prevented visual spill, and once the badge clips its own text there is nothing left to spill. The badge's cap is `.pbadge`'s **existing** `max-width: 100%` against the cell's 128px content box, so **no pixel number is repeated anywhere in the fix**. `min-width: 0` on `.cliptext` is load-bearing and silent when missing — the text is a flex item of the inline-flex badge and at `min-width: auto` refuses to shrink, so the ellipsis would simply never render. **Tooltip is one rule, two selectors** (`:hover::after` + `:focus-visible::after`) with `content: attr(aria-label)`, so the visible tooltip and the accessible name are the same string by construction; `position: absolute` keeps it out of the flex flow so it cannot grow the row and desync the pane from the Gantt. **Truncation is a MEASUREMENT, never a character count**: `refreshClips()` beside `refreshThumbs()`, comparing **strictly** (`scrollWidth > clientWidth`), writing `data-clipped` + `tabindex="0"` and **revoking both** every sweep — `{{#each g.rows}}` is unkeyed, so Ractive reuses badge nodes by index and a long→short row would keep a stale tab stop forever if the sweep only added. Both the tooltip and the tab stop hang off `data-clipped`, so *"no tab stop on a badge that fits"* and *"no tooltip when the value fits"* are **one mechanism**. Rides the four **pre-existing** rAF seams (`loadAll` — which is also the project switch — `selectTab`, `toggleBlock`, `toggleLeftPane`) plus one new `document.fonts.ready.then(refreshClips)`, because `index.html` loads Google Sans Flex with `display=swap` and the first paint measures fallback metrics. **No observer added.** Left-pane collapse needs no special case — `.c-req` is `display: none` there, both widths read 0, and a hidden cell correctly loses its tab stop. The badge carries the **existing** `noop` handler on keydown, because `.growr`'s `rowKey` reslots the deliverable a week on ArrowLeft/ArrowRight and keydown bubbles; this is the app's first *informational* focus target, where that side effect would be indefensible. **Column NOT widened** (#40: production requestors are "Andy"/"Chev"; the clipped value is a fixture `@handle`), **no `title`** (fails the keyboard half), **no `tabindex` in the template**. **Known gaps recorded, not built around: WCAG 1.4.13's Escape dismissal, and touch having no reveal path.** Hoverable and persistent are met by the `top: 100%` adjacency; a pure-CSS tooltip cannot be Escape-dismissed without putting JS state behind an informational cell — both carried to Miles/JP as explicit questions in **T152**, not left in a CSS comment. **Adversarial review, three changes on top of the build** (all inside the same cell): (1) the sweep shipped with `updateThumb()`'s `+ 1` epsilon and a browser check found a ~1px band where `text-overflow` draws the ellipsis but `scrollWidth === clientWidth + 1` — a visibly truncated badge with **no tooltip and no tab stop**, the bug itself surviving; now strict, because the costs are asymmetric (false positive = a harmless tooltip on a value that just fits; false negative = an unreachable name) and both metrics are integer-rounded anyway; (2) the badge gained **`role="note"`** — a bare `<span>` is `role=generic`, on which ARIA 1.2 **prohibits** an author name, and the badge joins the tab order once clipped (Chrome honoured the label regardless and the text content always carried the full value, so nothing was lost, but a conforming AT stack may drop it); (3) `test/schedule.test.ts` added to **T149**'s flake list after a setup-phase `POST /__test/login` → `404` in one run. Gates: `tsc` ✅ · `eslint` ✅ · `node frontend/build.js` ✅ · `vitest run` **682/682 (56 files)** ✅ · `TZ=UTC` **682/682** ✅; suite **645 → 682** (+37, one new file `test/gantt-requestor-clip.test.ts`). **Flake honesty:** one `TZ=UTC` run of six went red with 3 supertest *transport* failures in server files this batch does not touch (T149); the other five full runs were 682/682 and the new suite is 37/37 on both timezones in isolation. The measurement itself is **not exercised** — no vitest config, no jsdom, so vitest runs in `node` and nothing has a layout; every assertion about `refreshClips()` reads the shipped source and the suite's header says so. Proved to **bite** by **ten** negative controls, each reverted (the last two: reinstating the `+ 1` epsilon, and dropping `role="note"`). One test-plan assertion relaxed with reason: the plan's `not.toContain('136')` fails on *pre-existing prose* (`01-app.js:17` has documented the column arithmetic since phase 13i and points at the stylesheet rather than duplicating a constant), so the ban is enforced on **code** by a comment-skipping scanner with its own negative control; the standing `\b(999|417)\b` guard is re-asserted untouched. `git diff lib/` **empty**, `CLAUDE.md` / `.specify/` / every `src/**` file untouched, write registry unchanged at **W1 / W2 / W3**. **Owed: the live-browser pass (T152)** — a real ellipsis, a real Tab stop, the tooltip against a real viewport, the collapse cycle, the verdict *after* the webfont swaps, the **1px boundary case**, and Miles/JP's ruling on the two accessibility gaps. **Nothing committed.**

- 2026-08-17 — **BATCH 5b (owl #37 items 1–2) built via workflow — INTEGRATED, NOT DEPLOYED** (phase 13k cont., T146–T148; rulings **R-f-10 / R-f-11** in `gantt-frame-notes.md`, superseding the R7 note). Two behaviour rulings from Miles on the batch-4 sprints modal; item 3 (arrival pulse) she ruled **keep as built**, so the pulse, its 1.2s timing, `scrollIntoView` and the reduced-motion fallback were not touched. **Save now gates on UNSAVED CHANGES, not empty-vs-not** (#37 item 1): `openSprints` captures a `sprintBaseline` beside the draft — a fresh copy of the three fields a save PUTs, mapped off `stored` so an edit can never drag it along — and a `sprintDirty` computed compares the two on `{name, start, end}` in **draft order with no trimming** (a name changed to `"Sprint 1 "` is an edit the user made; whether the route trims on store is a separate question, untouched), a length change counting as dirty. Save is live iff `sprintDirty && no blocking banner`. **`sprintOpenedEmpty` is DELETED outright** — state key, the set at open, the template reference — and a test asserts the identifier appears nowhere in either shipped file, because a leftover key expressing a case the dirty check subsumes is exactly the drift the rule forbids. Both batch-4 behaviours survive as consequences (opened-empty → dead; emptied-by-the-user → live, so the last sprint can still be removed) and the reframe buys the case the flag could not see: **edit a field and put it back → dead again**. `saveSprints` gained a matching dirty lock — a no-op PUT would write a `sprints.replace` audit row for a non-change, the exact defect batch 4 fixed for Calendar Remove (**invariant 10 logs changes, not attempts**). Cancel's `dim` left alone: it tracks the rendered empty branch, not Save's state, and dim-Cancel beside live-Save was already the shipped pairing — verified by render, not by reading. **Blank sprint names now reject on both sides** (#37 item 2): blank is `String(name || '').trim() === ''`, client `sprintBlankNames` following the EXISTING `.sbanner` recipe (`variant:'err'`, carrying its row's draft index so placement stays data per R-f-4, no CTA per R-f-5, title *Sprint name required*), ONE banner per blank **ROW** — unlike duplicates, which are one per NAME — and a route-owned `blankNameIssues()` in the same `SprintSaveIssue` shape and the same `422 SPRINT_CONFLICT` envelope, appended LAST so `issues[0]` is byte-unchanged for every pre-existing case, returning before `deleteMany` so a rejection **writes nothing and audits nothing**. Copy is byte-identical on both sides — `A sprint starting 17 Aug 2026 has no name. Name every sprint to save.` — the route's `longDate` reproducing the client's `fmtLongIso` as pure string math, no `Date` (invariant 11) and no locale (en-GB emits "Sept"); a client-only fallback (`This sprint has no name. …`) covers a cleared start, which `snapSprintStart` really does write as `''`, and is unreachable on the server where `start` is `DATE_ONLY`-required. **Recon found the two sides already disagreed, and the SERVER was wrong**: `duplicateNameIssues` did not skip blanks, so two unnamed rows collided on the key `''` and were reported as `Multiple sprints are named ""` — wrong and unreadable; it now carries the guard the client always had, and a blank reports once, as a blank, never also as a duplicate. **One Zod constraint relaxed**: `name` loses `.min(1)`, keeps `.max(80)` — an empty name is a user mistake with a friendly fix, not a malformed body, and Zod's `400 INVALID_BODY` carries **no `issues[]` at all**, so the modal's `issues[0].text` fallback would have printed a raw developer string at the user; a test pins `''` → `SPRINT_CONFLICT` and never `INVALID_BODY`, another pins an 81-char name still 400. **645/645 dual-TZ (+35 from 610, 55 files)**, `npx tsc --noEmit` and `npx eslint .` clean, `node frontend/build.js` OK — but that number is **not deterministic, and the cause is older than this batch**: a 12-run loop (6 default + 6 `TZ=UTC`) went red 4 times, each time on ONE test in a DIFFERENT pre-existing file (`acks` ×2, `admin`, `basepath`), never on a batch-5b test. The failures are transport-layer, not logic — `socket hang up` / `ECONNRESET`, `Parse Error: Expected HTTP/, RTSP/ or ICE/` (`HPE_INVALID_CONSTANT`, i.e. the client read non-HTTP bytes off the socket), and `200` answered as `404 PROJECT_NOT_FOUND` (a request reaching a different file's app) — the signature of ephemeral supertest listeners colliding across 55 files that each start their own `mongod`. Both TZs fail at the same rate, so it is not calendar math. The same file flaked twice in six runs at **443** tests on 2026-08-17 15:52, three batches before this one. Tracked as **T149**: one red run on those files is not a batch-5b regression — and equally must not be used to wave off a real one. Both new computeds are **executed out of the shipped `frontend/scripts/01-app.js`** and then drive the shipped markup through `renderSprintModal` — opened-empty dead, emptied live, edit-and-revert dead, a blank rendering `sbanner err` + `role="alert"` after its own row with no `<button>` and no duplicate banner, two blanks rendering two, a real duplicate still rendering, a gap still advisory with Save live. Copy parity extended to the new class, counted once per interpolated form on each side so no third copy can drift in. `scripts/batch4-probe.ts` retargeted off the retired flag and grown to **65 checks** on an isolated in-memory mongod (whitespace-only → 422 `blank-name` with the exact banner copy, zero writes zero audits; two blanks → two blank issues and zero duplicate ones; the same list named → 200 and one more audited replace; the duplicate path unchanged). `git diff lib/` **empty**; write registry unchanged at three entries; no Trello or Sheets path touched; every `Sprint`/`AuditLog` query still filters `project_id`. **Owed to Miles**: R-f-10 and R-f-11 as built, the client-only cleared-date fallback (not the parity sentence — only its second clause is shared), and the `saveSprints` dirty lock taken as a judgement call under invariant 10. T145 (batch-5 live-browser pass) **stays open**, and a browser pass over the new Save gating is owed with it.

- 2026-08-17 — **BATCH 5 (owls #34–#36) built via workflow — INTEGRATED, NOT DEPLOYED** (phase 13k, T141–T144; T145 live-browser pass open). **Requests STATUS is now two-valued** (#34/#35): `In Pipeline` (MC# present in Trello) · `For Filing` (absent, renamed from `To File`); the third value `For Clarification` is retired and the flag is NOTE state, surfacing only as the red `With Clarification` pill in Remarks. `SEGMENTS.clarification` re-keys to *unfiled AND `note.clarify`* — set-identical to the old third status — so **owl #14 survives by construction**: `forClarification ⊂ toFile`, and a filed+flagged row is `In Pipeline` and matches neither (R-req-a, default kept, asked of Miles). Every count is unchanged for every input, `counts.toFile` keeps its key, and the Breakdown card keeps reading `TO FILE` — owl #35 ruled that asymmetry deliberate. Client-side, `STATUS_TO_FILE`/`STATUS_CLARIFY` are **deleted outright** (the client never spells `For Filing`; the server sends it and the cell prints it) and ONE shared `clarified()` predicate feeds both the segment filter and the template branch; the optimistic note-save patch that rewrote `requests.N.status`, its `prev` capture and its rollback keypath are gone. **Pipeline: the incomplete-card banner becomes a row state** (#36, nodes 537:69131/537:69135): `.ipanel` deleted from template, client and stylesheet; the three conditions the server already computes per row (`missing`) now speak on the row that owns them — amber-50 fill with a **3px LEFT amber-300 accent** (the frame calls it a "stroke"; the 280px column and 113px row height were deliberately NOT hardcoded against a fluid table — R-warn-a), an underlined amber-600 message **button** under the card name whose label is a VARIABLE string in all three render sites (R-warn-c/d), and a 235px `role="dialog"` popover listing the card's own MC#/name first then one item per missing field with the rationale that says **why it matters** (new Figma-attachment copy = R-warn-b, proposed to Miles). No wire change. The popover **reuses `openOverlay`/`placeBox`** and **joins** the `.selectmenu, .duepop, .warnpop` recipe (slate-100 stroke its only fork); its height is data-derived (~202px for one problem, ~346px for three), so it is **placed a second time against the measured box** through the Requests select's re-place path, generalised to `placeMeasured()` — without it a three-problem card low in a 900px viewport ran ~70–80px off the bottom with `Open Card` unreachable (R-warn-h). **Focus-return did not exist on any of the four pre-existing overlays and was added ONCE in the shared close path** (R-warn-f) — on Escape, and on any dismissal that unmounts the element currently holding focus (a scroll while tabbed onto `Open Card`). The separator draws only when `Open Card` does, and the dialog's accessible name is the same composed label + MC# + name its trigger carries. `corrections` stays on the wire — OPEN WORK (`kpi.open`) is now the aggregate signal, which is why a searched-away warned row needs no special case. Integrate added a blocked+warned precedence rule so a 🛑 row keeps its red fill (R-warn-g, flagged). **610/610 dual-TZ (+46)**, typecheck + lint clean, `node frontend/build.js` OK; 25 pipeline-warning + 15 requests-render tests render the SHIPPED template through Ractive `toHTML()` with the SHIPPED recipes executed out of `01-app.js`, **both suites verified to bite by negative control**; 49-check `scripts/batch5-probe.ts` on an isolated in-memory mongod (four filed×flagged combinations, all three `?filter=` segments, counts + both invariants, the client predicate reproducing the server segment row-for-row, project isolation, a read that writes no audit row, and the warning map checked against the tokens a real incomplete card emits). Carry-over #32 verified by reading the code: **drag during a pending Suggest proposal is not broken** — the proposal survives the move and `ghostBar` keys per card, so no fix. Owed to Miles: R-req-a, R-warn-a, R-warn-b, R-warn-g, the `452:24801` "read-only Remarks" contradiction (the note editor's only entry point *is* that cell), and the unmeasured `subtone-offset` gap (built 6px).

- 2026-08-17 — **BATCH 4 (owls #27–#31) built via workflow + DEPLOYED + LIVE-VERIFIED** (phase 13j, T136–T140; 7 agents; recon verified all six Figma nodes via the official Figma MCP — Rex offline, PASS 1:1 vs the owls). **Sprints modal, 4 states** (#28–#30, nodes 528:113433/322:30031/328:38162/328:38454): batch Save/Cancel on the existing audited PUT; per-project duplicate-name rejection server-side (trimmed/case-insensitive, 422 SPRINT_CONFLICT, zero writes zero audits on reject) + live client ERROR banner (blocking, NO CTAs per Miles's #30 ruling, the 1450px component slots suppressed); **overlaps = invariant 12** answered #28.4 — error banner + the existing lib/planner sprintIssues rejection; gap warning counts WORKING days against the wire's `holidays: getHolidays()` (ARES-canonical — a weekend or a holiday Monday between sprints fires nothing), renders BETWEEN the rows it names, one per gap, non-blocking; deletion warns with the displaced-row count; Mon/Fri-snapping pickers; LENGTH derived; **opened-empty → Save dead, emptied-by-user → Save live** (integrate fixed the R7 dead end with a flag set at open). **Drag reversal** (#31, supersedes R7 row-drag): the BAR is the horizontal drag source, per-week snap, row relocates on drop as a derived outcome with a 1.2s arrival pulse (+prefers-reduced-motion fallback) + scrollIntoView; unscheduled rows keep row-drag (no bar to grab — R-drag-a); grip removed from scheduled rows (R-drag-b); BR-8 multi-select + /replot byte-identical. **Row action icons** (#27, I262:33396;251:27040): Copy·Pin·CalendarRemove 13px sprites from the node paths, tooltips+aria+keyboard, hover/focus reveal, always visible pinned; CalendarRemove routes through the existing moveRows(null) unslot and is disabled on pinned AND already-unslotted rows (integrate caught /replot auditing a null→null "move"); status-note relocated to the manual chip (+ghost pencil) — flagged to Miles. **PINS SUPERSESSION enforced**: #27/#31's "pins allow manual drag" language predates JP's ruling B — pinned rows stay fully frozen everywhere, probe asserts /replot skips them with no audit row. Verify found 2 (both fixed): drag-scoped `pointer-events` so short bar-drags drop cleanly (segments were winning hit-testing — refused drops), empty-state Cancel tone. **564/564 dual-TZ (+96)**; 46-check server probe + 48 modal render tests + live browser pass (bar-only drag sources, 3-icon cluster with aria, modal empty→filled, dup error banner appears/clears live with Save gating, Mon/Fri defaults, Cancel discards). Flagged to Miles in the deploy owl: R7 emptied-vs-opened-empty split, whitespace-only names accepted (no rule specified), status-note home.

- 2026-08-17 — **INVARIANT 13 AMENDED (v4.3.0) + CAPACITY-IN-ACK-KEY built via workflow + DEPLOYED** (JP rulings on the two open items: FR-5.9 pins = **B, keep pins fully frozen** (no code change; Miles's requested change declined); ack-key = **A**, OD-4's capacity slice — broader OD-4 expiry stays OPEN). Constitution: ack key is now `week | rule | capacity | sorted card:phase pairs` (CLAUDE.md + mirror, `d6665b4`). Build (T135, commits `a7637d7`/`712ae35`): ONE `conflictKey()` recipe in conflicts.ts (drift guard asserts the pipe layout appears exactly once), uniform across all three acknowledgeable rules — hard-mix confirmed a planner FLAG, not a Conflict (guard test pins it); invalidation is a NON-MATCH, no audit row, superseded rows stay, and reverting capacity re-suppresses through the ORIGINAL ack (a test exists specifically to fail if anyone slips a timestamp into the key); migration `007-ack-capacity` lifts legacy 3-component keys to each project's OWN current capacity (suppression preserved exactly, idempotent by key shape, audited per row). Builder-flagged gaps applied post-workflow (R1–R3): `writeCapacity` refetches /deadlines once after the queue settles so re-surfaced banners appear WITHOUT a reload (the ruling's whole payload), ack tooltip copy mentions capacity, ack/restore failures surface via flashBanner (was a silent rejection), `conflict_key` Zod cap 2000→8000 (latent 400 at ~61 card:phase pairs on rt-837). R4 noted, not built: superseded rows accumulate — existing design, bounded by the Option-B lock. **463/463 ×3 TZs** (+18); verify lens: ZERO findings; 29/29 seeded probe (ack → capacity change → re-surface under new key → re-ack → revert → original ack re-suppresses; locked-project leg 403). Deployed; host verify: 007 recorded, **prod has 0 acks so backfill correctly no-oped** (0 spurious audits). Suite flake note: two transient single-test failures on HTTP-heavy runs, different files each time — consistent with the 2026-08-17 port-squatter root cause, monitored not chased.

- 2026-08-17 — **BATCH 3 (owls #23–#26) built via workflow + DEPLOYED + LIVE-VERIFIED** (phase 13i, T130–T133; rulings R-a…R-e in `gantt-frame-notes.md`). Five features. **Capacity lock (owl #23, Option B — the answer to the 13f flag JP raised about the rt-837 pin at 120):** `projects.capacity_locked`, `PATCH /capacity` → **403 `CAPACITY_LOCKED` as the handler's first statement**, before the Zod parse, and writing no audit row (a refusal is not a state change); admin-only toggle audited both directions with actor + before/after; migration `006-capacity-lock-rt837` locks that one project, idempotent, rt-test untouched; the slider dims with a padlock and the reason on it, plus a second guard in `writeCapacity` because a lock can flip in another tab. **Suggest bar (owl #25, node `262:34499`):** replaces the Suggest button in its own slot; all three counts derived CLIENT-side from the payload already held — proposed `|plan|`, flagged `|plan ∩ notes|`, hard-heavy `|strain|` — so **no `/suggest` field was added and 12.9% is never retyped**; one computed drives both `disabled` and the reason (R-e), off-week tripwire keeps precedence. R-d was **not true and is fixed**: `resetForProjectSwitch` cleared neither `suggest` nor `collapsedBlocks`. **Legend (owl #26, node `262:33342`):** tightened to the node's facts and the deadline tick's `width: 3px` fork dropped, so the shared `.gantt .gdl` 2px/slate-400 recipe reaches it — one marker recipe, one phase→colour map. **Collapse ×2 (owl #24):** sprint blocks collapse behind real `<button>` chevrons (presentation only — `plannerGroups`/`schedRows`/the footer never see it, so hidden rows still count against capacity), and the left pane collapses through the one `--gleft` variable 999 → 417, taking ~5 → ~11 visible week columns at 1600px and closing product flag #1. **443/443 dual-TZ (+68 from 375)**; tsc/eslint/`frontend/build.js` green; `git diff --stat lib/` empty; write registry unchanged at three entries. Integrate added the two template test files the builders left owed and switched them from grepping to **rendering the shipped template through Ractive's own `toHTML()`** (`test/helpers/gantt-render.ts`), plus a parse-tree scan for the `{{!`-comment leak that `frontend/build.js` provably cannot catch (with a negative control); the seeded isolated-db probe was strengthened from inequalities to **hand-computed counts** and gained a second fixture that proves `flagged` non-zero — 28/28. **Three rulings to flag in the deploy owl:** the admin lock route deliberately omits `ensureProjectMember` (the admin surface is global by construction; membership would lock out the admin who must unlock); a locked project + malformed body answers 403 not 400; Accept keeps the label "Accept" against the frame's "Apply". T134 browser pass DONE against production (2026-08-17): rt-837 lock (slider disabled, 'Capacity locked — admin can unlock'), legend 5 entries right-aligned zero-interactive, pane 999→417→999, sprint collapse 8→0→8 rows with footer totals unchanged, Suggest bar '8 proposed · 2 flagged · 2 hard-heavy' with 8 violet ghosts (rendering for the first time — the calendar fix unblocked R8) and clean Discard. LIVE REGRESSION found+fixed during the pass: a deep link to a NON-default project settled on projects[0] — the two-way-bound header select picked option one before the guarded route-apply ran; route project now chosen before render in ONE suppressed set, source-shape regression test added (13h hotfix). Live drag/keyboard reslot in the collapsed pane state still worth one manual JP pass. Product flags #1–#4 and #7 closed in the frame notes; R6 half-closed (checkbox still not built). Excluded pending JP and deliberately not built: the FR-5.9 change (pins block Suggest only) and the invariant-13 conflict-ack amendment. **Gate note — the "known mongodb-memory-server race" is now root-caused, and it is environmental, not a repo defect:** ~1 full-suite run in 5 fails on this machine, in a *different* file every time and wearing a different face (`Parse Error: Expected HTTP/`, `socket hang up`, a stranger's 404, mongod's `Port "63981" already in use`). macOS allocates ephemeral ports from 49152–65535 and local services squat inside that range **on the loopback address** — while diagnosing, `limactl` held 127.0.0.1:63981 (the exact port mongod then failed on), MongoDB 127.0.0.1:50423, Redis 127.0.0.1:59198. `server.listen(0)` binds the WILDCARD address, which BSD lets coexist with an existing specific-address bind, so the OS hands a test a port that is already someone's on 127.0.0.1; supertest then dials `127.0.0.1:<port>` and the kernel routes it to the squatter, not to the app under test. Parallelism does not cause this — it only consumes more of the range per run, raising the odds. No repo change made: the one-line fix (bind loopback) is not available where it is needed, because a host-qualified `listen` goes through `dns.lookup` and is therefore async, and supertest reads `address()` synchronously in its constructor. A real fix means every server-using suite opening ONE awaited `app.listen(0, '127.0.0.1')` in `beforeAll` — 21 files, its own task. Until then: green on rerun, and a red suite in a file the diff does not touch is this, not the change under review.
