# 2026-09-05 — Block 1: urgency + difficulty move to the WORK card (owls #78/#79)

Four new owls from Miles (#77–#80) triaged into six build blocks, ordered
by JP: (1) this block, then (4) Pipeline filter/sort rework, (2) the
Sprint Schedules search-based add flow, (3) Deadlines part 2 + the W2
setter moving to Sprint Schedules, (5) Requests filter/sort + UNIT, (6) W4
after its amendment. Block 4 moved up behind block 1 because block 1 parks
the URGENCY/DIFFICULTY filter axes and the two Priority sorts until block 4
rebuilds them over work-card values (D5).

Full-mode workflow. SURVEY: four code/spec readers + synthesis (drift table
24 items); Figma via Rex (JP opened it) — the table reader confirmed every
owl claim from nodes; the components reader failed its output schema and
the main thread read the Badge set and the metric strip by hand. The nodes
added two refinements the prose lacked: the Urgent label and chevron are
amber-50 (#FFFBEB), not white; the Easy label is green-600. Medium/Hard are
not instantiated in the frame — read from the component set's Outline/
Notice and Outline/Negative variants, mapping flagged to Miles (D10).

Production fact before build: the live board's project has
writes_enabled false; all 15 urgency/difficulty writes on record are on the
test board. The defect was in code, not client data.

GATE (JP "proceed"): PLAN.md with 10 decisions. D1 main-card values stay
stored+reconciled read-only (forecast key, Needs Info, Deadlines,
conflicts, planner untouched); D2 the deliverable-scoped W1/W3 routes are
DELETED (cross-kind 404); D3 kpi.urgent counts urgent work cards (asked of
Miles); D4 col-due → col-deadline, picker stays until block 3; D5 axes
parked; D6 dated W1/W3 scope note in the contract; D7 search blob drops
requestor; D8 tile label UNATTACHED CARDS; D9 no ⚡; D10 badge recipes from
the nodes supersede owl #04.

BUILD: five agents, exclusive files (S server on Fable; F/T/C/V on Opus).
S made writeGuards' `kind` REQUIRED (a defaulting kind is the shape of the
wrong-target write). One main-thread edit: patchWorkCard registered in
eslint's cross-file globals. VALIDATE 1: 63 files / 1134 green UTC+Manila,
NY calendar suites green; anchor b40df03; 15 revert proofs bit (proof 9's
helper misread a long diff tail — confirmed by hand).

REVIEW: 4 lenses → 14 findings → 2 refuters each → 9 survived. Applied
(a64d6d6): chooseUrgency reloads after the PATCH (the sprint chip and the
URGENT tile are server-derived; it went stale until an unrelated load);
Priority sorts parked (D5 parity; DIFF_RANK deleted with its only
consumer); scripts/urgency-roundtrip.ts re-targeted at a WORK card by
label (it picked a "Main Card" by NAME — the one surviving main-card write,
run from the deploy smoke list); cross-project 404 case per W1/W3 suite
(removing the project_id filter had left 63 files green); frame notes
R-pf-b/f/h/i/l/n annotated SUPERSEDED in place; footprint guard rewritten
as a paint-property allow-list; one stale title. Skipped: the duplicated
ten-column anchor (deliberate non-vacuity anchor). Refuted: W1 no-op guard
(pre-existing, out of scope — worth a later block), mirror-lag window,
stale http-api.md (Phase-1 sketch), D10 propagation to other tabs (shared
by contract §3.10, moves them toward their own nodes). VALIDATE 2: 63 /
1135 (+3 todo for block 4) green both TZ; 4 more proofs bit (19 total).

Slip: while masking secrets in a local .env read, the sed alternation
missed the ARES key and its value printed into this session's transcript.
Never left the machine; reported to JP with a rotate recommendation.

E2E rig: local watching server on :3956; a local copy of the rt-test
project (board tx8gDsTH) synced once from the ARES read API (8 main, 8
work cards); PROD_TRELLO_BOARD_IDS set locally to the live board id.

SIMPLIFY (303e11f, after correctness was clean): one commitRegistryWrite()
owns the W1/W2/W3 commit order (invariants 8 + 10 stated once);
registryFields() in the sync; writeTaskDue via patchWorkCard; the
redundant .col-diff badge width rule gone; kpi() single pass; one shared
write-test fixture (per-file G7 cases kept — writes-disabled's app has no
Trello client and cannot prove the refusal precedes the call). Skipped:
unifying the two badge handlers (four guards pin their bodies) and the
template option loop. VALIDATE 3: 63 / 1135 (+27 todo) green both TZ, NY
calendar green; 5 re-proofs on the refactored lines bit (24 total).

E2E (real browser, real pointer, local :3956 against the local rt-test copy):
ten headers in the frame's order; main rows static em-dash, no control;
URGENT tile amber-600 on both nodes, UNATTACHED hidden at zero (#61 rule
kept); expanded work rows: Non-Urgent slate-100/dashed slate-400/slate-500
label+chevron, Easy green-100/green-500/green-600, Medium amber-100/500/600,
Hard red-50/red-500 — 96px footprint on all. W1 round trip on the TEST
board: click → Urgent label on the WORK card (Trello read), main card
untouched, audit entity work_card, SyncRun kind work_card,
registry_written_at stamped, badge amber-600/amber-50, tile 1; one
syncProject afterwards left the write intact; reverted through the UI
(labels back to Difficulty: Easy only). W3 round trip: Easy → Hard label
swap on the work card, main card's Easy untouched, reverted. Rollback
path on the fixture project: Trello 400 → 502 → badge reverted, banner
"Urgency write failed — reverted …", urgency.set_failed audit row,
document untouched. Console clean apart from the browser's own 502 note.
Observed, not fixed: after a badge write the reload re-renders the row
and keyboard focus leaves the trigger (chooseDifficulty had this already)
— backlog. The Sprint Schedules chip was not exercised visually (the
local rt-test copy has no sprint rows); its own-card rule is unit-tested
and traced in review.

Commits: b40df03 build · a64d6d6 review fixes · 303e11f simplify · close
commit. #78 and #79 acked. Deploy waits for JP. Rex: the plugin sufficed
for geometry and variants; annotations came through the owls.

Sent jp → miles #61 on JP's yes (block 1 report, the three provisional
calls — tile count, Medium/Hard mapping, the parked axes and sorts — and
the #80 §1 answer: lane from the LIST, difficulty from the LABEL). JP
declined rotating the ARES key after the transcript slip; the value sits
only in the local session log.

SHIPPED: JP's "deploy" 06:22. Gate green on the FIRST run (63 / 1135 +27
todo, typecheck, build) — no flake this time; log captured from the start
per the 08-30 lesson. rsync + npm ci + migrate + pm2 restart; sirius and
sirius-worker online, uptime 20s, startup clean (the worker's "no Sheets
credential" line is the standing GOOGLE_SHEETS_CREDENTIALS gate). Live:
healthz 200 ×3; the bundle carries metric urgent / metric quiet /
UNATTACHED CARDS / col-deadline / both /workcards/ PATCH URLs and no
/deliverables/ urgency or difficulty URL. Pushed: main = origin/main at
d882e89. The two remaining "⚡ Urgent" strings in the bundle belong to the
Deadlines card badge (50-deadlines.html) and the urgent-overlap conflict
chip — out of this block; the Deadlines colours are on Miles's hold (#79).

---

## Block 4 — Pipeline filter + sort rework (owl #78 §4/§5)

SURVEY (workflow, five Sonnet readers + one synthesis; Rex for both
panels and every item node): 49 drift rows — 22 match, 7 drift, 4 missing,
9 parked from block 1, 2 spec-vs-ruling contradictions (STATUS list order
vs R-pf-e; the owl's "build-spec §7a", which does not exist), 5 undrawn
edges. Twelve questions came out of it; eleven were reversible and
decided at the gate (PLAN.md B1–B11), none needed JP beyond the go.

GATE: drift report + PLAN.md posted; JP "yes" 07:5x. Full mode.

BUILD: three agents in parallel (STATE Fable · TEMPLATE Opus · TESTS
Fable), exclusive files. The STATE agent stopped on a frozen rule rather
than working around it: the facet arithmetic "unchanged" from block 1
under-counts when BOTH work-card axes are ticked — a group with an Urgent
Easy card and a quiet Hard card fell out of both pools once Urgent and
Hard were live, so Easy read 0 on a panel where ticking it would widen the
table (446/1000 random boards off R-pf-c). PLAN.md amended (B12: the
work-card axes fail as ONE conjunction; a row failing only that counts in
every work-card pool with its own-ignoring set), the agent's validated
patch applied by the main thread, a tests round added the two-axes proofs
— and corrected the numbers in my own brief from the rule (Easy 1, Hard 1,
not Hard 2). The TEMPLATE agent changed three expressions and two words.

VALIDATE 1 on the anchor (`50def71`): typecheck, lint, 63 files / 1177
passed + 24 todo in UTC and Manila, calendar + client-timezone in New
York. Fourteen revert proofs on the committed tree, all bit (parent
deadline ignored · derived flag · B12 conjunction · Non-Urgent as a value
· label · both observers · pipeOpen's two gates · pipeKids identity ·
template gate · toggleMc door · reset key · the except-j leak).

REVIEW (five Fable lenses, a Fable refuter per finding): 20 findings, 11
stood, 9 refuted. Nothing blocking. The one product defect predates the
block: `pipeBackToTop` reset the table box's scrollTop, but that box only
scrolls sideways — the PAGE is the vertical scroller — so no filter, sort
or search change had ever returned the reader to the top since owl #62
(C4; the refuter reproduced it in a real browser). Fixed to reset the
document scroller too. Also: an own-key guard on the DIFF_RANK lookup
(C1/C3, defensive; 'constructor' as a label produced a function-valued
key), and seven test hardenings the mutants exposed (order through the
shipped computed, toggleMc executed not grepped, the no-Date rule
asserted, an URGENCY fixture without urgency, the E2 rule at the filter
edge, the axis-count prose derived, the count unit named as MAIN ROWS —
MC-825's 99 rows read "Urgent 99" with one urgent card; B4 said "groups"
and was wrong). Refuted but recorded: a MAIN-axis tick also resets a
hand-collapse under a derived sort (B10 as written); the default order now
carries the B7 tail. Fix commit `a4c4376`; the C1 guard got its own pin
(`5432079`) after its revert proof did not bite. VALIDATE 2: 1179 + 24.

SIMPLIFY: four read-only lenses (reuse · simplification · efficiency · altitude) over the two commits: 26 findings, 20 applied, 6 skipped as frozen or low-gain. The work-card absence rule and nulls-last-then-compare each stated once; B12's pools encoded as one pool id; the facet pass and the derived sort keys memoised on the shared `r.work` array (MC-825's 99 rows recomputed the same answer 99 times — 90% of the pass); R-pf-h stated ONCE as an observer on pipeFilters, pipeSort and searchQ, the five handler calls gone, the page scroller the only target (`.pscroll` is x-only); the three hand-copied test decl lists replaced by one exported prelude. A 3,000-board old-vs-new sweep found zero differences; the one behaviour change is that a stale sort key now falls back to the natural order. Commit `fa43f10`. VALIDATE 3: 1179 + 24 in both TZs, NY green; three proofs on the moved guards bit. Not done: `pipeWorkLive` still reads PIPE_FILTERS (a cross-file global needs an eslint FRONTEND_SHARED line) — backlog.

E2E (local :3956, rt-test copy of the TEST board, real pointer, console
clean throughout): filter panel 276×563 inside the viewport, four axes in
the frame's order, counts by hand (Easy 3 · Medium 3 · Hard 2 ·
Non-Urgent 4), zero-count Urgent hidden, STATUS verbatim lanes
alphabetical in the 168px scroller, Clear disabled #94A3B8. Difficulty=Hard
→ two groups, both auto-opened, ONLY the Hard child drawn under each, own
axis counts untouched, chip "Difficulty is Hard", "Filter, 1 applied".
Chevron by hand → `pipeShut` takes it, `expanded` untouched, the outside
click both closed the panel and reached the chevron. Ticking Medium →
override reset, four groups reopen with Hard+Medium children. Clear
filter → every group closed, `pipeKids` is the store's own map again.
Sort panel: three groups, ten labels, "Deadline farthest from now" wraps
to 53px as the node. Deadline closest → MC-908 first on its child's 10 Sep
(the parent cell still says 31 Aug — the B5 interim until block 3),
keyless rows by MC ascending, childless plain, button "Dates: Deadline
closest to now" at 166px under the 240 cap. Scroll-to-top: page at 261px,
one keystroke in search, page at 0. Escape closes the sort panel and
returns focus, sort kept. Real Trello round trip: MC-906 "Render Icon"
(DoPgUoR6) set Urgent through its badge — Trello read back `Urgent`,
audit `urgency.set work_card`, sync_runs ok; Urgent first → MC-906 leads,
the all-quiet groups follow as a value, childless trail; Urgency=Urgent →
one group, one child, "Urgency is Urgent"; restored to Non-Urgent through
the same badge — Trello read back `['Difficulty: Medium']`, tile 0, and
with the Urgent filter still live the no-results verdict fired.

CLOSE: frame-notes batch 15pf (R-b4-a..h) + RESTORED notes on the five
block-1 SUPERSEDED rows + R-pf-e re-ask recorded; PLAN.md rotated to
2026-09-05-plan-block4.md; owl reply to Miles drafted (awaits JP's yes).
NOT deployed — JP's word. Console noise seen once after a cache-bypassing reload: `/favicon.ico` and `/site.webmanifest` 404 — pre-existing, no icon shipped; unrelated to the block. Backlog gained: arrow keys through the popovers
(owl #62, never built); ARES list position for STATUS order; the B5
interim parent-deadline mismatch until block 3; the count unit on a
shared MC flagged to Miles.

Commits: `50def71` build · `a4c4376` review fixes · `5432079` C1 pin · `fa43f10` simplify · close commit (docs). VALIDATE ×3, proofs ×20 (14 + P15/P16b + P17–P19), all bit.

Sent jp → miles #62 on JP's yes (in reply to #78): the six calls (siblings hidden, counts as rows, TYPE/STATUS on the main card, STATUS alphabetical, the double-colon label, §7a struck) and the three held (arrow keys, the frame's panel scrollbar, the scroll defect fixed).

SHIPPED (block 4): JP's "deploy" 11:02. Gate green on the first run (63 /
1179 + 24 todo, typecheck, build); migrate up to date; rsync + npm ci +
pm2 restart — sirius and sirius-worker online, uptime 28s at the check,
startup clean (the worker's Sheets-credential line is the standing gate).
Live: healthz 200 ×3 at ~130ms; the bundle carries the ten labels
(`Deadline closest to now` … `Deliverable Name: Z–A`), PIPE_WORK_FILTERS,
pipeShut, pipeSortDef, cmpNullsLast and the page-scroller reset, and none of
`Due dates closest` / `Card name A–Z`. Smoke: urgency round-trip on the TEST
board (MC-906 Render Icon, set + unset, labels read back both ways). Pushed:
main = origin/main at `a530af6`. The one remaining "five axes" string in the
bundle is the historical performance note in 10-constants.js ("the five axes
this shipped with"), true as history.

# 2026-09-05 (later still) — Block 2: Sprint Schedules search-based add + Add All (owl #77 §0)

Full mode. SURVEY: main-thread reads of #77/#72/#73 and the four nodes via
Rex (840:31597 results state with Miles's two annotations, 841:33668 +
841:33689 no-matches, 833:68629 resting) plus a two-reader workflow
(wf_79a8efa0-ab9): the retired-flow inventory (67 references across five
frontend files, section 7 of the gantt sheet, four test describes, the
collapse and sprints-modal assertions) and the #77 §4 checks (`sheet_row`
exists and sorts; NO Business Unit column anywhere — Use Case only, single
string; Requests has no header-label pin). Drift 31 rows; PLAN.md B1–B14.
GATE: JP "Yes" (single ask).

BUILD: five agents (S/F Fable, V/C/T Opus), none stopped. VALIDATE 1: 63 /
1217 + 24 both zones, NY calendar suites; anchor `0cbbe60`; 20 revert
proofs (P17's first cut re-sorted by name against a fixture that made the
alphabetical order coincide — re-cut on cardId, bit; P20 a deliberate
pattern-miss control). Simplification held until correctness.

REVIEW (wf_f4b02d31-740, four lenses → 21 findings → two refuters each):
12 stood, 1 refuted, 8 lost their refuters when the session limit hit
(judged by the main thread from the evidence in the journal). Fixed
(`55f572f`): the partial-result banner was flashed BEFORE the reload,
which writes the banner slot — the summary lived as long as the fetch
(B2-R1); the template disabled links per sprint while the handlers
guarded on ANY add in flight, so another sprint's Add rendered live and
answered nothing (B2-R2/B2R-1) — one add per screen now, greyed
everywhere, and the adds no longer take the placement lock; a stale
refusal reloads before it banners (B2-R6); both 404s carry plain words
(B2-R5); the query clear spares text typed mid-flight (B2-R4); focus
returns to the field when the clicked link left with its row (B2-R7);
`lazy="250"` like the app's other three search fields (surface-4); the
no-match line is `role=status` and a match count is spoken off-screen
(surface-3/B2R-5); a busy row greys its link and holds its label
(surface-1/B2-R3); the seam above the footer drawn once (surface-2); the
search pane pads 15 below the field so 16 + 45 + 15 + the rule = 77
(surface-5). Server: the sprint is re-asserted AFTER the batch — gone →
the rows are taken back, audited, 409 SPRINT_GONE (S1); an audit row
failing takes its row back and 500 PARTIAL says how far it got (S2).
Refuted/recorded: the 2000 cap (frozen; ~480 active work cards on the
board), MC tokens as substrings (progressive typing), label slate-900 via
the surface token (no slate-800 token). VALIDATE 2: 63 / 1230 + 24 both
zones (one Manila red in schedule.test.ts — the rule-5 flake, three clean
re-runs); 17 more proofs bit (P21–P33, S6–S9).

SIMPLIFY (wf_ce196a71-81c, `b435ac1`): 9 of 17 applied — one failure
owner for the two catches, `createdIds.length` for the counter, derived
handler heads and the shared keypath resolver in the harness, one
second-project fixture and one race-seam helper in the server suite.
Skipped with reasons: addPanels memoisation (lazy + ~480 cards; sharing an
items array across two keys is an identity change), the shared audit
shape (its guard exists to catch the two routes drifting), alphaSort for
the MC tiebreak (locale collation), a Ractive partial for the three inert
tracks (the harness passes no partials). VALIDATE 3: 63 / 1230 + 24 both
zones, NY green.

E2E on the local :3956 rig, real pointer, console clean throughout: two
sprints seeded into the LOCAL rt-test copy (Sirius-owned data; no Trello
write in this block). Resting: two 77px rows, field 45 slate-50 with the
1px slate-200 stroke, placeholder verbatim, no Add All, footer border-top
0 and contiguous (seam once). "MC-905" → two rows at 54px, label 14/400
slate-900 at x=94, Add blue-300 at right 992 = Add All's right, Add All
blue-600 text with no box, `.typed` 600, "2 work cards match" spoken.
Row hover: that row's label 600 and Add blue-600, the other row and Add
All unchanged. Add by click: the row landed unplotted, "· 1 item", the
query stayed, the panel shrank to one, focus back in the field. "MC-06
Jollibee" in sprint B: Add All slate-400 disabled, "No cards found for
this query" 14/400 slate-500 in a 54px row at the label inset, the field
still 45 and the row still 77. "sibling" → four rows MC-905..908 in MC
order; Add All by click → four rows in list order (positions 0–3,
unplotted), the query cleared, the panel gone, focus back in B's field,
no banner — and sprint A's still-open "MC-905" panel refreshed to
no-match because B consumed MC-905's last card. Escape in A: query
cleared, focus kept. Collapse A: its search row gone, the seam still
single. Rig restored: the five items removed from the local copy, the
two sprints kept for block 3.

CLOSE: gantt-rules.md §8 (R8-a..i); STATE.md 18d/18e → phase-log, 18f
added; PLAN.md rotated to 2026-09-05-plan-block2.md; owl draft for Miles
in the scratchpad (block2/owl-reply-draft.md) — sent only on JP's yes.
NOT deployed — JP's word. Backlog gained: the single add's own
sprint-gone window (narrower than the batch's, same shape); Enter as Add
All (suggestion to Miles); the #77 §4 answers ride to block 5.

Commits: `0cbbe60` build · `55f572f` review fixes · `b435ac1` simplify ·
close commit (docs). VALIDATE ×3, proofs ×37, all bit.

Sent jp → miles #63 on JP's "yes to both" (in reply to #77): the five calls
(substring token matching, no cap, the clear-after-Add-All rule, MC order,
one add per screen), the three held (Enter, the resting stroke
contradiction, the parked narrower pane) and the §4 answers.

SHIPPED (block 2): JP's "send the owl then deploy" 15:14. Gate green on the
first run (63 / 1230 + 24 todo, typecheck, build); migrate up to date; rsync
+ npm ci + pm2 restart — sirius and sirius-worker online, uptime 90s at the
check, 0 unstable restarts, startup clean (the worker's Sheets-credential
line is the standing gate). Live at the public base URL: healthz 200 ×3 at
~130ms; the bundle carries the search row, the field, Add All and the batch
route (14 markers) and none of the retired flow (0); the API wall answers
401. Smoke: urgency round-trip on the TEST board (MC-906 Render Icon, set +
unset, labels read back both ways). Two lessons for the rig, recorded in
memory: the host port's `/healthz` 404s because the app is mounted under a
path — check through the public base URL read from the host's env by name;
`pm2` over ssh needs `source ~/.nvm/nvm.sh` first. Pushed: main =
origin/main at the ship commit.

# 2026-09-05 (evening) — Block 3: Deadlines part 2 · the W2 setter on Sprint Schedules · Pipeline DEADLINE read-only (owls #74/#75/#78 §2, #80 §2)

**Mode** Full. JP: "yes, open block 3" → survey; "Yes and yes" → the acks
retire from the screen (server half parked) and the build goes.

**Survey** (wf_e2c419a9-3d2, five readers + main-thread node reads via Rex):
the old tab was still #64's milestone-unit build whole; the "date-range
picker" is a prev/next month navigator over a static label (731:100859); the
expanded lane lives at 810:121954 (731:100872 is the COLLAPSED variant); the
card's inner geometry, the done card's opacity 0.4 (read two ways), the empty
copy `None slated today` / `None slated this week` (the nodes, not the owl's
"Nothing…"); no setter is drawn on the schedule cell (five hidden Date Picker
instances); the ARES read API exposes NO lanes endpoint and Miles never
answered #59's Apollo rule — the keyword classifier stays as JP's interim.
Drift report: 46 rows (scratchpad block3/drift.md). The rulebook split
(`97e905b`) landed first: gantt-rules.md (drag/geometry/verification) +
sprint-rules.md (behaviours/chips/add row), rule numbers global.

**Gate asks** (1) retire the conflict warnings + acknowledgements from the
screen, park the server half until R-dl-n; (2) build as planned. Decided and
reported: inheritance retired (a row's deadline is its card's own due);
the tab reads `sprintItems.rows` — no new payload; the card's day = its
forecast FINISH; weeks = the month's `monthWeeks` set (a straddling week
under both months); counts independent; plain-count progress; asset badge
only when the MC group agrees; rollover after each ARES tick, sprint follows
the finish day, none → stays; the Pipeline popover recipe reused verbatim.

**Build** (wf_a0821c85-397, six agents): S1 rows + W2 half deleted · S2 the
rollover · F scripts · V templates (+ the `dlCard` partial) · C styles · T
tests (deadlines-frame retired, deadlines-tab new, drag-hittest's source set
to zero). Cross-lane fixes on the main thread: a stranded expectation on the
deleted route, the pinned pane lifting while it holds the popover (a sticky
pane with a z-index is a stacking context — the fixed popover was confined
to it and later rows painted over it), the trigger locked while saving, the
orphaned planner CSS. `36e1976`. VALIDATE ×2 (one loopback flake, green ×2
on re-run). 24 proofs; P4 did not bite until the unplotted fixture carried a
finish the server never emits — re-cut, bit.

**Review** (wf_fbb80824-f1d: 5 reviewers, 2 refuters each): 18 findings, 13
stood, 3 split, 2 refuted. Headline: the quote bar painted 8px — the frame's
export carries a 2px inset, so the view box starts at path x=2 and is 8 wide
(band = the ruled 4px). Rollover: a project whose sync failed still rolled;
save() lost a PM's concurrent write; an audit failure left an unaudited move;
the worker never loaded the stored calendar. Fixed by one Fable agent: a
freshness gate (latest `ares` OR `ares_push` row ok within 65 min — the first
cut read `ares` alone at 20 min and gated a push-healthy project to one roll
an hour; fix-agent note), a conditional updateOne keyed on the values read,
audit-or-revert with per-row/per-project isolation, the six counts, one
`sync_runs` row per project. Also: the setter disarmed on an off-board row;
the month follows the clock through state; the helper memo; stale contract
and map lines. Split kept as parity: a sideways gantt scroll dismisses the
popover (R4-2). `4086dc3`. VALIDATE 3 green ×2 zones (1338 + 24).

**Simplification** (wf_9a969e00-5e7 → wf_fe4ff043-118): 31 findings, 24
applied — the date helpers reuse mondayIso/mondayShift/fridayIso/monthShiftYm;
dlBuild loses the holiday flag nothing rendered and the card fields nothing
read; patchRow and PipeRow.overdue/trelloDue/deadlineSource gone; the write
door work-card only; tailPosition/nextTailPosition shared; the rollover reads
only the rows' cards, memoises the engine, skips provenance; latestRead in
sync-status.ts; topDecl/tabViewCode + schedule-fixture in the test helpers.
Rejected: ALT-7 (skipped count), ALT-6 (rot alarm → backlog), the other
latest-read variants. `7a930d8`. VALIDATE 4 green ×2 (1335 + 24); all 30
proofs re-cut pattern-based against the final code, all bit.

**E2E** (local :3956 rig, rt-test copy, real pointer): Add All ×2 → four
rows; four bars placed by hover + click on tagged week cells; the DEADLINE
setter opened on a bottom row (flipped up, in viewport, pane z 3) and a top
row (opened DOWN over three later panes — 55/55 sampled points resolve inside
the popover: the lift holds); 25 Sep applied → Trello due
2026-09-25T09:00Z, audit `due.set work_card`, the tick appeared; Escape
closed without a write and returned focus. Pipeline: every main row an
em-dash, the work row `25 Sep 2026` as a read-only span, zero pickers, zero
popovers. Deadlines: `Aug 31 – Sept 30, 2026`, five 350×799 lanes, cards
308×180, the done card (a LOCAL lane fixture) at opacity 0.4 counted Done,
the Urgent card (W1 set through Pipeline, a real write) with the band
measured 4px by isPointInFill and curving at the corner, `None slated this
week`, progress `2 / 96 Work Cards`; expanded Week 4: 1830×799, five 350×683
columns at 12px gaps with their own scroll, `1 Pending • 1 Urgent • 0 Done`
in the ruled colours, `None slated today`, `.dlscroll` the one sideways
scroller, the body never. Rollover (harness with a fixture sync row): today
09-11 → the Hard card rolled 09-07→09-08 (finish 09-11), the done card and
the future rows untouched; today 09-19 → 09-16 (finish Mon 09-21), sprint
E2E-A → E2E-B at the tail, audit rows actor `system`, one `sync_runs` row
per run; both tabs agree, no marker. Console: the standing favicon 404 only.
Cleanup: Clear Due Date via the popover (Trello due none, audited), urgency
back via the control, the four rows deleted through the audited route, the
local lane restored; all four cards read back from the board as found.
Recorded, not fixed: focus drops to body after the setter's reload (the
badge-write class); the local rig now holds a fixture `ares` sync row.

**Docs**: deadlines-rules.md (new, R-d2-a…s) · sprint-rules.md §9 · the W2
contract narrowed · deadlines-frame-notes.md archived · pipeline-frame-notes
supersessions · worker.md's rollover row · maps regenerated · REBUILT_TABS
→ deadlines-rules.md. PLAN rotated to 2026-09-05-plan-block3.md. jp→miles
#64 drafted (scratchpad block3/owl-reply-draft.md), send on JP's word.
NOT deployed — JP's word.
