# PLAN.md — block 5: Requests filter + sort, UNIT, default sort, footer, shared no-results (owl #77 §1–4)

_Ephemeral. Exists while this build is in flight; rotates into `docs/history/state-log/2026-09-06-plan-block5.md` at CLOSE. Drift report: scratchpad/block5/drift.md. Mode: **Full**. Interfaces are frozen HERE and nowhere else; an agent that needs to change one stops and reports._

## Decisions (gate; JP may veto any)

| # | Decision | Reason |
|---|---|---|
| D1 | **UNIT is a label.** Storage and wire keep `use_case`; the parser aliases BOTH `business unit` and `use case` → `use_case`; the screen says `Unit`, the class is `col-runit`, the sort/axis key is `unit` | `use_case` also lives on Deliverable and the schedule duplicate flow — a rename is a migration for a word; Miles's own rule puts the sheet's name in the parser, not on screen |
| D2 | **STATUS filter axis has three DERIVED values**: `In Pipeline` (status filed) · `For Filing` (status unfiled — includes clarified rows) · `For Clarification` (`clarified(r)`). The badge stays two-valued (Batch 5); one row can belong to two values | the tiles already use this ⊂ rule (owl #14); nothing in Batch 5 is reopened |
| D3 | **Panel scroll**: the filter panel gets a viewport-capped `max-height` with its own scrollbar; REQUESTOR (the one open-ended axis) keeps the in-group `pmscroll` | six axes cannot fit; #49's reason (a long axis pushes the others out) is kept by the in-group scroll |
| D4 | **The tiles drive the STATUS axis.** Tile click sets `reqFilters.status` to exactly `[value]`; clicking the pressed tile clears the axis; REQUESTS tile clears it; pressed state derives from the axis. `requestFilter` state is RETIRED | two controls over one field cannot disagree if they are one state |
| D5 | **Divergence guard** is client-side: `reqOrderDivergence` counts adjacent pairs along ascending `sheet_row` whose (year, monthIdx) go BACKWARDS (undated rows skipped). When > 0 the sync strip gains one sentence | all rows are loaded once; no wire change; placement asked of Miles |
| D6 | **Header-click sorting is retired** — the sort panel is the one sort door. `reqSortBy` deleted; headers render as plain `<th>` | "sort replaces"; two sort doors with different option sets cannot agree. Node 809:85709 overturns this only if the headers draw affordances |
| D7 | **Chips: one per VALUE on Requests**; Pipeline stays one per axis (R-pf-l). One chip recipe (`.fchip`), two list builders. A chip's ✕ removes ITS value; hovering opens the whole axis group (R-pf-m) | Miles's explicit ruling; asked whether Pipeline should follow |
| D8 | **Footer renders whenever rows exist**, one page included | the count line is information at one page too; node confirms |
| D9 | **Shared no-results**: `{{#partial noResults}}` in a NEW partial file; both tabs call `{{>noResults}}`; `.pnores*` CSS MOVES from 20-pipeline.css to 10-ui.css. Requests' other two `.rempty` states stay | content moves, never copies |
| D10 | **The filter-group partial dispatches by table**: `on-click="['toggleFacet', table, key, v.value]"`; every facet object carries `table: 'pipe' \| 'req'`; `toggleFacet` routes to `togglePipeFilter` / `toggleReqFilter` | the partial stays one copy; no reliance on a dynamic proxy-event name |
| D11 | **None is a value** on every Requests axis except STATUS, derived (offered only when a row lacks the value), stored `null`, drawn `None`, sorts last | R-pf-i unchanged |
| D12 | The **sort default is an unlisted order** (Pipeline's shape): `reqSort: null` = `Recently requested`; the listed option with the same effect is `recent`. Clear Sort → `null`. Whether the panel highlights it at rest is decided by node 809:87722 | keeps the shipped panel behaviour |

Nodes READ 2026-09-06 through Rex (reports: scratchpad/block5/figma/*.md). **§Node amendments at the end of this file WINS over any provisional line above.**

## Frozen interfaces

**Constants + machinery — `frontend/scripts/20-requests-table.js` (agent C)**
- `REQ_FILTERS` — six axes `{ key, col, label, pick(r) → value|null, order? }` in the panel's order (PENDING NODES; provisional YEAR · MONTH · TYPE · REQUESTOR · STATUS · UNIT): keys `year` (col-ryear) · `month` (col-rmonth, `order` = calendar) · `type` (col-rtype) · `requestor` (col-rwho, `scroll: true`) · `status` (col-rstatus, `values(r) → string[]`, no None) · `unit` (col-runit). `label` MUST equal the column's label (guard U4).
- `REQ_FILTERS_EMPTY()` → `{ year: [], month: [], type: [], requestor: [], status: [], unit: [] }`
- `REQ_STATUS_VALUES = ['In Pipeline', 'For Filing', 'For Clarification']` in this order; `reqStatusOf(r) → string[]` (D2) — the ONLY new client literal, the tiles' own vocabulary
- `REQ_SORTS` — eight `{ key, label, group, dir, value(r) }` (labels/groups PENDING NODES); `key: 'recent'` = sheet_row desc is one of them
- `REQ_SORT_DEFAULT = { key: null, dir: -1, value: (r) => r.sheet_row }`
- `REQ_COLS` — as today with `{ cls: 'col-runit', label: 'Unit', sort: 'unit', val: r.use_case }`; `sort` keys stay on the entries (the guard reads them); header-click removed at the template
- `reqColLabel(cls)` · `reqFacetList(rows, selected)` · `reqMatches(r, selected, exceptKey?)` · `reqSortRows(rows, def)` · `reqChipList(selected, facets)` → `[{ key, axis, value, label }]` one per value · `reqSortLabel(def)` = `Group: Label`
- reuse from 12-constants-pipeline.js: `cmpNullsLast`, `mcRank`; from 10-constants-core.js: `STATUS_FILED`, `clarified`, `monthOrder`, `monthShort`
- DELETE: `REQ_SORT_COLS`, `reqComparator`, `reqDefaultOrder`, `reqFiltersCleared`, `reqFilterKeys`, `descNullsLast`

**State — `frontend/scripts/40-app-state.js` Requests block (agent C)**
- data: `reqQ` (kept) · `reqFilters: REQ_FILTERS_EMPTY()` · `reqSort: null` · `reqSortMenu: null` · `reqFilterMenu: null` · `reqPage` (kept) · `reqCols` (kept) · `reqThumb` (kept)
- DELETE: `requestFilter`, `reqYear/reqMonth/reqType/reqRequestor`, `reqMenu`, `reqMenuPos`, `reqSortKey`, `reqSortDir`, `reqFilterDefs`
- computed: `reqSearched` (search blob) → `reqFiltered` (search + axes; name kept) → `reqSorted` (name kept) → `reqRows` (page slice; kept) · `reqFacets` (each facet `{ table: 'req', key, label, scroll, values: [{ value, label, count, on }] }`) · `reqChips` · `reqFilterCount` · `reqSortDef` · `reqSortLabelText` · `reqNoResults` (= no `reqFiltered` rows AND (query or chips)) · `reqPageRange` → `{ from, to, total }` · `reqOrderDivergence` (number) · `reqStats` (kept; `on` derives from `reqFilters.status`) · `reqPageCount`, `reqPages` (kept)
- `pipeFacets` entries gain `table: 'pipe'` (one line)

**Overlays — `frontend/scripts/60-overlays.js` (agent C)**: OVERLAY_KEYS: `reqMenu` OUT, `reqSortMenu` + `reqFilterMenu` IN; matching OVERLAY_SHIELDS entries (own trigger `.reqtools .sfbtn`, own box `.reqtools .pipemenu`).

**Loaders — `frontend/scripts/80-loaders.js` (agent C)**: pager-reset observer keyed `reqQ reqFilters reqSort`; `requestBlob` unchanged (reads `use_case`); `sheetRowUrl` unchanged.

**Handlers — `frontend/scripts/90-events.js` (agent H)**
- NEW: `openReqSort` · `openReqFilter` · `toggleFacet(ctx, table, key, value)` (D10 dispatcher) · `toggleReqFilter(ctx, key, value)` · `pickReqSort(ctx, key)` (re-pick = clear) · `clearReqSort` · `clearReqFilters` · `removeReqChip(ctx, key, value)` · `reqChipPopIn(ctx, key)` / `reqChipPopOut(ctx)` through the shared `openHoverOverlay` / `scheduleHoverClose` door, reading `reqFacets`
- KEPT: `setRequestFilter(ctx, segKey)` (now writes `reqFilters.status` per D4) · `reqGoPage` · `reqPageStep` · `reqScrolled`
- DELETE: `openReqMenu`, `pickReqFilter`, `reqSortBy`; `applyRequestFilter` becomes the D4 mapping
- `resetForProjectSwitch`: resets `reqFilters`, `reqSort`, `reqQ`, `reqPage`
- Pipeline's filter-group call path changes ONLY in that `toggleFacet` now fronts `togglePipeFilter`

**Templates (agent T)**
- `frontend/templates/views/20-requests.html`: toolbar `.reqtools` = `.searchbar.reqsearch` + `.sortfilter` (buttons `.sfbtn`, panels `.pipemenu.filtermenu` / `.pipemenu.sortmenu` — Pipeline's classes verbatim, aria-labels say "requests"); chips `.fchips` from `reqChips`, each `.fchip` with ✕ → `removeReqChip`, `.fclearall` → `clearReqFilters`; `thead` via `{{#each reqCols}}` as plain `<th class="{{c.cls}}">`; footer `.reqfoot` inside the rows branch after the table: `.reqfoot-range` (`Showing <b>{{from}}-{{to}}</b> of <b>{{total}}</b> requests`) + `nav.pager` (classes `.pnav`, `.pbtn`, `.pbtn.on`, `.pgap` kept); `{{>noResults}}` when `reqNoResults`, else the two `.rempty` states as today
- `frontend/templates/partials/20-filter-group.html`: on-click → `['toggleFacet', table, key, v.value]` (D10)
- NEW `frontend/templates/partials/30-no-results.html`: `{{#partial noResults}}` = Pipeline's block verbatim (`.pnores` / `.pnores-head` / `.pnores-sub`, the two strings character-for-character)
- `frontend/templates/views/30-pipeline.html`: its no-results block → `{{>noResults}}`; nothing else
- `frontend/templates/partials/00-req-sync-strip.html`: `{{#if reqOrderDivergence}}` one sentence: `The sheet's row order and its Year/Month columns disagree on {{reqOrderDivergence}} rows, so Recently requested may be out of order.`

**Styles (agent Y)**
- `frontend/styles/25-requests.css`: `.reqtools`, `.reqfoot` (1784×32; range text 14px Regular slate-500 with SemiBold slate-900 counts; active `.pbtn.on` 32×32 bordered, siblings 28), `.col-runit` width (was `.col-rcase`), REMOVE the `.filterbar` / `.rfilter` / `.selwrap` select recipes and `.reqmenu`; the filter panel's viewport cap (D3) as `.reqtools .filtermenu { max-height: …; overflow-y: auto }`; ALL numbers from nodes
- `frontend/styles/10-ui.css`: receives `.pnores`, `.pnores-head`, `.pnores-sub` verbatim (D9)
- `frontend/styles/20-pipeline.css`: those three rules removed; nothing else changes

**Server (agent S)**
- `src/services/intake-parser.ts`: `HEADER_ALIASES['business unit'] = 'use_case'` (keep `'use case'`); `IntakeParseResult.warnings: IntakeWarning[]` with `IntakeWarning = { sheet_row: number; field: 'use_case'; reason: 'multi-value' }` pushed when the trimmed value contains a comma — the value is stored WHOLE
- `worker/syncIntake.ts`: one `warn` log line per warning (`intake: multi-value unit at row N (project P)`); nothing written anywhere
- `scripts/fixtures/intake.csv`: header `Use Case` → `Business Unit` (exercises the alias); `scripts/seed-intake-test.ts` header array likewise
- `test/intake.test.ts`: alias test (both names map), multi-value warning test (comma value stored whole + one warning), the existing header arrays updated

**Tests (agents V1, V2)**
- V1 NEW: `test/helpers/requests-sortfilter.ts` (recipe slicer over 20-requests-table.js + the Requests computed block, executed — test/CLAUDE.md rule 2) and suites `test/requests-sortfilter-filters.test.ts` (facets, R-pf-c/d, D2 membership, None), `-sorts.test.ts` (default = sheet_row desc FIRST, eight sorts, nulls last, tiebreak), `-panels.test.ts` (overlay keys, anchoring, Clear disabled), `-indicator.test.ts` (per-value chips, wrap, Clear all, the four-part header-label guard), `-noresults.test.ts` (verdict + real toHTML: partial shared, footer absent, tiles present), `-footer.test.ts` (range arithmetic, active-page geometry, D8)
- V2 UPDATES: `test/requests-render.test.ts` (col-runit, footer placement, plain headers), `test/requests-status.test.ts` (tiles → axis), `test/helpers/gantt-render.ts` (`REQ_COLS_STUB` derives from the shipped source; `ReqRow` unchanged), `test/pipeline-sortfilter-*.test.ts` (facet `table` field, `.pnores` read from 10-ui.css, `toggleFacet` in the rendered on-click), `test/template-partials.test.ts` if it lists partial files
- eslint cross-file globals (`eslint.config.*`): new names from agent C and H registered by the MAIN THREAD after BUILD

## Agent split — exclusive file ownership

| Agent | Model | Files |
|---|---|---|
| C constants+state | Fable | `frontend/scripts/20-requests-table.js`, `frontend/scripts/40-app-state.js`, `frontend/scripts/60-overlays.js`, `frontend/scripts/80-loaders.js`, `frontend/scripts/10-constants-core.js` (ONLY the deletion of `REQ_MENU_W` / `REQ_MENU_H`) |
| H handlers | Opus | `frontend/scripts/90-events.js` |
| T templates | Opus | `frontend/templates/views/20-requests.html`, `frontend/templates/views/30-pipeline.html` (no-results swap only), `frontend/templates/partials/20-filter-group.html`, `frontend/templates/partials/30-no-results.html` (new), `frontend/templates/partials/00-req-sync-strip.html` |
| Y styles | Opus | `frontend/styles/25-requests.css`, `frontend/styles/10-ui.css`, `frontend/styles/20-pipeline.css` (the three `.pnores` rules out, nothing else) |
| S server | Opus | `src/services/intake-parser.ts`, `worker/syncIntake.ts`, `scripts/fixtures/intake.csv`, `scripts/seed-intake-test.ts`, `test/intake.test.ts`, `test/syncIntake.test.ts` |
| V1 new tests | Opus | `test/helpers/requests-sortfilter.ts`, `test/requests-sortfilter-*.test.ts` (six new files) |
| V2 test updates | Opus | `test/requests-render.test.ts`, `test/requests-status.test.ts`, `test/helpers/gantt-render.ts`, `test/pipeline-sortfilter-*.test.ts`, `test/template-partials.test.ts` |
| main thread | — | eslint globals, `npx tsx scripts/generate-index.ts`, `docs/architecture/map-frontend.md` regen, frame-notes at CLOSE |

Law per layer: `frontend/CLAUDE.md`, `test/CLAUDE.md`, `specs/001-sirius-v1/requests-frame-notes.md`, `specs/001-sirius-v1/pipeline-frame-notes.md` R-pf-c/d/g/i/j/l/m. Tests first for S3 (business rule). No Trello or Sheets write anywhere in this block.

## Sequence
SURVEY ✅ → GATE (this) → Figma reader on Rex → §Frozen amended with labels/geometry → BUILD (C, H, T, Y, S, V1, V2 parallel) → VALIDATE (typecheck · eslint · vitest UTC + Manila · frontend build · every new guard revert-proven from a /tmp snapshot after an anchor commit) → REVIEW (correctness with two refuters per finding → one simplify agent) → E2E (seeded intake fixture, real pointer, footer/panel/chips measured) → CLOSE.

## §Node amendments (Rex reads 2026-09-06 — these WIN over provisional lines above)

**Filter axes — order and values (795:71554 / 795:67981)**: `REQ_FILTERS` order is **YEAR · MONTH · TYPE · UNIT · REQUESTOR · STATUS** (UNIT fourth). `REQ_STATUS_VALUES = ['For Clarification', 'For Filing', 'In Pipeline']` (the node's alphabetical order; `In Pipeline` is `STATUS_FILED`, the other two are new client literals — the tiles' own vocabulary). `reqStatusOf(r)` derives from the ONE predicate table: `Object.keys(REQUEST_SEGMENTS).filter((k) => REQUEST_SEGMENTS[k](r)).map((k) => REQUEST_SEGMENT_STATUS[k])` with `REQUEST_SEGMENT_STATUS = { filed: STATUS_FILED, filing: 'For Filing', clarification: 'For Clarification' }` (20-requests-table.js). REQUESTOR carries `scroll: true` (in-group `pmscroll`); the panel itself gets `max-height: min(912px, calc(100vh - <toolbar offset>))` with `overflow-y: auto` (D3 — the node draws a PANEL-level scrollbar, 13×872 track). Panel recipe = Pipeline's `.pipemenu` verbatim; node parity for Y to VERIFY, not re-declare: panel 276 wide, white, 1px #E2E8F0, r4; heading 12px/600/#94A3B8; row 32 tall; checkbox 16×16 r4, unchecked 2px #CBD5E1 stroke, checked #0F172A fill; label 14px/400/#0F172A; count 12px/400/#94A3B8 right-aligned; Clear button 12px/600 (#94A3B8 disabled, #1D4ED8 active). Where Pipeline's shipped recipe differs from these numbers Y REPORTS the difference and changes nothing — same component, one recipe.

**Sort options (809:87722 / 809:87697)** — eight, two groups, in this order. Group strings follow PIPE_SORTS' own casing convention (read it); the headings render as the node draws them (`DATES`, `IDENTITY`):
| key | group | label (character for character) | value / dir |
|---|---|---|---|
| `due-near` | Dates | `Deadline closest to now` | `r.deadline` asc, nulls last |
| `due-far` | Dates | `Deadline farthest from now` | `r.deadline` desc, nulls last |
| `recent` | Dates | `Recently requested` | `r.sheet_row` desc |
| `oldest` | Dates | `Oldest request first` | `r.sheet_row` asc |
| `mc` | Identity | `MC Number: Low to High` | `r._mcRank` asc |
| `mc-desc` | Identity | `MC Number: High to Low` | `r._mcRank` desc |
| `name` | Identity | `Deliverable Name: A–Z` (U+2013) | `r.name` ci asc |
| `name-desc` | Identity | `Deliverable Name: Z–A` (U+2013) | `r.name` ci desc |
Equal keys → `sheet_row` asc (S8). `REQ_SORT_DEFAULT` = `recent`'s effect, unlisted (D12); nothing is highlighted at rest (the node draws no default marker). The node's HIDDEN `PRIORITY` group (`Needs clarification first` / `For filing first` / `Furthest along`) is **NOT built** — invisible in both nodes, flagged to Miles. Selected = #0F172A fill, white text, weight 400; hover = #F1F5F9 — Pipeline's `.sortmenu .pmitem.on` / hover recipes reused verbatim (Y verifies parity, reports any difference). The sort BUTTON names its selection exactly as Pipeline's does (R-pf-f) — the node's `isLabeled=false` is an instance default, and Miles ruled the buttons identical; flagged.

**Strings**: the three Clear controls use Pipeline's SHIPPED strings verbatim — `Clear filter`, `Clear Sort`, `Clear all` (the node writes `Clear Filter`; Pipeline's own reference node writes `Clear sort` — both casing mismatches go to Miles, one recipe ships).

**Table header (809:85709)**: eleven labels `YEAR · MONTH · MC # · DELIVERABLE · TYPE · UNIT · REQUESTOR · DEADLINE · BRIEF · STATUS · FROST NOTES` = REQ_COLS order with `Unit`; widths 80/80/120/200/120/160/120/128/320/136/320 (col-runit stays 160). Every header carries an identical inactive chevron — decoration from the old select design, not a sort affordance: D6 STANDS, headers render plain, no chevron.

**Footer (809:85294 / 809:85301)** — `.reqfoot`: full table width (1784 in the frame), 32 tall, `display:flex; justify-content:space-between; align-items:center`, no background, no border, sits 16px under the table scroller (the container's own stack gap). Left `.reqfoot-range`: 14px, words 4px apart, `Showing` / `of` / `requests` weight 400 slate-500 (#64748B), the two counts weight 600 slate-900 (#0F172A). Right `nav.pager`: buttons 28×32, r4, no fill/stroke, padding 8/6, text 12px weight 400 #0F172A, spacing 0; ACTIVE `.pbtn.on` 32×32, fill #F1F5F9, 1px #CBD5E1, r4, text stays weight 400; chevron `.pnav` 28×32 with a 4×8 icon stroke #0F172A; ellipsis `.pgap` 16px weight 500. The current `.pager` recipe (34px min-width, 600 weight on `.on`, `margin-left:auto`) is REPLACED, not patched.

**Chips (795:72905 / 841:37792)**: one chip = one value, confirmed (`Type is Assets`, `Type is Lottie File` are two chips). Recipe = Pipeline's `.fchip` verbatim (30 tall, white, 1px #0F172A, r4; axis word 12px/400/#64748B; value 12px/600/#0F172A; ✕ segment 28; gap 8 between chips and before `Clear all` 12px/600/#1D4ED8). Y verifies parity. Row sits between toolbar and table as on Pipeline.

**No-results (809:111116)**: strings and offsets equal Pipeline's within 0.5px — D9's shared partial needs no Requests-specific number. Footer absent, tiles + toolbar present.

**Toolbar (809:85709)**: search 1685×54 (its own pale stroke #F1F5F9 — Pipeline's `.searchbar` as shipped), buttons 38×33 / 37×33, 8px gap, white, 1px #64748B, r4 = `.sfbtn` as shipped.

**Server (agent S, DONE)**: the parse-result type is the shipped `ParseResult` (not `IntakeParseResult`) — accepted; warn line carries the worker's `[syncIntake]` module tag — accepted.

## §Fix amendment 1 (post-BUILD, 2026-09-06) — chip hover keyed per VALUE

Found by agents T, H and C independently: `chipPop` holds the AXIS key, but a Requests chip is one VALUE (D7), so `{{#if chipPop === c.key}}` draws the hover panel under EVERY chip of that axis, and a ✕ on the hovered chip can leave `chipPop` naming a value that is gone while the axis still has others (the prune observer only checks the axis emptying).

Frozen fix (one fix agent owns all four files sequentially — BUILD is over, no parallel edits):
- `frontend/scripts/40-app-state.js`: state `chipPopValue: null` beside `chipPop` / `chipPopFlip`. Pipeline never sets it (its chips are per axis); it is NOT an overlay key.
- `frontend/scripts/90-events.js`: `reqChipPopIn(ctx, key, value)` — sets `chipPopValue` to `value` (null for None is a legitimate value) at the same moment it sets `chipPop`; `reqChipPopOut` and the shared close path leave `chipPopValue` alone (a stale value with `chipPop === null` is inert; it is overwritten on the next open). Pipeline's `chipPopIn` unchanged.
- `frontend/templates/views/20-requests.html`: the chip's hover panel renders on `{{#if chipPop === c.key && chipPopValue === c.value}}`; the hover handlers pass `c.value` as the third argument.
- `frontend/scripts/80-loaders.js`: the Requests prune observer closes `chipPop` when the open axis no longer CONTAINS `chipPopValue` (`(app.get(\`reqFilters.${open}\`) || []).includes(app.get('chipPopValue'))` false → `app.set('chipPop', null)`), which subsumes the axis-empty case. Pipeline's observer unchanged.
- Guard (V1's `test/requests-sortfilter-indicator.test.ts`, extended by the fix agent): two chips of one axis → rendering with `chipPop = 'type'`, `chipPopValue = 'Assets'` draws exactly ONE `.chipmenu`, under the Assets chip; and the executed prune closes `chipPop` when only `Assets` is removed while `Lottie File` stays.

## §Fix amendment 2 — two suites nobody owned
- `test/pipeline-warning-wiring.test.ts`: the OVERLAYS roll-call (line 69) and its regex (line 239) replace `reqMenu` with `reqSortMenu`, `reqFilterMenu`; the handler list (line 88) replaces `pickReqFilter(` with `pickReqSort(` (the Requests handler that closes a menu on pick). Assert the RULE the test states, not the old names.
- `test/pipeline-warning-dismissal-css.test.ts:40`: `.selwrap` leaves the OVERLAY_SHIELDS list (retired with the select bar); `.reqtools .sfbtn` / `.reqtools .pipemenu` join it if the test enumerates every shield.

Main thread done: eslint cross-file globals registered (13 in, 5 out); index regen. Then VALIDATE.

## §Fix amendment 3 (REVIEW round 1, 2026-09-06) — decisions for the frontend fix agent (runs AFTER the guards agent)
- **S1 stale page**: ONE clamp — new computed `reqPageCur` = `Math.max(1, Math.min(reqPage, reqPageCount))`; `reqRows`, `reqPageRange`, `reqPages`, the template's `.on` / `aria-current` / the two `.pnav` disabled tests, and `reqPageStep` all read `reqPageCur`; nothing else clamps.
- **L2 rejects-only**: `reqNoResults` also requires `requests.length > 0` — the shared empty state is for a table the READER emptied; a project with no usable rows keeps its own `.rempty` whatever is typed.
- **H2 tiles under a multi-value STATUS selection**: a tile is pressed when its value is IN the selection (`on = sel.includes(REQUEST_SEGMENT_STATUS[key])`; REQUESTS pressed when the selection is empty); dimming stays `status.length && !on`. Click rules unchanged (D4: sole value re-click clears; otherwise set exactly [value]).
- **H1 focus**: `closeMenus`' held-focus selector list gains `.pipemenu` (60-overlays.js) — Escape / pick / Clear inside any panel returns focus to its trigger, Pipeline included.
- **H5**: `applyRequestFilter` reads the segment vocabulary with `Object.hasOwn` (a foreign key → null → clears), the same guard `toggleFacetValue` already carries.
- **S2/H6 divergence copy**: the number is PLACES, with a singular: `The sheet's row order and its Year/Month columns disagree in {{n}} place{{#if n !== 1}}s{{/if}}, so Recently requested may be out of order.`
- **L1/H3 panel cap**: `max-height: clamp(280px, calc(100vh - 464px), 912px)` — a floor so the panel never collapses to zero at small viewports or high zoom; the fixed offset stays (recorded as a residual: the cap does not grow when the page is scrolled or shrink when the strip wraps).
- **S3/L3/H4 dead join**: `reqChipList(selected)` — ONE parameter; `reqChips` no longer reads `chipPop` or `reqFacets`; the chip's hover panel keeps reading `reqFacets` in the template. (Frozen-interface change, amended here.)
- **R2 / R4 guards**: execute `removeReqChip` (a ✕ removes ITS value and nothing else; an absent value is a no-op) and assert the MONTH facet's calendar order with an unsorted fixture — both in the Requests suites.
- Files for the frontend fix agent: frontend/scripts/{20-requests-table,40-app-state,60-overlays,90-events}.js, frontend/templates/views/20-requests.html, frontend/templates/partials/00-req-sync-strip.html, frontend/styles/25-requests.css, test/requests-sortfilter-*.test.ts, test/helpers/requests-sortfilter.ts, test/requests-render.test.ts.
- Server fix agent (parallel, disjoint files): **R3** — when a tab carries BOTH `Use Case` and `Business Unit`, `Business Unit` WINS regardless of column position (the ruled name; last-wins-by-position would make a rename silently pick the stale column); **R6** — the worker's warn line is built from `w.field` / `w.reason` (no hard-coded kind); **R7** — the syncIntake warning test seeds a deliverable so the join is observed and the title claims only what it proves.
- To the SIMPLIFY pass, not fixed here: S4 (REQ_COLS dead sort/val/cmp + comparators + the misleading comment), R5 (V1's helper re-declares `partialBody` / `renderRequests`), L4 (`.reqsearch` inert class + two stale comments in 20-pipeline.css), the `clearReqFilters` unconditional `closeMenus`, `.reqtools` as a hand-copy of `.pipetools`.
- To CLOSE: L5 map lines (done by the main thread), requests-frame-notes rulings 4/19/§3/Batch-5 supersessions, the owl notes (R3's comma-in-a-name warning, semicolon/slash separators unflagged, the STATUS facet summing past the row count, the one-page footer's two disabled chevrons).

### Refuter verdicts on amendment 3 (24 refuters, 2026-09-06)
- CONFIRMED (0 refutes): R2, R4 (guards), S2 (copy), L1 (cap floor), L2 (rejects-only empty state), H1 (focus return), H2 (tiles under a multi-value selection).
- SPLIT (1–1): R3 — fixed anyway as hardening (Business Unit wins by NAME, by rank not position; server agent done); H5 — the `Object.hasOwn` guard ships as hardening (unreachable from the shipped tiles, kept for symmetry with `toggleFacetValue`).
- KILLED (2 refutes): **S1 is DROPPED** — `app.observe('requests', …)` at 80-loaders.js:~450 already clamps `reqPage` on any descendant change (submitNote included); no `reqPageCur` computed is built. R6/R7 were killed as non-defects but the server agent's changes stand as improvements: the warn line is now built from the warning's fields (`intake: multi-value use_case at row N (project P)` — the frozen literal above is superseded) and the syncIntake test observes the deliverable join.

## §Simplify brief (one agent, after the review commit; any edit re-runs VALIDATE in full)
Behaviour-preserving only; every new/changed guard stays green; no interface change without stopping.
1. S4 — `REQ_COLS` drops `sort` / `val` / `cmp`; `numCmp` / `ciCmp` / `alphaSort` go if unreferenced (check `reqSortRows`' comparators and the test helper `reqCols(src)` in test/helpers/gantt-render.ts, which passes the comparators as never-called parameters — adjust the slicer, keep the header-label guard deriving from source). Fix the comment that claims the guard reads `sort`.
2. R5 — test/helpers/requests-sortfilter.ts re-declares `partialBody` and `renderRequests` that test/helpers/gantt-render.ts exports; collapse onto gantt-render's (the direction V2 named: gantt-render's `partialBody` also backs `dlCardPartial`), reconcile the stubs (real `monthShort`, icons), keep every Requests suite green.
3. L4 — `.reqsearch` class off the search field in 20-requests.html; the two stale comments in 20-pipeline.css (~167, ~209) that describe it as a modifier; the `.sfbtn .sflabel` comment's "eight labels" (Pipeline has ten) corrected.
4. `clearReqFilters` / `clearReqSort` call `closeMenus({ restoreFocus: true })` unconditionally; `clearReqFilters` is also the chips row's Clear all outside any panel — close only when a menu is open (mirror what Pipeline's Clear does; if Pipeline has the same shape, leave both and note it).
5. `.reqtools` is a hand-copy of `.pipetools` (25-requests.css vs 20-pipeline.css) — one rule `.pipetools, .reqtools` in the earlier-loading sheet if the cascade allows, else leave and note.
6. OVERLAY_SHIELDS' scoped `reqSortMenu` / `reqFilterMenu` entries have no effect (only the union OVERLAY_SHIELD is used) — correct the comment, or drop the scoping to match the Pipeline entries; do not change behaviour.
7. Comments: any comment left describing the pre-block-5 selects/header sort in the touched files.
Not to touch: 12-constants-pipeline.js, lib/, the golden tests, anything outside the block-5 diff.

## §E2E brief (after the simplify commit; real browser on the local rig)
Rig: dev server already on 3956 (`tsx watch server.js`, DEV_AUTOLOGIN) — rebuild the bundle first (`npm run build`); route `/rt-test/requests`; seed ≥25 synthetic intake rows into the LOCAL rt-test copy only (multiple years/months incl. an out-of-order month pair, 3+ types, 3+ units, 4+ requestors, a few clarified notes, one blank unit) via scripts/seed-intake-test.ts or a scratch script over parseIntake + syncIntakeRows against the local db; remove them at the end. chrome-devtools MCP quirks in the e2e-browser-rig memory (screenshot paths inside the repo, fresh snapshot after reload, orphan-profile lock).
Measure (programmatic) + screenshot + clean console: toolbar (search + two buttons 38×33/37×33, 8px gap); filter panel 276 wide, six headings in node order, counts, REQUESTOR in-group scroll, panel scroll at a 700px-tall viewport and at 200% zoom (floor 280); Clear filter disabled → enabled; sort panel eight labels/two groups, selected fill #0F172A, hover #F1F5F9, button names its selection, re-pick clears; chips: two TYPE values → two chips, ✕ removes one, six chips WRAP, Clear all; hover a chip → panel under THAT chip, slide to the sibling → panel moves; tile click ↔ STATUS axis (pressed state, two values ticked → two tiles pressed); footer 32 tall, "Showing 1-10 of N requests", active page 32×32 bordered, siblings 28×32, one-page project shows the footer; no-results: type gibberish → shared partial, footer gone, tiles stay; rejects-only state untouched; divergence sentence present with the out-of-order pair ("1 place"); keyboard: Escape from inside a panel returns focus to its button; Unit column header + a comma value drawn whole. Pipeline regression: its filter/sort/chips/no-results unchanged (one screenshot each).
