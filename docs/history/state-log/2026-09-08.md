# 2026-09-08 — Block 6: the lane-state table, start-day Deadlines, the September rulings, the acknowledgement deletion

## Morning — the queue after two idle days

Seven owls from Miles had landed on 7 Sept: #81 (Pipeline DEADLINE plain text,
§4.4 withdrawn), #82 (lane mapping unblocked: 146 lanes, exclusion by named
lane, retire the keyword classifier), #83–#85 (build spec v1.3 in three parts,
"supersedes v1.2 entirely"), #86 (quote bar amber against the node; header sort
stays retired; missing difficulty closed), #87 (delete the parked §6.3 server
half). Read once by a Sonnet reader into the scratchpad; the main thread never
opened the inbox bodies again.

JP's rulings (one reply, then five answers): block 6 opens with the lanes, the
start-day placement and the four display rulings; the acknowledgement machinery
is deleted with the stored rows ARCHIVED, never dropped; the ARES team is asked
for a board-lists read route; the questions owl goes to Miles. On the five
questions: the lane names come from ARES ("that's the right source of truth");
OPEN WORK — ask; missing difficulty is handled at ingestion on the ARES side;
W4 — JP doesn't know, ask; the day-capacity paragraph — ask.

## Survey (seven Sonnet readers, ~700K tokens, reports on disk)

Findings that shaped the plan: one choke point for the classifier
(`classifyList`, nine call sites) and an unrelated `lane` bucket in
`lib/model.ts` (naming trap); §7a's "146 = 8+16+112+15" does not reconcile —
48 Screens/Assets/Motion lanes are pattern-only and three Ongoing lanes are
unnamed; ARES's external API has no lists route but its internal
board-discovery has the data behind session auth; §6.2 is one key change in
`dlBuild` (rollover already matched the worked example); the §4.4 warning
feature was fully live; migration 007 imports helpers from the file #87
deletes; v1.3 §6.3 says "parked" while #87 says deleted.

The ARES inventory (local `ares` Mongo: 22,328 cards, 12 boards; the test
board only in `sirius`): 79 names on hLL7WW2V (35 seen only in movement
history), 14 on tx8gDsTH; 20 names no spec rule reached — an undocumented
Generation/Refinement process family (398 cards), five `Backlog: …` sub-lanes,
the `➜ Ready for X` anchors (a shape the first regex missed), five ad hoc lanes
(`For Archive`, `For Client Approval`, `Hard Deadline: Monday Mar. 23`, `NOTE`,
`On Hold: Ryse, NBG`). ARES stores no empty-list inventory anywhere.

## Build (agents L, D, W, A in parallel; R ripple; X docs; F fixes)

Anchor `28b6080` (69 files, +3555/−4068). Frozen interfaces held; deviations
were files nobody owned (the ripple list) and the Pipeline STATUS cell, which
renders the list NAME not the state — the `excluded` rule was amended to a
neutral `.pbadge.s-excluded` with the filter untouched (#82: "unaffected").
Resolution order frozen: exact table → `➜ Ready for` → family-stage for ANY
family word → `^backlog` → unknown Ongoing (logged in
`sync_runs.stats.unmappedLists`). Done and Excluded only from the table.

## VALIDATE

First run red on two leftovers (three panel-suite lines W had listed, a MAP path
of a deleted suite) → `547effd`. Proofs: 47 named guard failures over 24
mutation passes; one vacuous guard (L's table-before-rules ordering, recorded);
tree byte-identical to HEAD after. Then 78 files / 1485 tests green in both
zones.

## REVIEW (5 lenses, 24 findings, 2 refuters each, 53 agents, 3.4M tokens)

10 confirmed · 4 disputed · 10 refuted. Rulings at the main thread →
`81000d0`: Deadlines skipped excluded rows only on the server — `dlBuild`
drew and counted them (S-1/D-1, high); the sync's span walk dropped every move
into an excluded lane, nulling STARTED/DONE for ops cards and shifting design
cards that passed through ops (S-3) — now any non-pending lane anchors STARTED,
`Operations Backlog` the named exception, DONE only from a Done lane; rollover
re-homed a rolled row by FINISH while the card's day became its START (D-2) —
membership follows the new start, R-d2-q rewritten, supersedes #75 §2; the
excluded card was refused with the done copy (S-5) — `CARD_EXCLUDED` with its
own words, client map included; two regex-anchor guards (T-2/T-3); comment and
doc drift (F-2/3/4, A-1/2/3/4/5, D-5). Refuted and left: T-1/T-4/T-5 (frozen
test scope), S-2 (`On Hold: Ryse, NBG` now ongoing — by rule), S-4, D-3, D-4,
F-1, A-3/A-4 as scoped (both fixed anyway, one line each).

Simplification (one agent): four changes, net −3 lines; seven skipped with
reasons (module cycle, pinned names, verbatim migration claim).

## E2E (local rt-test copy, real pointer, `fe1089f`)

All six checks green, no defect. Pipeline: four tiles (the fourth hides at
zero), no warning node anywhere, the work-row DEADLINE cell byte-equal in
element, class and computed style to STARTED, `.dimcell` em-dash when empty,
the staged `Discarded Work` card in a slate `s-excluded` badge; the filter-chip
hover panel opens on hover, closes on pointer-out and outside click, survives
the 4px gap. Schedules: a card added by real search + Add and plotted by real
pointer (`startsOn 2026-09-07`, finish 09-08); the excluded card absent from
search and `addable`, a direct POST answered 409 `CARD_EXCLUDED` with the ruled
words; its pre-existing row kept the neutral list-name chip. Deadlines: the
card in Monday's column (its START; Tuesday, the finish, empty); week load
`1 / 96` (rt-test capacity is 96); the excluded plotted card not drawn, not
counted; the quote bar `rgb(217,119,6)` = `--amber-600`, `isPointInFill` true at
4px. The four withdrawn routes 404. Console clean on all three tabs. Cleanup
proven: three sprint items deleted through the audited route, `current_list`
and the one staged `urgency` restored in the LOCAL mirror only; the audit log
holds only `sprintItem.add/plot/remove` — no Trello write of any kind.
Screenshots: `.claude/e2e-block6/` (untracked).

## Sent and received

- jp→miles **#66** (JP's yes): three questions (OPEN WORK, W4, §6.2 day
  capacity); answers (classifier reads the LIST; held copy was v1.2 without
  §7a; header sort already retired; start day built); lane names from ARES;
  missing difficulty handled in ARES.
- Sirius→Ares **#11** in `../owl/` (JP's yes): open the board-discovery lists
  to the read key, or a `/api/v1/trello/boards/:id/lists` route.

## Decided without JP (all reversible)

The OPEN WORK tile removed (5 → 4) · the orphaned `/deadlines` routes and
`lib/dayplan.ts` leave with the acknowledgement half · archive by collection
rename, refusing the both-present case · the fourth-write (W4) ask held until
Miles answers · the five ad hoc lanes left unknown on purpose · the
`Generation`/`Refinement` family classified Ongoing by rule · sprint membership
by start day after a roll.

## Not built, deliberately

§6.2's day-capacity paragraph · an ingestion-health surface for missing
difficulty (ARES's side per JP) · W4 · any ARES call for lane names.

## Ship (JP: "deploy and send message to miles")

Pushed `f1db33b..2359a65`; `./deploy.sh` green (suite 78 files, build, rsync,
`npm ci`, migrate, pm2 restart). Live: healthz 200 ×3 through the host's
`APP_BASE_URL` (0.16–0.38s), API wall 401, `s-excluded` and `plaincell` in the
served bundle, the only `OPEN WORK`/`warnbtn` strings left are CSS comments
recording the withdrawal, the acknowledge and `/deadlines` routes 404, pm2
`sirius` + `sirius-worker` online since boot with no error, the worker ticking
(ARES sync every 15 min, calendar loaded). Migration
`011-archive-conflict-acknowledgements` applied on the host —
`conflict_acknowledgements_archive` holds 0 rows (production never stored an
acknowledgement), `conflict_acknowledgements` is gone. Standing smoke: the
urgency round-trip on the TEST board (set → present, unset → absent) green.
Owl **#67** sent to Miles; **#81–#87 acked**. Main is level with origin.

## Evening — JP's fine-tunes (committed, deploy HELD on JP's word)

- `540c2dd` — Sprint Schedules add panes: search pane pads 16 all round (was
  16 / 24 / 15 / 16, surface-5), result pane 16 vertical / 18 horizontal (was
  the node's 29px inset). Browser-measured on the local rig: 16/16/16/16 and
  16/18/16/18; the search pane renders 76px tall.
- `9855e09` — the status cell's right inset 16 (the global border-box reset
  keeps the five-column sum at 952).
- `2aa1177` — **the "Non-Urgent" chip leaves the Deadlines card and the
  Sprint Schedules row** (JP: a read-only chip stating an absence is noise;
  Urgent shows only when urgent). The Pipeline urgency BUTTON keeps both
  states — it is the write control. Two dead rules removed, five guards
  flipped and proven from /tmp snapshots, R-d2-m and sprint rule 57 amended.
  Fresh-page browser check: no `.gub.nonurgent`, no "Non-Urgent" text, the
  Pipeline control intact in both states. (A first measurement on a page
  loaded before the rebuild reported a stale chip — the served bundle never
  carried it.) Product's card node draws the chip; Miles to be told with the
  next owl as JP's ruling.
- Full validate after each: 78 files / 1493 tests, both zones. Main is three
  ahead of origin; nothing pushed, nothing deployed.
- **DEPLOYED** late evening on JP's "push and deploy": `8b35369..13159b9`
  pushed; `./deploy.sh` green; live healthz 200 ×3, API wall 401, the served
  bundle carries the 16 / 18 result-pane inset and no non-urgent chip class on
  either surface while the Pipeline control keeps both states; pm2 web + worker
  online since boot with no error; urgency round-trip smoke on the TEST board
  green. Miles to be told of the chip ruling with the next owl.
