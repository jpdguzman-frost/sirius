# 2026-08-30 — Pipeline no-results state (owl #76)

Full-mode workflow, Light-sized build. Survey: frame 748:18444 verified
from nodes (the "slightly above centre" placement is asymmetric padding —
64 top / 180 bottom around a centred 12px-gap column; headline 32/700
deliberate over the house 600, Miles's build-what-the-frame-holds).
Drift: the state was entirely NEW — before this, empty results rendered
headers over nothing. One build agent (30-pipeline.html, 40-app-state.js,
20-pipeline.css, pipeline-sortfilter suite): computed `pipeNoResults`
reads the chips' own derivation (pipeChips) so state and chips cannot
disagree; the whole .pscrollwrap block (thead included) swaps for
`.pnores`; copy verbatim, no term echo, mustache-free block; all five
type/colour/spacing values resolved to existing tokens exactly.

Decided without JP (reported, vetoable): fresh-empty (zero rows, no
search, no filters) keeps the table — the state's advice would point at
controls that can't help; UNATTACHED tile stays clickable (tiles describe
the project).

VALIDATE surfaced 4 context-architecture reds, all housekeeping: the
2026-08-28 test-board script was never indexed (maps regenerated, purpose
filled) and STATE.md was 158 bytes over cap (compressed). Then 63 files /
1102 green dual-TZ. Five revert proofs bit (copy, 700, swap gate,
fresh-empty, chips-source); proof 2's first sed missed and was caught by
the pattern-hit check, re-aimed, bit.

REVIEW (adversarial, single pass): ONE confirmed finding — the
state→table remount re-drew the horizontal slider from stale pipeThumb
over a fresh node at scrollLeft 0 (no scroll event fires on remount; the
selectTab seam's own never-stale rule). Fix: observe('pipeNoResults') →
remeasure() (already frame-deferred) in 80-loaders.js + guard; full
dual-TZ re-run 1103 green; proof 6 bit. Reviewer verified the build
agent's degrade claims (null-safe hidden scroll machinery) and cleared
truth-table edges incl. whitespace-term+filter and project-switch
coherence (searchQ deliberately survives the switch, per #62 scope).

E2E (real browser, fixtures): search-only / filter-only / both all show
the one state — 32px/700/#0f172a head, 24px/#64748b sub, 64/64/180
padding, min-height 606, no fill/border, no thead, metrics + term +
chips row (with Clear all) surviving; return seam restores thead and a
re-swept thumb {needed:false,left:0,width:100}. Console clean but the
pre-existing favicon 404. Screenshot sent to JP.

Commits: b14ef20 build · (review fix) · docs. #76 acked. Deploy waits.

Flake note (deploy gate, 09:17): one red in the gate's full run, gone on
the immediate full re-run (1103/1103). Same environmental class as the
2026-08-28 gate flake; recorded, not masked.

SHIPPED: JP's go 09:16. The gate red hit TWICE consecutively (same
1-of-1127 count each time) then cleared on the third, instrumented run —
suite green inside the gate, deployed; the failing test's name never
surfaced (tail cut it twice, third run passed). If it recurs, capture
the gate log FIRST. Live verified: healthz 200×6, bundle carries
pnores / both copy strings / pipeNoResults / the remeasure observer.
sirius + sirius-worker restarted (uptime 0s).
