# 2026-09-06 — Block 5: Requests filter + sort, UNIT, Recently requested, the footer, one no-results (owl #77 §1–4)

Fresh session per the 2026-09-05 token rules; resumed from MAP.md + STATE.md.
Full mode. JP: "go, rex connected on 7782" after the gate.

SURVEY: four Sonnet readers (Requests frontend, Pipeline filter/sort machinery,
server + data, law + tests; block 4's drift table reused for the standing
rulings) → 47-row drift table. Figma: three Sonnet readers over eight nodes
through Rex, geometry from nodes only. The nodes gave what the prose lacked:
axis order YEAR·MONTH·TYPE·UNIT·REQUESTOR·STATUS (UNIT fourth); eight sorts in
DATES / IDENTITY (Recently requested, Oldest request first — no Urgent/Hardest);
STATUS lists For Clarification · For Filing · In Pipeline (Filed absent); footer
page buttons 28×32 with a 32×32 #F1F5F9/#CBD5E1 active page; one chip per value
confirmed; no-results offsets within 0.5px of Pipeline's. Twelve node findings
(hidden PRIORITY sort group with a second selected row; the sort button
`isLabeled=false`; `Clear Filter` greyed in the checked state; a stray MONTH
placeholder row; mock page numbers; casing mismatches on the Clear strings).

GATE (JP "go"): PLAN.md with D1–D12 — D1 UNIT is a label (storage/wire keep
`use_case`, both sheet names aliased); D2 STATUS is a three-valued FILTER axis
over the two-valued badge (Batch 5 untouched); D3 viewport-capped panel scroll
+ REQUESTOR in-group; D4 the tiles drive the STATUS axis; D5 client-side
divergence count → one sync-strip sentence; D6 header-click sorting retired;
D7 chips per value on Requests, per axis on Pipeline, one recipe; D8 footer at
one page too; D9 one noResults partial, `.pnores` CSS moved to 10-ui.css; D10
the filter-group partial dispatches by table (`toggleFacet`); D11 None derived
on every axis but STATUS; D12 the default order is unlisted, `recent`
reproduces it. Four mock-vs-ruling conflicts called out (panel scrollbar vs
#49/F9; three STATUS values vs Batch 5; per-value chips vs R-pf-l; a sort panel
beside owl #18's header sort).

BUILD: S (server, Opus) ran first — alias `business unit` → `use_case`,
`ParseResult.warnings`, one worker warn line, fixture header moved. Then six
agents in parallel with exclusive files (C constants+state on Fable; H
handlers, T templates, Y styles, V1 new tests, V2 test updates on Opus). Two
frozen names the agents stopped on: `REQ_SORT_GROUPS` (C exposed it, derived
like PIPE_SORT_GROUPS) and the chip hover keyed on the axis while chips are
per value → fix amendment 1 (`chipPopValue`, `reqChipPopIn(ctx, key, value)`,
containment prune) and fix amendment 2 (re-entry from a sibling chip moves the
panel; the shared door untouched). Two unowned suites pinned retired names
(`reqMenu`, `pickReqFilter`, `.selwrap`) — re-aimed at their rules. One
Pipeline guard had been slicing the FIRST `.fchips` in the composed template,
which after this block is Requests' — re-aimed at `tabView('pipeline')`.
Main thread: eslint globals (13 in, 5 out), `generate-index`, map purposes.

VALIDATE 1 (Sonnet runner): one red — the generated map carried `TODO:
describe` for the new partial; filled; green. Anchor commit `f6eb941`:
83 files / 1478 tests + 24 todo, UTC and Manila; NY calendar suites; index
--check. Proofs (Opus, /tmp snapshots, never git checkout): 62 mutations
across 43 rules, 56 bit, six GREEN — D10's dispatch table (both guards read
text), the open chip's `table`, the panel cap (property name only), re-pick =
clear, MONTH `monthShort` normalisation, sort-before-paginate (fixture already
in sheet order). Runner caveat found: vitest collected the finders' worktrees
under `.claude/worktrees/` → `vitest.config.ts` now excludes `**/.claude/**`.

REVIEW: four Opus finders in isolated worktrees → 22 findings (7 dedupe to
simplify/docs), 12 correctness claims → 24 refuters (2 each, budgeted) →
7 confirmed, 2 split, 3 killed. Killed: S1 (stale page — the `requests`
observer already clamps), R6/R7 (the frozen text; the server changes kept as
improvements). Confirmed and fixed: tiles under a multi-value STATUS selection
(H2 — pressed = membership), a rejects-only project showing the shared empty
state (L2), focus dropping to body from inside any `.pipemenu` (H1 —
`closeMenus` held-focus list gains `.pipemenu`, Pipeline benefits), the
prototype-key tile lookup (H5, `Object.hasOwn`), "1 rows" (S2 — "in N
place(s)"), the panel cap collapsing at high zoom (L1 — `clamp(280px, …,
912px)`), the dead facet join on chips (S3 — `reqChipList(sel)`, `reqChips` no
longer recomputes on hover); split R3 fixed as hardening (Business Unit wins
by NAME when a tab carries both headers, by rank never by column). The six
vacuous guards plus R2 (chip ✕) and R4 (MONTH calendar order) now execute the
shipped code; the overlay roll-call reads the OVERLAY_KEYS declaration.
Every fix seen red first (10 reds across four files). VALIDATE 2 green:
1494 tests. Commit `5d88db4`.

SIMPLIFY (one Opus agent): REQ_COLS is `{cls, label}` (29 dead properties,
two comparators gone); the Requests test harness collapses onto
gantt-render's (the two copies had already drifted on `chipPopValue`);
`.pipetools, .reqtools` one rule; the inert `.reqsearch` class and three
stale comments gone; `clearReqFilters`' unconditional close left — Pipeline's
Clear has the same shape. 179 lines out. VALIDATE 3 green (one UTC loopback
flake, green on re-run and in Manila — rule 5, recorded). Commit `98b8433`
with the Requests frame-notes Batch 6 (R-rq-a..k: rulings 2, 6, 8, 18, 19
superseded; Batch 5's badge kept).

E2E: one Opus agent, real pointer, chrome-devtools MCP on the running 3956 dev
server; 30 synthetic intake rows (MC-9901..9930, sheet rows 9001..9030, one
inverted month pair, a comma unit, blank units, three clarified notes, four
deliverables for In Pipeline) inserted DIRECTLY into the local rt-test copy
and deleted after (intake back to 0; screenshot 15 shows the project's own
empty state). 24 measurements, 22 interactions, 15 screenshots under
`.claude/e2e-block5/`, console clean but for the pre-existing favicon 404.
Green: toolbar recipe, six axes in node order, counts (STATUS 3+26+4 over 30 —
the D2 union), MONTH in calendar order, the comma unit whole in cell / facet /
chip, the 280px floor at 1400×460 and 800×500@2x, Clear filter grey→blue,
eight sort labels with U+2013, selected/hover recipes, the button naming
"Dates: Oldest request first", re-pick clears, per-value chips, ✕ removes one,
six chips wrap at 760, hover panel under the hovered chip and MOVING to the
sibling, Clear all, Escape returns focus to the button on BOTH tabs, tiles ↔
axis (TO FILE → chip; + For Clarification → two tiles pressed), no-results
with the footer gone and tiles present, the one-page footer (screenshot 03 for
JP), "disagree in 1 place", pager paging, Pipeline's panels/chips/no-results
unchanged. TWO DEFECTS: (1) HIGH — inside the capped panel the REQUESTOR
in-group scroller collapsed to 8px (a flex child scroll container shrinks to
zero under a max-height) — its five values were unreachable; fixed with
`flex: none` on the scroller inside `.reqtools .filtermenu` (+ guard);
(2) LOW — pager chevrons 30×32 not 28 (the transparent 1px border on top of
6px padding); fixed by padding (+ guard). Re-verified: the REQUESTOR group holds 168px inside the capped panel at 1600×1000 and at the 280px floor (a DOM probe with four extra rows scrolled inside the group while the panel kept its own scroll), a real click on a requestor value ticks it and draws its chip, chevrons 28×32 with the numbers 28 and the active page 32; data back to baseline (intake 0, deliverables 8).
Parity notes for Miles, not defects: `.sfbtn` is 40×33 on both tabs (node
38/37); panel-item hover is slate-50 #F8FAFC on both tabs (node #F1F5F9).

CLOSE: STATE.md, this log, PLAN.md rotated to
`docs/history/state-log/2026-09-06-plan-block5.md`, owl draft for Miles in the
scratchpad — sent only on JP's yes. NOT deployed — JP's word. Three untracked
items appeared at 08:45 (`.agents/skills`, `.codex/config.toml`, an `AGENTS.md`
copy of the constitution) — not this session's, left uncommitted, flagged.

Agent spend (subagent tokens as reported): survey 0.48M · figma 0.48M ·
build 1.34M · fixes 0.44M · proofs 0.19M · finders 0.89M · refuters 1.20M ·
guards 0.16M · simplify 0.22M · validate ×3 0.15M · E2E 0.28M + fixes 0.09M — ~6.4M in all.
Main thread stayed under the 250K rule by delegating every heavy phase.
