# 2026-09-09 — Block 7: plotting guardrails on Sprint Schedules (day grain, the three guards, the pointer bar drag)

## The brief (JP, evening of 2026-09-08; approved plan built today)

JP's four rulings from a screenshot of an unplotted row: a row cannot be placed
outside its sprint's dates; a row cannot START after its deadline (a finish past
it stays allowed and red — already shipped); placement is at DAY grain and a
placed bar can be dragged by day; the red past-deadline bar needs no change.
Survey (three Sonnet readers, reports in `.claude/block7/`), drift report and
`PLAN.md` were produced last night and approved this morning ("i've approved a
plan for development. proceed"). Mode: Full. Anchor `e3a1765`.

Drift that shaped the plan: nothing enforced either rule (`test/schedule.test.ts`
accepted 2028 on a 2026 sprint); placement was week-grain via `weekAtX` while
the bar geometry was already day-indexed (60 units, 18.4px/day); drag had been
removed 2026-08-28 and `bars-footer` asserted its absence; `gantt-rules.md` §1
still documented the deleted HTML5 contract; `starts_on` accepted any calendar
date (holidays included); rule 30 said membership was derived (false since #72).

## Build (S server · G client in parallel, X rulebooks after; one workflow, 3 agents, 620K tokens)

- **S** — `plotIssue({ sprint, startsOn, deadline, calendar })` in
  `sprint-items.ts`: sprint range (inclusive) → start after the deadline → not a
  working day (weekend, or a holiday on the ACTIVE `lib/calendar.ts` set the
  ARES sync loads). Wired into `PATCH /sprint-items/:id` and the single
  `POST /sprint-items`, 422 in the house envelope, nothing written or audited on
  refusal. Rollover untouched, never calls it (§6.2 exemption written beside the
  validator). Long date = the codebase's existing "16 Sep 2026" form.
- **G** — `dayAtX` beside `weekAtX`; `plotDay` replaces `plotWeek`; the hover
  slot one unit wide; `placeable(row, day)` gates the affordance; a placed row's
  coloured run is a pointer drag source (`barDragStart/Move/End/Cancel`,
  `dragRow/dragDay/dragLeft`, `.dragging`/`.refused`, Escape cancels, no HTML5
  drag, no ghost); `drag-hittest` enumerates `.gitem` as a real source again.
- **X** — rule 30 STORED; R9-d the START day; §10 the guards; gantt-rules §1–§2
  the pointer contract; deadlines R-d2-p rollover exempt; maps regenerated.

Build-time deviations (drift.md D1–D7): the batch add has no `starts_on` at
all (`.strict()`), so PLAN's per-item batch guard had nothing to guard; the 422
body is the house envelope; FRONTEND_SHARED grew by three names; a bare
`sprint_id` move was left unjudged (S's call — overturned at review); gantt-rules
§1 renumbered 1–11; `plusLeft` pins a pre-window day to 0.00.

Anchored: `98aaff4` server · `aee7de4` client · `e8da4a2` rulebooks.

## VALIDATE 1 (one Opus agent, 105K tokens)

tsc, eslint, full suite UTC + Manila (1555), 12 calendar suites New York,
map check, byte caps, all green; no flake appeared. 26 neuters over 24 named
guards, none vacuous, restored by `cp` from `/tmp/b7-validate/`; tree
byte-identical. Two observations passed to review: the drag-commit condition's
three clauses share one test; the non-date shape test leans on the range guard.

## REVIEW pass 1 (4 lenses, 20 findings → 16 deduped, 2 refuters each, 36 agents, 2.26M tokens)

Confirmed (6): R10-b's wire shape/route count; the drag preview bypassing
MIN_GRAB's final-column slide (bar jumps 5.5px on mousedown at the last unit,
overflows the track) plus, merged by dedup, a lost mouseup leaving the drag
armed; gantt rule 9's listener wiring; the `+` wider than its column and clipped
at both ends; Escape mid-drag wiping the add-search query; rule 55's orphaned
citation.

Split 1-1 (8), resolved at the main thread: **editing a sprint's dates strands
plotted rows** — pre-existing, QUEUED for JP; **D5 bare sprint move** — STRICT
(judged against the target's range only; rollover re-files rows itself via
`sprintFor()`, so S's stranding reason covered a narrower case than claimed);
`plotHover` not gated mid-drag — fix; **no grab offset** (the bar's left edge
teleports under the pointer) — fix, `dragGrab` added to the frozen interfaces;
rule 30's cross-sprint gesture, rule 5's holiday claim, the dropped
`.gweek`/`.gtrack` hit-test law — fix (X); `weekAtX` callerless — to
simplification.

Refuted (2): a rollover deadline twin test (the source guard + §6.2 suite cover
it); the reload-after-write banner shape (pre-dates the block, same as
`plotPlace`). 68 clean items recorded. The holiday source in the request
process was attacked and held: the server loads the ARES set.

`PLAN.md` amended (two new client interface items `dragGrab` + `barLeftAt`,
the drag hygiene rules, the `+` one unit wide, D5 strict, `weekAtX` deletable).

## FIX + SIMPLIFY + RULEBOOKS (4 agents, 660K tokens)

- **S** — `sprintRangeIssue()` factored out of `plotIssue` (one range rule, two
  callers); a bare move of a PLOTTED row is judged on the target's range;
  unplotted rows move freely; `http-api.md` states it.
- **G** — `barLeftAt(row, day)` (itemBar's clamp; `itemBar` now takes its left
  from it), `dragGrab`, `buttons === 0` cancels, capture-phase Escape with
  `stopPropagation`, `plotHover` inert while `dragRow` is set, `.gplus` one
  unit (18.4px circle — visual call for E2E/JP). The drag handlers are now
  EXECUTED in tests against a modelled keydown order, not only source-pinned.
- **SIMPLIFY** (one agent) — `weekAtX` deleted with its ten-test suite and a
  no-return guard; `barWidthUnits` one owner of the run's width; `findPlotSprint()`
  folds three identical sprint reads; `nextSprint()`/`withHoliday()` fixtures.
- **X** — R10-b/R10-f, rules 4/5/9/18/30/55, the `.gweek` hit-test law restored,
  `weekAtX` retired from the map. Caps: 19842 / 18988 / 12344.

Anchored: `5ffeb29` server · `2a689ea` client · `dd1ec29` rulebooks.
Fix-round deviations D8–D12 in drift.md.

## VALIDATE 2 (one Opus agent, 139K tokens)

Nine checks green (one loopback flake in `sprint-items.test.ts` under Manila:
green alone and on the full re-run, recorded). 17 neuters over the fix-round
guards, none vacuous; the `before.starts_on` clause is defended by the type
system (removing it fails `tsc`, not a test — the semantic neuter fails one).
All 13 fixed findings re-executed from the finders' probes: closed. Tree
byte-identical to `dd1ec29`.

## E2E (local rt-test copy, real pointer, one Opus agent, 226K tokens, `.claude/e2e-block7/`)

Thirteen scenarios, twelve pass, one unproven: Escape held MID-drag — no
available tool can hold a mouse button across a call (chrome-devtools' drag is
atomic; claude-in-chrome's `computer` has no mouse-down/up), so the mid-drag
branch rests on the executed handler tests in `bars-footer` (:614/:682/:864);
the no-drag branch was proven in the browser. Everything else by real pointer
through injected anchors at unit centres: the slot 18.3984px vs 92/5 = 18.4;
aria-label "Place the bar on 9 Sep 2026"; no + before Sep 7 / after Sep 18 /
after a staged deadline, the deadline day itself offered; no jump on mousedown
(3.33% → 3.33%), grab offset −2 held, +2 units → `starts_on` 2026-09-11 and
FORECASTED 16 Sep 2026 agree; drop past the deadline `.refused`, no PATCH,
snap back; another row inert mid-drag; far right pins at unit 55.998 = 60 −
width, right edge −0.01px; late bar rgb(220,38,38) = the legend swatch; the
five direct PATCHes byte-exact (en dash confirmed), the strict bare move
naming E2E-B's range, the unplotted row moving freely; a server refusal through
the UI (stale client copy) shows the server's sentence and restores the row;
the + one unit at units 0 and 59, unclipped; console clean. Cleanup proven:
3 items deleted (incl. the block-6 leftover MC-905), mirror dues restored,
Trello-write audit kinds unchanged (7/6/2), sprints as found.

Visual calls for JP (no defects): the 18.4px + reads as a dot, not a button;
a refused preview on an already-red late bar differs only by wash; out-of-range
days give no hint (parked by PLAN); the + covers the deadline tick on the
deadline day; the start date lives only in the bar's title (no START column).

## Decided without JP (all reversible)

- The batch add keeps refusing a day outright (it never took one).
- The 422 message uses the codebase's "16 Sep 2026" long-date form.
- A bare sprint move of a plotted row is judged against the target's range.
- The dragged bar keeps its grab point (`dragGrab`).
- `weekAtX` deleted (no caller).
- The sprint-shrink case (editing a sprint's dates so plotted rows fall
  outside) is QUEUED, not built — pre-existing, JP to rule.

## Not built, deliberately

§5.2's multi-row relative-shift and cross-sprint drag · dimming of
out-of-range weeks · hiding holidays on the client (the server refuses them) ·
keyboard nudging · any change to rollover · batch placement.

## Tokens

Subagents ≈ 4.0M (build 620K · validate 105K · review 2.26M · fix+simplify+X
660K · validate 2 139K · E2E 226K); 82 agents; the main thread read only
verdicts and the finding tables.

## Ship (JP: "yes / yes")

Pushed `e3a1765..434ffc1`; `./deploy.sh` green (suite, typecheck, build,
rsync, `npm ci`, migrate "up to date", pm2 restart of `sirius` +
`sirius-worker`). Live through the host's `APP_BASE_URL`: healthz 200 ×3
(0.12–0.28s), API wall 401, the served bundle carries `barDragStart`,
`dayAtX`, `barLeftAt`, `placeable`, `ghovcell`, `.gitem.refused`; `plotWeek`
gone, `weekAtX` only inside its retirement comment. pm2 both online, 0
unstable restarts, the error log untouched since 13 Aug, the worker's first
rollover tick "0 moved, 0 skipped" over 2 projects. Standing smoke: the
urgency round-trip on the TEST board (set → present, unset → absent) green.
Owl **#69** sent to Miles in plain words (the three rule changes, the drag,
what stays, the spec notes, the queued sprint-shrink question). Inbox: only
#80 §1/§3 still waiting on Miles. Main level with origin.

## Agent backlog (rotated out of STATE.md today to hold its 10KB cap; unchanged by block 7)

- **Agent backlog**: next split candidates (files >50KB read whole by agents): `test/helpers/gantt-render.ts` 147KB · `frontend/styles/20-pipeline.css` 54KB · `90-events.js` 47KB · `40-app-state.js` 44KB · the single add's own sprint-gone window (same shape as the batch's S1, narrower) · Enter as Add All (suggestion to Miles) · arrow keys through the Pipeline popovers (owl #62, never built) · Trello list order for STATUS needs an ARES list position (own slice, R-pf-e) · the setter's popover dismisses on a sideways gantt scroll (parity with Pipeline, review R4-2) · focus drops to body after the setter's reload (the badge-write class) · a rot alarm for FRONTEND_SHARED (ALT-6) · the rollover's `skipped` counts rows nobody looked at (ALT-7) · Pipeline's work-row DEADLINE keeps the datefield box look (ask Miles) · the local rig carries a fixture `ares` sync row · `pipeWorkLive` → PIPE_WORK_FILTERS once eslint lists it · badge writes drop keyboard focus after the reload · W1 has no
  server no-op guard (contract rule 4, pre-existing) · T075 AC sweep · non-member 403 check · `Last Synced`
  browser-TZ + col-done width · schedules-tab tokenization beyond the planner ·
  per-tab URL sub-state (raised to Miles in #40) · `worker/CLAUDE.md` unwritten
  (highest-consequence path) · pipeline/requests rulebook extraction from their
  frame-notes (Layer-2 law until extracted — `docs/README.md`).
