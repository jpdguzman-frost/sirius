# 2026-09-11 — block 9 set up, then held

Early: the post-deploy watch on block 8 closed (rt-837's first full sync
with the lane check, 00:33 Manila — see the 09-10 log). Block 9 set up: a
self-contained brief at `.claude/block9/brief.md` (the five rulings, the
sources, the code map by layer, the survey plan, the gate artifacts, the E2E
checks); STATE.md's row points at it; to run in a fresh session (token
discipline).

**JP: "compact the conversation first. hold the build new specs came in."**
Block 9 HELD before its survey: product sent new specs (the doc set 3/5–5/5
and/or the swept BRD / a spec v1.4 — the fresh session reads them via the
owl MCP first, caches to `.claude/owls/`, then re-checks the brief's
rulings against them). The brief carries the HOLD banner. Nothing built.
The new specs are Miles **#99–#113** (13:32–13:59Z): BRD v3.0 (#99–#102),
build spec v1.4 (#103–#108), engineering swept (#109–#111), security
readiness (#112–#113) — the full set. A delegated reader caches every body
and a digest under `.claude/owls/2026-09-11-*` for the fresh session; JP has
not read them; the held copy stays v1.3 until JP adopts v1.4.

## Digest of #99–#113 (`.claude/owls/2026-09-11-digest-99-113.md`; assembled docs in `.claude/specs-inbound/`, 189KB)

- **BRD v2.2 → v3.0** (50 corrections): the registry is four writes (W4); BR-10's
  keyword classifier retired — **replaced by "the ARES lanes-endpoint state
  model"**; the forecast **keys on the work-type label, not lane** (BR-2/BR-4/
  FR-7.3); review time retired (BR-1b); the work card is the scheduled unit;
  BR-6 conflict detection withdrawn for counts; tiles never filter.
- **Build spec v1.3 → v1.4** (22 corrections, 11 amendments; two reversals):
  §8 tiles rescope to the filter; §5.1b week-grain placement only (reverses
  block 7's shipped day placement); new §4.0 tile set, §4.3a W4 (server rule
  only, UI unspecced), §6.5 day-drag un-retired on Deadlines, §5.3 re-dating
  → Outside any sprint, §7.1 work-type axis, **§7a "superseded by the ARES
  lanes endpoint"**; §5.6 carried the retired keyword classifier (fixed).
- **Engineering (swept)**: schema re-based on the work card; SQL is a
  proposal (Postgres) — two structural errors self-flagged; Sirius is Mongo.
- **Security readiness (swept)**: the go/no-go gate no longer demands a
  signed DPA + Mynt vendor assessment for v1; the write-scope test covers
  all four writes.
- **Block 9**: v1.4 §5.1b/§5.2/§5.3/§6.2/§6.5 state JP's accepted rulings
  (#88–#90) with NO contradiction; the shipped block-7 code is what is stale.
  One wording drift: v1.4 says "starts on the Monday of that week by default"
  where #89 §2 says the first WORKING day — the owl wins (JP's yes).
- **Two conflicts with what is built, for JP before block 9**: (1) the
  forecast AXIS — product's docs say work-type label; block 8 reads Ares's
  per-LANE cells (lane × difficulty, lane from the label FAMILY fold) per
  JP's Q1/Q2 yeses; (2) lane STATE — v1.4 retires §7a's name table for the
  ARES lanes endpoint; block 6 built the name table and #13 recorded "two
  tables, two questions". Also to confirm: the docs attribute the axis
  change to "JP, 2026-09-09, after a 17,986-card audit".
- Claimed approvals quoted in the digest for verification; W4's UI still
  unspecced; the held copy stays v1.3 until JP adopts v1.4.

**JP "yes" → build spec v1.4 ADOPTED as the held copy** (`docs/product/
build-spec-v1.4.md`, assembled verbatim from #103–#108, no gaps, v1.3 kept
beside it). The brief and STATE.md point at v1.4. BRD v3.0, the engineering
and security documents stay inbound (`.claude/specs-inbound/`) pending JP's
answers on the forecast axis and the lane-state source.

**JP's answers on the two conflicts** ("yes on point 2", "go with 1"):
the forecast keys on lane × difficulty (lane from the label family, ARES's
per-lane cells) — product's BR-2/BR-4/FR-7.3 wording to be corrected; §7a's
name table stays the source of a list's STATE (the 8 Sept ruling); the
lanes endpoint serves the reconcile check and Apollo's type/group only, at
most a fallback for unknown names if product asks. **jp→miles #74 sent**
(the full set received; v1.4 adopted; the Monday vs first-working-day drift;
the two sentences to correct; the open items). Decision-log rows: forecast
axis · lane-state source · build spec v1.4. Block 9 stays HELD until JP
lifts it; its brief is current.

## Block 9 STARTED (JP "let's start block 9", evening session — hold lifted)

Mode **Full**. Owl inbox on start: **#114 (§5.1c Sprint Schedules toolbar)** and
**#115 (§5.1d confidence picker; its arithmetic HELD by product — a flat +3/+6/+9
ladder put back to Miles)** arrived 14:34–14:39Z; both say block 9 is
unaffected ("the toolbar is its own piece of work"). **Decided: both stay OUT of
block 9** — queued as block-10 candidates; #114 also confirms v1.4 = #103–#108 and
that anything labelled v1.3 is superseded. #88–#90, #114, #115 cached to
`.claude/owls/` (the brief said #88–#90 were cached; they were not until now).
Rex MCP failed to connect this session — the figma reader uses the official Figma
MCP. SURVEY launched as one workflow (`wf_399d4305-496`): four Sonnet readers
(code · law · figma · tests) → `.claude/block9/survey-*.md`.
SURVEY done (4 readers, 802K subagent tokens, 12.7 min): reports in
`.claude/block9/survey-*.md`. **Gate artifacts written**: `.claude/block9/
drift-report.md` (12 items; the high one: #90 overturns #72 §2's deliberate
"no Outside any sprint group" — `sprint_id` is schema-required) and `PLAN.md`
at the root (A1 server · A2 client logic · A3 template+style · A4 tests · A5
law docs; frozen names). Figma has NO frame for the Deadlines drag, the refused
state, the re-date notice or a placed bar — build provisional (decided). Seven
reversible calls decided (drift §H); ONE ask for JP: who may drag on Deadlines
(by user vs by surface). **HARD STOP at the gate — awaiting JP.**
