# 2026-09-09 — Block 7 plan + drift report (rotated from the root at CLOSE)

## PLAN.md (as approved, with the review amendments)

# PLAN.md — Block 7: plotting guardrails on Sprint Schedules

**Ephemeral.** Rotates into `docs/history/state-log/<day>.md` at CLOSE. Never committed (`.git/info/exclude`).
Mode: **Full.** Anchor: the HEAD at build start. Drift report + survey reports: `.claude/block7/` in the repo (untracked): `drift.md`, `survey-client.md`, `survey-server.md`, `survey-spec.md`.
JP's rulings (2026-09-08): a row cannot be placed outside its sprint's dates; a row cannot START after its deadline (a finish past the deadline stays allowed and red — already shipped); placement is at DAY grain and a placed bar can be dragged by day; the red-past-deadline bar needs no change.

Laws: CLAUDE.md (invariant 11: workday math through `lib/calendar.ts` only; `lib/forecast.ts`, `lib/planner.ts`, `lib/calendar.ts` never edited; every write audited), `test/CLAUDE.md`, `frontend/CLAUDE.md`, `specs/001-sirius-v1/sprint-rules.md`, `gantt-rules.md` (§1 is being rewritten by X — the standing principle survives: a drag source stays hit-testable in every state), spec v1.3 §5.1b/§5.2/§6.2.

## Agents and exclusive file ownership

### S — server guards. Model: Opus.
Owns: `src/services/sprint-items.ts` · `src/routes/schedule.ts` · `test/sprint-items.test.ts` · `test/schedule.test.ts` · `specs/001-sirius-v1/contracts/http-api.md`.
Builds: one validator `plotIssue({ sprint, startsOn, deadline, calendar })` in `sprint-items.ts` returning `null` or `{ code, message }`; called by `PATCH /sprint-items/:itemId`, `POST /sprint-items` and `POST /sprint-items/batch` whenever a `starts_on` is supplied (batch: per item, reported in the skip list with the code). Checks, in order: (1) `starts_on` within `[sprint.starts_on, sprint.ends_on]` else `OUT_OF_SPRINT`; (2) `deadline` present and `starts_on > deadline` → `PAST_DEADLINE`; (3) not a working day per `lib/calendar.ts` (weekend or an ARES holiday via the injected set) → `NOT_A_WORKDAY`. Status 422 (the range-violation style of `SPRINT_CONFLICT`), body `{ code, message }` with the messages frozen below. Rollover (`src/services/rollover.ts`) is NOT touched and never calls the validator — its §6.2 freedom to leave the sprint and outrun the deadline is a documented invariant; write that sentence beside the validator. Audit unchanged (a refused write audits nothing). Tests: each code on each of the three routes, the boundary days (sprint start, sprint end, the deadline day itself — all allowed), a holiday from the injected set refused, a Saturday refused, rollover still moving a row past its sprint end and past its deadline (the existing rollover suite must stay green untouched — if it goes red, stop and report). `test/sprint-items.test.ts:671` (moving into a sprint whose range does not cover the start) and `test/schedule.test.ts:440` (2028 accepted) are rewritten to the new rule. `http-api.md`: the three codes.

### G — day-grain placement + bar drag (client). Model: Opus.
Owns: `frontend/scripts/50-gantt-geometry.js` · `frontend/scripts/90-events.js` · `frontend/scripts/40-app-state.js` · `frontend/templates/views/40-schedules.html` · `frontend/styles/35-gantt.css` · `test/sprint-schedule-bars-footer.test.ts` · `test/sprint-schedule-deadline.test.ts` · `test/planner-weeks.test.ts` · `test/drag-hittest.test.ts` · `test/helpers/gantt-render.ts` (only the slicers these need) · `test/helpers/sprint-schedule-tab.ts` · `eslint.config.js` (FRONTEND_SHARED only).
Builds: (a) `dayAtX(clientX, rect, weeks)` beside `weekAtX` — the workday ISO date under the pointer: unit = `rect.width / (weeks.length * 5)`, column i → week `floor(i/5)`, weekday `i % 5` (Mon–Fri), clamped like `weekAtX`; `weekAtX` stays for anything else that uses it. (b) Placement: `plotHover` stores `plotDay` (replaces `plotWeek`), the hover slot `.ghovcell` is ONE unit wide at `plusLeft(plotDay)`, the + sits on it with aria-label "Place the bar on <long date>"; `plotPlace` PATCHes `starts_on: plotDay`. (c) Affordance guard `placeable(row, day)`: inside the row's sprint dates, not after `row.deadline`, Mon–Fri by construction — false → no hover slot, no +, cursor default, click no-op. The server stays the backstop: on a 422 the client shows the server's message in the existing error surface and restores the row. (d) Bar drag: `mousedown` on a placed row's `.gitem` (the coloured run only — the standing hit-test principle) starts `dragRow`/`dragDay`; `mousemove` on the track updates `dragDay` through `dayAtX` and the bar renders at the preview offset with its current width (state key `dragLeft`); a day that is not `placeable` shows the bar with class `refused` and does not commit; `mouseup` PATCHes `starts_on: dragDay` when it changed (optimistic: the bar stays where dropped until the row reloads; on 422 it snaps back and the message shows); `Escape` cancels. No ghost element, no HTML5 drag API (`draggable` stays absent). The deadline tick, `itemBar` widths, `late`, MIN_GRAB and the 12-week window are unchanged. (e) Tests: `dayAtX` executed over all 60 units and the boundaries; the hover slot one unit wide; `placeable` truth table; the drag handlers bound only on a placed row's run; the refused state; the Escape cancel; `bars-footer`'s "no drag, no drop, no ghost" describe becomes the new contract (pointer drag on the run, no HTML5 drag, no ghost); `drag-hittest` enumerates `.gitem` as a real source again and proves it stays hit-testable in every state (`refused`, `late`, hovered, saving).

### X — rulebooks (after S and G land). Model: Sonnet.
Owns: `specs/001-sirius-v1/sprint-rules.md` (rule 30 → membership is STORED (`sprint_id`); R9-d → the roll follows the START day (§6.2, 2026-09-08); new rules for the three guards and the day grain) · `specs/001-sirius-v1/gantt-rules.md` (§1–§2: the HTML5 contract replaced by the pointer-drag contract: source = the coloured run, hit-testable in every state, day snap, Escape cancels, no ghost, the server is the backstop) · `specs/001-sirius-v1/deadlines-rules.md` (R-d2-p/q: one sentence — rollover is exempt from the plotting guards) · `docs/MAP.md`/`docs/architecture/map-*.md` (regenerate). Caps: rulebooks ≤20480 bytes.

## Frozen interfaces
- Error codes, all 422, body `{ code, message }`: `OUT_OF_SPRINT` — "That day is outside the sprint's dates (<start> – <end>)."; `PAST_DEADLINE` — "That day is after the card's deadline (<long date>)."; `NOT_A_WORKDAY` — "That day is not a working day." Batch skip entries carry the same codes.
- `plotIssue()` exported from `src/services/sprint-items.ts`; rollover never calls it.
- Client: `dayAtX` in `50-gantt-geometry.js`; state keys `plotRow`, `plotDay`, `dragRow`, `dragDay`, `dragLeft`; handlers `plotHover`, `plotLeave`, `plotPlace`, `barDragStart`, `barDragMove`, `barDragEnd`, `barDragCancel`; classes `.ghovcell` (one unit), `.gitem.dragging`, `.gitem.refused`; `plusLeft(day)`.
- Unchanged: `itemBar`, `deadlineTick`, `late`, `.gdl`, `WEEK_PX`, `WEEK_COUNT`, `WORKDAYS_PER_WEEK`, the sprint-item payload shape (`startsOn`, `finish`, `deadline`, `late`), the add flow.

## Not built, deliberately
§5.2's multi-row relative-shift drag and cross-sprint drag · a visual dimming of out-of-range weeks (no affordance only; product may want a look) · hiding holiday days on the client (the server refuses them; a later polish) · keyboard nudging of a bar · any change to rollover.

## VALIDATE / REVIEW / E2E
Typecheck, lint, full suite `TZ=UTC` and `TZ=Asia/Manila` (calendar suites also `America/New_York`); every new guard proven non-vacuous from /tmp snapshots after an anchor commit. Review: correctness with two refuters per finding, then ONE simplification agent, then VALIDATE again. E2E on the local rt-test copy by real pointer: place on a Wednesday; hover outside the sprint and after the deadline (no +); drag a bar by day and see the FORECASTED column and the bar agree; drop past the deadline (refused, snaps back); Escape mid-drag; a late row red; the three 422 bodies from a direct fetch; console clean. Deploy only on JP's word.

## Amendments (main thread, 2026-09-09, after REVIEW pass 1 — 20 findings → 6 confirmed, 8 split, 2 refuted; `.claude/block7/review1.json`)
- **Frozen interfaces grow by two client items**: state key `dragGrab` (the unit offset between the pointer and the bar's left edge at mousedown; applied in `barDragMove` before the day is named) and `barLeftAt(row, day)` in `50-gantt-geometry.js` (the drag preview's left, applying `itemBar`'s own MIN_GRAB final-column slide so the preview never leaves the track and never jumps on mousedown). `dragLeft` is computed by `barLeftAt`, never by `plusLeft`.
- **Drag hygiene rules**: `barDragMove` with `e.buttons === 0` (a lost mouseup) cancels the drag; `plotHover` is a no-op while `dragRow` is set; the drag's `Escape` listener is bound with `{ capture: true }`, calls `stopPropagation()`, and is removed with the same flag, so the add-search field's own Escape handler never sees it.
- **The `+` fits its column**: `.gplus` is one unit wide (or overhangs without being clipped at units 0 and 59).
- **D5 resolved STRICT** (main thread's call, reversible): a bare `sprint_id` move of a PLOTTED row is judged against the target sprint's RANGE only (`OUT_OF_SPRINT`; not the deadline, not the calendar — the day was already accepted once). An unplotted row moves freely. Rollover still writes through Mongo and is untouched.
- **Simplification may delete `weekAtX`** (no callers remain) with its drag-hittest coverage; X documents whatever ships.
- **Queued, not built (pre-existing; JP to rule later)**: editing a sprint's dates so that already-plotted rows fall outside it (review split S1). The weekly roll re-files rows; a manual sprint edit does not.
- **Refuted, not built**: a rollover deadline twin test (the source guard + existing §6.2 suite cover it); the reload-after-write banner shape (pre-dates the block, same shape as `plotPlace`).


## Drift report (with the build-time and fix-round deviations)

# Block 7 — drift report: plotting guardrails on Sprint Schedules (JP, 2026-09-08)

Mode: **Full** (server validation + a new pointer interaction + rulebook rewrites). Survey: `survey-client.md`, `survey-server.md`, `survey-spec.md` (this directory).

| # | JP's ask | Code today | Law today | Drift | Plan |
|---|---|---|---|---|---|
| 1 | No plotting on weeks outside the sprint | Nothing enforces it: `weekAtX` clamps to the shared 12-week window; `PATCH /sprint-items/:id` and both add routes validate date SHAPE only (`test/schedule.test.ts:440` accepts 2028 on a 2026 sprint) | Silent — §5.1b never restricts placement; `sprint-rules.md` rule 30 says membership is DERIVED from dates (false since #72: `sprint_id` is stored) | New rule | Server guard in the three write routes (`OUT_OF_SPRINT`, 422); client shows no affordance outside the range. Rollover is EXEMPT (§6.2: a roll may leave the sprint; it writes through Mongo, not the route). Rule 30 corrected |
| 2 | No plotting outside the deadline's range | Nothing enforces it; `deadline` (the card's Trello due date, W2) and `late = finish > deadline` are already on the row | §5.1: "a bar to the right of its marker is late … an input and an output" — a late FINISH is meant to be visible | New rule, read as: the START may not be after the deadline; a finish past it stays allowed and red | Server guard `PAST_DEADLINE` (422) on start > deadline; client no affordance after the deadline. Rollover exempt (§6.2: "moves indefinitely … no cap") |
| 3 | "Dragging schedule on a given day" | Click-to-place at WEEK grain (`weekAtX` → the week's Monday); no drag exists — removed 2026-08-28, and `sprint-schedule-bars-footer.test.ts:346-388` asserts "no drag, no drop, no ghost". Bar geometry is already day-indexed (60 units, 18.4px/day) | §5.1b/§5.2 are week-grain; §5.2 specifies a row drag that was never built (multi-row relative shift); §6.2: "the PM owns the week … the design lead owns the day" | Extension + a product rule change (day ownership moves to the PM on this tab) | (a) day-grain placement: `dayAtX` → the workday under the pointer; the hover slot and the + sit on the day; (b) a placed bar drags by its coloured run, snapping to workdays, inside the guards, committed on release. §5.2's multi-row shift stays unbuilt |
| 4 | The block turns red past the deadline | **Already shipped**: server `late`, template `.late`, CSS `.gitem.late { red-600 }`; tooltip "past the client deadline" | Spec reader read bar colour as urgency-only — the shipped law is R-d2/… `late` | None | No change. JP's screenshot shows an unplotted row, so the red bar has not been seen yet |
| 5 | (found) Working days | `starts_on` accepts any calendar date; the grid is Mon–Fri; a holiday under the pointer is placeable | Invariant 11: workday math uses `lib/calendar.ts`; the ARES calendar is canonical | Gap | Server guard `NOT_A_WORKDAY` (422) using the calendar; the client's geometry yields Mon–Fri only |
| 6 | (found) Stale law | — | `gantt-rules.md` §1–§2 documents the deleted HTML5 drag contract; `sprint-rules.md` R9-d says a roll follows the finish day (superseded by §6.2) | Drift | Rewritten with the new pointer-drag contract; `drag-hittest.test.ts` returns to real sources |
| 7 | Add vs plot | The two add routes also accept an optional `starts_on` | #72: add and plot are separate acts | — | The same guard on all three write paths (one validator), so an add with a start is held to the same rule |

**Product rule changes JP is making** (told to Miles at CLOSE): placement at day grain on Sprint Schedules (§6.2's two-owner model moves the day to the PM on this tab); a start after the deadline is refused (§5.1 never restricted placement); a start outside the sprint is refused. Rollover keeps its §6.2 freedom.

## Build-time deviations (recorded 2026-09-09 after BUILD; anchor `98aaff4`·`aee7de4`·`e8da4a2`)

| # | PLAN.md said | Built | Why | Reversible |
|---|---|---|---|---|
| D1 | Batch add guarded per item, skip list carries the code | Nothing: `POST /sprint-items/batch` has no `starts_on` (`.strict()` → 400 INVALID_BODY, pinned by an existing test) | Drift row 7 was wrong about the batch; adding the key changes the add flow (frozen "Unchanged") | yes — a new build if product wants batch placement |
| D2 | `<long date>` | "16 Sep 2026" (the codebase's `longDate`, byte-matched to the client's `fmtLongIso`) | One existing renderer, not a second form; service carries its own copy (route module can't be imported by a service; a test runs the two against each other) | yes |
| D3 | body `{ code, message }` | house envelope `{ ok:false, error:{ code, message } }` | Same shape as SPRINT_CONFLICT; client reads `error.message` as it already does | yes |
| D4 | FRONTEND_SHARED gains `dayAtX` only | `dayAtX`, `plusLeft`, `placeable` | all three cross the 50→90 file boundary; no-undef otherwise | trivial |
| D5 | (unstated) | ~~A bare `sprint_id` move is NOT judged~~ → **STRICT after review (2026-09-09)**: a bare move of a plotted row is judged against the target sprint's RANGE only (`OUT_OF_SPRINT`); an unplotted row moves freely | Review split S1/S2: rollover re-files rows itself via `sprintFor()`, so S's stranding reason covered a narrower case than claimed; the ruling reads strict | yes |
| D6 | — | gantt-rules §1 renumbered 1–11 (gap 12–18 left; §2 keeps 19–25) | no code/test cites a §1 number | yes |
| D7 | — | `plusLeft(day)` pins a pre-window day to `0.00` (matches `itemBar`'s clip), returns null beyond the window | drag preview opens where the bar visibly is | yes |

## Fix-round deviations (2026-09-09, after REVIEW pass 1 fixes + simplification)

| # | Note | Reversible |
|---|---|---|
| D8 | FRONTEND_SHARED grew by `barLeftAt` (amendment) and `dayIndex` (lint scope only — read across the 50→90 boundary for `dragGrab`); `barWidthUnits` is a private geometry helper, one owner of the run's width | trivial |
| D9 | `.gplus` is an 18.4px circle around the 16px icon (was 24px) — the "fits one unit" option; the overhang option was rejected because unit 0 sits under the opaque sticky pane. **Visual call for E2E/JP** | yes |
| D10 | `sprintRangeIssue()` exported from `sprint-items.ts`; `plotIssue` delegates its first clause to it. `deadlineFor` accepts null | yes |
| D11 | `weekAtX` DELETED (no caller) with its ten-test suite; a guard now forbids its return | yes |
| D12 | `itemBar` now takes its left from `barLeftAt` (one owner of the MIN_GRAB clamp); `findPlotSprint()` folds three identical sprint reads in `schedule.ts` | yes |
