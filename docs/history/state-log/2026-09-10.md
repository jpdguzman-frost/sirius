# 2026-09-10 — three yeses: the reversal accepted, re-date ruled, block 8 started

## Morning — what arrived overnight

**Miles #88–#90** (09:35–09:44Z, all answering our #69), read in full:

- **#88 — REVERSES block 7's day placement.** Miles: the Design Lead controls
  the day, in the Deadlines tab; the PM controls the week only. Sprint
  Schedules DISPLAYS the start day and cannot edit it; the one-day hover slot,
  the day-precise `+` and the bar day-drag come out; a click inside a week
  places on that week. The Deadlines day-drag is built and is the only one
  (this un-retires spec §6.5's planner surface). §6.2's two-owner model stands.
  Rollover, add-vs-place, `Add All`, the red past-deadline bar, §5.2's
  multi-row shift and cross-sprint (week-grain) drag: unaffected.
- **#89 — the three held points confirmed.** The Design Lead's drag is BOUNDED
  by the assigned week (Miles: "Don cannot move a card beyond a week"); days
  outside the week are not a drop target (pale bar + snap back). Only rollover
  crosses a week, as a system action. Week placement defaults to the week's
  first working day. The three block-7 guards carry across intact, so a valid
  drop day meets four conditions: inside the assigned week · inside the
  sprint · no later than the deadline · a working day. Spec §5.1b/§6.5 updated.
- **#90 — sprint re-date ruled.** Cards falling outside a re-dated sprint keep
  their exact day and move to *Outside any sprint*. Not re-placed, not pushed
  to the next sprint, not unslotted (that branch withdrawn). Rationale: the
  next-sprint move can manufacture a start after the deadline, which the
  guard refuses from a user; it discards the Design Lead's day; surfacing
  beats redistributing. Deletion stays confirmed + destructive; re-dating is
  light. Spec §5.3 carries it. Miles's remaining #66–#70 answers (OPEN WORK,
  W4, §6.2 day capacity, Generation/Refinement, the five unreachable lanes,
  the Urgent chip colour) still to follow.

**Ares #03/#04** (file drop): the model endpoint is BUILT then LIVE.
`GET /api/v1/trello/cycle-time/model?rtProjectId=837&dateFrom=&dateTo=`,
key-gated; `rtProjectId` optional (omit = firm-wide, every cell `source:
"firm"`); dates default to a rolling 12 months. Fields: `generatedAt`,
`window`, `floorMinutes` 15, `overrideDays` 14, `workingDayHours` 24 (divide
by it, never assume), `workTypeKeys`, `historyUnverified`, `dropped`
`{considered, sampled, reasons×13}` with `considered === sampled + Σreasons`
asserted, `cells[]` `{workType, difficulty, source project|firm, n,
meanWorkingDays, p70, p85, p95, meanCalendarHours}`. A sample = the SUM of a
done card's working-list stretches, in working days on the Ares calendar
(weekends + holidays removed, ÷24, deliberately not a shift length). New
`createdAt` on `/cycle-time` records (decoded from the card id's ObjectId).
Answers: the client-review WAIT is NOT a cell (JP via Ares: a queue, and the
lane is no longer in active use); refresh once per Manila day, byte-stable.
Asks done: non-numeric id → 400, unmapped id → 404, bad dates → 400,
`syncedAt` on lanes (null until a sync), `historyFetchedAt` added — but
`historyComplete` unchanged and unverified samples ADMITTED and counted in
`historyUnverified` rather than dropped (dropping would empty the model).
`doneWeek`/`doneYear` now resolve in Manila (some cards near a week boundary
moved). Their TZ advice — add a negative-offset leg to a UTC+Manila sweep — is
already met here (calendar suites run America/New_York).

**Verified live** (main thread, read key, 837): 135 cells (42 project / 93
firm), 52 work-type keys across prefixes 3D · Asset · Build · Components ·
Content · Design System · Design · Dev · Motion · Ops · Production · Strategy;
`historyUnverified` 8,337 of 8,337 sampled (considered 19,970) — **every cell
provisional**; 37 full Easy/Medium/Hard trios of which **24 fail p85
ordering**; 71 cells with n < 15; lanes `syncedAt: null`. Ares's own advice:
do not unfreeze on wholly unverified samples without JP saying so.

## JP's answers — "yes, yes, yes" (three asks, in order)

1. **Accept the reversal of block 7** (#88/#89) — yes.
2. **Product's sprint re-date rule** (#90) over the 09-09 REFUSE — yes.
3. **Start the model reader now** despite 100% unverified samples — yes; the
   model stays frozen, unfreeze stays a separate JP decision.

Recorded in `docs/history/decision-log.md` (day owner · sprint re-date ·
block 8 start) and STATE.md.

## Decided without JP (reversible)

- **Block split.** Block 8 = phase 19 (T179 labels, T180 model reader, T181
  cells → grid, T182 gate, plus the lane reconcile check off the lanes route)
  — worker/model/tests, no frontend, Full mode. **Block 9 = the reversal +
  the re-date rule** — frontend/planner, Figma (Deadlines tab), Full mode,
  next fresh session. Swap on JP's word.
- **Miles #88–#90 stay UNACKED** until block 9 builds them (the #81–#87
  precedent: ack on deploy). Their content is processed and ruled.
- **Outward messages held**: jp→miles acceptance note (the three rulings
  accepted; block 9 will carry them) and Ares #14 (thanks; the 100%
  unverified number; a sync to stamp `syncedAt`; the ordering-gate result).
  Both are per-approval from the main thread.

## Block 8 — SURVEY started this session

Three mid-tier readers (code · specs/owls · tests) over the model path, the
phase-19 law and the test conventions; full reports to `.claude/block8/`,
compact findings back to the main thread. Next: the drift report + `PLAN.md`
at the gate, then JP.

## Afternoon — outward notes sent, block 8 at the gate

JP: "yes - when you reply to them please use simple words. yes" → **jp→miles
#71 sent** (id `6aa287efde26ad7f21207db9`, in reply to #90; the three rulings
accepted in plain words; the change ships the block after the current one;
asked whether the updated spec is a v1.4 or v1.3 edited in place) and
**Sirius→Ares #14 sent** (our own verification numbers, how we read the model
nightly, the ordering-gate result, one ask: a lanes sync so `syncedAt`
stamps; #03/#04 and our #13 pruned from the drop). Miles #88–#90 stay UNACKED
until block 9 builds them.

Survey done (3 readers, 302K subagent tokens, reports in `.claude/block8/`).
Load-bearing findings: `gate-t045.ts` asserts nothing today (T182 writes the
first automated checks); the loader fills only ABSENT cells from the snapshot,
so the gate must run at WRITE time; the model endpoint is aggregate-only, so
`model_samples` cannot be fed from it; `EmpiricalModel.review` has no Ares
source (review is retired per §7.1 — stays on the snapshot); three "lane"
vocabularies collide; no key on 837 carries both `project` and `firm` cells
(verified — no preference policy needed); nothing anywhere reads `/lanes`.

**Gate artifacts written**: `.claude/block8/drift-report.md` (16 items, one
question) and `PLAN.md` (four agents, exclusive files, frozen interfaces).
**Q1 to JP**: fold Ares's 12 work-type prefixes into our three lanes (A,
recommended: Design/Design System/Components/Strategy/Content → design;
Asset/3D/Motion/Production → assets; Ops → ops; Build/Dev unmapped) or adopt
Apollo's six groups (B, touches `lib/model.ts`). Decided and reported: min
n = 15, window ≥ 60 days, 100% `historyUnverified` fails every cell, no
`model_samples` from the Ares path, provenance in `sync_runs.stats`, the lane
check on the sync tick. HARD STOP until JP answers Q1.

## Evening — JP's fold, the data check, and the question re-framed

JP answered Q1: "Content → content; Strategy → design; assets, 3D, motion,
production → design. Check again that's the lane where they go." Checked two
ways on 837 (4,407 done cards, read key, main thread):

- **Where the work happens** (work-type label × Ares `laneKey`): Asset
  1,925/1,933 · Motion 37/37 · Production 230/237 · Strategy 35/35 · 3D 1/1 in
  `design` (start list "Working on Design"); Ops 991/992 in `ops`; no
  `assets`/`content` lane key exists on 837; 843 cards (19%) carry no
  work-type label; none carries two.
- **How long it takes** (project cells, p70 working days): Asset E 0.04–0.28
  · M 0.14–0.74 · H 0.54–1.95 — the design range (snapshot design E 0.94 /
  M 1.20 / H 2.09) and nothing like the snapshot's `assets` lane (E 13.88,
  n=353), which is a TITLE-classification artefact of `laneOf`'s
  `/asset|illustrat|render|icon/` on deliverables. BR-4's assets/design split
  does not survive contact with the label data.

JP's fold stands, confirmed. **The question was framed wrong on my side**:
the design lane now spans ≥ 8 work types and p70/p85/p95 cannot be pooled
from per-work-type cells — only from samples. Re-framed at the gate as
**Q2** (Ares pools cells per lane key — recommended — vs. keying our lookup
on the work type) and **Q3** (`lib/model.ts` amendment: `Lane` += `content`,
`laneOf` gains a label-family branch ahead of the list regex — today
`laneOf` already reads labels, so with T179 an `Asset: Icons` card would
land in `assets`, the opposite of the fold). Drift report + `PLAN.md`
patched (fold table frozen; `laneCells?` optional on the model interface;
A1 owns the `lib/model.ts` amendment if JP grants it). HARD STOP holds.

## Night — gate passed, BUILD launched

JP: "yes, yes" → **Sirius→Ares #15 sent** (cells pooled per `laneKey` as
`laneCells`, optional in our reader until it lands) and **the `lib/model.ts`
amendment granted** (`Lane` += `content`; `WORK_TYPE_LANES` in lib;
`laneOf` label-family branch ahead of the verbatim regex fallback; recorded
in `lib/CLAUDE.md` by A1). Decision-log rows: lane fold · lane cells ·
lib/model.ts amendment. `PLAN.md` marked GATE PASSED; build order: four
agents in one tree, exclusive files, no commits, own tests only; the main
thread anchors the commit and runs VALIDATE.
(Agent backlog: unchanged; pointer rotated out of STATE.md for the cap — see
`docs/history/state-log/2026-09-09.md` §"Agent backlog".)

## Night — Ares #05: `laneCells` live, lanes stamped (verified)

`2026-09-10_ares-to-sirius_05_lane-cells-live.md` (19:25) answers #14/#15.
Verified read-only on production: `laneCells` present (5 lane keys × 3
difficulties = 15 cells; `laneKeys` ["content","design","dev","ops",
"working-on-design"]); design and ops `source: project` (design E/M/H n
498/1,273/216, p85 0.64/0.91/1.97; ops n 374/448/150, p85 1.01/0.98/1.03);
content, dev and the stray `working-on-design` (11 firm samples — Ares: map
to design or ignore; we IGNORE: it is not in the Lane union, so
`cellsFromAresModel` counts it in `unmapped`) fall back to firm.
`/boards/hLL7WW2V/lanes` → `syncedAt: 2026-09-10T11:12:05.994Z` (Apollo sync
11:12Z, 22 boards, 103 pairs). Our Zod envelope is not `.strict()`, so the
new `laneKeys` field is stripped, not rejected. Two rules from Ares to carry
into provenance: a card's lane is the lane of the LAST start list it worked
in (all its hours included — rare on 837); per-lane `dropped` deliberately
absent. n over project lane cells (design 1,987 / ops 972) vs. project
work-type cells summed by family (≈1,958 / ≈961) differ by the last-start-
list rule — consistent, not a defect.

**Gate finding**: `historyUnverified` is now 8,337 of 8,376 sampled — 39
cards were re-fetched today, so the ratio is 99.5%, and `DEFAULT_GATE`'s
`unverifiedRatioFail: 1.0` no longer fires: one verified card un-fails every
cell. Decided (reversible): tighten the default to **0.5** (fail while more
than half the backing samples are unverified) in the review-fix round, which
re-VALIDATEs in full anyway. The model stays frozen regardless. Reply #16 to
Ares drafted (scratchpad), held for JP; #05 stays in the drop until it goes.

JP "yes" → **Sirius→Ares #16 sent** (laneCells verified; the stray key
ignored; the last-start-list rule into provenance; the 0.5 unverified rule).
Ares #05 and our #11/#12/#14/#15 pruned from the drop — only #16 remains.
VALIDATE #2 clean at fec8afc (`.claude/block8/validate-2.md`, `proofs-2.md`):
80 files / 1,640 tests green under UTC and Manila, 25 calendar tests under
New York, 13 + 33 revert proofs failed-as-expected and restored. REVIEW
launched: five adversarial finders (a1 · a2 · a3 · a4 · cross), two refuters
per finding, splits to the main thread.

## Late night — REVIEW verdict and the fix round

REVIEW (5 finders · 2 refuters per finding · 81 agents · 4.78M tokens; JP's
session limit hit at 22:10 Manila mid-refute, resumed on "resume"): **38
findings — 35 stood with both refuters, 2 killed (A3-3 window TZ, A3-5 fail-
open NaN), 1 refuted on a single verdict (X6), 6 low ones never refuted (X4
duplicate of A2-8; X7–X11 docs/dead code/test pin/script) — judged by the
main thread rather than re-spent.** Reports: `.claude/block8/review-*.md`.

The one HIGH: **X1** — `src/services/rollover.ts` projects work cards
without `labels`, so the rollover engine's finish (list lane) differs from
the Sprint Schedules bar (label lane) for the same card. Mediums: A1-F1/X2
(the verbatim fallback regex reads list + ALL labels, so an unmapped
`Dev: Render …` or a two-label card leaks label text into `assets`);
A1-F2/X3 (work-type label shape diverges from Ares's — `Asset:Icons` is a
work type there, not here); A2-1 (no `source` marker in provenance); A2-2
(an all-fail night empties the grid with no delta alert); A3-1 (the 1.0
unverified threshold); A3-2 (the gate script says PASS on an empty grid).
Lows: prototype-key lookups, negative numbers admitted by the schema,
`laneCells: []` vs absent, `rt-test` erroring nightly and losing throughput,
stale contract/tasks text, silent null lane table, vacuous rule-tier tests,
resync path dropping lane stats, adjacent suites printing lane warnings,
Manila dates in the gate script, dead dwell code.

**Main-thread rulings inside the lib amendment** (reversible, JP can veto):
the fallback regex is fed the list + NON-work-type labels only; the label
shape mirrors Ares (trim, family before the first colon, `:\s*\S`,
`Difficulty:` case-insensitive). Other rulings: `laneSources`,
`laneCellCount`, `modelSkipped` added to the model stats; `lanesSeen` to
SyncStats; `DEFAULT_GATE.unverifiedRatioFail` 0.5; throughput refreshes for
every ongoing project and only the Ares half is skipped for a non-`rt-<n>`
code; the dead dwell path deleted in this round (X7); "once per PROJECT per
run" reworded rather than memoised (A4-F4). `PLAN.md` amended accordingly;
findings split into `.claude/block8/fixes-a{1..4}.json` (8 · 12 · 7 · 8).
FIX workflow launched: four agents, ownership extended (A1 += rollover.ts;
A2 += tasks.md T181 line; A4 += drainPush.ts + three stub clients).

## Fix round done — `e27ede0`

Four agents, 558K tokens: **34 of 35 findings fixed with guards** (A1 8 ·
A2 13 incl. X4/X6/X7/X10/X11 · A3 5 · A4 8); deferred as documentation
A3-4/X5 (the window rule is on Ares's REQUEST window — stated in
`model-gate.ts` and `worker.md`); the second half of X10 (the
`sync-unmapped-lists` whole-document test bypasses `runAresSync`) left as a
residual (A4's file, low). One deviation, accepted: A4's rulings F1 and F8
contradicted (a null lane table warns; adjacent stubs must not warn) — the
three stubs answer an EMPTY table `{ lanes: [], syncedAt: null }` instead of
null. A3 added a `at >= scriptStart` filter on the gate script's sync_runs
read so a stale successful row cannot pose as tonight's envelope. Golden
suites re-run after every `lib/model.ts` edit — no expectation moved. Deploy
note (A1): the rollover now agrees with the bar, so label-lane-faster rows
that were previously spared will roll on the first tick after deploy.
Simplification pass launched (one agent, alone in the tree); VALIDATE #3
(full checks + proofs of the ~27 new guards + a verify pass re-running every
finding's failing input) follows, then E2E.

## CLOSE — E2E green, block 8 built, deploy held

**VALIDATE #3** (`b7b5469`): typecheck · lint · 80 files / 1,671 tests green
under UTC and Manila · 25 calendar tests under New York · 29 fix-round
proofs failed-as-expected and restored (75 proofs across the block, none
vacuous) · VERIFY re-ran all 35 findings' failing inputs: 33 fixed, 2 low
residuals (X10 second half; the once-per-board wording — the contract row
fixed in `7cff47d`, the code comment already right).

**E2E** (`.claude/block8/e2e.md`, live read key, local Mongo, nothing
written to any source system): `refreshProjectModel` for the local rt-837
(fixture board) against the live model — generatedAt 11:19Z, window
2025-09-11..2026-09-10, workingDayHours 24, historyUnverified 8,337 /
sampled 8,376 / considered 20,029; **36 cells mapped** (design/ops/content ×
3 difficulties × 4 confidences), 6 source cells unmapped (`dev` 3,
`working-on-design` 3), `laneSources` design=project · ops=project ·
content=firm, `laneCellCount` 15, **0 passed / 36 failed** (`unverified`,
some also `ordering`), **model_grid 0**, `loadProjectModel` served the
shipped EMPIRICAL snapshot (`fallback: true`), `model_frozen` still true.
Lane reconcile on the real TEST board tx8gDsTH: 37 lanes seen, **0 unknown**,
`syncedAt 2026-09-10T11:12:05.994Z`. Gate script (`ROWKEY=837
BOARD=fixtureBoardA`): exit 1, `T182 FAIL — 36 cell(s) failed the gate (0
passed)`, no throw; console clean apart from the expected fixture-board lane
warning. Side effect noted: the script's board sync against the fixture
board id returned 0 cards and DEACTIVATED the 4 local rt-837 fixture work
cards (local rig only; re-seed from fixtures if a later local pass needs
them). Local `rt-test` untouched.

**Rotation**: `PLAN.md` + `.claude/block8/drift-report.md` →
`docs/history/state-log/2026-09-10-plan-block8.md`; `PLAN.md` leaves the
root; block 7's phase row (18j) → `docs/history/phase-log.md` (its gate,
Miles's reply to #69, answered by #88–#90); block 9 queued in STATE.md.
**Deploy waits for JP.** Unfreeze stays JP's, once cells pass on verified
samples. Token cost of the block (subagents): survey 0.30M · build 0.62M ·
validate 0.37M + 0.14M + 0.48M · review 4.78M · fix 0.56M · simplify 0.12M ·
E2E 0.09M ≈ **7.5M**, the review being 64% of it.

## Ship (JP: "yes - deploy. notify miles on owl")

Pushed `daaaa4b..4fcbb24` (15 commits); `./deploy.sh` green (suite,
typecheck, build 538,431-byte bundle, rsync, `npm ci`, **migrate applied
`012-work-card-labels`**, pm2 restart of `sirius` + `sirius-worker`, both
online, 0 unstable restarts). Live through the host's `APP_BASE_URL`: healthz
200 ×3 (0.11–0.14s), API wall 401. Migrations ledger ends 010 · 011 · 012;
`work_cards` missing `labels`: 0. Worker since boot: calendar loaded, ares
sync complete, rollover "0 moved" over 2 projects; `sirius-error.log`
untouched since 13 Aug, the worker's error log only the standing Sheets
warning. Standing smoke: urgency round-trip on the TEST board green (set →
present, unset → absent). **First live lane check** (rt-test, 15:28Z):
`lanesSeen 37`, `unknownLanes []`, `lanesSyncedAt 2026-09-10T11:12:05.994Z`,
8/8 work cards labelled. rt-837's post-deploy sync row was still pending at
ship time (its pre-deploy row lists `unmappedLists` For Archive · Hard
Deadline: Monday Mar. 23 · NOTE · On Hold: Ryse, NBG — the lane check should
add `For Client Approval`, the cardless fifth); 49 legacy `source:'model'`
rows exist from the dwell era; the first Ares-read model row lands on the
worker's next daily tick. **Owl #72 sent to Miles** (id `6aa2ccc8…`, plain
words: reads Ares's figures, still frozen, the one visible change on Sprint
Schedules, the lane log, the open spec-version question). Main level with
origin.

## Late — Miles #91–#98 (read by a delegated reader; bodies in `.claude/owls/`)

Eight notes, all today, none acked, no JP word yet. #91 (answers #66 Q1):
Pipeline tiles become PENDING · ONGOING · DONE · URGENT, all counting WORK
cards; tiles RESCOPE to the active filter/search (reverses spec §8) and
never drive filtering (retires §3.7/§3.1 selected state); MAIN/WORK/
UNATTACHED move to ingestion health §2.6. #92/#93: tile geometry and colours
from the nodes (PENDING yellow-600, ONGOING blue-500, DONE emerald-600 —
amended off green-700 for the Easy-green collision, URGENT amber-600);
390.5×82, flush, slate-300 stroke (contradicts §3.1 "no boxes"); figure
weight 600 on the nodes vs 700 in §3.1 — asks which is live. #94/#95
(answers #66 Q2): product says **W4 is AUTHORISED** — a fourth write,
classification tagging, narrowed to ONE field (business unit, the UNIT
field, from the sheet's Business Unit column), assign/unassign EXISTING
board labels only, exact match after case/whitespace normalisation, never
fuzzy, never create, unmatched surfaced; BRD → v2.9, amended across five
docs; UI half (tab, card kind) NOT yet ruled; asks (a) whether
`ensureUrgentLabel`/`ensureDifficultyLabel` create-if-absent (they do — see
the ship-day check below), (b) whether ingestion-triggered tagging records
actor `system` or becomes user-initiated. Our constitution still says
THREE writes; growing the registry is JP's amendment, not product's — HELD.
#96: AGENTS.md §4 swept of five retired rules (Friday render, sketch/review
percentiles, BR-6c weight on the planner tabs, conflict acks, day planner);
build-spec §7.1 corrected to "difficulty AND work-type label"; the 50-move
Trello bug and the borrowed Content/Motion figures documented. #97: doc set
1/5 = AGENTS.md in full ("Miles asked me to send all five" — unverified).
#98: HOLD on 2/5 — the BRD body is ~v2.5 in everything the work-card rework
touched (BR-10 still names the retired keyword classifier; BR-1a vs BR-1b
contradict); only §9/the registry are current; build spec authoritative
until swept; 3/5–5/5 to follow, audited first.
Ship-day check for #94's question: `lib/trello.ts` `ensureUrgentLabel`
(l.73) and `ensureDifficultyLabel` (l.105) both FIND-OR-CREATE — they POST a
new board label when absent (BRD §4: 0/26 boards carried `Urgent`). So "Sirius
never creates a label" is true of W4 only; W1/W3 create by design. For #92/
#93's weight question: the live tile figure is `.kpi .kvalue { font-weight:
700 }` (`frontend/styles/10-ui.css:5`) — 700 is live, the nodes say 600
[likely: `.kpi` is the tile class]. Both answers HELD for JP's word before
they go to Miles.
