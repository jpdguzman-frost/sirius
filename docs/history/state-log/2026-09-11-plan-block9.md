# Block 9 — PLAN + drift report (rotated out of the repo root at CLOSE, 2026-09-12)

_Built `7b83ef9` → reviewed `04d248e` → E2E-fixed `47ce7ab`. Layer 1 stays STATE.md alone; this file is the archive of the build's ephemeral plan._

# Block 9 — DRIFT REPORT (gate artifact, 2026-09-11)

Mode **Full**. Spec = owls #88/#89/#90 (JP yes ×2, 2026-09-10) + build spec v1.4 (adopted 2026-09-11) §5.1b/§5.3/§6.2/§6.5/§8. Code = HEAD `87d4a40`. Survey reports: `.claude/block9/survey-{code,law,figma,tests}.md`. Where an owl and v1.4 differ, the owl wins (JP ruled on the owls).

## A. Spec vs code — item by item

| # | Ruled behaviour (authority) | Code today | Drift | Block 9 does |
|---|---|---|---|---|
| 1 | Day belongs to the Design Lead, on Deadlines only (#88) | Day-grain hover/`+`/click-place and bar day-drag live on Sprint Schedules (`90-events.js:761-943`, `40-schedules.html:292-299`, state keys `plotRow plotDay dragRow dragDay dragLeft dragGrab`) | **REVERSED** | Remove all six state keys, seven handlers, `.gplus`/`.ghovcell`/`.gitem.dragging`/`.gitem.refused`, `dayAtX`/`plusLeft`/`placeable` from Sprint Schedules |
| 2 | PM works Sprint Schedules at WEEK grain; click inside a week places on that week (#88, v1.4 §5.1b) | No week-grain placement path exists (block 7 deleted `weekAtX`/`plotWeek`; `.gweek` cells still rendered, hit-testable — the law test survives) | **MISSING** | Week-grain hover + click on `.gweek` cells; PATCH body `week` |
| 3 | Start day DISPLAYED, not editable, on Sprint Schedules (#88) | `itemBar`/`deadlineTick`/`.late` draw the day-precise bar — stays | none | `.gitem` cursor off `grab`; hint text rewritten |
| 4 | Week placement = first WORKING day of the week (#89 §2; v1.4 §5.1b says "Monday" — owl wins) | No helper exists; `workday(d,0)` returns a holiday Monday unchanged | **MISSING** | `firstWorkdayOfWeek()` composed in `sprint-items.ts` from `lib/calendar` primitives (invariant 5: `lib/` untouched); server resolves — the client holds no holiday data |
| 5 | Deadlines day-drag, bounded by the assigned week, four conditions (#89 §1/§3, v1.4 §6.5) | Deadlines has ZERO interaction machinery; cards are read-only `<article class="dlcard">` in `.dlday` columns; the old §6.5 planner was deleted, not parked; no route, handler, CSS or state exists | **MISSING — built from zero** | Pointer drag within the expanded lane (`dlDrag*`), client gate `dlDayPlaceable`, server `OUT_OF_WEEK`; refused = pale wash + snap-back (block 7's treatment reused) |
| 6 | Fourth guard `OUT_OF_WEEK` in the same voice (#89 §3) | `plotIssue` has three codes, no week input | **MISSING** | `PlotIssueCode += 'OUT_OF_WEEK'`, checked FIRST; message names the week's dates |
| 7 | Only rollover crosses a week (#88/#89, §6.2 untouched) | `rollRow` never calls `plotIssue` — stays | none | `test/rollover.test.ts` must pass unmodified |
| 8 | Re-dated sprint → displaced rows to *Outside any sprint*, day kept (#90, v1.4 §5.3) | `PUT /sprints` has NO displacement logic; `sprint_id` is `required: true` (`models/index.ts:496`); `sprintGroups()` says "NO outside group — absence is the design (#72 §2)" | **CONTRADICTION (high)** — #72's deliberate removal is overturned by #90 | `sprint_id` becomes nullable; post-save pass nulls `sprint_id` on rows whose day left the range; the *Outside any sprint* group renders when any row has `sprintId === null`; the unslot-to-pool branch is NOT built |
| 9 | Re-date is NOT refused; the notice names each displaced card (#90 + JP 09-09 LIST half) | No notice; JP's 09-09 "refuse+notify" is half-withdrawn by #90 | **MISSING** | PUT response `displaced[]`; dismissible banner listing `display_id · title · day`; one `audit_log` row per displaced row, actor = the editor (never `system`) |
| 10 | Deletion unchanged: confirmed, destructive, names the count (#90) | As built (`sprintDeleteConfirm`) | none | One test proving a shrink never enters the deletion flow |
| 11 | Keyboard path for the Deadlines day-drag (v1.4 §8, new sentence: "NFR-9 is not optional decoration") | gantt-rules rule 44 records keyboard nudging as "not built, by product" | **CONTRADICTION (law doc stale)** | Build the smallest form: focused card + ArrowLeft/ArrowRight = one working day through the same write and the same four refusals; rule 44 re-worded |
| 12 | Add/place two acts; `Add All` never takes a day; red past-deadline bar; §5.2 shift/cross-sprint unbuilt (#88 "not affected") | As built | none | untouched |

## B. Law documents that contradict the rulings (rewrite in this block, docs agent)

| Doc | Rule | Says | Must say |
|---|---|---|---|
| `sprint-rules.md` | R10-a, R10-b, R10-e (`:227-261`) | day-grain placement/drag on Sprint Schedules; "both write paths that take a day from a person" | Sprint Schedules takes a WEEK; the day-taking path is Deadlines alone; four conditions, `OUT_OF_WEEK` first |
| `sprint-rules.md` | rule 30 (`:37-54`) | nothing on re-date displacement | displaced rows → *Outside any sprint*, day kept, save not refused, notice lists them, one audit row each |
| `deadlines-rules.md` | R-d2-o (`:131-136`) | day-drag planner "removed, never reintroduced" | un-retired (#88/#89, v1.4 §6.5): the bounded day-drag is THE day control |
| `deadlines-rules.md` | R-d2-c (`:36-47`) | `startsOn` = "the PM's own click" | = the PM's week placement (first working day) or the Design Lead's drag |
| `deadlines-rules.md` | R-d2-k | the node draws the quote bar red | node is amber-600 now (figma survey) — doc is the stale side |
| `gantt-rules.md` | rules 1–18, 44, R10 refs, the `.gitem` hit-test target in rule 25 | day-grain pointer contract on `.gitem` | `.gitem` is display-only; `.gweek` click = week placement; the pointer-drag contract moves to `deadlines-rules.md` |
| `contracts/http-api.md` | PATCH sprint-items row (`:44`) | three checks | `week` body + four checks + `NOT_PLACED`; PUT /sprints `displaced[]` |
| v1.4 §5.1b (`:822`) | "Monday of that week" | owl #89 §2 wins — reported to Miles in #74 already; NOT edited by us |

## C. Figma vs rulings (survey-figma)

- **No frame exists** for: the Deadlines drag (handle, drop target, refused state), the re-date notice, a placed bar showing its start day on Sprint Schedules (the live sample row is a placeholder box). Block 7's refused wash was code-invented too. **Decided: build provisional, reusing block 7's treatments; report to Miles at CLOSE** (JP may veto → wait on design).
- Reverting Sprint Schedules to week grain IS the original frame's rule (gantt-frame-notes R6 "bar is display-only"; hover-cell/`+` nodes 731:100271/100277 untouched by the work-card rebuild).
- Off-scope node findings, logged only: the Deadlines Non-Urgent chip is still drawn (withdrawn by spec); urgent badge ink red-50 vs rule's amber-50; stray invisible character before "Urgent".

## D. Tests that flip (survey-tests)

- DELETE: `sprint-schedule-deadline.test.ts:379-637`, `sprint-schedule-bars-footer.test.ts:436-890` (incl. Escape-mid-drag `:864-890`), `drag-hittest.test.ts:554-840` (`.gitem` drag-source sweep) and `:928` (the literal `weekAtX` ban — a snapshot guard, rule-1 hazard).
- INVERT: `sprint-schedule-groups.test.ts:75` (NO outside group → the group renders when a row is displaced, absent otherwise); `deadlines-tab.test.ts:979` title/law ("writes NOTHING" → bounded day write only).
- SURVIVE: `.gweek` hit-test law `drag-hittest.test.ts:694-788`; all of `rollover.test.ts`; geometry (`dayIndex`, `itemBar`, `barLeftAt` as display).
- NEW: `test/deadlines-drag.test.ts`; `test/sprint-items-plot.test.ts` (split of `sprint-items.test.ts` at `:1176` — the file is 73KB); re-date suite in `schedule.test.ts`; first-working-day tri-TZ tests.

## E. Residuals carried / moot

Moot (no analog on Deadlines): the `+` reads as a dot; the `+` covers the deadline tick. Carry to Deadlines: the refused-preview wash; Escape-mid-drag unit-proven only (E2E proves by pointer). "No START column" on Sprint Schedules: resolved as intended (title-only display).

## F. #114 / #115

Out of block 9 (Miles: "nothing here blocks block 9"). One sequencing note: #114's adjustable range control will touch gantt-rules' "shared 12-week window" assumption when built.

## G. Open for JP at the gate

1. Who may drag on Deadlines — **JP 2026-09-11: gate by SURFACE only** (any member, from the Deadlines screen; no role check).

## H. Decided without JP (reversible; veto at the gate)

- `sprint_id` nullable = the *Outside any sprint* storage (no data migration: Mongoose-level `required` flips in code; the build agent confirms no collection validator exists, else migration 013).
- The assigned week is DERIVED from the row's current `starts_on` (local Monday via `lib/calendar`), never persisted — self-consistent because every guarded write stays inside the week and rollover is exempt.
- The client sends a WEEK for Sprint Schedules; the server resolves the first working day (invariant 11; the browser holds no holiday set).
- Guard order: `OUT_OF_WEEK` → `OUT_OF_SPRINT` → `PAST_DEADLINE` → `NOT_A_WORKDAY`.
- A PM week-click on an already-placed row re-places it on that week's first working day (the Design Lead's day is the PM's to overwrite by week, per #88's "the PM owns the week").
- A displaced (`sprint_id: null`) row re-slots by the same week click: membership derives from the sprint containing the resolved day, else stays outside (invariant 12: gaps allowed).
- Keyboard nudge (item 11) is IN scope, smallest form.
- Figma-less visuals built provisional (C).

---

# PLAN.md — Block 9 build plan (EPHEMERAL; rotates to docs/history/state-log/2026-09-11-plan-block9.md at CLOSE)

Drift report: `.claude/block9/drift-report.md`. Brief: `.claude/block9/brief.md`. Mode **Full**. Deploy waits for JP.

## Rulings in one line each (authority in brackets)

R1 Sprint Schedules is WEEK grain: hover a week, click places the row on that week's first working day; the bar is display-only [#88, #89 §2]. R2 The Deadlines tab owns the day: a pointer drag (and arrow-key nudge) within the card's week lane, four conditions — inside the assigned week · inside the sprint · ≤ deadline · a working day — refused = pale wash + snap back, nothing written [#89, v1.4 §6.5/§8]. R3 A re-dated sprint nulls the membership of rows whose day left its range; they keep their day, render under *Outside any sprint*, are listed in a notice, one audit row each; the save is never refused [#90]. R4 Rollover, deletion, add/place, `Add All`, the red bar: untouched [#88, #90].

## Agents and EXCLUSIVE file ownership

| Agent | Tier | Owns (nobody else touches) |
|---|---|---|
| **A1 server** | top | `src/services/sprint-items.ts`, `src/routes/schedule.ts`, `src/models/index.ts`, `scripts/migrate/migrations.ts` (only if a collection validator forces 013), `specs/001-sirius-v1/contracts/http-api.md` |
| **A2 client logic** | top | `frontend/scripts/40-app-state.js`, `frontend/scripts/50-gantt-geometry.js`, `frontend/scripts/90-events.js`, `frontend/scripts/80-loaders.js` (only if the payload needs a field) |
| **A3 template + style** | top | `frontend/templates/views/40-schedules.html`, `frontend/templates/views/50-deadlines.html`, `frontend/templates/partials/20-deadline-card.html`, `frontend/styles/35-gantt.css`, `frontend/styles/40-deadlines.css`, `frontend/styles/30-planner.css` |
| **A4 tests** | top | everything under `test/` (new `test/deadlines-drag.test.ts`, new `test/sprint-items-plot.test.ts` split, edits to `drag-hittest`, `sprint-schedule-*`, `deadlines-tab`, `schedule`, `sprint-items`, `calendar`/golden-style TZ tests, `test/helpers/gantt-render.ts`) |
| **A5 law docs** | mid | `specs/001-sirius-v1/gantt-rules.md`, `sprint-rules.md`, `deadlines-rules.md`, `frontend/CLAUDE.md` (only its drag paragraph) |

`lib/*` is untouched by everyone (invariant 5). `frontend/build.js` untouched. An agent needing a change outside its files or to a frozen name STOPS and reports.

**JP ruling at the gate (2026-09-11): the Deadlines day write is gated by SURFACE only** — any project member may PATCH `starts_on`; the route re-checks session + project membership like every route (invariant 9) and adds NO per-user or role check. "The Design Lead" is a team rule, not a permission.

## FROZEN INTERFACES (amended only by the main thread)

### Server (A1)
- `PlotIssueCode` gains `'OUT_OF_WEEK'`. Message, same voice as the three: `That day is outside the card's assigned week (Mon D Mon – Fri D Mon).`
- `plotIssue({ sprint, startsOn, deadline, assignedWeek? })` — `sprint` may now be `null` (row outside any sprint → the sprint check is skipped); `assignedWeek` is a Monday ISO date; when present it is checked FIRST. Order: `OUT_OF_WEEK` → `OUT_OF_SPRINT` → `PAST_DEADLINE` → `NOT_A_WORKDAY`. Existing call sites unchanged.
- New exports in `sprint-items.ts`: `weekKeyOf(iso: string): string` (the local Monday, via `lib/calendar` `toMonday`/`parseDate`/`localIso`) and `firstWorkdayOfWeek(mondayIso: string, calendar?: WorkingCalendar): string | null` (Mon..Fri scan with `isHoliday`; `null` when the whole week is holidays).
- `PATCH /api/projects/:projectId/sprint-items/:itemId` body: `{ starts_on?: DATE|null, week?: DATE, sprint_id?: OBJECT_ID }`; `week` and `starts_on` together → 400 `INVALID_BODY`. `week` (must be a Monday, else 400): server sets `starts_on = firstWorkdayOfWeek(week)` and judges the three non-week checks against the target sprint (`sprint_id` given → bare-move semantics as today; row has `sprint_id: null` and none given → the sprint whose range contains the resolved day, else stays `null`). Non-null `starts_on` (the Deadlines day write): the four checks with `assignedWeek = weekKeyOf(currentStartsOn)`; a row with no `starts_on` → 422 `NOT_PLACED` (route-level, same `{ code, message }` shape). `starts_on: null` unplots, unchanged. Audit shape unchanged (before/after `starts_on`, `sprint_id`).
- `SprintItem.sprint_id`: `required: false`, `default: null`. Payload `sprintId: string | null`. Queries that group by sprint tolerate `null`.
- `PUT /api/projects/:projectId/sprints`: after the update loop, for every remaining sprint, rows with `sprint_id = that sprint` and non-null `starts_on` outside `[starts_on, ends_on]` get `$set: { sprint_id: null }` (day untouched); response gains `displaced: [{ id, display_id, title, starts_on, from_sprint }]`; one `audit_log` row per displaced row, `action: 'sprint_item.displaced'`, actor = the request user, `before.sprint_id` → `after.sprint_id: null`. The existing `removed_items` (deletion) path is untouched.

### Client state (A2) — `40-app-state.js`
- REMOVED keys: `plotRow`, `plotDay`, `dragRow`, `dragDay`, `dragLeft`, `dragGrab` (and their project-switch reset).
- NEW keys: `hoverRow: string|null`, `hoverWeek: number|null` (index into `plannerWeeks`); `dlDrag: null | { rowId, fromDay, day: string|null, refused: boolean, weekKey }`; `sprintDisplaced: null | Array<{ id, display_id, title, starts_on, from_sprint }>`.
- `sprintGroups()` appends one group `{ id: 'outside', name: 'Outside any sprint', rows: items with sprintId === null }` ONLY when that list is non-empty; rendered last.
### Client geometry (A2) — `50-gantt-geometry.js`
- REMOVED: `dayAtX`, `plusLeft`, `placeable`. KEPT verbatim: `itemBar`, `deadlineTick`, `dayIndex`, `barLeftAt` (display caller only), `barWidthUnits`, constants.
- NEW: `dlDayPlaceable(row, dayIso, weekKey, sprint|null): boolean` — the client half of the four bounds (week, sprint range, deadline; weekday by construction of the columns; holidays are the server's).
### Client handlers (A2) — `90-events.js`
- REMOVED: `plotHover`, `plotLeave`, `plotPlace`, `barDragStart`, `barDragMove`, `barDragEnd`, `barDragCancel`, `barDragUp`, `barDragKey`, `barDragStop`, `barDragClear`.
- NEW: `weekHover(rowId, weekIdx)`, `weekLeave()`, `weekPlace(rowId, weekIdx)` → PATCH `{ week }` (optimistic: bar drawn at the week's Monday until the server answers with the real day, rollback on 4xx). `dlDragStart(rowId, ev)`, `dlDragMove(ev)`, `dlDragEnd(ev)`, `dlDragCancel()` (Escape, window `keydown`), `dlKey(rowId, ev)` (ArrowLeft/ArrowRight → `dlNudge`, Escape → cancel; other keys ignored), `dlNudge(rowId, delta)` (−1 / +1 weekday within the lane) → PATCH `{ starts_on }` optimistic with rollback; `dismissDisplaced()`.
- Hit-testing on Deadlines: `document.elementFromPoint` → closest `.dlday[data-day]` INSIDE the same `.dllane[data-week]`; anything else = `refused`.
### Template (A3)
- `40-schedules.html`: `.gweek` cells gain `on-mouseenter="['weekHover', row.id, @index]"`, `on-mouseleave="['weekLeave']"`, `on-click="['weekPlace', row.id, @index]"` on every row's track (placed rows included); class `gweek hover` when `hoverRow === row.id && hoverWeek === @index`; `.gitem` loses its mousedown binding and the `dragging`/`refused` classes; `.ghovcell`/`.gplus` removed; the hint (line 54) becomes `Hover a week and click to place the card on that week; the day is set on Deadlines.`; `.gitem` gets `title="Starts {{row.startsOn}}"`. A new banner partial `frontend/templates/partials/40-displaced-notice.html` (A3 owns) rendered above the groups when `sprintDisplaced` is non-null: `N cards moved to Outside any sprint` + one line per card + a Dismiss button (`on-click="['dismissDisplaced']"`).
- `50-deadlines.html`: expanded lanes get `class="dllane" data-week="{{w.key}}"`, day columns `data-day="{{d.iso}}"` and class `dlday target` when `dlDrag && dlDrag.day === d.iso && !dlDrag.refused`. Collapsed lanes: no drag.
- `20-deadline-card.html`: `on-pointerdown="['dlDragStart', c.rowId]"`, `on-keydown="['dlKey', c.rowId]"` (routes to `dlNudge`), `tabindex="0"`, `role="button"`, `aria-label="{{c.title}}, starts {{c.startsOn}}; use left and right arrows to move the day within the week"`, classes `dlcard dragging` / `dlcard refused` from `dlDrag`.
### Styles (A3)
- `35-gantt.css`: remove `.gplus`, `.ghovcell`, `.gitem.dragging`, `.gitem.refused`; `.gitem { cursor: default }`; add `.gweek.hover` (the violet week tint, block 7's `.ghovcell` colour). `.gtrack.placing` stays.
- `40-deadlines.css`: `.dlcard { cursor: grab; touch-action: none }`, `.dlcard.dragging` (lifted: shadow + opacity .9), `.dlcard.refused` (block 7's pale wash: opacity .45 + `outline: 1px dashed`), `.dlday.target` (violet tint), `.dlcard:focus-visible` ring. Never `pointer-events: none` on `.dlcard` or an ancestor.

### Tests (A4) — written against the names above; test/CLAUDE.md law applies
Delete/invert/new per the drift report §D. Vacuity proofs (run by the main thread AFTER an anchor commit, in the main tree, never a worktree): `OUT_OF_WEEK` check disabled → week-boundary tests fail; `sprint_id` nulling removed → the inverted groups test fails; `.dlcard` given `pointer-events: none` → the hit-test sweep fails; `firstWorkdayOfWeek` returns the raw Monday → the holiday-Monday test fails.

## Sequence
BUILD (A1–A5 parallel, one workflow) → VALIDATE (anchor commit; typecheck, lint, vitest `TZ=UTC` + `TZ=Asia/Manila`, calendar suites `America/New_York`; then the four proofs alone) → REVIEW (correctness with two refuters per finding, then one simplification agent; any edit re-runs VALIDATE) → E2E on the local rt-test rig by real pointer (Deadlines drag: a valid drop, the four refusals, Escape, arrow nudge; Sprint Schedules week click on a holiday-Monday week; a sprint re-date that displaces a card → notice + group; console clean) → CLOSE (STATE.md, day log, this file rotates out; owls #88–#90 acked on deploy; jp→miles note).

## AMENDMENTS (main thread, after BUILD — 2026-09-11)

1. Template field spellings follow the shipped data: day columns `data-day="{{d.day}}"` (not `d.iso`); the card's aria-label uses `c.label` (not `c.title`); cards carry `c.rowId`, `c.startsOn`, and `data-row="{{c.rowId}}"` (for `dlRefocus` after a nudge).
2. `.gitem` keeps rule 42's title contract `{{b.title}}` (`startsOn → finish`, "· past the client deadline" when late) — NOT `Starts {{row.startsOn}}`; the start day is displayed by that title.
3. The `outside` group (`sprintGroups()` id `'outside'`) renders NO add-search field and NO Add links — it has no sprint to add into.
4. PATCH 200 body: `{ ok: true, starts_on, sprint_id }` (noop answers carry the same). `weekPlace` reads both from it.
5. `displaced[].from_sprint` = the sprint's NAME as saved in the same request.
6. Exact copy: `OUT_OF_WEEK` → `That day is outside the card's assigned week (Mon 3 Aug 2026 – Fri 7 Aug 2026).` (longDate register, with the year); `NOT_PLACED` (422) → `That card has no week yet — place it on Sprint Schedules first.`; a `week` whose five days are all non-working → 422 `NOT_A_WORKDAY` `That week has no working day.`
7. Audit action is **`sprintItem.displaced`** (house camelCase, beside `sprintItem.plot` / `sprintItem.rollover`) — the snake_case `sprint_item.displaced` is replaced everywhere.
8. Rollover (main-thread decision, option b): `src/services/rollover.ts` becomes null-tolerant — a row with `sprint_id: null` still rolls its day (§6.2: every placed card rolls); the R3-2 conditional update keys on `sprint_id: null` for such rows; if `sprintFor(next)` finds a covering sprint the roll adopts it exactly as any cross-boundary roll does today (system action, R10-d), else it stays outside. Audit before/after null-tolerant. The fix agent owns `rollover.ts` and adds the two tests in `test/rollover.test.ts`.
9. `eslint.config.js` `FRONTEND_SHARED`: add `dlDayPlaceable`; drop `dayAtX`, `placeable`, `plusLeft`.
10. `docs/MAP.md` + area maps regenerate at the anchor (`npx tsx scripts/generate-index.ts`), not by a build agent.

## AMENDMENTS 11–18 (main thread, after REVIEW — 2026-09-12)

11. **`.gantt .gitem { pointer-events: none }`** — a multi-week bar was swallowing the week click on every week it covers, which defeats ruling 2. Rule 42's title moves with it: A2 exports `rowTitle(row)` (the same `startsOn → finish` string, "· past the client deadline" when late) beside `itemBar`; A3 binds it on the row's `.gtrack`. Ruling 3 (the start day is DISPLAYED) is preserved by that title.
12. `dlKey` and `dlDragStart` ignore any event whose target is not the card node itself (inner links must keep their own keys), and `dlKey` returns early on Alt/Meta/Ctrl chords.
13. `dlNudge` steps OVER holidays using the `holidays` set already in app state (`80-loaders.js`), so the keyboard reaches every day the pointer reaches (v1.4 §8 / NFR-9). `dlDayPlaceable`'s frozen signature is unchanged and the server stays the holiday backstop for the pointer drop.
14. `dlRefuse` also flashes the matching refusal sentence, and the shell `.banner` carries `role="status"` — a refusal must reach a screen reader by key and by pointer alike.
15. The deadline card's `role` becomes **`group`** (was `button`): an ARIA button has presentational children and the card contains two links.
16. `weekOffered` mirrors the server: a week is offered only when its FIRST WORKING DAY is inside the sprint and on/before the deadline. A2 adds `weekFirstWorkday(weekKey, holidays)` (A2 also owns `frontend/scripts/30-dates.js` for this round). Consequence to report to product, not to fix here: a sprint that starts mid-week cannot take a card in its own first week.
17. `placeRow` reads the PATCH 200 body and re-stamps the row from `starts_on`/`sprint_id` BEFORE `loadAll()`, so a swallowed reload failure can never leave a day the server did not give (invariant 8).
18. The PUT /sprints displacement pass **(18a)** judges only sprints whose dates actually changed in this request, and **(18b)** writes per-row conditional updates keyed on the `starts_on` and `sprint_id` it read (rollover's R3-2 discipline), listing and auditing only rows it actually moved. _(Lettering matches the code comments and the test names; an earlier draft of this line had them the other way round.)_

## AMENDMENTS 19–21 (main thread, after FIX-2 — 2026-09-12)

19. **Amendment 12 is narrowed for the POINTER half**: `dlDragStart` refuses a press that began on an inner link or button (`target.closest('a, button')`), NOT one that began on any child. The whole card is the ruled drag source (#89 §1) and its visible content is all child elements, so strict node identity would leave only the padding ring draggable. `dlKey` keeps the strict `target !== node` test, because a key press has one focused target.
20. The client's fourth refusal sentence, for a row whose sprint is no longer on the schedule, is **invented copy**: `That card's sprint is no longer on the schedule.` The route answers that stale row a bare 404 with no message, so there is no server twin to match. Accepted; flag to product only if the wording matters.
21. **The live region is always present and visually hidden.** A region created together with its text is unreliably announced, so the shell carries a permanent hidden region holding the banner sentence; the visible banner keeps the text and loses `role="status"`. (Supersedes amendment 14's mechanism, not its rule.)

## RESIDUALS — recorded, not fixed in block 9

- `.gantt .gdl` (the 1px deadline tick) stays pointer-opaque and intercepts a 1px column of one week cell; it carries the deadline date as its only readout, which transparency would kill.
- The Deadlines day heading's DONE count fails AA at rest (3.15:1) and on the drop tint (2.84:1). Pre-existing palette/frame question for product, not a block 9 regression.
- A sprint that STARTS MID-WEEK cannot take a card in its own first week: the week resolves to its first working day, which is before the sprint starts, and the server refuses. The client now mirrors that refusal rather than dangling the offer (amendment 16). **Product question for Miles/JP.**
- A repeated IDENTICAL banner sentence is not re-announced (a zero-difference text change in a live region). True of every banner in the app, not a block 9 regression; it bites when a user presses repeatedly against the same bound. Cure would be a clear-then-set or a nonce in `flashBanner`.
