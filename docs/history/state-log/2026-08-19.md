# STATE.md session log — 2026-08-19

_Entries newest-first. Later sessions append this date's entries here when STATE.md rotates them out._

- 2026-08-19 — **Leftover sweep + the template split DEPLOYED; the worktree is gone and its `--dir test` workaround is retired.** JP asked where the previous session left off, "specially the worktree". **The worktree was already resolved and only its instructions survived**: `git worktree list` returns the main checkout alone, `.git/worktrees/` does not exist, `.claude/worktrees/` is empty, and `worktree-template-split` pointed at `dad3114` — the identical commit as `main`, both directions of `git log main..branch` empty. Branch deleted (recoverable: same SHA as main). STATE.md's standing "**run the suite with `--dir test`**" instruction was the only trace left of the concurrent session, and it was stale — a bare `TZ=Asia/Manila npx vitest run` collects **62 files, 982 passed + 24 todo** and nothing else. Retired in the build-health block. **DEPLOY** (`b401bac`): first attempt ABORTED at deploy.sh's own gate — `test/context-architecture.test.ts` "STATE.md stays at or under 10KB" went red at 10,255 vs 10,240 because the retirement note I had just written pushed the file 15 bytes over its cap. The cap did exactly its job; trimmed the note, then folded the now-redundant `ctx t-split` phase row into the `13k b13` row, which bought back 109 bytes and left one row where there were two (10,146 bytes). Second run green end to end: tests + typecheck + build + rsync, `npm ci --omit=dev`, `npm run migrate` → **[migrate] up to date** (still through 007), pm2 restart of `sirius` (id 12) and `sirius-worker` (id 13), both back online. **LIVE VERIFICATION**: `BASE_PATH=/sirius` on the host, so `/healthz` 404s and `/sirius/healthz` is the real probe — `{"status":"ok","mongo":"connected","redis":"connected","env":"production"}`, 200. Host `public/index.html` sha256 `5fdd14aa…` **equals** the local build byte-for-byte, and `frontend/templates/` is correctly absent from the host — the split is build-time only, the server receives the composed page and never the nine sources. **A WRONG CLAIM I MADE AND CORRECTED**: before deploying I told JP the built output was "byte-identical to what's already live", inferred from an empty `git diff 4499484 HEAD -- public/index.html`. That diff was empty because **`public/` is GITIGNORED** (`.gitignore:15`) — the path is untracked, so a diff over it can never report anything. The claim was unfounded, not merely unverified; the honest basis is last session's source-level proof that the composed template equals b13's single file, plus today's host-vs-local hash match. Recorded in the phase row so the next session hashes the host file instead of reaching for `git diff`. **`build.js` byte-count fix**: `[build] … written (${out.length} bytes)` reported 378,363 for a 379,784-byte file — `String.length` counts UTF-16 code units and the template is full of multibyte `·`, `—`, `✓`, so it under-reported by 1,421 bytes. Now `Buffer.byteLength(out)`; the log matches `ls` exactly. Build re-run twice pre-fix confirmed determinism (same sha256 both times), so the discrepancy was never a build instability. **Gates**: 982 passed + 24 todo, 62 files, green `TZ=Asia/Manila` and `TZ=UTC`; tsc and eslint clean; no loopback flake in four suite runs today. **Still parked from the split session, now unblocked**: the Ractive pin (`^1.4.2` → 1.4.4 installed, CDN serves 1.4.2, so the build's parse gate validates a version the browser never runs) — the stated blocker was that fixing it meant an install mutating the main checkout's `node_modules` from inside a worktree, and there is no worktree any more · `.gitignore`'s `node_modules/` trailing slash · the duplicated due-popover markup at the task row · the three read-only Ractive two-way-binding warnings on Forecast.
