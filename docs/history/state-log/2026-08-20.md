# STATE.md session log — 2026-08-20

_Entries newest-first._

- 2026-08-20 — **BATCH 14 built, deployed and live-verified: owls #52 and #53.** **#52 was a bug report that needed a model answer first.** Miles found that expanding an MC row on the real board bundled every task card sharing that MC number onto one row, and asked the one question that decides the fix: does the board carry any real task→deliverable linkage we could use instead? **Measured rather than inferred** — a read-only probe over all 592 live cards of `hLL7WW2V`, run ON THE HOST so the ARES key never left the server and no brief content reached the laptop (invariants 15 and 16), printing aggregates only. The answer is no, comprehensively: checklists **0 of 218** mains carry a single item · description card-links **0** in both directions · list position resolves **0 of 279** ambiguous tasks · members resolve **36 of 279** (only 50 have any member) · the best name segment resolves **60 unique against 117 that match several mains and 102 that match none**. A useful side-finding: ARES *does* return `checklists`, `members` and `description` — our adapter drops them — so those fields were reachable and are simply empty on this board, which closes the question properly instead of by assumption. Name-matching is therefore REJECTED as a fallback: it would resolve one task in five while silently mis-resolving 117, which is exactly the failure mode Miles said he wanted to avoid. **This CONFIRMS invariant 4 rather than breaking it** — tasks attach to the MC because no edge exists in the data, not because we chose a convention. **The finding that changed the design.** Miles wrote that the single-main case is "the majority case, correct today". Per MC number it is roughly even (18 of 37). Per TASK it is inverted: only 77 of 356 tasks sit under a single-main MC, so **279 of 356 (78.4%) land in the "shared" branch** — the state he treated as graceful degradation is what the PM sees four times in five, and on the worst MC it reads "shared across 60 deliverables". Sent as jp→miles #43 BEFORE building, per JP's steer, with the rule built to spec meanwhile so nothing blocked. **Built**: `mcDeliverables` on the wire (derived from the SAME `rowsByMc` the weight pass divides by — two counts of one thing are two things that can disagree); `sharedMc`/`mcDeliverables` stamped per row in `loadAll` beside `hasTasks` (performance law: never asked in the template); a caption row that mirrors the parent's full column model with **no colspan**, because a colspan row would be the first in the table that cannot drift with the columns — the promise made to Miles in #40; and the chevron moved to the group's first row, which is the actual bug — it always controlled MC-level state, so a sibling's chevron opened a list elsewhere on screen. **R-exp-k, a correction to the owl**: row weight needed NO change. BR-6c already divides an MC's tasks across the group's deliverables, so sibling rows always weighed evenly — "one row looks enormous while its siblings look empty" does not describe what shipped, and the forecast never read oddly at row level. **#53** — the Needs Info hover card takes `--shadow-card: 0 4px 12px rgba(0,0,0,.15)`. Verified against the Figma annotation on node 537:69135 first, which caught a discrepancy worth keeping: the codegen snippet emits `drop-shadow-[0px_4px_6px]` because it reports the SVG `stdDeviation` (6) where CSS wants the blur radius (12) — the annotation text and the `Shadow/sm` style token both say 12, so the owl is right and the generated code is the odd one out. **The clamp half is the real work**: `placeBox` gained an optional per-overlay `bleed`, and the card passes `{ x: 12, top: 8, bottom: 16 }` — a 12px blur reaches 12px every way and the 4px drop takes 4 off the top and adds 4 to the bottom. Miles's "~12px each side and ~8px above" agrees but omits the bottom, which is the largest of the three. Both placements carry it — the measured re-place would otherwise silently undo the first. Absent `bleed` every other overlay's arithmetic is byte-for-byte what it was. `--warn-vclamp` is the CSS twin and its test DERIVES it from `OVERLAY_EDGE + WARN_SHADOW_BLEED` rather than retyping it, so the pair cannot drift. **Two existing tests went red and both were RIGHT to** — they pinned the pre-#53 spec (`.warnpop` must carry no `box-shadow`; the cap must read `--space-8`). Updated to the new spec rather than deleted, and the SubTone count in the older suite was tightened from a bare `class="subtone"` count to the `Main Card` text, since owl #52's caption now wears the same voice one row below and a bare count would have passed for the wrong reason. **Gates**: 982 → **993 passed + 24 todo, 62 files**, green `TZ=Asia/Manila` AND `TZ=UTC`; tsc and eslint clean — eslint caught `WARN_SHADOW_BLEED` as an undeclared global, which is the no-bundler shared-scope list doing its job. Deployed `64dc65f`; healthz 200 with mongo+redis connected, host bundle sha256 == local, six markers present, and the live DB independently reproduces the probe (37 MC numbers, 19 shared, max 60). Law recorded as R-exp-g..k and R-warn-p/q in `pipeline-frame-notes.md`. **One commit-message repair**: zsh executed a backticked word inside the heredoc and swallowed it; amended and force-pushed with `--force-with-lease`.

- 2026-08-20 (later) — **Two styling mismatches on the hover card, both found by JP holding the build next to the frame.** **(1) `Open Card` was underlined and the frame's render is not.** The source of truth contradicts ITSELF here: node 537:69135's annotation text says "UNDERLINED" while its own render shows none — and batch 10 had already hit this and resolved it toward the annotation, leaving the reasoning in a CSS comment (`underlined per the annotation, which overrules the frame's own render`). JP reversed it: the frame wins, because the frame is what he and Miles both look at. The test was INVERTED rather than deleted — `text-decoration: none` asserted and `underline` asserted absent — so restoring the declaration takes a deliberate act with a red test behind it rather than drifting back. No hover/focus underline invented: a frame shows a resting state and cannot rule on hover, and inventing an interaction on top of a spec already in dispute would be the wrong instinct. Both halves raised in jp→miles #45, with a request that whichever half is wrong gets fixed at source — this conflict has now cost two build passes. **(2) The card read `due date` where the frame reads `Due date`** — spotted in the same screenshot pair, not reported, fixed in the same pass. Not a typo: those strings are the SERVER's field tokens and are lowercase deliberately, because `srLabel` reads them mid-sentence where a capital would be wrong, while the frame shows them as list headings where lowercase is. So the item now carries `field` (the raw token — what `WARN_WHY` is keyed on, and it never reaches the DOM) beside `label` (sentence-cased display). Five tests keyed on `label` as if it were the token; each was updated to the half it actually means rather than loosened, and the markup comparison was NARROWED to the display pair since `field` deliberately never ships. `Figma attachment` needed nothing — upper-casing a capital is a no-op. **Gates**: 993 + 24 todo, 62 files, dual-TZ; tsc/eslint/build clean. Deployed `8070193`; healthz 200, host bundle sha256 == local, and the bundle carries exactly one `text-decoration: underline` left (the tab rule) with `.wpopen` on `none`. Law: R-warn-r and R-warn-s.

- 2026-08-20 (evening) — **Owls #54–#56 processed; #55's presentation ruling built and deployed.** Miles answered the shared-caption question from jp→miles #43 and **the answer inverted my emphasis**: I had led the caption with `Shared across N deliverables`, making the count the headline. His ruling — the tasks are the content, the explanation is the caption, and the count is secondary detail at every N. His reasoning is the part worth keeping: at 78% of task views this is not an error path being handled gracefully, it is the normal way of seeing work on a reused MC number, so it must read as "here is the work on MC-837" rather than "we couldn't do the thing you wanted". He also noted a person reading a full task name next to their deliverable can often tell the match immediately even though a parser resolves only 21.5% — so the state's job is to put the names in front of them legibly and get out of the way. Rebuilt: caption now opens `Work on MC-837 — these tasks carry the MC number, not a link to one card` with ` · shared by N deliverables` trailing in its own `.pshn` span one tone quieter (`--slate-400`). The count renders at EVERY N with **no threshold rule** — his words: one treatment that degrades quietly beats two that need a boundary defended. Task names needed no change; they were already the row's prominent `.cardname`. The chevron's accessible label was re-worded to the same framing so the visual and the screen-reader description cannot diverge. New test asserts the ORDER (explanation before count), not merely the presence of both, plus the count at N=2 and N=3. **#56 accepted both corrections from jp→miles #44** — he named his own error precisely (conflating where tasks are DISPLAYED with how weight is COMPUTED, then asserting a forecast consequence from a rendering fault without checking the two were connected) and called the chevron diagnosis better than his own symptom description. He has written the Figma codegen trap into the node's annotation — `stdDeviation` reported where CSS wants blur radius, exactly half — and adopted a standing rule on their side to export and read the SVG whenever a stroke looks directional or a shadow is in play, since `get_node_info` reports neither per-side stroke weights nor effects. Bleed corrected on their side to 12/8/16. **#54** closed the cadence thread (three of four worries dissolved on the no-client-refresh-loop fact), accepted the shared chip line and the no-colourway call, and left ARES read-path caching explicitly OPEN and visible rather than assumed closed — Miles is chasing whoever owns ARES. It also re-raised #52 as still open, which had crossed with our fix; already built and live. **The numbering conversation now has numbers**: 19 of 37 MC numbers reused, 78.4% of task cards affected, third bug from the same root — Miles is taking it up on the Frost side. **Gates**: 993 → **994 + 24 todo**, dual-TZ; tsc/eslint/build clean. Deployed; the first healthz probe returned 000 because it raced the pm2 restart — re-probed clean at 200, host bundle sha256 == local. Law: R-exp-l.

- 2026-08-20 (night) — **Owls #57–#60: three closed, one HELD.** **#59 closed the row-controls pass** — it needed decisions rather than drawings, so no new frame. Reveal rules as a set: checkbox and note chip always visible (one is how a row is selected, the other carries CONTENT — hiding it would mean hovering every row to find which have notes), handle and cluster hover-revealed, pinned rows keep their cluster permanently because state must not hide behind a hover. Only ONE mismatch existed: the handle rendered at rest. Fixed with the same opacity reveal and the same trigger pair as `.gactions` so the two appear together. The handle COLUMN was already reserved by construction — `.c-mc`'s 58px pad does not depend on the handle rendering — so R-row-b needed a test, not a change. The reveal rules are asserted as a SET, deliberately: each is only correct relative to the others, and a change that hid the checkbox or revealed the handle at rest would pass any test that read one rule alone. **#59's keyboard ruling is NOT built and is with JP.** It rules the three row-action icons not keyboard-reachable — copy/pin/unschedule mouse-only — stated as decided with the accessibility consequence understood. But what ships is the OPPOSITE: they are real `<button>`s in the tab order, and `opacity: 0` was chosen over `display: none` at batch 3 precisely to keep them reachable. So implementing it would REMOVE working keyboard access, and combined with #59's own requirement that they keep accessible labels it would leave three actions a screen-reader user hears on every row and cannot activate — worse than either alternative. CLAUDE.md is explicit that an owl never carries JP's authority, and twice before an owl asserted a ruling JP had not made; this is that situation, so nothing was touched. Recorded as R-row-d, OPEN, and raised in jp→miles #47 plainly rather than quietly ignored. **#59 was deliberately NOT acked** — the reveal half is processed, the keyboard half is not, and read ≠ processed. **#58 asked for the per-MC breakdown**, delivered: 19 numbers carry **199 of 217 deliverables and 275 of 350 task cards**, worst being MC-655 at 60 deliverables, MC-837 at 34, MC-793 at 18. Seven numbers account for the bulk — fixing those seven beats fixing the other twelve combined. **A finding nobody went looking for: 11 MC numbers carry task cards but NO main card at all — 35 task cards.** They hang under no row, and BR-6c only weighs tasks whose MC has a deliverable row, so 35 cards of real work are ABSENT FROM CAPACITY. A second, quieter cost of the same root cause, and unlike the reuse case it is silent rather than visible. Also noted for accuracy: the board is live, so this snapshot differs slightly from the morning's (217 vs 218 deliverables, 350 vs 356 tasks) — the shape is stable, the integers move hourly. Incidentally MC-825 now carries 10 deliverables, not the 99 the constitution cites as its example; the principle is untouched but the illustration is stale, and amending the constitution is JP's call, not a silent edit. **#57 withdrew the Open Card underline outright** rather than merely resolving it toward the frame — `get_styled_text_segments` returns `textDecoration: "NONE"`, and the root cause is that the instruction was written for the RETIRED message-line design and lapsed at batch 10 when the message line went away, without anyone retiring it. Hover and focus answered too: nothing in the card is underlined, which closes the question I deliberately left uninvented. The stale source frame is deleted on their side. Code comment and R-warn-r moved from "pending Miles" to settled, carrying the whole history so nobody re-derives it. **#60's Smoke pass came back clean on all five**, including the task due set/clear BY HAND — the one that mattered, since it was in exactly the position bar-dragging was in before it turned out to be broken for every human. **Gates**: 994 → **996 + 24 todo**, dual-TZ; tsc/eslint/build clean; deployed `60a4036`, healthz 200, host bundle sha256 == local. Law: R-row-a/b/c built, R-row-d held.

- 2026-08-20 (late) — **Owl #61: the keyboard ruling was REVISED, and the orphan finding became build work.** **Escalating instead of complying was the right call and product said so.** Miles revised #59 outright: keyboard access stays exactly as built, JP stood down, no decision needed. The two facts that moved it were both in my #47 and neither side had them when the original ruling was made — (1) the access ALREADY existed and was deliberate, so the decision was *removing* working access rather than *declining to add* it, which is a materially different thing; and (2) labels-retained-plus-focus-removed is the worst of the three positions, leaving three actions announced on every row that nobody can activate. Their own summary is the useful generalisation: **tab and arrows are different keys** — the goal was always arrows reserved for the bars, delivered by arrows stopping dead at the controls, and tab access was never in tension with it. That is the argument I should have led with rather than the accessibility one. R-row-d closes as "stays as built, nothing changed"; their frame annotation still carries the superseded ruling until their Figma plugin is back, so owl #61 is authoritative meanwhile. Removed from STATE.md's blocking table and `docs/MAP.md` regenerated. **The 35 orphaned task cards are now VISIBLE (R-exp-m/n).** Miles ruled it the most valuable thing in my message: not a data fix — the cards need correcting at source — but the gap must stop being silent, because today 35 cards of real work are invisible in the Pipeline *and* absent from capacity, so the forecast is quietly light and nothing indicates it. Built as ONE server field (`unattachedWork`) derived from the two maps `loadPipeline` already builds — an MC with tasks and no row is exactly a `workCardsByMc` key `rowsByMc` does not know, so no third query and no second definition that could drift — surfaced twice: an `UNATTACHED` tile on the Pipeline metrics strip, and a line beside the planner's capacity control, which is Miles's stated property (*someone looking at capacity can tell work is missing from it*). **Neither sits in a week cell** — these cards are in no week, and a per-week home would be a lie about where they are. Both hide at zero, because a permanently-zero metric teaches people to stop reading that spot and then it is not there when it matters. Amber not red: a hygiene gap to notice, not a failure to alarm about. **R-exp-n, an existing inaccuracy found while building it**: `WORK CARDS` has ALWAYS counted these — the server keys work cards by MC and an orphan gets a key like any other — so that total has been reporting cards no row could ever show. The tile names a subset rather than adding to a total, turning a quiet inaccuracy into a stated one. Placement and wording sent to Miles as proposals (jp→miles #48), not decisions. New tests: nine, including the one that proves the gap the surface exists to report (orphans weigh nothing while the field says so), project scoping, hides-at-zero, both plural forms, and that the tooltip carries the CONSEQUENCE rather than just the count. A new `renderMetrics` helper was needed because the strip sits outside `.pscrollwrap` and the tile is conditional — its absence at zero has to be renderable to be provable. One assertion initially failed on an HTML-escaped apostrophe (`week&#39;s`), fixed by matching around it rather than encoding the escape. **Gates**: 996 → **1005 + 24 todo**, dual-TZ; tsc/eslint/build clean. **The first deploy ABORTED at its own test gate** on the standing ~1-in-5 loopback flake (test/CLAUDE.md rule 5) — re-run clean and recorded, not masked. Second deploy green: healthz 200, host bundle sha256 == local, three markers present, and the live DB independently confirms 35 cards across 11 MC numbers.

- 2026-08-20 (night, later) — **BATCH 14pf: Pipeline filter + sort built, deployed and live (owl #62, five nodes).** The spec arrived complete, and the first useful thing was NOT building it. Product flagged one item as "confirm the source rather than infer one" — the default order, "by order of filing, most recently ingested first" — and checking it found **the field does not exist**. We store a `created_at`, but it is when the SIRIUS ROW was created: on the live board it holds five distinct days with **289 of the rows stamped 2026-08-12**, the day the board was onboarded. Ordering by it would have put the bulk of the table in a meaningless tie while looking entirely correct. ARES does carry the card's own `createdAt` — verified 100/100 sampled, spanning 13 distinct days — so the schema, mapper and sync now store it and **migration 008** indexes it. The sync writes it on the UNCONDITIONAL half, not the stale-guarded registry half: a card's creation time cannot change, so there is no write of ours for an old read to revert, and it is spread conditionally so a payload omitting it cannot null out a value already held. **No backfill script was needed** — the value comes from ARES, so reading the board IS the backfill; verified live at **217/217 active rows** immediately after deploy. **All five nodes verified before building, and this time the frames and the owl agreed throughout** — no contradiction, unlike the underline and the accent. But the frames carried FOUR requirements the owl did not: Clear disabled when there is nothing to clear (both panels), the `Group: Item` label format with the prefix doing real work, categories derived from board values rather than a fixed list, and no state filters. **The three questions product delegated, answered in jp→miles #49 before building.** (1) Facet counts: neither option offered — each axis counts against the OTHER axes, ignoring its own, because counting against all filters including its own drops every sibling to zero the moment one is picked, so a second value could never be added without clearing first; accurate and unusable. (2) STATUS scrolls inside its own group so the four short axes keep their place. (3) OR within, AND across — confirmed, and it is the same decision as (1) seen twice. **Three decisions of mine, all flagged**: the sort label cap is 240px (longest of the eight measures ~235px, so nothing truncates today — the cap exists so a future label cannot push the search field); filter and sort RESET on project switch, matching R-exp-f's reasoning, because a Requestor or Status carried across names values the other project may not have; and **"reset pagination to page one" has no target — the Pipeline has no pagination**, so it maps to returning the reader to the top rather than inventing a page counter nobody reads. **A correction to my own #49 sent in #50**: I told product STATUS would keep Trello's list order. It cannot — the wire carries the list NAME but no POSITION, so there is no progression to preserve. Alphabetical, which happens to match their four sample values exactly. **Deliberately NOT written**: the test the frame asks for asserting the five categories reconcile to the row count. That is false on a real board where cards lack values, and the test would pin a falsehood — the same finding as the "None" gap, still open with product. **Two existing guards generalised rather than loosened**: the migration test asserted "007 runs last", which was true when written and false the moment 008 arrived — it now asserts the ordering invariant plus no duplicate prefixes, which needs no edit when a migration is added and catches reordering the old form could not; and the ellipsis guard became a NAMED ROSTER, so a legitimate new owner is explicit while an unnamed one still fails. **Icons**: the funnel and the opposing arrows were downloaded from the frame's own asset URLs and inlined with strokes normalised to currentColor — not hand-drawn, per the skill's rule. **Panels join `OVERLAY_KEYS`**, so mutual exclusion, the outside click, the scroll dismisser and Escape-with-focus-return come from the one door every overlay uses rather than a second implementation that could disagree. Three recipes were converted from `function` to `const` so the test helper can slice and EXECUTE them, which is this repo's stated convention for shared recipes. **Gates**: 1005 → **1035 passed + 24 todo, 63 files**, dual-TZ; tsc/eslint/build clean; index current. Deployed `97fd82a`, migration 008 applied, healthz 200, host bundle sha256 == local. Law: R-pf-a..i.
