# 2026-08-28 — the Sprint Schedules rebuild: first build under the workflow law

## The workflow itself became law first
JP set the standing process (CLAUDE.md §Build workflow, rev b — an agent
draft validated with two amendments: PLAN.md made ephemeral against the
HANDOFF-revival hazard the architecture suite guards, and Light mode's gate
made a veto not a wait, per the 2026-08-17 asking-vs-deciding ruling).
Model tiers: survey low, build top, review top always.

## The build (owls #72/#73, frame 731:98513)
Four layer-owning agents off PLAN.md's frozen interfaces (rotated into
2026-08-28-plan-sprint-schedules.md). The unit change landed whole: rows
from sprint_items only, nothing auto-populated, no derived membership, no
Outside-any-sprint/Unscheduled tails; checkbox → violet + → click-to-place
(week's Monday); one bar startsOn→finish (finish day inclusive, colour by
prefix — COLOUR only); 1px red deadline rule; add flow with both upward
dropdowns (fixed 218px scroll, weight-only selection, MC re-pick clears the
card); footer counts work cards by bar overlap; Suggest UI withdrawn; drag
machinery, requestor/type columns, clip tooltip retired repo-wide.

**Mid-build coordination that worked:** agent C escalated two unnamed
interfaces instead of guessing (frozen as proposed); C's Rex read overruled
the plan's bar radius; C caught the 48px header revision-miss; C's stroke-2
tick reading LOST to owl #72's explicit 1px ruling (ruling > node > prose).
Agent B caught the main thread's own baseline dropping deadlineTick.

**A near-catastrophe, mine:** revert-proof restores ran `git checkout`
against UNCOMMITTED fleet work — five files reverted to HEAD. Recovered
byte-exact from the built bundle (concatenation markers). Law absorbed:
anchor the build in a commit BEFORE any revert proof; snapshots to /tmp,
never git checkout.

## The adversarial review earned its place
Three lenses, 19 agents, every finding verified by an agent told to refute
it: 15 confirmed, 1 refuted. The headline: **PUT /sprints regenerated every
sprint id on every save** — latent for months under derived membership,
converted by this build into silent unrecoverable loss of every scheduled
row on a routine rename. Fixed: id-preserving upsert, removal cascades the
sprint's items in the same audited act (the #30 confirm banner's count is
now a kept promise), unknown id → 409 SPRINTS_STALE. Client carries the id
through sprintPayload. Plus: locks now span their reloads AND the server
grew a before==after no-op guard (invariant 10 logs changes, not attempts);
loadAll gained a generation counter (out-of-order responses resurrected
cleared bars); Escape-mid-POST phantom draft; focus drops to <body> on menu
picks (.gddmenu joined closeMenus' held-focus set); the collapsed pane hid
the add affordance whole after its cells spilled 568px over the timeline;
the upward menu measures headroom and flips (allowed by #73); the lane chip
truncates before evicting the only unplot control; the add zone answers
Enter/Space; the delete-confirm copy and the modal explainer left the
derived-membership era.

**The simplification pass caught a misplaced review fix:** the stale-pick
guard had been anchored on `computeDeadlines()` — a NON-UNIQUE line — and
landed in writeCapacity's refetch, which never replaces `addable`.
Re-seated in loadAll on a unique anchor. Lesson: anchor on unique lines,
and a green suite does not prove a guard is in the right place.

## Verification
Dual-TZ 1073/1073 + 24 todo throughout (two environmental-flake reds,
green on re-run, recorded). 11 template/geometry revert proofs + 5 server
snapshot-revert proofs, all non-vacuous. Browser E2E on seeded synthetic
fixtures, full flow: add (Done card correctly never offered) → place
(violet + tracks, Monday written, bar 55×31 render+late, FORECASTED
matches) → footer counts → unplot → re-place → **rename-survival** (the
finding-1 fix, live: row keeps its sprint, bar, and count through a modal
save). Console clean. NOT deployed — JP's gate.

## Owed / carried
- Deadlines part 2 (#74) next; then #75 (lane mapping via Apollo→ARES per
  JP — owl #59 sent asking product to rule the mapping on Apollo's fields;
  rollover; the 40% done card).
- Flagged to product eventually: the icon trio renders copy/pin inert;
  no remove-row control exists because none is drawn; keyboard placement
  path is pointer-only (the add flow's entry is now keyboard-reachable).
- HARD_IDEAL/HARD_CEILING now dead in 10-constants.js (future sweep).
- The old deliverable week-editing is paused until Deadlines part 2 (JP
  yes, 2026-08-28).

## SHIPPED (addendum)
JP: "Go and ship it. Inform Miles as well via Owl." Deployed same day —
healthz steady over six polls, live bundle carries the rebuild (add zone,
violet +, menus, groups, footer; Suggest and ghost markup absent; the one
surviving `draggable` is Deadlines' day-drag, which is meant to survive).
jp→miles #60 sent: what shipped, the Render-before-Sketch prediction
landing as predicted, the flip allowance used, FOUR defaults flagged for
ruling (checkbox-as-selection, inert copy/pin, no row-remove control,
pointer-only placement), and the sprint-save identity fix with the
re-worded #30 confirm. #72 and #73 acked.

## Addendum 2 — the spot-fix block (same day, after the ship)

JP spot-checked the live build against Figma and named two misses (nodes
733:108505, 731:100230). Survey from the nodes settled both: the deadline
marker was BUILT and correct but could never render — every real row's
client deadline predates the visible window and `deadlineTick` clipped
left — and the week-block + was checkbox-gated where Miles's note rules
hover. Full-mode workflow: drift report + PLAN gate (JP's go), 4-agent
build, VALIDATE (2 stale-pin reds re-pointed: the POST unknown-field
example and the sprint_id count), 6 revert proofs, adversarial review
(16 agents; **12 confirmed, 0 refuted**, 6 mechanisms — the headline: a
hover re-armed during the placement reload stranded ghost chrome on the
placed row with its mouseleave gone), fix wave + 5 new pins + 6 more
revert proofs, simplification (1 applied: addZoneKey routes through the
one open owner), real-pointer E2E (hover/click place, one-act draft
place, Escape-with-chrome, keyboard open, console clean, fixtures
restored). DATE_ONLY now proves the calendar at its one definition —
healed a pre-existing hole in PATCH and the sprints PUT. Commits:
`6136aac` build · `15f9495` review fixes · `0d80d3b` simplify.
PLAN rotated to 2026-08-28-plan-spotfix.md. Deploy awaits JP.

Flake note (deploy gate, 11:12): one red in test/schedule.test.ts setup —
`/__test/login` 404 in a single test while 1089 passed. Green in isolation
on immediate re-run and in all four full runs earlier today. Recorded as
the third observed environmental flake shape (test-login route race under
full-suite parallelism); not masked, suite unchanged.

SHIPPED (spot-fix): JP's go 11:10. One deploy-gate flake (recorded above),
green on re-run; deployed via ./deploy.sh — sirius + sirius-worker
restarted (uptime 0s), healthz 200×6 production, live bundle carries
ghovcell/draftPlace/plotRow/the left-pin recipe/the hover hint/the
Highlight aria-label; the old sprintSel gate is absent. e7ad007 live.

## Addendum 3 — the test-board group join (JP's spot check, same day)

JP: Sprint Schedules' MCs (MC-6/7) matched nothing on Pipeline (MC-901..908)
on the production TEST board. Root cause was ours: create-test-board.ts
built task titles with mc.replace(/\d+$/, i-4) — MC-909 → MC-5, whole
digit run swallowed — so the four work cards matched no group and sat in
main-card lanes. Measured the live board via ARES (594 active cards):
main and work cards live in DISJOINT lane worlds (client pipeline w/ ➜
prefix vs internal design lanes), work titles are MC-FIRST
("MC-837 Render Icon: …"), every work card carries Difficulty.
scripts/fix-test-board-lanes.ts (guarded: explicit board arg,
PROD_TRELLO_BOARD_IDS + hLL7WW2V refused, name must say TEST; guard
proven live) created 6 internal lanes, renamed/moved the orphans to
MC-905..908, added one sibling per group. Blocked briefly: ARES held a
one-shot Aug-24 snapshot of the board (not in its polling roster; JP
fixed the roster). Then push-live sync landed in Sirius; deleted the two
stale rows (they snapshot mc_number at add time — by design unhealable by
rename) via the audited route; full proof ON PRODUCTION rt-test: add
MC-908 Icon Clean Up → one click on the Aug-31 week committed AND placed;
deadline 2026-08-31 inherited from the MAIN card's due through the group,
red rule at the Aug-31 column, urgent:true from the group's label, finish
computed 09-01, late flagged. Recorded for #75: TASK_PREFIX_RE (letters-
only, start-anchored) matches NO live-convention title — taskPrefix is
null on real data, so phase colours never fire on production; colour
should key on lanes when the Apollo mapping lands.
