# STATE.md session log — 2026-08-18

_Moved verbatim from STATE.md on 2026-08-18. Entries newest-first. Later sessions append this date's entries here when STATE.md rotates them out._

- 2026-08-18 (late, same evening) — **TEMPLATE SPLIT: `00-app.html` (1,229 lines) → `layout.html` + `templates/partials/` + `templates/views/`, built output PROVEN unchanged; new standing guard for a hazard the split created.** JP asked to explore how ARES eases its HTML out with compiled templates/partials, then to build it. **What ARES does**: `frontend/build.js` concatenates ordered CSS/template/JS into six separate shells, with `templates/partials.html` as a 16-partial registry injected at its own top-level marker above the view files (13 of them). **What was adopted, and what deliberately was not**: the layout/partials/views split and the second injection marker, YES; ARES's explicit `readDirOrdered` order arrays, NO — Sirius has one page and the numeric-prefix sorted glob is already mirrored by `test/helpers/source.ts` + `test/source-order.test.ts`; ARES's five near-identical per-page blocks, NO — nothing to page over yet. The `tpl-app` wrapper and `<main>` stay in `templates/layout.html` rather than moving into `frontend/index.html` the way ARES puts them in its shell, so the document shell stays a document and app markup stays in `templates/`. **Slices are byte-exact** (`sed -n 'A,Bp'`): 42-line layout · 11+23 partials (reqSyncStrip, dueCalendar) · views 20-requests 167 · 30-pipeline 256 · 40-schedules 524 · 50-deadlines 83 · 60-forecast 45 · 70-admin 74. **build.js** 62 → 105 lines: new exported `composeTemplate()` (reads layout, parse-checks it, fills its two markers from the two directories parse-checking every file, parse-checks the composed whole); file-writing moved behind a run-directly guard so importing writes nothing; `readDir`'s name/signature/body untouched because `source-order.test.ts` slices and executes it. `test/helpers/source.ts` `template()` now CALLS `composeTemplate()` — derived, not a second copy of the composition (test/CLAUDE.md rule 2). `frontend/build.d.ts` (10 lines) exists only because tsconfig keeps `allowJs` off. **The proof**: banner-comment lines filtered and whitespace stripped, baseline and new `public/index.html` hash IDENTICAL (`02390c77…`); the banner-stripped line diff is TWO added blank lines, both at a `{{/partial}}` seam. Check "whitespace-stripped hashes match" as originally specified is unsatisfiable and was correctly refused by the workflow rather than met by weakening the banner convention — one banner became eight, and the banners ARE the intended output. **NEW HAZARD, now guarded**: a `{{#partial}}` registers template-wide ONLY at the top level; nested inside an element it goes local, every `{{>call}}` elsewhere renders EMPTY, and the build still passes because the nested form parses clean. Structurally impossible before the split, live after it — `<!-- inject:partials -->`'s position is now the only thing holding it. `test/template-partials.test.ts` (4 tests, AST-derived: top-level registry vs per-element `p`, plus a negative control) is the standing guard; proved by sinking the marker into `<header>` — `npm run build` still succeeded, three assertions went red. Recorded in `frontend/CLAUDE.md` §Ractive hazards (file re-trimmed to 4,049 bytes after `test/context-architecture.test.ts` caught the 4KB cap). **Workflow**: 6 agents (carve → wire → 3 parallel verifiers → fix), 8 findings, the two that mattered — the old file's deletion staged while its nine replacements sat untracked (a `git commit -a` would have shipped a repo with no template), and the run-directly guard comparing an unresolved `argv[1]` against a symlink-resolved `import.meta.url`, which would have made a build through a release symlink write nothing and exit 0. **BROWSER PASS** (both builds served side by side on throwaway ports, identical scripted sequence, one row injected via `app.set()`): Ractive **1.4.2** from the CDN registers `["dueCalendar","reqSyncStrip"]` on both · all six tabs render with identical breadcrumb and child counts · due popover opened by a REAL click renders 42 day cells, "August 2026", the dow strip, three shortcuts and the per-kind Clear/Apply footer · rendered DOM identical once whitespace-only text nodes between tags are collapsed (`2ca4866b…`, 31,355 chars both) · the residual 5-char difference is two whitespace text nodes inside `<main>` where Ractive drops the banner comments but keeps the surrounding newlines — layout geometry pixel-identical on eight probes plus document height · console identical on both, including three pre-existing Ractive two-way-binding warnings on Forecast (`forecastRows.0.confidence`/`.slaSketch`/`.slaRender` are read-only — checked against baseline, NOT introduced here; a separate one-line cleanup). No backend and no sign-in, so no Trello write was possible and none was attempted. **Gates**: 962 → **968 passed + 24 todo, 62 files**, green `TZ=UTC` AND `TZ=Asia/Manila`; tsc/eslint/build clean; `generate-index.ts --check` → all 3 map files current. Two runs hit the standing loopback flake (rule 5, `requests-status` then `syncIntake`, both untouched files, both green isolated and on the immediate re-run) — recorded, not masked. **Flagged, not fixed**: `package.json` pins `"ractive": "^1.4.2"` which resolves to 1.4.4 installed, while the shell loads `ractive@1.4.2` from the CDN — the build's parse gate validates against a version the browser never runs (pre-existing; fixing it needs an install that would mutate the main checkout's `node_modules` from inside a worktree) · `.gitignore`'s `node_modules/` has a trailing slash so it matches a directory but not a symlink, which is how a worktree usually gets one · the duplicated due-popover markup at the task row is STILL duplicated on purpose — deduping changes markup and would have destroyed the unchanged-output proof; it is now a cheap, well-guarded follow-up. **Merge with batch 13** (main moved to `22e503c` mid-session — `4499484` touched `00-app.html` by +17 lines, the status-note chip): resolved by RE-DERIVING every carved file from b13's template rather than hand-applying the hunk, then proving all nine slices byte-equal to it — eight already were, only `40-schedules.html` moved (524 → 541). The proof was re-run at source level after the merge: composed template, banner lines stripped, equals `main:frontend/templates/00-app.html` with the same two blank lines at the `{{/partial}}` seams and nothing else, whitespace-stripped identical. STATE.md and this file merged by hand (b13 restructured the phase table; the 10-line index window was over the 10KB cap, so the two oldest lines went to their archive entries here). Suite after merge **982 + 24 todo, 62 files**, dual-TZ.

- 2026-08-18 (batch 13, later the same evening) — **Owls #47–#51 processed end-to-end: one server-side correctness fix and two ruled visual corrections. JP approved both asks ("yes / yes").**

  **The mailbox.** Five owls: #47 confirmed both batch-12 defaults (childless-MC chevron removed, SubTone expanded-only — nothing to flip) and blessed the gap-banner placement; #48+#49 ruled the status-note chip must carry the note text in neutral dress; #50 asked whether the 37-second push had ever been checked against views written for 15-minute staleness; #51 corrected the clarification note from a four-sided border to a 4px left accent, with the frame's SVG as evidence.

  **Verification before reporting, which changed two of the answers.** #47's SubTone correction — "build it as a real element, not by parsing 'Main Card:' out of the name" — did not apply: the template already renders a literal `<span class="subtone">` gated on `firstOfMc && expanded`, and never touched the name string. Reported back as a non-diff rather than silently building nothing. #47's suggestion to glance at anything else keyed on MC number alone was run: only `expanded[row.mcNumber]` and `workCardsByMc` remain, both built inside one project's scope, so neither can leak across projects the way the first one did.

  **#50 — the decisive fact product did not have: Sirius has no client-side refresh loop.** No `setInterval`, no `EventSource`, no `WebSocket` anywhere in `frontend/scripts/`. The 37-second push is entirely server-side and the browser re-reads only on user action. That closes the narrow version (the Deadlines conflict view does not recompute 24× more often — it recomputes when touched) and three of the four surfaces outright: the warning hover card, an in-progress drag, and transient view state including unsaved Sprints edits cannot be disturbed by a push, because a push never reaches the browser.

  **The fourth was real, and it was the one that shows a WRONG value.** `upsertDeliverable` and `upsertWorkCard` unconditionally `$set` urgency, difficulty and the due pair from whatever the ARES read returned — no version, no read timestamp, no comparison against when Sirius last wrote. Losing sequence: a push event is pending for card X; the user sets a difficulty and Sirius writes Trello first (a few hundred ms); inside that window the drain reads X and gets the pre-write value; the route's save lands, then the drain's update lands after it and the stale value wins. Self-healing in the ordinary case (Sirius's own write fires its own webhook), but the wrong value is what the user sees on their next reload, and with push down it stands until the hourly reconcile. Exactly product's description: sets it, sees it stick, watches it flip back. The full board sync has the WIDER window — one board read, then a loop of upserts — which the fix also covers.

  **The guard (`decisions/0025`).** Every successful registry write stamps `registry_written_at`; every reconcile stamps the instant it ISSUED its ARES read and passes it down; registry-owned fields are written under a filter matching only when Sirius's last write is strictly older than that read. Four choices worth keeping: it is a FILTER and not an aggregation-pipeline expression, because in pipeline form every `$set` value is an expression and a card named `$name` would store a field path instead of its title — a hazard on the hottest path traded for one round trip. It needs no expiry, being time-bounded by construction, so invariant 8's promise is unchanged: a manual Trello change still surfaces, at most one reconcile later. A tie counts as stale (`$lt`), because the two mistakes are not symmetrical. And it needs NO migration — an absent stamp already means "never written by Sirius", which is exactly the value that should reconcile normally. Reported that back to JP, who had approved "a small one-time database update" that turned out unnecessary.

  **#48+#49 — the note chip.** It shipped rendering the hardcoded word `manual` with the note only in the tooltip, which meant you had to open the editor to read your own note. Now it renders the note through the shared `.clipbadge`/`.cliptext` recipe — measured truncation, hover AND keyboard reveal. Neutral was implemented better than the ruling asked: rather than copying the Requestor badge's slate tokens, the amber override was DELETED so the chip inherits the base badge rule the Requestor badge already uses — same rule, not the same values twice, guarded by a test that fails if any `.gnote` rule declares a background again.

  Three build details flagged to product because two touch what was specified. (a) The chip's real truncation budget is the cell minus the urgency and difficulty chips beside it, not the full cell width product described. (b) **The chips line had to stop wrapping**: the note is the first chip whose width is freeform, and a second chips line grows the row past its 84px floor, which is what keeps the pinned pane aligned to the timeline bars — the same layout invariant the clip tooltip is absolutely positioned to respect. The note shrinks instead; urgency and difficulty carry fixed vocabularies and are never cut. (c) The note's tooltip wraps and is capped at 260px, the recipe's one documented deviation — a name or an asset type always fits one nowrap line, a freeform note does not, and uncapped it runs off the viewport. One accessibility divergence: the other clipped badges feed their tooltip from `aria-label`, correct while name and value are the same string, but this chip is a BUTTON whose name must say what pressing it does, so its tooltip reads `data-note`. Both written from one expression, so they cannot drift. Rules 57–62 in `gantt-rules.md`.

  **#51 — and an honest overturn.** Built as corrected: `border-left: 4px solid var(--red-500)`, no border on any side, padding-left 7 so border-box puts the text at the frame's 11px inset. The uncomfortable part: ruling 24 in `requests-frame-notes.md` had ALREADY recorded the frame reading as "~3.9px LEFT" against an annotation saying "a red-500 border", and concluded the annotation reading was "ACCEPTABLE — recorded, not churned". That call is now marked overturned in place. The measurement was right, deferring to the prose shipped the defect, and the same word produced the same wrong build on the Pipeline row's amber — where we read the frame correctly and the prose incorrectly. The rule that survives: a directional-looking stroke is settled from the SVG, never from annotation text and never from `get_node_info`, which reports no per-side weights.

  **Gates.** 976 tests + 24 `it.todo` across 61 files (962 → 976), green under `TZ=Asia/Manila` and `TZ=UTC`, calendar suites also `TZ=America/New_York`; `tsc --noEmit`, `eslint .`, `frontend/build.js` and `generate-index --check` all clean. Two self-inflicted test failures caught and fixed rather than worked around: a `not.toContain('height:')` guard that `line-height` legitimately tripped, and a duplicate `.gantt .gchips` selector where `cssRule` reads the FIRST match — the override was folded into the original declaration instead. The `decisions/0025` record was trimmed three times to fit the 20–60 line cap the context-architecture guard enforces; the guard was right each time.

  **Environmental, new this session:** a second session's git worktree now lives at `.claude/worktrees/template-split`, and a bare `npx vitest run` collects ITS copy of every suite — reporting that worktree's in-flight failures as if they were main's. Runs are scoped with `--dir test`; the worktree was left untouched and NOT added to `.gitignore` or the vitest config, since changing shared config under a concurrent session is not a silent call to make.

  **Owls:** jp→miles #42 sent (all four answered in one), #47–#51 acked. Left open on product's side: the row-controls design pass, the Smoke pass over the expanded row, and whether ARES's own read path is cached — the one part of the guard this side cannot see.

- 2026-08-18 (review pass, same evening) — **/simplify + /code-review over `93c1edb..HEAD` (batches 11+12); 14 cleanup findings applied (`47c65e0`), then 15 review defects fixed — ten CONFIRMED correctness bugs, one introduced by the sweep itself.** /simplify (4 agents: reuse/simplification/efficiency/altitude): writeGuards made generic so `ctx.doc` is compiler-typed per kind (casts gone); due dispatch made EXPLICIT — the template passes 'task' on the three task handlers, the client never re-derives kind from set-membership; the calendar became ONE top-level `dueCalendar` partial; `OVERLAY_EDGE` unified the clamp margin and the anti-oscillation compare; `DATE_ONLY` restored on the work-card due; `.wpwhy, .wpfix` share one rule; `sectionOf`/`tableRows` exported from the generator and imported by the guard (one STATE.md grammar); `APP_JS_CODE` + `fnBody` hoisted into the render helper; six casts dropped in pipeline.ts; `WorkCardWire` named, helper derives from it. Skipped: the helper DEFAULTS reshape (standing add-when-needed convention). /code-review (high; 10 finder angles + adversarial verification, two findings empirically confirmed) — all 15 fixed: **(1)** writeTaskDue's post-success `loadAll` RESTORED — the sweep's removal let a concurrent pre-commit reload permanently overwrite the optimistic due (the efficiency agent was wrong; correctness beats the reload cost); **(2)** its failure revert now RE-FINDS by cardId (the pre-await keypath could hit the wrong card or fabricate a phantom entry in another project's map); **(3)** sync hardened — `dueInstant()` nulls Invalid Dates in both upserts and mapper's `dateOnly` nulls garbage, so one malformed ARES string can no longer abort a project's sync loop every tick; **(4)** the task list + SubTone render under the group's FIRST row only (`firstOfMc` stamped beside hasTasks — mc_number repeats, invariant 3: MC-825 would have rendered 99 copies of everything incl. an open popover); **(5)** kind-flipped cards: the push drain deactivates the twin in the other collection, and writeGuards' lookups gained `active: true`; **(6)** calendar-impossible dates (2026-02-30) now 400 via a Zod refine instead of an unaudited 500; **(7)** `pipeRowKey`'s Enter refuses childless MCs exactly where the chevron refuses to render; **(8)** `dateOnly` is now the MANILA day, both W2 halves (a 00:00–07:59-Manila due stored a day early beside Manila-true Started/Done; stored values self-heal on the next sync's $set — fixed for BOTH kinds so the halves agree, the one deliberate behaviour change); **(9)** the shipped `dueCalendar` partial is registered on the test renderer (it silently never rendered — every calendar guard was vacuous; now asserted non-vacuous); **(10)** task dues wear the parent's OVERDUE dress (`overdue` on the wire, same Manila-day compare); **(11)** the pre-deploy backfill window verified closed by the live sync; **(12)** phase-log's premature 'NOT deployed' rows corrected with dated [CORRECTED] markers, this file reordered newest-first, STATE.md b11/ctx rows rolled up (cap headroom restored); **(13)** dead `dueAt` dropped from the wire; **(14)** the no-op guard asserted as ORDER not a byte snapshot + redundant test renders hoisted; **(15)** `writeGuards` generic replaced the overload pair (no dispatch ternary) and sync_runs rows now carry `kind`. **Gates**: 950 → 962 passed + 24 todo, 61 files, dual-TZ; tsc/eslint/build/`--check` green; one 2-fail full run was the standing loopback flake (rule 5) — changed suites stressed 3× clean, two subsequent full runs green.

- 2026-08-18 (evening session, cont.) — **BATCHES 11 + 12 DEPLOYED LIVE; owls #42–#46 processed end-to-end; #45 built to spec same-day** (T167–T178; commits `5934c26` · `2fca981` · `ef2874b` · `07eeb62` · `8765083` · `5cd662e`). **Deploy 1** carried batch 11 + the stage-5 script split (the split's first production outing — health 200, batch-11 markers verified in the served bundle). **Live pass, batch 11** (chrome-devtools, rt-test): closing line verbatim in the real card, order items→fix→sep→Open Card, wraps at 34px ✓ · hit target measures exactly 24×24 ✓ · base card uncapped on a normal viewport (max-height none, overflow visible) ✓ · focus-opens works; Asset Type recipe shipped (fixture rows carry no type values, so the truncation path is unexercisable there — same situation batch 6 accepted for Requestor). The sub-350px scroll state stays unit-guarded only: the browser turned out to be SHARED WITH JP mid-pass (their clicks — a project switch to rt-837 and a tab switch — first read as spontaneous navigation; zero clicks and zero writes from the agent side, and rt-837 refuses writes in observation mode regardless), so the resize check could not be driven to completion and is owed. **Batch 12, the #45 expanded MC row**: work cards gained the `trello_due`/`trello_due_at` pair through the whole read chain (mapper → `upsertWorkCard` shared by full sync and push drain → the pipeline wire, which also now carries started/done/ts per task); ONE `dueHandler(kind)` serves both card kinds through the same `writeGuards` door (audit `entity: 'work_card'`, cross-kind lookups 404 each other); client `writeDeadline` stays the one door and falls through to `writeTaskDue` → `/workcards/:id/deadline` with the same optimistic revert; the task cell duplicates the popover MARKUP (two data shapes) over one CSS recipe, one calendar state, one handler set. Template: task rows became per-column `<tr>`s in the parent's grid (no colspan; empty col-mc = the indent), R-exp-a..f recorded in `pipeline-frame-notes.md` §Batch 12. Defaults taken and flagged in jp→miles #41: childless-MC chevron removed per the #40 proposal (spacer holds alignment), SubTone `Main Card` expanded-state-only. The #40 recon defect fixed: `expanded: {}` joined `resetForProjectSwitch` (mc_number repeats across projects — invariant 3). **Deploy 2** carried batch 12; smoke per the runbook's writes-touching rule: `due-roundtrip.ts` on `tx8gDsTH` set 2026-09-15 and RESTORED the original null, zero net change ✓. **Gates both batches**: suite 911 → 947 passed + 24 todo (61 files; new `test/pipeline-expanded.test.ts`, deadline-write's task-half describe, mapper due pair, render helper learned expansion state + calendar stubs), green `TZ=Asia/Manila` AND `TZ=UTC`; tsc/eslint/build clean; one UTC run earlier in the session hit the standing loopback flake, green on rerun, recorded. **Concurrent session note**: STATE.md was reshaped mid-session (phase-log/decision-log extraction, one-line session summaries) — adopted, not reverted; batch facts merged cleanly. **Owed**: task-due UI round-trip in a real browser (endpoint smoke-tested; UI path unexercised — Miles invited to run it on Sirius Smoke in #41) · the sub-350px scroll live check · Miles's confirms on the two batch-12 defaults.

- 2026-08-18 — **BATCH 11 — owls #42–#46 processed end-to-end, four ruled changes BUILT + COMMITTED (`5934c26` · `2fca981` · `ef2874b`), NOT deployed** (phase 13k cont., T167–T171; the deploy will also carry the stage-5 script split). **Process**: all five owls read → verified → acted → acked same session; jp→miles #40 sent (in-thread on #45). **#42** — requestor tooltip RULED kept pure-CSS, gaps and all (Escape, touch, last-row flip accepted; interactive version banned); closes T152; ruling recorded AT the recipe comment in `20-pipeline.css` so it cannot creep back. **#43** — four rulings on our #26: (A) sprint-dates banner accepted as written, nothing to change; (B, T167) the deleted banner's closing sentence restored VERBATIM — *"Fix in Trello and it corrects on the next sync."* — after the field list, above Open Card, rendered for no-URL rows too (message copy, not the link's caption); `.wpfix` wears `.wpwhy`'s recipe; `WARN_POP_H` 346 → 390 (pre-measure only; the measured second placement still owns the truth); recorded as **R-warn-x**. (C, T170) Asset Type joins the shared clip recipe — identical markup contract (clipbadge/cliptext, aria-label = visible text byte-asserted, role=note, measurement-granted tab stop), `.c-type` releases its clip below the `.gcell` rule by source order; `test/gantt-requestor-clip.test.ts` re-scoped to the two-column rule (recipe on req + type and NOTHING else, counts and per-column negatives). (D, T168) last-resort popover scroll — no-internal-scrolling STAYS the rule; `placeBox` now returns `over` (measured height vs viewport, `>=` deliberately: the scroll state caps the box at exactly viewport-minus-margins, so a strict compare would read the capped re-measure as "fits", drop the class and oscillate); template spells `.warnpop.scroll` (`max-height: calc(100vh - var(--space-8))`, `overflow-y: auto`, `overscroll-behavior: contain` so the wheel at the card's end doesn't chain to the page dismisser); `.warnpop` joined the scroll dismisser's self-scroll exemption — safe unconditionally since on normal viewports the card has no overflow and a scroll's target is never inside it; wheel swallow stays duePopover-only; base `.warnpop` asserted UNCAPPED so the ruling can't be silently repealed; known+accepted: the hover bridge is clipped in the one capped state (no gap left to span); recorded as **R-warn-y**, amending R-warn-h in place. **#44** — Miles ran our five checks on Sirius Smoke, all pass incl. the visual regression; filed. **#46** — four rulings on our #30: flip-corner mirroring CONFIRMED (R-warn-i annotated), close delay KEPT (R-warn-j), dark variant CLOSED by Miles's own correction (his "in use on Sprint Schedules" was a dark *tooltip* misread; LIGHT ONLY — R-warn-k closed), occlusion RULED intentional (left the live checklist); plus (T169) hit target grown to min 24×24 via `--space-24` minimums on `.warnbtn` — the threshold lives in the token sheet, not component CSS — recorded as **R-warn-z**; Figma filler cleared their side (R-warn-q annotated). **Also in the batch**: the 7 byte-frozen shipped-source comments still naming `01-app.js` repointed at the real split pieces (00-router→40-app-state/95-routing, 20-pipeline.css→70-measure, 35-gantt.css→10-constants/50-gantt-geometry ×3, template ×2) — this was the reserved product-touching pass. **#45 recon (T171)**: `get_design_context` on `520:54192` — the two annotations retrieved match the owl POINT FOR POINT (1570×375, parent 109px white, children 75px `#f8fafc` in the Additional Rows slot, empty 144px indent cell, bordered `#e2e8f0` 1426px container, 144+1426=1570, the 1610px question, the three confirms; output truncated before a third annotation could be counted — build pass re-verifies, plus the SubTone element the returned render didn't show). **Answers sent in jp→miles #40**: (alignment) our Pipeline is ONE `<table>` — parent and child rows share one column grid BY CONSTRUCTION, one scroll extent, Links not pinned; the 1610 is a Figma-side artifact and no 40px drift is possible; (childless) today every row renders the chevron and a childless expand shows nothing — PROPOSED chevron only when the MC has task cards, awaiting his confirm; (persistence) in-memory — survives tab switches, resets on refresh; DEFECT FOUND: it also survives project switch because `expanded` is keyed on `mc_number` alone (invariant-3 smell — project A's expanded MC-655 arrives pre-expanded in project B) — fix scheduled into the #45 build (`expanded: {}` joins `resetForProjectSwitch`); (multi-expand) yes, unbounded, staying. **JP ruled the W2 question** (this session's one ask): task-card due dates are the SAME registry entry — same field, same `setDue()`, same rules; scope note recorded in `contracts/trello-write.md` §W2, explicitly NOT a registry growth; precedence/`deliverables_v` stay deliverable-only. **Law**: `pipeline-frame-notes.md` gained the Batch 11 section (R-warn-x/y/z) + in-place closure annotations; the batch-10 occlusion `it.todo` removed as ruled, three new live-pass todos added (closing line, sub-350 scroll, 24×24 measure). **Gates**: 911 passed + 24 `it.todo`, 60 files, green `TZ=Asia/Manila` and `TZ=UTC` (calendar suites also NY); tsc/eslint clean; `node frontend/build.js` clean. **Flake honesty**: one UTC run failed one server suite on the standing loopback contention (rule 5); green on rerun, nothing masked. **Owed**: the live pass for the four batch-11 changes (runs against the deployed site — so after the deploy that also carries the stage-5 split), and the #45 build itself once Miles confirms the childless-chevron proposal (build can start regardless; the proposal only shapes the empty state).


- 2026-08-18 — **CONTEXT RESTRUCTURE stages 1–3 — DONE + COMMITTED (`0d5e3cb` · `e8d14db` · `cdc31bb`)** (JP-approved staged plan + gates in `docs/history/context-restructure.md`; owl record jp→miles #28/#29/#31 + closing note; workflows wf_63080de1 / wf_5cd98be4 / wf_0f3b8602, 13 agents, every stage independently verified before its gate). **Docs-only: no code, no build-output change, no deploy; write registry untouched at W1 / W2 / W3.** Motive: agent navigation and token burn — session-start anchors cost ~34k tokens and STATE.md alone (123KB, ~49k tokens) exceeded a single tool read. **Stage 1**: the session log rotated to `docs/history/state-log/YYYY-MM-DD.md` — 60 entries, 59 archived byte-verbatim (sha256-multiset conservation proven three times: builder abort-on-mismatch, independent verifier rebuild, gate re-run), STATE.md 123,191 → 22,123 bytes, rotation convention written into the section header; the concurrent batch-10 session followed it unprompted, and JP confirmed the restructure to that session (`03da2dd`). **Stage 2**: `specs/001-sirius-v1/gantt-rules.md` = ONE home for current planner law, 56 numbered source-tagged rules; the completeness verifier traced all 56 to sources (zero of the old sections' 39 normative statements lost, zero invented); the drift verifier FAILED its first pass on a real duplicate — HANDOFF's sprints-modal paragraph still stated rules 31/34 — fixed at the gate, plus two source-verified additions (rule 13's pinned-row-in-multi-select carve-out per `rowKey` in `01-app.js`; rule 39's sprint-block collapse made explicit). HANDOFF's two planner sections → pointer stubs (25.2 → 20.3KB, imperative force kept); `gantt-frame-notes.md` banner declares it the batch-history archive with gantt-rules winning on disagreement; four stale "in STATE.md" pointers repointed at `docs/history/state-log/`. **Stage 3**: `docs/MAP.md` (one line per source file, every line from an opened file; test-guard index), `frontend/CLAUDE.md` + `test/CLAUDE.md` (directory-scoped law that auto-loads only for agents working under those directories; every claim traced), `docs/README.md` (authority index — governing vs historical, AGENTS.md §2 wording corrected against the three-entry registry; where-law-lives table closes the R-warn-* pointer gap). The accuracy verifier checked 28 MAP lines against the real files with zero omissions; the trace/drift verifier confirmed law is pointed at, never restated; six nits fixed at the gate (model name, 401 wording, suite count, golden-suite location, a circular citation retagged, the index now lists itself and MAP). **Graphify assessed and SKIPPED** — code-graph MCPs pay off at hundreds of source files; Sirius is 12.3k source lines and the weight was prose; revisit at 50–100k lines or sustained cross-repo ARES work. **Stage 4 PARKED for JP's explicit go**: split `01-app.js` (2,942 lines) into numbered concat files behind a `test/helpers/source.ts` migration of the source-regex guards; gate = built JS byte-identical modulo build banners + tsc/eslint/vitest dual-TZ. Net effect: session-start anchor cost ~34k → ~10k tokens; planner law readable in one 16KB file instead of a 172KB history; code discovery via an 8KB map instead of exploration.
  **Stage 4, same session — JP ruled the T-shape adoption** (rule files one-per-AREA, provisional with a standing Rigidity log; skim replaces MAP.md; layer definitions JP's own): architecture codified in `docs/architecture/context-architecture.md` (`d7d8ef4`) · **4a** (`229fb39`): MAP.md rebuilt as the Layer-0 skim (127/150 lines); `scripts/generate-index.ts` owns the file list, merges hand-written purposes, `--check` is the rot alarm; `test/context-architecture.test.ts` asserts the full caps table (bijection via shared import — the guard and generator cannot drift); STATE.md index windowed to 10 lines, 51 deleted with every target verified in `docs/history/state-log/` first · **4b** (`61ed639`): 21 decision records `decisions/0001–0021`, each traced to a JP ruling, constitution text, or recorded gate; the first gate FAILED correctly — two invented "alternatives rejected" and two compound records — fixed, split, renumbered, drift trimmed to pointers, independently re-verified before commit · **4c** (`d601314`): `lib/CLAUDE.md` + `src/CLAUDE.md` (one trace failure fixed: `.strict()` scoped to mutating routes — the webhook envelope is deliberately tolerant of unknown fields); guard suite grown to 25 (derived staleness set, decision `# Title` + README-index-coverage assertions) · **End-to-end gates ALL GREEN: full suite 888 passed + 22 todo under BOTH `TZ=UTC` and `TZ=Asia/Manila`, first try; `node frontend/build.js` → `public/index.html` sha `76fd1f1732ade3…` BYTE-IDENTICAL to the pre-restructure baseline — four stages of docs restructure provably never touched the product.** **4d** (`9af9f5a`, decision 0022 — JP: decompose the maps early, "cheap way to decouple agent context"): `docs/MAP.md` → 53-line main index that no longer grows with the codebase; per-file lines → `docs/architecture/map-frontend.md` (14) + `docs/architecture/map-backend.md` (62), Layer 2, all 76 purposes conserved byte-verbatim; generator maintains the set with the area partition exported for the guard (25 → 30 tests); coherence verifier caught two dangling `MAP.md §MODULES` pointers in lib/src CLAUDE.md — fixed at gate; `map-test-suites` deferred with its trigger in the Rigidity log. **Stage 5 — DONE on JP's go (`f2d203a`)**: 01-app.js (2,942 lines) → ten numbered pieces (`10-constants` … `95-routing`), partition byte-exact (pieces re-concatenate to the original sha, independently verified); `test/helpers/source.ts` migrated every source-reading guard to the shipped bundle FIRST with the suite green pre-split and zero assertions weakened; banner-stripped builds identical — **new built baseline `4dd5186…`** (the old sha can never recur: ten banners where one stood; source-level parity is the standing proof); **897 + 22 todo green both TZs**; eslint's FRONTEND_SHARED grew the cross-file names; 7 shipped-source comments still naming 01-app.js are FROZEN by the byte gate — fix on the next product-touching pass. Deploy NOT performed — the next feature deploy carries it (JP's call if wanted sooner). **Same session, JP's three workflows**: WF2 = "How Sirius Remembers", the plain-language architecture guide, published as a private artifact after an accuracy/jargon/theme review (three wording drifts caught and fixed). WF3 = docs/ hygiene audit (3 lenses + synthesis): 24 verdicts; 8 no-judgement fixes applied + committed (`96881a1` — DEPLOY BASE_PATH + rt-test example + no-staging banner, SERVER_SETUP status/G7/port, AGENTS three-writes/ack-key/OD/Mongo/filenames with names+numbers preserved, v1.1 superseded banner, Impl-Plan + ARES_PUSH banners, gate-t045 snapshot + ignore glob, .DS_Store); **JP answered all three asks same-day, executed (`896c0a9`)**: v1.1+errata pair → `docs/history/` (README rows + spec.md history mention updated) · interim engineering banners on the BRD and build-spec v1.2 (rulings-only, self-expiring on product's amendment) · sole-copy records COPIED to the staging folder `../sirius-local-records-backup/` (screenshots 7MB, forecasting-block.csv, gate-t045 evidence + snapshot, with a README on sensitivity) for JP to relocate off-laptop; originals untouched in place. Residuals recorded in `docs/history/context-restructure.md` §4c: `worker/` CLAUDE.md still to write (with a traced pass); GEN:DOCMAP is the one hand-edited index `--check` cannot police; decisions/0012's "84/84" is the historical phase-3 validation count beside today's 40 sanitized fixture rows — both true.
- 2026-08-18 — **BATCH 10 (owl miles → jp #41): the Pipeline warning becomes an ICON + HOVER CARD, and the amber wall is ruled away — DEPLOYED + LIVE-VERIFIED + COMMITTED `7bdf6b4`** (phase 13k cont., **T163–T166**; fifteen rulings in `pipeline-frame-notes.md` under *Batch 10*). Figma nodes `578:56516` (row), `I578:56516;484:27906` (MC# cell) and `537:69135` (tooltip) read with `get_design_context` and verified against the owl on every point BEFORE the brief was written. **821 → 865 tests + 22 `it.todo`, green under `TZ=UTC` and `TZ=Asia/Manila`.** Frontend + tests only: no server, no wire, no schema, no migration; `lib/**` and `src/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency, no token invented.
  **THREE CHANGES.** (1) The full-width underlined `Needs Info` message under the card name — `.warnwrap`/`.warnmsg`/`.warnlabel`, all deleted — becomes a **14×14 alert icon immediately after the MC#**, in a new `.mcid` group. Row height stays **content-derived**: the annotation's 113px → 95px is an OUTCOME of deleting the message line, and R-warn-a's rule that neither number is hardcoded is unchanged (a guard now bans `height`/`min-height`/`max-height` on any `tr`/`td`/`.prow` rule rather than banning the two integers file-wide, which had been failing an unrelated `.col-done { width: 95px }`). (2) **The amber-50 row wash is REMOVED** — this is Miles ruling the amber-wall question raised in my #21 (247 of 249 live rows warn, at which density the fill stopped discriminating). The warning is now the 3px amber-300 left accent **plus** the icon, nothing else. `.ptable tr.prow.warn.blocked > td { background: var(--red-50) }` is deleted with it: it existed only to stop the wash demoting a blocker, so **R-warn-g now holds BY CONSTRUCTION** — verified live and guarded. (3) The click popover becomes a **hover CARD**: radius asymmetric (top-left `0`, other three 8px) so it reads as a caption growing out of the icon; opens on pointer hover **AND** keyboard focus; a transparent `::before` bridges the 4px gap; `WARN_CLOSE_MS = 150` close delay; dismissal on pointer-leave, Escape and focus-out with focus returned to the icon. It stays a **card and not a tooltip** because it contains `Open Card` — a pointer-only overlay would put that link out of reach, which is the constraint Miles wrote explicitly.
  **The icon is now the ONLY textual carrier of the warning**, so it is a real `<button>`, tabbable, `aria-haspopup="dialog"`/`aria-expanded`, 22×22 hit area around a 14×14 glyph, with an accessible name composed IN THE RECIPE — `srLabel` = `WARN_LABEL` + pluralised missing-field count + card identity (R-warn-c holds: the label is a variable string at every render site, and the template does no arithmetic). Verified live: *"Needs Info — 1 missing field — MC-908 …"* and *"— 2 missing fields — MC-907 …"*.
  **FOUR DEFECTS CAUGHT BY THE VERIFY LENSES AND FIXED BEFORE DEPLOY**, each recorded as its own ruling. **R-warn-u**: every earlier overlay opened on a CLICK of its own button, so `overlayTrigger` was also what the browser had just focused — `warnPop` is the first a POINTER opens, and the shared focus return therefore *stole* focus instead of returning it. Escape typed in the Pipeline search field moved the caret onto a warning icon and swallowed every keystroke after; the restore now also requires focus to be ours (`heldFocus || !ae || ae === body || ae === t`), and all four pre-batch paths still satisfy one of the three. **R-warn-v(a)**: the close timer stood down for ANY `.warnhost`, and every warned row has one — Tab to row A's icon, hover row B's, move away, and B was stranded open and unclosable; scoped to the host containing `overlayTrigger`. **R-warn-v(b)**: `warnPopFocusOut` asked *"is a card open"* rather than *"is MY card open"*, so a Tab off row A dismissed a card the pointer had opened on row B; scoped to the host that actually contains the card. **R-warn-w**: the icon sat in the click-ignore list while carrying no `on-click`, making it a doubly dead click that neither opened the card nor dismissed what WAS open — a regression against the message line it replaces; now shielded only while its own card is up, which is also what keeps a touch tap from closing itself.
  **One documentation error corrected rather than shipped**: the integrator's report claimed the bridge spans ~201px inset 17px each side, confusing the padding box with the content box — an absolutely-positioned child resolves against the PADDING box, so `left:0;right:0` spans 233px and the direct pointer path IS bridged. Measured live: only a **1px** band at the icon's bottom edge falls outside, and a real pointer crosses it harmlessly because the close is *scheduled* (150ms) rather than immediate and the card's own `mouseenter` cancels it.
  **LIVE PASS, real pointer and real keys** (TEST board `tx8gDsTH`, `rt-test`, synthetic fixtures): 7 warned rows → 7 icons, zero `.warnmsg` ✓ · row fill `rgba(0,0,0,0)` — **the wash is gone** — with the accent measured at `rgb(252,211,77) 3px inset` ✓ · card 235px, radius `0/8/8/8`, left-aligned to the icon at **0px** offset, **4px** below ✓ · bridge `top:-4px; height:4px; left:0; right:0` ✓ · **real hover from the icon into the card and onto `Open Card` — it stayed open and the link was hit-testable** ✓ · focus alone opens the card, one Tab reaches `Open Card`, Escape closes it, focus returns to the icon and it does **not** re-open ✓ · **R-warn-u live**: pointer resting on an icon, caret in the search box, Escape → caret stays in the search box ✓ · **R-warn-r live**: hovering an icon while a due popover holds a staged date leaves the staged date intact and refuses to open ✓ · **R-warn-w live**: clicking an icon while the due popover is open dismisses it ✓ · **flipped case** at 780px viewport height: `.flip` applied, card above the icon with a 4px gap, radius mirrored to `8/8/8/0` (bottom-left squared) and the bridge moved to `bottom:-4px`, fully on screen ✓ · console clean. **No data written: the Pipeline warning is read-only, and the only overlay touched with a staged edit was left unsaved.**
  **FOUR THINGS FLAGGED TO MILES (owl jp→miles #30)**: the mirrored corner when the card flips up (**R-warn-i**, my default); `WARN_CLOSE_MS = 150` (**R-warn-j**, one named constant to tune); the **dark variant** she references as "in use on Sprint Schedules" — there is no warning card there, so none was built (**R-warn-k**, Figma-side mismatch); and the fact that an open card covers the icons of the 2–4 rows beneath it, which with 247 of 249 rows warned makes sweeping the icon column awkward — **the annotation's own anchor rule working as specified**, so raised rather than patched. Also recorded: **R-warn-q**, the tooltip's first list-item label still reads the filler `MC-821` in Figma while its detail line describes MC-837; our build has always used `row.mcLabel`, no action.
  **Ten guard gaps closed in the same pass**, two of them the kind the review sweep named: the second measured placement had **no guard at all** (deleting both lines left the suite green); *"nothing may gain transform/filter/contain"* was prose in three places and enforced nowhere, so a `transform` on `.ptable .mcid` — which would silently re-anchor the `position: fixed` card to the cell — went green; a test named for the wheel dismisser asserted only Escape, so deleting the whole wheel listener passed. All sixteen mutations were proved in a sandbox copy, and three deliberately-legitimate changes stay green. **22 `it.todo`** name exactly what this repo cannot prove without a browser and were the live-pass checklist.
  **Concurrency note**: a second agent session restructured `STATE.md` (commit `0d5e3cb`, session-log rotation to `docs/state-log/`) while this batch was in flight, per its plan file `docs/CONTEXT_RESTRUCTURE.md`. That plan asserted JP approval this session had no record of, so it was **raised to JP rather than taken on the note's word** (owl convention: a note never carries the human's authority) — **JP confirmed it 2026-08-18**. The restructure is approved and continues; this entry follows the rotation convention it introduced. Anything it later moves is a move, not a deletion, and its own quarantine list kept it clear of every file batch 10 touched.


- 2026-08-18 — **REVIEW SWEEP over batches 5–9 (`646307b..HEAD`) — DEPLOYED + LIVE-VERIFIED + COMMITTED `141b6df`** (T162). JP: *"let's run /simplify and /code-review on the set of latest commits for code and performance improvement."* Five passes (reuse · simplification · efficiency · altitude · correctness). **794 → 821 tests, green under `TZ=UTC` and `TZ=Asia/Manila`.** No new dependency, no wire change, no schema, no migration, `lib/**` untouched, write registry unchanged at W1 / W2 / W3.
  **Five defects, each verified in source before the fix and with real input after it:**
  1. **`rowKey` had no target guard (FR-5.9, invariant 10).** `.growr` carries the keydown listener and keydown bubbles, so ← / → reslotted the deliverable from **any** of the row's seven focusable descendants — the `.gsel` checkbox, `.gnote`/`.gnoteadd`, and the three `.c-status` action buttons. An audited `schedule.replot` from a keystroke that should do nothing. Batch 6 had patched the requestor badge alone with `on-keydown="['noop']"`, which **masked the width of the hole**; the guard is now the one `pipeRowKey` has always had (`ctx.event.target !== ctx.node`) and the per-element stop is deleted.
  2. **`moveRows` had no non-change guard (invariant 10, BR-8).** Releasing a bar inside the column it already occupies POSTed `/replot` and wrote an audit row recording nothing — and batch 8 made that gesture easy by making the coloured run itself the handle. Refuses only when **every** member is a no-op, so a mixed multi-select still goes.
  3. **A cleared sprint date left Save live (FR-5.15).** `sprintOrder` filters a row lacking `start`/`end` **before** overlaps and gaps see it, and `sprintBlankNames` reads only the name — so no validator could see it, Save stayed enabled, and the PUT failed `DATE_ONLY` with an envelope carrying no `issues[]`, which the modal printed to the user as the literal `INVALID_BODY`. Exactly the failure 5b removed for blank names. New blocking computed `sprintMissingDates`; **route deliberately unchanged** — it already refuses the shape, and now the modal never asks it to. **Copy is PROVISIONAL, flagged to Miles (owl #26 item A).**
  4. **`longDate` indexed `MONTHS_SHORT` unchecked** while `DATE_ONLY` (`/^\d{4}-\d{2}-\d{2}$/`) accepts `00` and `13`, rendering the word `undefined` into 422 copy the modal shows verbatim. Falls back to the raw month.
  5. **R-warn-f's focus return only covered dismissal.** `chooseUrgency` / `chooseDifficulty` / `dueApply` / `dueClear` / `pickReqFilter` each nulled their own state key, bypassing `closeMenus()` — so a keyboard user who pressed Enter on an option was dropped at `<body>` and restarted the next Tab from the top of the document, the very regression Escape was written to fix, surviving on the path people actually use. The direct writes also left `overlayTrigger` pinning a detached node. All five now use `closeMenus({ restoreFocus: true })`, and the restore is `focus({ preventScroll: true })` because the same path runs from the capture-phase **scroll** dismisser.
  **Three hot paths (NFR-1):** `rowWarning(row)` was an expression in SEVEN template positions, so it rebuilt an object graph per row on every re-render — the table re-renders on every search keystroke (`lazy="250"`) — now stamped once per load beside `r.blob`, which also hands `{{#each}}` a stable array identity · `refreshClips` interleaved layout reads with `data-clipped` writes (a live selector), forcing one full style+layout pass **per changed badge**, worst on the left-pane collapse where every badge flips at once — split into a read pass and a write pass, one layout · `Sprint.create` in a `for` loop → `insertMany`, one round trip, order preserved, `project_id` on every doc · the capture-phase scroll dismisser walked the DOM before the cheap `anyMenuOpen()` read.
  **One spelling each:** `OVERLAY_KEYS`/`NO_OVERLAYS` (was three lists that had to agree — adding `warnPop` had meant three hand-edits) · `sprintBlocked` (was three: the `disabled` binding, the `title` condition, `saveSprints`' second lock — any one missed silently unlocked Save) · `sprintRowBanners` (was a three-way `.concat` **inside** the per-draft-row loop) · `sprintPayload` (was three projections of the persisted fields; `sprintDirty` now derives its comparison from it) · `remeasure()` (was four byte-identical rAF lambdas) · sprint row identity settled once at the route boundary, so `duplicateNameIssues`/`blankNameIssues` drop their index params and the blank-skip moves to the call site · `--space-12`/`--space-4` for three raw px in the new popover.
  **Two guards rewritten to assert the RULE, not a snapshot** — the `.toHaveLength(4)` on the rAF lambda text *actively blocked* naming the pair (and a fifth seam calling only one half still passed), and the per-element `noop` assertion pinned the patch instead of the rule. **Two drift guards now derive instead of copying**: the `missing` tokens are read out of `src/services/pipeline.ts` (a reworded token used to ship a blank rationale with a green suite), and `longDate`/`fmtLongIso` are **executed against each other** across twelve months + leap day + the out-of-range cases, instead of having their interpolation source strings compared.
  **Live pass (real input, TEST board `tx8gDsTH`, project `rt-test` — synthetic fixtures only):** ← on a focused `.gsel` → no move, no `/replot` ✓ · ← / → on the row → moves and restores, one `/replot` each ✓ · **real pointer drag** MC-901 `2026-09-14` → `2026-09-21` ✓ (proves removing `dropOnBar`'s `|| ctx.node` fallback did not break the drag) · **real pointer drag released in its own column → zero `/replot`** ✓ · Delete on a sprint date → `sprintDirty` true, `sprintBlocked` true, Save disabled with tooltip, banner rendered ✓ (before the fix Save was live here) · Enter on `.warnmsg` → popover with both items and no empty rationale ✓ · Escape → focus back on trigger ✓ · **Enter on a Requests select item → focus back on trigger** ✓ (was `<body>`) · 7/8 rows warned and 7 stamped `row.warning` ✓ · 4/4 requestor badges clipped ✓ · console clean but for a pre-existing root `favicon.ico` 404. **All eight rows restored to their pre-verification weeks; sprint modal cancelled without saving; tree clean.** Net effect on the test project: 4 `schedule.replot` audit rows (two moves, two restores), zero net data change.
  **Not applied, deliberately:** `.warnpop` overflow/max-height — **R-warn-h settled it** (no internal scroll, so it stays out of the scroll dismisser's self-scroll exemption; the second measured placement is the mitigation), residual edge raised to Miles rather than reversed · the deleted banner's *"Fix in Trello and it corrects on the next sync"* sentence and a `.c-type` clip sweep — **product copy and scope, Miles (owl #26 items B and C)** · CSS de-duplication of the four shared `.grun`/`.gghost` declarations — the gain is four lines against selectors `gantt-run-geometry`/`drag-hittest` look up by name, in the area that cost three batches · `corrections` on the wire (now kept only for `kpi.open`) — a payload change with tests pinning it deliberately · z-index tokens and delegated drag listeners — both real, both wider than this diff. **Deploy aborted once on the standing environmental flake** (six suites, `Port "…" already in use`, zero assertion failures); green on rerun, recorded not masked.

- 2026-08-18 — **BATCH 9 (JP, live-site report) built — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., **T160**; live pass owed as **T161**; full mechanism in `gantt-frame-notes.md` under *Batch 9 — the drag ghost is the coloured bars*). **ONE frontend file (`frontend/styles/35-gantt.css`, two rules) and ONE test file.** No template change, no JavaScript change, no server, no wire, no schema, no migration; `lib/**` and `src/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency, no token invented. **JP, dragging on the deployed build:** *"the ghost that follows the pointer shows a background with the week grid lines and a drop shadow. I want only the coloured bars."* **Cause, measured on the live page**: `.gantt .grun` — the drag source since batch 8 — was `top: 0; bottom: 0`, i.e. the **full row** (99px measured) by the run's width (184px measured), and transparent. The browser's default drag image is **a snapshot of the source element's box**, and Blink paints that snapshot from the nearest stacking context clipped to the source's bounds — its own source comment says it "will also paint the contents behind the object if the object contains transparency" — so the `.gweek` gridlines crossing that 99×184 rectangle came with it and the shadow wrapped the tall silhouette. Nothing was wrong with the bars; the wrong thing was the **box**. **The fix**: `.gantt .grun` becomes the **bar strip** — `position: absolute; top: 50%; transform: translateY(-50%); height: var(--gbar-h)`, the same 26px band the segments already occupied — and `.gantt .gtrack .gseg` fills it with `top: 0; bottom: 0` instead of centring itself against the row. **The centring did not disappear, it moved up one level**: the identical triple came off the segment and went onto the strip, and its `50%` resolves against the **same containing block it always did** (`.gbar`, `inset: 0` of `.gtrack`, untouched), so the strip's top edge lands on the pixel the segment's top edge landed on and the segment then fills it. Same place on screen, in **exact reals** — no rounding on this axis at all, unlike the horizontal one. **Horizontal geometry is byte-for-byte untouched**: `phaseRun`'s arithmetic, the percent `left`/`width`, the 24px minimum grab clamp; `01-app.js` was not edited. **The one rule above all held**: `pointer-events: auto` stays on `.grun` and on `.grun .gseg` in every state — Chrome cancels a drag whose source cannot be hit, proven with real mouse input in batch 7 — and **no `pointer-events` declaration anywhere in the app changed**; the inventory is still 8. **The corollary is the ruling, not a regression**: the vertical grab area shrinks from the whole 99px row to the 26px band. That is exactly what JP ruled — only the coloured bars are the handle — and it is T157's cursor ruling finally applied to the hit area too, so affordance and target agree on both axes. Nothing was added to widen it back. Horizontally the ~6px invisible minimum-grab extension survives, so a one-day phase is still catchable. `transform` makes `.grun` a stacking context (paint order unchanged — `.gdl` and `.gghost` are later siblings of `.gbar` at `z-index: auto` and still paint on top) and a containing block it already was. `--gbar-h` now has exactly **one owner inside the track**; `.gseg` deliberately does not restate it, because a second copy would over-constrain the box and could then be re-tuned alone. **Checked before editing, as the brief required**: `.gantt .gtrack .gseg` (changed) · `.glegend .gseg` (the 20×10 legend swatch — `.glegend` is a sibling of `.gwrap` and sits **outside** every `.gtrack`, so the changed rule cannot select it) · the four phase colour classes (colour only, no box, shared by both) · `.gantt .grun .gseg` + `:active` (T157 cursor) · `.gantt .growr.pinned .grun .gseg` + `:active` (the pinned refusal). Only the first was touched; `test/gantt-legend.test.ts` is untouched and green, which is the check. **Verification (T160)**: `test/gantt-run-geometry.test.ts` grows **SUITE 6 — the vertical position invariant**, built the way SUITE 1 was built for the horizontal one — a **frozen** transcription of the `79c1ad3` declarations as the oracle, the **shipped** declarations sliced out of the stylesheet on the other side, and a small **total** CSS resolver between them that throws rather than defaulting on anything it does not model (it encodes the one subtlety the batch turns on: a `translateY` percentage resolves against the element's **own** height). Both sides are exact reals, so the assertion is **equality, not a tolerance**, across four row heights (84 frame nominal, 99 measured live, 120, and the degenerate 26). SUITE 5's two `.grun` shape tests were **repointed, not weakened**: the box must now name `top: 50%` / `translateY(-50%)` / `height: var(--gbar-h)` and must **not** name `top: 0` or any `bottom` — restoring either restores the ghost silently with the bars still perfectly correct, which is precisely the failure a test has to catch — and the `.gseg` rule must restate neither a height nor a translate. **Mutation-probed on the real sheet**: putting `top: 0; bottom: 0` back on `.grun` fails **7 named tests**; a `not vacuous` case pins the two shapes that could have shipped instead (a top-aligned strip, 36.5px wrong on a 99px row; segments left double-centring inside the strip). **Gates**: `npx tsc --noEmit` clean · `npx eslint .` clean · `npx vitest run` **58 files / 794 tests** green **first run**, and the same under `TZ=UTC` (783 → 794, the 11 new tests; no T149 flake seen this pass) · `node frontend/build.js` 335,370 bytes. **One defect found at build, by an unrelated guard**: a new CSS comment naming the four phase colour classes turned `test/gantt-legend.test.ts` red on *"declares each phase colour exactly once in the whole stylesheet"* — that guard counts occurrences in the **raw stylesheet text, comments included**. The guard was right and the comment was reworded, not the guard. Second time in two batches that a comment collided with a drift guard reading it as code (batch 8's was `0.083` in `01-app.js`); worth stating as a standing constraint. **What is ours and what is Chrome's, honestly**: CSS controls the **size and content of the snapshot** — now a 26px band containing the coloured segments instead of a 99px box containing the gridlines behind them. CSS does **not** control the translucency the drag image is composited at, the drop shadow macOS/Chrome draws around it, the pointer's grab offset within it, or the fact that the source stays painted in place. The only lever over any of that is `setDragImage()` in `dragRow` — a JavaScript change the brief put out of scope, and the right next step **only if** the live check still shows something unwanted. **Residual risk [likely]**: the snapshot still paints what is behind a transparent source *within* the source's bounds, and the gridlines run the full row height, so they still cross the band — the opaque segments cover them across the run's extent, leaving two possible slivers, the `--radius-xs` corners and the up-to-6px invisible grab extension. Expect bars; do not be shocked by a hairline at a corner. **Still owed, and stated in the test file**: nothing here proves a drag works and **nothing here can see a repaint** — no jsdom, no browser runner, **no synthetic `DragEvent` was added and none may be**. **T161** is the real-mouse pass: the ghost is the coloured bars only; a before/after screenshot of the resting planner identical to batch 8's to the pixel; the grab still starts anywhere on the band; the 26px vertical target comfortable in practice (JP's call); and whatever Chrome still adds of its own recorded rather than fought

- 2026-08-18 — **BATCH 8 (JP's structural ruling of 2026-08-18) built via workflow — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., **T158–T159**; full mechanism in `gantt-frame-notes.md` under *Batch 8 — the handle becomes the coloured run*). **Numbering note:** the entry below labels **T157** "BATCH 8" as well — the frame notes are authoritative and read T153–T157 = batch 7 + 7b, T158–T159 = batch 8. Three frontend files, three test files + one new test file. No server, no wire, no schema, no migration, `lib/**` and `src/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency, no token invented. **The drag SOURCE moves off the track-wide wrapper onto the coloured run's own box.** JP read the live DOM himself: *"gbar spans across the whole of the timeline so dragging it makes the whole thing draggable… those colored bars should be wrapped into a draggable container instead. Gbar stays."* Batch 7 made the drag work by making the source hit-testable; T157 moved the *cursor* onto the segments; this moves the source. **Structure**: `.gtrack > .gbar > .grun > .gseg × N`, `.gdl` and `.gghost` still direct children of `.gtrack` and untouched. `.grun` (new; grep-clean at `1c571f1`) carries all five directives (`draggable`, `on-dragstart`, `on-dragend`, `on-dragover`, `on-drop`) plus its own inline `left`/`width`; `.gbar` keeps its element and class and carries none of them; neither box carries a `title`. **`.gantt .gbar` goes back to `pointer-events: none` ON PURPOSE, and it is not the 13g/13j bug returning** — the bug was a *drag source* Chrome could not hit, and `.gbar` is now the source's parent, not a source. At 1104px it swallows every drop landing outside the coloured run and the `.gweek` columns never see them; transparent, it hands those columns their drops back while `.grun` keeps its own, both ending in the **same** `moveRows`. Legal only because `pointer-events` **inherits and a child's explicit value overrides an inherited one**, so `.gantt .grun { pointer-events: auto }` is written out rather than omitted — **the two rules ship together or not at all**, and the guard now asserts exactly that, plus a test that strips the `auto` out of the *real* rule set and proves the sweep fires. **The geometry had to move, in one place.** `phaseBars` emitted each segment as a percentage of the 60-workday window; inside a narrower box every one re-bases and the bars move. `phaseBars` is **DELETED**, replaced by a column-0 `const phaseRun = (row) => …` returning the box **and** its re-based segments in one shape — no second helper, and the template does no arithmetic (no `*`, `+`, `calc(`, no `{{#if}}` on geometry; every value arrives as a 2dp string). `L = max(0, min(R0, TOTAL_UNITS − W))` is the whole direction rule: grow the invisible part right, slide left only if it would pass the end of the track, never go negative — one expression, no branch. The invariant `left + left'ᵢ·width/100 = 100·sLᵢ/T` holds because **W cancels**, which is why the minimum-width extension is free of visual consequence. `unitPct` redefined as `pctOf(u, TOTAL_UNITS)`, output byte-identical, so the file holds one rounding rule. **No forecast math changed** — same server dates, same order, same drop rule; only the denominator moved. **The 24px minimum is arithmetic, never CSS**: `24 / (92/5) = 1.3043478…` units = 2.17% of track, exact because `--gw` is declared once and `.gtrack` is content-sized at 12 columns. A CSS `min-width` would widen the *rendered* box after the arithmetic and visibly stretch every short bar, because the `.gseg` children resolve their percentages against it — the trap, and it is banned by name in the tests along with `padding`, `border`, `min-inline-size` and `box-sizing`. `.grun` is `top: 0; bottom: 0`, so `.gantt .gtrack .gseg`'s `top: 50%` centring resolves against a containing block of exactly today's height and that rule stays **byte-identical**; the grab target is the full 84px row instead of 26px, free because the box paints nothing. A row whose phases the window clips away renders `<div class="gbar"></div>` — `phaseRun` returns `[]`, the `ghostBar` idiom — so no box, no handle, no `draggable`. **NO VISUAL CHANGE, MEASURED not asserted** (integrate): the same fixture row rendered through the OLD template + `phaseBars` at `1c571f1` and the NEW template + `phaseRun`, real Ractive, absolute positions read back out of both HTML strings, every fixture pinned **and** unpinned — worst composed drift **0.0075 pp = 0.083px** on a 1104px track (F2, three touching segments); F6 the final column 0.0063 pp = 0.069px with its right edge at exactly 100.00%; a sweep of **every** one-day run at units 0…59 rendered both ways at 0.0063 pp with **0** boxes off the track and **0** under 2.17%; segment count, `cls` and `title` identical every time. **The build brief predicted F6 as the worst and was wrong** — three roundings feed the composed error and the omitted one is the largest, the `1c571f1` value being compared against was rounded too and does not cancel; the 0.02 pp tolerance is therefore the analytic bound, not a round number, and both the helper's comment and the test's were corrected at integrate from "≈0.006 pp, F6" to the measured numbers. **The guard moved with the source and did not shrink**: source count still **3** (`['entry','growr','grun']` — it moved, it did not multiply), and the ancestor sweep **grew a rule it did not have**, per source, that an inherited `none` is cured by the source's own explicit `auto` (`.gbrows { none } + .grun { auto }` is still caught for `.growr` — an exemption, not a hole); the SOURCE sweep and the `.gdragging` sweep take no exemption at all. Legitimate transparencies 4 → **5**. **Unchanged and green, which is itself the check**: every `dropOnBar` body assertion, both `weekAtX` suites, `draggingSubjects === ['gdl']`, and `test/gantt-legend.test.ts` + `test/planner-weeks.test.ts` untouched — the colour map did not fork and `TOTAL_UNITS`/`dayIndex` survived verbatim. **Three contract deviations at build, all forced, none weakening** (sorted names are `['entry','growr','grun']` because `'o' < 'u'`; `draggable` adjacency is not assertable because Ractive 1.4.4 emits `style` first whatever the template order — verified across three variants — so three render assertions read the extracted open tag instead; the ancestor sweep is a de-duplicated union, which the contract's own fixture table requires and which the pre-batch guard already did). **Gates**: `node frontend/build.js` 333,049 bytes · `npx tsc --noEmit` clean · `npx eslint .` clean · `npx vitest run` **58 files / 783 tests** green, and the same under `TZ=UTC` (baseline 57 / 735; 780 at integrate, +3 from the fix pass). **Fix pass (post-integrate review), test-only — no frontend file changed, so the build bytes are unchanged**: the `.gweek` cells got their own derived sweep (`WEEK_CELLS` + `weekCellOffenders()`), because T158's `auto` exemption correctly drops `.grun`'s chain — and `.gtrack` is an ancestor of `.grun` and of nothing else that drags, so the chain carrying the week cells stopped being swept exactly when T158 made those cells the only drop path outside the coloured run; mutation-probed on the real sheet, `.gantt .gtrack { pointer-events: none }` now fails one named test and nothing else. And `declaresAuto()` no longer takes a `:hover`- or `@media`-scoped `auto` as a cure — a ban may bite sometimes, a cure may not. **One real defect found at integrate, by an unrelated guard**: correcting the helper's comment in `01-app.js` to the measured drift turned `test/suggest-counts.test.ts` red — its drift guard strips lines beginning `/*`, `*` or `//` and then requires the `HARD_IDEAL` share to appear on exactly one line of the client, and this file's block comments are indented continuation lines with no leading `*`, so a prose pixel measurement survived the strip and read as a second copy of the constant. **The guard was right and the comment was changed**, not the guard; the pixel figure is spelled in words now, with a note saying why a bare decimal must not go back. **Flake honesty (T149)**: the first run went red on ONE test in `test/dayplan-api.test.ts` (`/__test/login` → 404), a file this batch does not touch — the same parallel-startup contention signature; rerun clean, nothing retried, capped or adjusted. **Still owed, and stated in two test-file headers**: none of this proves a drag works — no jsdom, no browser runner, and a synthetic `DragEvent` never enters Chrome's drag machinery, which is exactly why every automated check from 13g/13j to batch 6 passed while the live bar was un-draggable. **No synthetic DragEvents were added and none may be.** T155 is amended: (a)–(g) must be re-run against this build, and five checks join them — (i) the drag starts only from the coloured run, (j) a drop on empty track outside the run still lands through the `.gweek` cell, (k) a one-day phase is catchable at the 24px minimum including one in the final column, (l) a pinned row still refuses the grab on its colour and still accepts someone else's drop, (m) a before/after screenshot identical apart from the cursor

- 2026-08-18 — **BATCH 8 (JP's ruling of 2026-08-18) built via workflow — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., **T157**; closes **T155(h)**; ruling recorded in `gantt-frame-notes.md` under *The affordance is the coloured bars only*). Two frontend files, two test files. No server, no wire, no schema, no migration, `lib/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency. **The pointer affordance moves onto the coloured bars; the drag mechanics do not move at all.** Batch 7 correctly made `.gbar` span the whole 1104px track and stay `pointer-events: auto` — and `cursor: grab` plus `title="Drag along the timeline to reslot"` spread with it, so blank track to the right of a two-week run advertised a handle that is not drawn there. T153's consequence 3 argued that was honest because the wrapper *is* the drag source; it is honest about the mechanism and wrong about the user, and JP overruled it. **`pointer-events` was not touched anywhere** — `.gbar` and `.gseg` stay `auto`, the five directives on the open tag, `weekAtX`, `dropOnBar`, `moveRows`, geometry, colours, z-order and `.gantt.gdragging .gdl` are byte-identical, and `test/drag-hittest.test.ts` still bans a transparent drag source in any state. **Cursor**: `.gantt .gbar` `grab` → `default` (`default`, not `auto`, because `.growr` declares no cursor at all — the track now reads exactly like the rest of the row); `.gantt .gbar .gseg` `inherit` → `grab` (`inherit` is *how* it picked the wrapper's `grab` up; naming its own value stops the two silently re-merging); `:active` `grabbing` unchanged; and the pinned rule's subject moves off the wrapper — `.gantt .growr.pinned .gbar,` becomes `.gantt .growr.pinned .gbar .gseg,`, keeping the `:active` clause — only the RESTING clause is load-bearing (at (0,5,0) it outranks both `grab` and `grabbing`), the second is defensive. Segment-scoped for the same reason `grab` was: a refusal is an affordance too, and a 1104px `not-allowed` would withhold a track that never offered anything. **Title**: deleted from `.gbar` in BOTH branches — no new tooltip mechanism invented, and nothing added to the segment titles, which name the phase and its end date and would go noisy restating the mechanic on every run. The reslot sentence survives as **standing text** in the `.fnnote` hint above the Gantt (*"Drag a bar along its row to reslot it — an unscheduled row drags onto a week. Pinned rows never move."*): already there, not hover-gated, and strictly the better home, since a tooltip that appears only once the pointer is on the bar teaches nobody how to find the bar. The pinned sentence survives twice — `.growr` carries `title="Pinned — unpin to move"` and `title` is **inherited by a title-less descendant**, so the row's attribute covers the `.gtrack` and `.gbar` inside it (deleting the bar's copy removed a duplicate, not the message), and each `.gseg` still appends `· Pinned — unpin to move` to its phase title. **What a pinned row hovers as, verified against the render**: empty track = `default` cursor + "Pinned — unpin to move"; a coloured segment = `not-allowed` at rest **and** while pressed + "Sketch · Pinned — unpin to move"; `draggable="false"` unchanged, and the bar keeps its drop directives so it stays a valid landing strip for someone else's drag. An unpinned scheduled row, for contrast: empty track `default` and **silent**, segment `grab`/`grabbing` with its phase title only. **Tests moved, not weakened**: the old `expect(r).toContain('title="Drag along the timeline to reslot"')` is not softened — it is wrong now — and four assertions took its place (the wrapper emits no `title=` and the string is absent from the row; `.gbar` is `cursor: default` and **not** `grab` while `.gseg` is `grab`/`grabbing`; the instruction is present as standing template text; the segment titles still carry a title with no `reslot` in it), plus a segment-title assertion and a **negative subject guard** on the pinned block — no rule may name `.gantt .growr.pinned .gbar` as a subject again. Five of the six new CSS assertions flip against the pre-ruling stylesheet; the sixth (`.gseg:active` = `grabbing`) is byte-identical before and after and is labelled in the file as a preservation guard, not a regression proof. Gates: `tsc --noEmit` ✅ · `eslint .` ✅ · `vitest run` **735/735** (57 files) ✅ · `TZ=UTC vitest run` **735/735** ✅ (both first-run green — no T149 flake seen this pass) · `node frontend/build.js` ✅ (325,694 bytes). Suite **731 → 735**, the four net new tests. **Still owed**: T155 (a)–(g), the live-browser pass with a real mouse — this change cannot break the drag because it never touches hit-testing, but the batch-7 drag itself is still unproven outside a browser

- 2026-08-18 — **BATCH 7 (JP, live-site report) built via workflow — INTEGRATED, NOT DEPLOYED, NOT COMMITTED** (phase 13k cont., T153–T155; full ruling R-g-1 in `gantt-frame-notes.md` under *Batch 7*). Three frontend files, two test files, one test helper. No server, no wire, no schema, no migration, `lib/**` untouched, write registry unchanged at W1 / W2 / W3, no new dependency. **The Gantt phase bar could not be dragged with a real mouse — and had not been draggable since batch 4.** The app's handlers were never the problem; they were never reached. `.gbar` carries `draggable="true"` while its `.gseg` children carry the pixels, but **Chrome begins a drag from the draggable ancestor and hit-tests *that*** — so `.gantt .gbar { pointer-events: none }`, shipped since 13g/13j, made the drag get created and cancelled in the same tick. Root-caused with **real mouse input (CDP) before any code was written**: the shipped build produced exactly `mousedown:.gseg → mousemove → dragstart:.gbar → dragend:.gbar` with `dropEffect: "none"` — no `drag`, no `dragenter`, no `dragover`, no `drop` — while at `dragstart` everything looked correct (`text/plain` set, `effectAllowed: "move"`, `.gantt.gdragging` applied, source connected, nothing calling `preventDefault`). Three controls closed it: the unscheduled ROW drags fine with the same tool (`.growr` is `pointer-events: auto`), so tooling and app are both sound; forcing `auto` onto `.gbar` makes the sequence live; and forcing `.gantt.gdragging .gbar { pointer-events: none }` brings the instant-cancel back, proving **"hit-testable at mousedown only" is not enough** — which also rules out moving `draggable` onto `.gseg`, since the shipped `.gantt.gdragging .gbar .gseg` rule would then blank the SOURCE mid-drag. **The fix inverts batch 4's reasoning: the bar owns its own drop.** `pointer-events: auto` on `.gbar`, written out explicitly rather than merely omitted (the property **inherits**, so the stated value defends the source against a future ancestor rule); `.gseg` dropped from the `.gdragging` rule; and `on-dragover="['dragOver']" on-drop="['dropOnBar']"` added on a **third** attribute line so the two lines above it stay byte-identical for the tests that pin them. **`ganttDragging` survives** — the brief allowed deleting it, recon proved otherwise: `.gdl` is a later sibling of `.gbar` in the same positioned `.gtrack`, both absolute at `z-index: auto`, so the deadline tick paints **over** the bar and wins hit-testing at its 2px column; left solid it would refuse the drop at exactly the deadline, and it cannot be permanently transparent because at rest it owns a real `title`. With the bar solid, the `.gweek` cells no longer see the drag on a scheduled row, so a new **named pure** `weekAtX(clientX, rect, weeks)` does the mapping they used to do by being hit — no `document`/`window`/`app.get`/`WEEK_PX`/`WEEK_COUNT`, dividing the **measured** width by the count rather than the hard-coded 92px, half-open, clamped both ends — and `dropOnBar` runs the **same `moveRows`** `dropOnWeek` runs: still exactly the two pre-existing `POST …/replot` call sites, one audit path, BR-8 group resolution byte-unchanged. The card id comes off the dataTransfer, never off the bar's own row, so an unscheduled row dropped on a scheduled row's bar moves the right card; the handler measures `ctx.node.closest('.gtrack')`, never `event.target`, because the event now fires on a 26px segment and bubbles. `.gweek`, `.gblockhead` unslot, the unscheduled row-drag, `rowKey`'s keyboard reslot, the phase→colour map, segment geometry and the legend are all untouched. A **pinned** row keeps `draggable="false"` and its refusal title but deliberately **keeps** the drop handlers — the pin freezes the pinned ROW, not the column, and suppressing them would carve a dead 1104px strip that silently refuses every drop. **Why every automated check missed this for three batches**: there is no jsdom and no browser runner in this repo, so every planner test is `toHTML()` or a source regex, and a synthetic `DragEvent` calls the app's handlers **directly** without ever entering Chrome's drag machinery. The standing answer is `test/drag-hittest.test.ts` (**44** tests): drag sources enumerated **from the shipped template** (count pinned at 3, so a fourth joins automatically), every `pointer-events: none` rule across **all seven stylesheets** read off disk and matched on its **rightmost compound**, plus an ancestor sweep (the property inherits), a class-less-subject check (`*` would slip a subset test), and parser self-tests. **Proved non-vacuous twice** — by permanent in-file fixtures covering the shipped rule and all three controls, and at integrate by reintroducing the bug in the real tree and watching the guard go red with ``35-gantt.css: `.gantt .gbar` makes the .gbar drag source un-hit-testable`` before restoring the line. Gates: `tsc --noEmit` ✅ · `eslint .` ✅ · `vitest run` **726/726** (57 files) ✅ · `TZ=UTC vitest run` **726/726** ✅ · `node frontend/build.js` ✅ (322,831 bytes). Suite **682 → 726**, exactly the 44 new tests; no test weakened or deleted without a replacement asserting more. **Flake honesty (T149, unchanged and not this batch's)**: eight full runs at integrate — 2 default (both green) and 6 `TZ=UTC` (3 green, 3 red). Every red was a **single** test in a long-standing server file this batch does not touch (`admin.test.ts`, `acks.test.ts`, one not captured), never an assertion in a file this batch edits; both are 3/3 green in isolation, and the five suites this batch touches or neighbours are **124/124 across five isolated `TZ=UTC` runs**. Nothing was retried, capped or otherwise adjusted to hide it. Ractive `{{! }}` hazard cleared: the rewritten template comment quotes no `}}` before its terminator and `Ractive.parse()` on the built `public/index.html` shows it fully stripped (an HTML comment there would have broken `expect(renderGantt()).not.toContain('gdragging')`). **Not deployed and not committed — and nothing here proves the drag works.** T155 is the live-browser pass that closes T153, and it is non-negotiable: this batch's whole subject is a defect class the suite is structurally blind to. **Review fix pass (T156)** then corrected two things and changed no behaviour: a comment in all four touched files claimed the segments left the `.gdragging` rule because blanking them would blank the drag source — **not true as shipped** (`draggable` is on `.gbar`, which stays `auto`, so a transparent `.gseg` falls through to it), and the honest reason is now written down: nothing needs them transparent once the bar owns the drop, and the ban is the deliberately stricter line that stops `draggable` ever moving down onto `.gseg`; and the guard itself had three ways past it — a case-/whitespace-sensitive matcher (`POINTER-EVENTS : NONE` was invisible), an eleven-entry **hand-kept** ancestor list (the same staleness vector the sources are protected from — the template is now parsed as a balanced tree and each source's ancestors come from its real nesting, which immediately picked up `view`, `pscrollwrap`, `pscroll` and `gscroll`, four wrappers the list had missed), and stylesheets-only reach (template inline styles and all four shipped scripts are swept now, plus a mid-drag sweep for `visibility: hidden`/`display: none` on `.gdragging` rules, since control 3 proved hittable-at-mousedown-only is not enough). Non-vacuity re-proved against the **real pre-fix file** (`git show 1e13088:…/35-gantt.css` dropped into the tree → 3 red, then restored). `drag-hittest` **44 → 49**; gates re-run: `tsc` ✅ · `eslint` ✅ · `vitest run` **731/731** ✅ · `TZ=UTC vitest run` **731/731** ✅ (rerun — one red at first pass was `test/webhook.test.ts` failing at SETUP with `Port "54463" already in use` from `mongodb-memory-server`, untouched by this batch, 9/9 in isolation; a sharper T149 signature than the `socket hang up` face, pointing at mongod port allocation racing across 57 parallel suites) · `node frontend/build.js` ✅ (323,355 bytes). The `cursor: grab` / `title` spread across the full track was left alone on purpose — it is JP's call at T155(h), and restyling here would pre-empt it

_Rotated out of STATE.md's session-log window on 2026-08-19 (10KB cap). Summary lines, verbatim; the full narratives are above and in `docs/history/context-restructure.md`._

- 2026-08-18 — Context restructure stages 1–5 + JP's doc workflows: state-log rotation, gantt-rules, T-shape, decisions/, script split (baseline `4dd5186…`), hygiene audit + archival rulings → docs/history/state-log/2026-08-18.md
- 2026-08-18 — Batch 10: Pipeline warning becomes icon + hover card, amber wash removed; four lens-caught defects (T163–T166, R-warn-u/v/w) → docs/history/state-log/2026-08-18.md
