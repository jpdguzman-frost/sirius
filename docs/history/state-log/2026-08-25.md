# 2026-08-25 — owl #66: every escalation confirmed

## What arrived

`miles → jp #66`, *"Rulings across #51–#55 — and the retired formula is ours to
fix"*. It closes five outstanding owls in one message. Every escalation this side
raised came back confirmed, and one of them came back with product saying the
frame was wrong in a way that would have breached the constitution.

## The retired formula

The headline. The Forecast tab's Model Constants panel, as drawn in `279:22602`,
quotes *Total Cycle Time · 1.28 × Forecasted Review Time + 2.96*. Those are
`LEGACY_CYCLE` — the workbook regression that invariant 6 confines to
`lib/forecast.legacy.ts` and that SC-3 says is never exposed, because it
overstates review waits 2.6–4.6×.

Product's words: **it should never have been in that panel.** Building it
verbatim would have printed the retired model in front of a PM as if it were
live, on the one screen whose entire purpose is that the old model is gone. The
provenance guess in R-fc-d holds too — inherited from the old prototype's panel,
back when those numbers *were* the model. The replacement wording (which states
the shipped engine's relation, including the Review SLA override case) stands,
and product is fixing the frame so it cannot be copied forward again.

This is the second time a frame has carried a retired instruction that lapsed
without being retired — the `Open Card` underline in R-warn-r was the first. Both
were caught by measuring rather than by reading the annotation.

## The one code change

Everything else in #66 ratified what shipped. One item did not:

**The Pipeline table header said `Client`. Everything else said `Requestor`.**

Four surfaces speak this field's name — the table column header, the filter
panel's group heading, the indicator chip, and the Requests table's own column.
Three said Requestor. The header said Client, because node `70:10009` names
Client in its definitive column order and the table was built to it. The cell
*underneath* that header has rendered `row.requestor` since the day it was built.

Product ruled **REQUESTOR everywhere**, with a reason worth keeping: the values
are people, and on a single-client board a Client filter selects every row or
none — which is exactly why the axis was never built as one. The disagreement was
never cosmetic; it was one column labelled with a concept the product does not
have.

Renamed the label and moved the class with it (`col-client` → `col-requestor`,
six sites across the template, the stylesheet and one test). A class naming the
wrong noun is how the wrong noun finds its way back into the markup.

### The guard

Asserted as the rule, not as the fix. Every filter axis narrows a column the
table draws, therefore every axis label must appear verbatim in the header row:

```
it('names each filter axis exactly as its column header names it', () => {
  const axes = (recipe.PIPE_FILTERS as Axis[]).map((f) => f.label);
  for (const label of axes) expect(headings).toContain(label);
});
```

A sixth axis is covered without touching the test, and renaming the word in one
of the three places it is spoken fails here. Proved non-vacuous by reverting the
header to `Client` — two tests fail, both by name — then restoring it.

## Ratified without change

**Pipeline.** Zero-count values stay hidden, not disabled: product withdrew the
frame's "expose empty categories" note outright, having not known at the time
that STATUS carries every Trello list name on a real board. The ticked-value
exception was named as the right one. All five departures from the frames
confirmed, and two singled out as better than what the frames said — anchoring
both panels to the toolbar rather than to their own buttons (a static frame
cannot show the sort button growing to 240px and dragging the filter button
sideways), and the heavier shadow. The Filter Indicator is beyond spec and the
per-axis wrap is the detail that makes the ✕ worth having.

**Deadlines.** The acknowledgement, the day planner and the month control all
stay — "build without it rather than inventing one" meant do not invent, and it
should not have put shipped, audited behaviour at risk. NEEDS REPLOTTING keeps
the broad count: it matches the rows enumerated beneath it, and a card displaced
by an over-capacity week genuinely needs replotting even with no deadline
problem. CONFLICTS counting conflicts was settled from the frame's own arithmetic
rather than by asking, and product thanked us for that. Detail rows stay
non-navigable — evidence, not navigation. No collapse control. The "1 replotting"
badge is derived, as built; where the sample showed it beside no rule badge, that
is sample error.

**Forecast.** All three label corrections confirmed (RENDER, CYCLE, CONFIDENCE),
all evidenced rather than stylistic. Difficulty stays read-only — the write
exists on Pipeline and a second write surface should not open here. One solid
header band per tier confirmed; the four cell heights are hug-sizing, not a
treatment. Empty states confirmed as required by §8 and absent from the frames.

**R-fc-r closed: do not build the drawer.** "How this forecast works" stays
pointed at Model Constants. Product's reasoning is the sharpest line in the owl —
a second surface carrying the same explanations *is exactly how the stale formula
survived this long*.

## Authority, temporarily inverted

Product is fixing seven frame defects: the retired formula, SKETCH → RENDER,
CYLCE → CYCLE ×3, TYPE → CONFIDENCE, the difficulty chevrons, the sheared
tier-one rule, and CLIENT → REQUESTOR on the Pipeline column. Their Figma plugin
is disconnected; they will confirm when the fixes are in.

**Until then, this build is authoritative over those frames** — stated by product
in the owl and recorded in all three frame-notes files, because the standing rule
in this project is the opposite (the frame wins) and a future session reading only
the rulebook would apply it.

## Still open

Two items remain with product: the ARES read-path caching answer, and the
Deadlines acknowledged-state design (R-dl-n).

## Build health

Typecheck clean, eslint clean, 66 files / 1182 passed / 32 todo. Three tests
added, none removed.

## Files

- `frontend/templates/views/30-pipeline.html` — the header label, four class sites
- `frontend/styles/20-pipeline.css` — the column measure
- `test/pipeline-expanded.test.ts` — two class references
- `test/pipeline-sortfilter.test.ts` — section H, the header/filter agreement guard
- `specs/001-sirius-v1/pipeline-frame-notes.md` — R-pf-n added, R-pf-c ratified
- `specs/001-sirius-v1/deadlines-frame-notes.md` — all three confirms answered, four rulings recorded
- `specs/001-sirius-v1/forecast-frame-notes.md` — R-fc-d ratified, R-fc-r closed, the raised list answered
- `STATE.md` — thread at #66, the owl block rewritten to what remains

---

## Deploy, 2026-08-25 — `41cc2a0`

`./deploy.sh`. healthz 200 (`mongo: connected`, `redis: connected`,
`env: production`), both pm2 processes restarted (uptime 0s, restart 69),
migrations reported up to date.

Verified by content, not by the script's exit code: the served page with the
server's injected `window.SIRIUS_BASE` line removed hashes
`b51ff31b…` — **byte-identical to local `public/index.html`**. `col-requestor`
appears five times and `Requestor` twice in the served bytes; `col-client` and
`Client` appear **zero** times.

## The stale queue — a correction worth recording

JP asked how I knew that four things were live and unopened in a browser. The
honest answer is that I read the **Live browser passes owed** list in `STATE.md`
and reported it as current. Checking it against the rest of the same file showed
two of the four entries were wrong:

| Claimed unseen | Actually |
|---|---|
| Forecast tab | ✅ true — live 2026-08-22, JP deferred the pass deliberately (`ec3706d`) |
| White background, Schedules + Deadlines | ✅ true — same deploy, no pass since |
| Pipeline filter + sort panels | ❌ **clicked through on rt-test 2026-08-21**, after the inert-panel fix, and again for the Indicator work |
| Deadlines tab v2 | ❌ **clicked through in that same pass** — which is where **DL-scope** came from, a finding only a rendered Breakdown could produce |

The mechanism: commit `56569fc` (2026-08-21 11:34) recorded the live pass at the
top of `STATE.md` — *"clicked through on rt-test — every fix confirmed, zero
console errors"* — and **left the owed-queue untouched further down the same
file**. The queue was written when those items were genuinely unverified and was
never pruned once they were. It then survived every later trim, and the phase
table two sections above it said *"clicked through"* the whole time.

Two lessons, both recorded in `STATE.md` itself rather than only here:

1. **Prune the queue in the same commit that records the pass.** A checklist that
   is only ever appended to becomes a list of things that were once true.
2. A stale "unverified" list is not harmless. It would send someone to redo work
   already done, and — worse — it dilutes the entries that are genuinely true.
   Forecast really has never been opened, and that fact reads weaker inside a
   list of four when two of them are wrong.

Corrected in place: the phase row no longer claims Deadlines v2 was never seen,
and the queue is down to the two items that are real.

---

## Reconfirming the 9-file test failure

JP asked me to reconfirm a claim from the overnight briefing: *9 of 66 test
files couldn't start their test database during the full run; re-ran one alone
and it passed in 2 seconds; looks like the machine struggling, not a broken
build.*

**The observation is real.** Found in the scheduled session's own transcript,
2026-08-25 01:30:

```
Test Files  9 failed | 57 passed (66)
Tests  1086 passed | 93 skipped | 32 todo
duration 973s
```

Every failure was `Hook timed out in 120000ms` at `beforeAll → startTestDb()`.
Zero tests failed — 9 files never executed their bodies at all, which is why
that run shows 93 skipped and a lower pass count than a green run.

**The mechanism is confirmed, not guessed.** `test/helpers/db.ts` calls
`MongoMemoryServer.create()` — a real mongod per file, not a stub — and **31 of
the 66 files use it**, on a 10-CPU machine with vitest running files across
workers. Nine of them failing to get a mongod inside two minutes is exactly what
resource contention looks like here.

**What I underweighted: the duration.** That run took **973 seconds**. A normal
full run on this machine takes **~27 seconds** — a 36× difference. That is the
strongest single piece of evidence for "environmental", and I did not report it.
It also reframes the conclusion: the machine was not "struggling", it was
saturated to the point where a two-minute startup budget was not enough.

**What I could not do: reproduce it.** Eight consecutive full runs on 2026-08-25
(three during the owl work, five deliberately for this question) all returned
`66 passed (66)` in ~27s. So the diagnosis rests on the failure's shape and the
isolated re-run, not on a repeat.

**A correction to my own reasoning.** The briefing justified "environmental" by
re-running `test/writes-disabled.test.ts` alone, where it passed in 2.13s. That
is weaker evidence than it sounds: a single file passing alone proves that file
is fine, and says nothing about contention — which by definition only appears
under parallel load. The right confirmation is re-running the **full suite**,
which is what was done today. The conclusion holds; the original evidence for it
did not carry the weight put on it.

**This is NOT the flake already documented** in `test/CLAUDE.md` rule 5. That one
is loopback-port squatting: ~1 run in 5, **one** random server suite, socket hang
up or a stranger's 404. This is a mongod-startup timeout hitting **many** files
at once. Filing the second under the first would have hidden it, so rule 5 now
names both and gives the tell that separates them (the run duration), with the
detail here.

**Not treated as a build problem, and not masked.** No retry cap, no timeout
bump, no `--no-threads`. Both would convert a visible environmental signal into a
slow green run.

## What ARES still owes us — the concrete question

JP asked what is actually needed from ARES. It is one question with one
consequence.

**The mechanism.** When Sirius writes a registry field to Trello (urgency, due
date, difficulty) it stamps `registry_written_at` on the row. When the worker
re-reads that card through ARES it stamps `readAt` **at the instant it issues the
read**, and `staleGuard` in `worker/syncAres.ts` applies the Trello-owned fields
only where `registry_written_at < readAt`:

```js
$or: [{ registry_written_at: { $exists: false } },
      { registry_written_at: { $lt: readAt } }]
```

Read plainly: *if my write happened before I started this read, the payload
already reflects it — trust it. If my write happened after, the payload predates
the user's change — leave it alone.*

**The assumption underneath.** That the moment Sirius *issues* the read is a fair
proxy for the moment the data was *fetched from Trello*. That holds only if ARES
reads live.

**The failure if it doesn't.** If ARES serves from a cache, a payload can be
minutes old while the request is one second old. `readAt` would then be newer
than `registry_written_at`, the guard would pass, and the stale cached value
would be written over a change the user just made — the push reverting an edit,
which is the precise bug the guard exists to prevent. Invariant 8 promises
Sirius never shows a state Trello lacks; this is the one path where it could.

**The question for whoever owns ARES**: *does the read path serve cached data,
and if so what is its maximum age?*

- **Reads live** → the guard is exactly right, and the assumption is retired.
- **Cached, bounded by N** → `readAt` must become "issue time minus N".
- **Best answer** → ARES returns a `fetchedAt` / `asOf` per card, and Sirius
  compares against the age of the *data* rather than the age of the *request*.
  That is drift-proof, where a hardcoded N is not.

Product's position (owl #54) is that neither they nor Miles can see the ARES read
path and will not guess; it is with whoever owns ARES. Their instruction was to
leave it visible as an unverified assumption rather than quietly assume it
closed, which is how it is recorded in the source comment and here.

**Urgency: low but not zero.** It only bites when a user edits one of three
fields and a push for that same card lands inside the cache window. Nothing is
blocked on the answer; the guard is sound in principle and tested
(`test/reconcile.test.ts`). What is missing is confirmation of the one fact it
stands on.

---

## The ARES question is answered — from their source, not from an owl

JP asked whether I have access to the local file-drop owl for ARES. I do:
`../owl/` holds four notes, all outbound (`sirius-to-ares` #06–#09). Inbound
notes are deleted once processed, so an empty inbound side means either nothing
came or everything came and was handled — and here it means nothing came. The
channel was **closed 2026-08-19** on JP's instruction after three notes on one
one-line ask drew no reply: *"if nobody is reading `../owl/`, that is more worth
knowing than the subscription is."* Nobody was.

**But `../ares/` is a sibling directory of this repo.** The question we have been
waiting on since 2026-08-20 was answerable by reading it, and it took minutes.

### The answer

**ARES reads never touch Trello.**

```js
// ../ares/src/services/trelloDataService.js
export async function getCard(cardId) {
  return TrelloCard.findOne({ cardId }).lean();
}
```

The board endpoint is the same shape (`TrelloCard.find(filter)`). ARES serves a
**materialised store** that its own worker refreshes on a **15-minute cycle**
(`TRELLO_REFRESH_INTERVAL_MS = 15 * 60 * 1000`). Confirmed against the live API:
`/api/v1/trello/health` returned `lastRefreshAt` and `nextRefreshAt` exactly
fifteen minutes apart.

So a payload can be up to ~15 minutes old **no matter how recently you asked**.
The age of the response says nothing about the age of the data.

### The field that fixes it already exists

```js
// ../ares/src/workers/trelloCardLogic.js — buildCardDoc
lastPolledAt: now,
```

Written for **every non-done card on every cycle**, changed or not — so it is a
genuine *fetch* time, not a last-modified. It is in the schema, no projection
strips it, and a live card came back carrying it with an age that tracked the
cycle position exactly.

Field names on a live card:
`__v, _id, boardId, cardId, checklists, createdAt, currentList, currentListId,
dateLastActivity, description, due, dueComplete, firstSeenAt, historyFetched,
labels, laneHistory, lastPolledAt, members, movements, name, projectName,
status, updatedAt, url`

### The bug this exposes

`staleGuard` in `worker/syncAres.ts` applies Trello-owned fields only where
`registry_written_at < readAt`, and `readAt` is stamped **when Sirius issues the
request**. That is a fair proxy for when the data was fetched *only if ARES reads
live*. It does not.

The reachable sequence:

1. ARES polls card X at 10:00.
2. A user changes urgency in Sirius at 10:05; we write Trello and stamp
   `registry_written_at = 10:05`.
3. Anything triggers a reconcile of X at 10:06 — a push for the board, or the
   Sirius sync cycle.
4. `readAt = 10:06`, which is **later** than 10:05, so the guard passes.
5. We apply the payload ARES polled at **10:00** — before the edit — and the
   user's change is silently reverted.

The guard was built to prevent exactly this and cannot, because it compares
against the wrong clock.

### Why nothing is on fire

`writes_enabled` is **off** on rt-837 (JP, 2026-08-21). With no registry writes,
`registry_written_at` is never set, absent always passes the guard, and the whole
branch is inert. **The bug is latent, and it becomes live the moment writes are
switched on** — which makes it a gate item on that decision rather than a
standalone fix.

### The fix

Compare against `card.lastPolledAt` instead of the request instant. Same guard,
right clock:

- write at 10:05, data polled 10:00 → `10:05 > 10:00` → **skip**, correctly.
- write at 10:05, data polled 10:10 → `10:05 < 10:10` → apply, correctly.

It needs `lastPolledAt` carried through `AresCard` and the mapper into the two
upserts. It is small, and it is currently inert, so it can land before the
write-enable rather than alongside it.

**One dependency worth naming**: ARES's own 2026-08-02 integrity survey records
`lastPolledAt` as *"written in three places and read by none"*. If Sirius starts
depending on it, ARES must be told, or a future cleanup deletes the field and
takes the guard with it.

### A note on the record

`contracts/ares-read.md` already said *"everything documented is served from ARES
cache"* — and then the same bullet said *"per JP (2026-08-03) the new ARES
delivers in realtime"*. Two claims, opposite meanings, one line apart, both
second-hand. That is how the question stayed open for three weeks while the
answer sat in a sibling directory. The bullet is now replaced with what was
measured.
