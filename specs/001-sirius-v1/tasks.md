# Tasks: Sirius v1 — Delivery Pipeline & Forecasting Platform

**Input**: [plan.md](./plan.md) (sequence, gates, estimates) · [spec.md](./spec.md) (FR/BR/NFR/AC) · [data-model.md](./data-model.md) · [contracts/](./contracts/) · constitution v2.0.0

**Organization**: Grouped by the plan's build sequence (phases 0–9), NOT by user story — the phase ladder is load-bearing (gates, dependencies). Story labels map tasks to spec user stories: US1 Pipeline · US2 Scheduling · US3 Deadlines · US4 Forecast · US5 Requests · US6 Urgency · US7 Multi-project. Every task cites the requirement it satisfies; a task with no ID would be scope creep. `lib/` and business-rule tasks are test-first: the test task precedes the implementation task. Tasks are sized 0.5–1 day; sanity bound ~60–63 dev-days.

**Gates** (JP only — the agent can NEVER check these off): T026 (AC-10, end of phase 3) and T045 (model validation, end of phase 6). Phase 7 must not start before T045 passes. Phase 8 is BLOCKED until the staging duplicate Trello board is confirmed.

---

## Phase 0 — Setup & infrastructure (~4d)

- [x] T001 Scaffold repository per plan.md layout — `server.js`, `src/{auth,routes,models,services}/`, `lib/`, `worker/`, `frontend/{templates,scripts,styles}/`, `scripts/migrate/`, `test/`; package.json with Express 5, Mongoose, ioredis, connect-redis, passport, passport-google-oauth20, zod, ractive, vitest; TypeScript `strict` config covering server/worker/lib (constitution stack; NFR-6 groundwork)
- [x] T002 [P] docker-compose.yml (mongo, redis) + `.env.example` with every variable from quickstart.md, values empty (invariant 15, NFR-5)
- [x] T003 [P] `frontend/build.js` concatenation pipeline with Ractive template parse-check, ARES conventions (NFR-1; plan.md frontend structure)
- [x] T004 CI workflow: typecheck · lint · `vitest run` · dual-TZ run (`TZ=UTC` + `TZ=Asia/Manila`) · dependency audit · frontend build (invariant 11, NFR-10; plan.md pipeline)
- [x] T005 [P] ARES contract probe in CI — verify documented shapes in `contracts/ares-read.md` against `openapi.yaml`, fail build on drift (FR-8.1)
- [x] T006 Deploy scripts targeting the ARES host pattern, staging + production configs; staging env asserts duplicate-board IDs only (invariant 17; NFR-4; OD-8 resolution)

**Checkpoint**: `npm run dev` boots an empty shell; CI green on an empty test suite.

---

## Phase 1 — Schema, migrations, seed (~3d)

- [x] T007 Tests for schema rules in `test/models.test.ts`: `(project_id, trello_card_id)` unique while `mc_number` duplicates freely (99-deliverable MC-825 case); `deliverables_v` precedence trello_due → sheet_deadline → none; sprint `ends_on ≥ starts_on`; every collection requires `project_id` (invariants 1, 3, 14; BR-9; FR-1.4)
- [x] T008 Mongoose models for all 15 collections in `src/models/`, fields/defaults/validators exactly per data-model.md ownership groups (FR-1.1, FR-1.4; invariants 1, 3, 4)
- [x] T009 Migration runner + `scripts/migrate/001-indexes.js` (unique compounds, secondary indexes) + `scripts/migrate/002-deliverables-view.js` (`deliverables_v` MongoDB view implementing BR-9) — version-controlled, never hand-applied (invariant 14; constitution)
- [x] T010 `scripts/seed.js` + fixtures: two projects (one sharing a Trello board via `trello_label`), deliverables incl. a multi-deliverable MC group, work cards, sprints, CSV intake fixture — fixtures only, never production data (invariant 16; enables AC-4, AC-5, AC-6 tests)

**Checkpoint**: T007 green; seeded db passes scope queries.

---

## Phase 2 — Auth, allow-list, audit (~4d)

- [x] T011 Authz matrix tests in `test/auth.test.ts`: non-Frost denied with reason (AC-1); Frost off allow-list denied (AC-2); unverified email denied; cross-project API call → 403 (AC-3) (FR-2.1–2.4)
- [x] T012 Passport Google strategy in `src/auth/` — the four server-side checks: email_verified, `hd` = ALLOWED_HD, matching email domain, active `users` document (FR-2.1–2.4; invariant 9)
- [x] T013 Redis session store (connect-redis) + httpOnly cookie in `server.js`; `ensureAuthenticated` middleware in `src/auth/session.js`, applied to every route (FR-2.3; NFR-6)
- [x] T014 `ensureProjectMember` middleware in `src/auth/membership.js` — session AND `user_projects` membership re-checked per project-scoped route (FR-1.2, AC-3, AC-4; invariant 9)
- [x] T015 [P] Append-only audit writer in `src/services/audit.js` (insert-only surface) + tests proving no update/delete path and before/after capture (FR-2.6; invariant 10; NFR-7)
- [x] T016 [P] [US7] Project switcher scoping in `src/routes/` handlers: project context resolution + every query filtered by `project_id` (FR-1.2, FR-1.4, AC-4)

**Checkpoint**: authz matrix green against a running server.

---

## Phase 3 — Port `lib/` + golden tests (~4d)

- [x] T017 Golden fixture set in `test/golden/` — bundle oracle + 40 sanitized workbook rows from JP's export — inputs + expected dates extracted from the Delivery Forecast workbook rows (AC-10 basis; BR-1)
- [x] T018 Tests for `lib/calendar.ts` in `test/calendar.test.ts`: `workday()`, `toFriday()`, holidays, Manila timezone, dual-TZ identical results (invariant 11; BR-1)
- [x] T019 Port `lib/calendar.ts` verbatim from the prototype bundle (invariant 5; STATE.md deviation)
- [x] T020 Golden tests for `lib/forecast.legacy.ts` — BR-1 mechanics + workbook WORKDAY cross-validation (old-model export; render rule superseded by BR-1, noted for gate) in `test/forecast.legacy.test.ts`: identical dates to the workbook for identical inputs across the full golden set — `WORKDAY` arithmetic, Friday render start, `1.28 × review + 2.96` (AC-10; BR-1)
- [x] T021 Port `lib/forecast.legacy.ts` verbatim — tests only, never imported by UI code (invariant 6; FR-7.2)
- [x] T022 Tests for `lib/planner.ts` in `test/planner.test.ts`: urgency → deadline → difficulty ordering, throughput ceiling fill, blocked-card exclusion, pinned immovable, WEIGHTS (1/2/4) only for hard-mix, unachievable-mix spread-and-report, multi-row relative shift (BR-6b, BR-7, BR-7a, BR-8; AC-16)
- [x] T023 Port `lib/planner.ts` (`suggestPlan`, `weekLoad`, WEIGHTS, HARD_MIX) verbatim (invariant 5)
- [x] T024 Tests for `lib/forecast.ts` (empirical) in `test/forecast.test.ts`: difficulty × lane grid lookup, confidence selection (Average/0.7/0.85/0.95), SLA override replaces modelled review and cascades (BR-2, BR-4; FR-7.3–7.5; AC-12 logic)
- [x] T025 Port `lib/forecast.ts` + `lib/model.ts` grid lookup verbatim (invariant 5; FR-7.3)
- [x] T026 **GATE — PASSED by JP 2026-08-03** (evidence accepted: oracle parity ~3,600 cases, BR-1 mechanics, workbook rows 84/84): AC-10 confirmed — golden tests prove the port; phase gate for everything that consumes `lib/` (AC-10)

**Checkpoint**: `lib/` fully green incl. golden set; JP sign-off recorded in STATE.md.

---

## Phase 4 — ARES read + mapping (~6d) — UNBLOCKED (OD-1 resolved 2026-08-03)

- [x] T027 ARES client in `src/services/ares.js` per contracts/ares-read.md — envelope unwrap by surface, `X-API-Key` header, 60 req/min pacing + `retryAfter` honor, pagination; recorded-fixture tests (FR-8.1)
- [x] T028 [P] Taxonomy mapper tests in `test/mapper.test.ts`: `Main Card` label → deliverable, verb-prefix titles → work cards attached to MC group (never a deliverable edge), `trello_label` board filtering, MC number extraction from titles, `display_id` assignment (`MC-655.3`) (invariants 3, 4; FR-1.3; AC-5; spec §Card taxonomy)
- [x] T029 [US1] `worker/syncAres.ts`: boards → cards sync, upsert on `(project_id, trello_card_id)`, blockers from 🛑 labels, `card_events` append idempotent on `source_event_id`; `sync_runs` written success or failure, last good data preserved (FR-4.1, FR-4.5, FR-8.5; AC-19)
- [x] T030 [P] [US4] Steering adapter: `referenceWeeks` + `effectiveWeeklyRate` via internal-tier endpoint behind `src/services/` adapter, copied into `projects` each run (BR-6a; FR-5.16)
- [x] T031 [P] [US1] BR-10 status classification in `src/services/status-rules.js`: configurable keyword rules mapping free-text list names → Pending/Ongoing/Done, tests in `test/status-rules.test.ts` (BR-10; OD-5 default documented as open)
- [x] T032 [US1] Movements backfill for the model window + cycle-time derivation from activity timestamps, cross-checked against `/api/v1/trello/cycle-time` (FR-4.5; BR-2 raw material)
- [x] T033 Worker scheduler loop in `worker/index.js` — 15-min cadences, health job with ARES `/healthz` freshness gate, alerting on failure (FR-8.5, FR-8.6; NFR-3)
- [ ] T034 ⚠ PARTIAL (ARES-side 15-min live cycle observed + healthz gate wired; Trello→Sirius end-to-end measurement needs staging) — NFR-3 freshness evidence in `STATE.md`: Trello change → Sirius visible, evidence recorded for JP (< 15 min per JP's realtime statement; STATE.md exit check)

**Checkpoint**: real board syncs into fixtures-shaped collections; re-running sync is idempotent.

---

## Phase 5 — Intake sheet sync (~4d)

- [x] T035 Parser tests in `test/intake.test.ts`: ragged-row padding, 1899-12-30 serial-date conversion, two `Type` columns disambiguated by position, current-data fixture yields **495 imported / 495 reserved / 8 rejected** (AC-6; §5.2 gotchas)
- [x] T036 [US5] `lib/sheets.ts` service-account read, `spreadsheets.readonly`, credential from server-side env only (FR-8.2, FR-8.3; invariant 15)
- [x] T037 [US5] `worker/syncIntake.ts`: mirror rows, skip pre-allocated MC rows silently + count, unparseable rows → `intake_rejects` with row/reason, vanished rows → inactive never deleted (FR-3.1, FR-3.4, FR-3.5, FR-8.4; AC-9)
- [x] T038 [US1] Deadline join tests in `test/intake.test.ts` then implementation in `worker/syncIntake.ts`: sheet deadline joined on `mc_number`, `deliverables_v` precedence live, coverage measurably rises ~1/269 → ~169/269 on fixture (BR-9; AC-8)
- [x] T039 [P] [US5] Sync status surfacing in `src/routes/requests.js`: last-success time + failure state from `sync_runs`, queryable for the UI (FR-8.6; AC-19)

**Checkpoint**: AC-6 counts exact on fixture; AC-8 join measured; AC-9 verified.

---

## Phase 6 — Model refresh + validation (~4d) — RELEASE GATE at end

- [x] T040 Percentile math tests in `test/model.test.ts` with fixture `card_events`: design dwell by difficulty × lane, review dwell in *Sent for Client Review*, Average/70/85/95, sample_n recorded; spot-check against Appendix constants (BR-2, BR-4; FR-7.6)
- [x] T041 [US4] `worker/refreshModel.ts`: nightly per project over `model_window_months` (default 12, OD-2 open) → `model_grid` + `throughput_grid` in worker code (BR-2; FR-7.6)
- [x] T042 [P] [US4] Delta recording vs previous run + alert on sharp shift, in `worker/refreshModel.ts` (§5.4 step 5; FR-8.5)
- [x] T043 [US4] Model provenance + sample sizes exposed for the UI via `src/routes/deliverables.js` (grid read) (FR-7.7; AC-11)
- [x] T044 [US4] Run refresh against real ARES data; produce the grid + a dates sample for PM review, sample saved under `specs/001-sirius-v1/` evidence (BR-3; gate preparation)
- [x] T045 **GATE — PASSED by JP + PM 2026-08-03** (evidence: per-project grid from 6 months / 257 samples, values tracking the Appendix shape): forecast dates recognised. **No forecast UI ships before this passes** (invariant 7; BR-3; Sequence item 6)

**Checkpoint**: recomputed grid within expected range of Appendix snapshot; PM sign-off recorded in STATE.md.

---

## Phase 7 — UI, five tabs (~15–18d) — do NOT start before T045

- [x] T046 Frontend shell: `frontend/index.html` + `frontend/scripts/app.js` Ractive instance, tab nav, project switcher wired to scoped APIs (FR-1.2; AC-4)
- [x] T047 [P] [US5] Requests tab in `frontend/templates/requests.html` + `frontend/scripts/requests.js`: mirror columns, source-row link, *In pipeline*/*Not yet filed* status, filed/unfiled/missing-deadline filters (FR-3.2, FR-3.3, FR-3.6)
- [x] T048 [US1] Pipeline tab in `frontend/templates/pipeline.html` + `frontend/scripts/pipeline.js`: deliverable list with FR-4.1 fields, read-only Trello fields, expandable MC group work cards (FR-4.1–4.3; AC-4)
- [x] T049 [P] [US1] Corrections list in `frontend/templates/pipeline.html`: cards missing difficulty/deadline/Figma link, with links to fix at source (FR-4.4)
- [x] T050 [US2] Schedules layout in `frontend/templates/schedules.html` + `frontend/scripts/schedules.js`: fixed list pane + scrolling gantt grouped by sprint, three segments (sketch/review/render) + deadline marker, late row red render bar (FR-5.1–5.3; AC-18 visual)
- [x] T051 [US2] Drag slotting in `frontend/scripts/schedules.js` with < 100 ms feedback; gantt is output only; dates/sprint group/load update on drop (FR-5.4; AC-13; NFR-2)
- [x] T052 [US2] Multi-select (checkbox, shift-range, whole sprint) + relative-shift drag via `lib/planner`, in `frontend/scripts/schedules.js` (FR-5.5, FR-5.6; BR-8; AC-14)
- [x] T053 [P] [US2] Pin, duplicate-without-links, manual status override with note in `frontend/scripts/schedules.js` + `src/routes/schedule.js` — visibly manual, reversible, audited (FR-5.9, FR-5.11, FR-5.12; invariant 10)
- [x] T054 [US2] Suggest plan in `frontend/scripts/schedules.js`: ghost previews, explicit accept, pinned rows never move, unachievable mix stated plainly; throughput setting conservative/typical/stretch (FR-5.7, FR-5.8, FR-5.10; BR-7, BR-7a; AC-15, AC-16)
- [x] T055 [US2] Sprint editor in `frontend/scripts/sprints.js` + `src/routes/schedule.js`: add/rename/re-date/reorder/delete, overlap rejected on save, gaps surfaced as *Outside any sprint* (FR-5.14, FR-5.15; BR-5)
- [x] T056 [P] [US2] Weekly footer in `frontend/templates/schedules.html`: cards vs capacity, weighted load, Hard share amber/red per BR-6b thresholds (FR-5.13, FR-5.16, FR-5.17; BR-6a, BR-6b)
- [x] T057 [US3] Deadlines tab in `frontend/templates/deadlines.html` + `frontend/scripts/deadlines.js`: month weeks navigable, two entries per deliverable (sketch + render), Trello/Figma links, read-only (FR-6.1–6.3, FR-6.6)
- [x] T058 [US3] Conflict detection display per BR-6 in `frontend/templates/deadlines.html` + `frontend/scripts/deadlines.js`: urgent overlap, over capacity, past deadline — on-screen explanation + replot list naming every affected deliverable and why (FR-6.4, FR-6.5; AC-17, AC-18)
- [x] T059 [US4] Forecast tab in `frontend/templates/forecast.html` + `frontend/scripts/forecast.js`: Delivery Forecast column names, single empirical forecast, difficulty read-only, confidence per card, provenance + sample sizes visible (FR-7.1–7.4, FR-7.7; AC-11; invariant 6 — legacy never imported)
- [x] T060 [US4] Review SLA override input in `frontend/scripts/forecast.js` + `src/routes/schedule.js` → replaces modelled review, cascades downstream, audited (FR-7.5; AC-12)
- [x] T061 Keyboard-only scheduling path in `frontend/scripts/schedules.js` — a row slottable without a pointer (NFR-9; AC-20)
- [x] T062 [P] Sync status indicator + degraded mode in `frontend/scripts/app.js`: last good data shown, error surfaced, app usable (FR-8.6; AC-19)
- [x] T063 Performance pass at 5,000 fixture cards — pipeline 107ms / deadlines 85ms server-side (test/perf.test.ts) in `server.js` + `frontend/scripts/pipeline.js`/`schedules.js`: server-rendered shell + list virtualization as needed, p95 < 2 s (NFR-1)

**Checkpoint**: five tabs demonstrable end-to-end on staging data; AC-13–AC-18, AC-20 pass.

---

## Phase 8 — Urgency write (~2d) — built; live round-trip pending the TEST board (invariant 17 as amended)

- [x] T064 [US6] Tests first in `test/urgency.test.ts`: optimistic update rolls back on failed write; `audit_log` + `sync_runs` documents on success AND failure; absence-means-non-urgent round-trip (FR-4.6, FR-4.7; invariant 8)
- [x] T065 [US6] `lib/trello.ts`: `setUrgency()` + `ensureUrgentLabel()` — direct Trello API, dedicated integration-account token from server env, board-ID safety check refusing production boards outside production (FR-4.6; invariants 2, 15, 17)
- [x] T066 [US6] `src/routes/urgency.js` + Pipeline toggle UI: optimistic with rollback, audited, urgency visible in list (FR-4.6, FR-4.7; invariant 10)

**Checkpoint**: on the duplicate board only — label round-trip, forced-failure rollback, audit trail complete.

---

## Phase 8a — Conflict acknowledgements (~1d)

- [x] T067 [US3] Key computation tests then implementation: `conflict_key` = week | rule | sorted card:phase pairs; any card change lapses the ack; card-level indicators never suppressed (invariant 13; BR-9a; FR-6.7)
- [x] T068 [US3] Ack endpoints + Deadlines UI in `src/routes/schedule.js` + `frontend/scripts/deadlines.js`: dismiss removes from banner + replot list, acknowledged count + restore, every ack/restore audited (FR-6.7, FR-6.8; invariant 10; AC-17 interplay)

**Checkpoint**: ack lapses on replot; audit trail shows who/when/why.

---

## Phase 9 — Security testing + pilot (~13d incl. migration/pilot support)

- [ ] T069 ⚠ LOCAL HALF DONE (13 routes, 401/403, test/authz-matrix.test.ts) — staging smoke authz matrix: non-Frost + off-list + cross-project against every endpoint → denied/403 (AC-1, AC-2, AC-3; NFR-6)
- [ ] T070 [P] Sheet un-share drill: service account removed → clean failure surfaced, re-share restores (AC-7)
- [ ] T071 ⚠ STATIC HALF DONE (test/log-hygiene.test.ts) — [P] Log hygiene audit: no brief text, no credentials in any log path (NFR-11; invariant 15)
- [x] T072 [P] Backup + restore drill on the shared Mongo server (`sirius` db), documented (NFR-8)
- [ ] T073 ⏸ ON HOLD (JP 2026-08-04: team UI update in flight — audit after it lands) — Accessibility audit: WCAG 2.1 AA incl. the keyboard scheduling path (NFR-9; AC-20)
- [x] T074 `scripts/migrate-open-cards.ts`: migrate open cards/requests into production data per deadline-precedence and taxonomy rules (AC-8; BR-9; plan.md layout)
- [ ] T075 Full AC sweep on staging: AC-1..AC-20 recorded in STATE.md scoreboard with evidence links (Definition of done)
- [ ] T076 Pilot support window: triage, fixes, STATE.md session logs; retention decision recorded when OD-7 lands (NFR-7; OD-7)

**Checkpoint**: AC scoreboard complete; pilot go/no-go per *Pilot Security Readiness*.

---

## Phase 10 — Two-way sync (~5–6d Sirius-side) — added 2026-08-04 (JP; constitution v4.0.0, FR-9)

Ships **before** the pilot per JP's sequencing decision — the pre-pilot security review widens
to cover the due-date write and the webhook receiver. The ARES half is built separately from
`docs/ARES_PUSH_BUILD_SPEC.md`; only T086 depends on it.

- [x] T077 [P] Reconcile tests FIRST: sync reads the `Urgent` label + due date from ARES card data into Sirius fields; ownership matrix proven safe (Sirius planning fields untouched); echo of a Sirius-originated write is a no-op with no audit row (FR-9.5; invariant 8)
- [x] T078 Reconcile implementation in `worker/syncAres.ts` + `src/services/mapper.ts` (FR-9.5)
- [x] T079 [P] W2 write tests FIRST: `setDue` add/change/clear, existing time-of-day preserved, 17:00 Manila default, rollback on Trello failure, audit `due_set`, board guard, no-op guard (FR-9.1; FR-9.3; invariants 14, 17)
- [x] T080 `lib/trello.ts` `setDue()` + deadline write route in `src/routes/` per `contracts/trello-write.md` W2 (FR-9.1; BR-9 preserved)
- [x] T081 Deadline edit in the Pipeline UI with optimistic revert, matching the urgency toggle pattern (FR-9.1; FR-4.7)
- [x] T082 [P] Receiver tests FIRST: HMAC over `timestamp.body`, ±5 min window, constant-time compare, `event_id` dedupe, 202-fast with `{accepted, duplicates, ignored}`, unknown-board drop, 401 leaks nothing, no sync work in-request; `ARES_WEBHOOK_SECRET` added to the log-hygiene forbidden list (FR-9.4; NFR-6; NFR-11)
- [x] T083 `POST /api/webhooks/ares` receiver + `push_events` collection (unique `event_id`, TTL 7d) + worker drain with per-card coalescing → targeted `GET /api/v1/trello/cards/{id}` reconcile per `contracts/ares-push.md` (FR-9.4; FR-9.5)
- [x] T084 Poll fallback adaptation: hourly reconcile while push healthy, revert to 15-min + alerting `sync_runs` row after 30 min push silence with ARES healthy; push freshness surfaced in the sync status UI (FR-9.6; FR-8.5; FR-8.6)
- [ ] T085 **JP**: hand `docs/ARES_PUSH_BUILD_SPEC.md` to the ARES build agent; provision `ARES_WEBHOOK_SECRET` on both hosts; confirm subscriber config points staging at the TEST board first
- [x] T086 End-to-end on staging once ARES push is live: manual Trello change (incl. a hand-removed `Urgent` label and a hand-set due date) reflected in Sirius in < 1 min; fallback drill — push disabled → 15-min poll takes over with the alert (NFR-3 as amended; FR-9.5; FR-9.6)

**Checkpoint**: registry writes + reconcile proven on the TEST board; pilot go/no-go (T076) now includes phase 10.

---

## Phase 11 — Admin panel (~2d) — added 2026-08-05 (JP; FR-10)

- [x] T087 [P] Admin authz tests FIRST: every `/api/admin` route walked mechanically — 401 anonymous, 403 active non-admin, 200 admin; domain validation; last-admin guard; deactivation revokes the live session on next request; audit rows per action (FR-10.2–10.7; NFR-6)
- [x] T088 `users.is_admin` + `ensureAdmin` middleware (`src/auth/`) + `src/routes/admin.ts` (list/add/activate/memberships, Zod-strict) (FR-10.1–10.7)
- [x] T089 `GET /api/me` + Admin tab in the frontend (admin-visible, prototype design language): user table, add form with project checkboxes, activate/deactivate, membership editing — optimistic with revert (FR-10.1–10.4)
- [x] T090 `scripts/allowlist.ts` `ADMIN=1` flag; seed JP as first admin on the host; deploy (FR-10.8)
- [ ] T091 ⏸ ON HOLD with T073 — Fold the new screens into the keyboard/WCAG AA pass (T073 scope grows) (NFR-9)

**Checkpoint**: JP manages a real account end-to-end from the UI.

---

## Phase 12 — Build-spec v1.1 adoptions (~6d) — added 2026-08-12 (JP; FR-11, FR-12, BR-6c) — BUILT 2026-08-12

- [x] T092 Migration 004: `frost_notes` + `milestone_day_plan` collections with unique indexes, both carrying `project_id` (invariant 1; FR-11.1, FR-12.3)
- [x] T093 [P] Day-capacity tests FIRST in `test/dayplan.test.ts` (largest-remainder exact sum incl. 22-over-4-days, holiday zero + redistribute + reject, all-holiday week) then `lib/dayplan.ts` — a NEW module over `lib/calendar.ts` holidays; calendar and planner untouched (invariant 5; FR-12.4; AC-22)
- [x] T094 [P] Frost-notes route under `/api/projects/:projectId` (Zod-strict, audit rows) + three-state status derivation in the requests assembler; tests: remark-only invariance, flag flip, membership 403, audit both ways (FR-11.1–11.4, 11.6–11.7; AC-21)
- [x] T095 Requests UI: FOR CLARIFICATION tile + inline note editor (remark → checkbox → reason; Escape/Cancel discard; optimistic with revert) + three-state pills (FR-11.3–11.6)
- [x] T096 Day-plan routes: GET week plan, PUT placement (day inside the milestone's week, non-holiday; audit) + lapse-on-week-change in schedule writes; tests (FR-12.3–12.6; AC-23)
- [x] T097 Deadlines UI: week expand to Mon–Fri (one open at a time), pointer drag between days + keyboard equivalent, day-capacity footers and tints (FR-12.1–12.2; NFR-9 basics — the full WCAG audit stays with T073)
- [x] T098 BR-6c weighted load in the schedule/deadlines assemblers: weekly footers, over-capacity tint and BR-6 *over capacity* conflict switch to card-equivalents; fixture proves 478 on the mirrored board shape; `lib/planner.ts` untouched (BR-6c; AC-24; FR-5.13, FR-5.17)
- [x] T099 Sync-strip copy: replace "every 15 minutes" with push-aware wording in `frontend/templates/00-app.html` (FR-8.6, FR-9.6)

**Checkpoint**: AC-21–AC-24 pass as tests. Errata answer received 2026-08-12 (`docs/sirus_errata-reply-v1.2.md`): BR-6c weight applies everywhere — the built default is final, their §6.1 was a documentation error. Phase closed.

---

## Phase 13 — Pipeline redesign to Figma frame 17:1015 (~4–5d) — added 2026-08-12 (JP; 32 annotations = the spec, `pipeline-frame-notes.md`)

Scope: the Pipeline tab + the shell it carries (top nav, tabs, stats). Other tabs keep today's design inside the new shell. Difficulty stays READ-ONLY (settled by the complete annotation set, 2026-08-12 — superseded the same day by BRD-§9-A1; see Phase 13a). "Simulate Trello sync" is dev-only — omitted. Success criterion: 1:1 against the frame at 1600px; every value binds to a token.

- [x] T100 Token layer: `frontend/styles/05-tokens.css` — CSS custom properties named per the Figma variables (slate/red/amber/blue/green scales, text/caption..display, radius, space, shadow) + the semantic aliases the annotations use (`--surface-*`, `--border-border`, `--status-*`, `--font-sans`); Google Sans Flex 400–700 via Google Fonts CDN (JP override of the self-host note) (frame §0; annotations 17:1015, 70:10522)
- [x] T101 Icon set exported clean from the file via Rex (`exportAsync SVG_STRING`) and inlined as Ractive partials — tab icons, chevrons, search, calendar, warning, external-link, Trello/Figma marks; no runtime Figma dependency (asset-fidelity rule)
- [x] T102 Shell rebuild app-wide: breadcrumb (`nav aria-label="Breadcrumb"`, aria-current), PROJECT selector + board link, Last Synced stamp, user chip, tab bar with icons + `role="tab"`/`aria-selected`/tabindex + hover states; Admin tab styled to match (annotations 17:1438, 19:920–949; v1.2 six-tab note)
- [x] T103 Stats row: four equal columns, overline+display type, URGENT red; OPEN WORK = incomplete-card count (mirrors the panel) (annotations 28:3683, 31:2736–2744)
- [x] T104 Incomplete panel: Row-N chip + bold MC + name + amber reason + right-aligned Open Card (new tab) + consequence copy; hidden when empty (annotations 70:1358, 70:970, 70:914)
- [x] T105 Search + callout: realtime substring over MC #, name, type, client, status with `<mark>` amber highlight; callout copy verbatim; NO simulate button (annotations 17:2057, 70:1211)
- [x] T106 Table per the header annotation (13 columns as first framed; trimmed to 11 the same day — Cycle Time + Weeks cut by the team, Links 94px): expandable rows (chevron + Enter + focusable), Type/Client badges, Difficulty read-only badge, Urgency select-menu write with "saving…" state (opacity .5, pointer-events none) + rollback, Status keyword badge with list name verbatim, Due date-picker chrome on the W2 write-through (annotation 251:7925 carries stale v1.1 precedence — v1.2 governs) with missing/overdue states (251:23859), Work Started/Done/Cycle Time read-only, Weeks display, source-icon links (annotations 70:10008/9/24, 169:26364/26074/26426, 70:10030/10037)
- [x] T107 Backend: `asset_type` joined onto the MC group (closes the FR-4.1 "type" gap) + row derivations for Work Started / Work Done / Cycle Time (min started / max done across the MC group's work cards; workdays between) + tests
- [x] T108 Horizontal scroll: custom slider (track/thumb/chevron nudge) — below 1600px the table scrolls, never collapses (annotation 251:6758); task rows restyled to the kit
- [x] T109 Regression pass: remaining five tabs render correctly inside the new shell; full suite dual-TZ, typecheck, lint, build
- [x] T110 Fidelity check at 1600px against the frame screenshot; STATE update; deploy on JP go

## Phase 13a — W3 difficulty writeback (~1d) — added 2026-08-12 (JP go; product approval BRD-§9-A1 via Miles, owl #01–#03; build target cell 415:54974)

Scope: the write registry grows two → three (constitution 4.1.0, `contracts/trello-write.md` W3, FR-4.8). The frame's GOVERNANCE BLOCKER annotation is superseded by product's APPROVED one. Label-swap mechanics assumed (`Difficulty: …` taxonomy) pending Miles's label-vs-custom-field confirmation.

- [x] T111 Tests-first write layer + route: `setDifficulty()` label swap in `lib/trello.ts` (add-first ordering, stale-label removal, restore-on-partial-failure, taxonomy bootstrap with cache) + `PATCH /deliverables/:cardId/difficulty` mirroring W1/W2 (no-op guard, audit `difficulty.set`/`set_failed` + sync_runs per attempt, WRITES_DISABLED gate inherited) — 8 new tests, writes-disabled suite extended to all three routes (invariants 2, 8, 17; FR-4.8)
- [x] T112 Pipeline difficulty dropdown per product's build spec: badge-chip trigger keeps the phase-13 `d-*` recipes (Hard red-50/red-500 = the spec's #fef2f2/#ef4444; Easy green / Medium amber pending product tokens), select menu mirrors urgency (fixed flip-up, unified dismissers, menu mutual exclusion, per-card saving chrome, optimistic set + rollback banner, `loadAll` re-keys forecast + hard-mix), unset chip settable on missing-difficulty rows, disabled with read-only tooltip when `writes_enabled` is false; browser-verified incl. rollback (FR-4.8; cell 415:54974)

**Assumptions taken (annotation-silent, flagged to JP):** Weeks column = slotted week (dash when unscheduled) · ~~Work Started/Done = earliest start / latest done across the MC group's work cards · Cycle Time = workdays between them~~ — superseded: T113 made Started/Done per-card; the Cycle Time and Weeks columns were removed. Difficulty badge keeps its chevron chrome (1:1 rule) despite being read-only — flagged to the design team.

## Phase 13b — Pipeline cells batch 2 (~1–2d) — added 2026-08-13 (JP go; product specs via owl #06–#10, Rex-verified on nodes 415:54979 / 431:17015 / 431:17016 / 431:17017 / 432:17733)

Scope: five cell specs, zero new writes. Built via end-to-end workflow (Opus build/test agents, Fable verification), JP-directed. Supersedes the phase-13 group-derived Started/Done assumption.

- [x] T113 Per-card Started/Done: deriveWorkSpans extended to deliverable cards (own card_events; started = first into ongoing/done, survives backlog bounce; done = latest into done, held only while currently done; idempotent); drainPush main-card branch; row payload workStarted/Done (Manila) + workStartedTs/DoneTs (tooltips) + mcLabel; spanByMc + cycleDays removed (no consumers); tests rewritten to per-card semantics (Figma 431:17015/16; invariant 11)
- [x] T114 Frontend: due-date popover w/ commit-on-Apply + baseline guard (415:54979; W2 route untouched), Started/Done plain cells + exact-instant tooltips, links off-state (30%, truly disabled, independent — 431:17017), MC# bare label w/ displayId kept in search blob (432:17733, JP decimal ruling), fixed 3-letter month formatter; dismissers unified across menus + popover; browser-verified incl. staged→Apply→rollback chain
- [x] T115 Quality: 235/235 dual-TZ, typecheck/lint/build clean, perf 117ms/89ms @5k; 36-assertion date harness in 4 host TZs; three-lens verification (invariants / spec fidelity / regression)

---

## Phase 13c — Requests tab v2 (~1–2d) — added 2026-08-14 (JP standing go; product specs via owl #11–#12, Rex-verified on nodes 452:23559 / 452:23561)

Scope: Requests tab restyled 1:1 to the frame + client-side search/filters/pagination; one bug fix (resolved deadline, invariant 14); zero new writes. Built via end-to-end workflow (Opus build/test, Fable verification), JP-directed.

- [x] T116 Backend: requests GET serves resolved deadline + `deadline_source` (earliest trello_due of the MC's deliverables, else sheet — invariant 14; `missing-deadline` filter follows); year/month intake chain (optional sheet columns: parser `parseYear` → schema → sync upsert → API), dormant until the Sheets credential lands; 12 new tests incl. cross-project isolation (FR-3.6, FR-11)
- [x] T117 Frontend: Breakdown stat bar (4 bordered cards, single-select toggle, REQUESTS=show-all), full-width search (250ms, `<mark>`), YEAR/MONTH/TYPE/REQUESTOR selects (options from loaded data, calendar month order, blue active tint), 10/page windowed pagination, 9-column table at frame widths in h-scroll wrapper (2-instance slider), badge chips, "17 Jan 2026" deadline + source tooltip + red `none` pill, 180-char brief, frost-notes 3-state cell + inline editor restyle (handlers untouched), tokenized empty/rejects states, per-project filter reset (452:23559/452:23561; build-spec §3)
- [x] T118 Quality: 253/253 dual-TZ, typecheck/lint/build clean; seeded E2E probe through real `parseIntake → syncIntakeRows` + live-server curl asserting resolved deadline + year/month; three-lens verification (constitution / spec fidelity / correctness) — 1 should-fix (rejects-only empty state) fixed in-workflow

---

## Phase 13d — Corrected status model + Frost Remarks restyle (~0.5–1d) — added 2026-08-14 (JP go "run workflow"; product owls #13–#15, Rex-verified on nodes 452:24801 / 452:24791 / 470:21130)

- [x] T119 Backend: Trello-presence status derivation (In Pipeline / To File / For Clarification; filed wins over flag per FR-11.3), cross-cutting counts (REQUESTS = IN PIPELINE + TO FILE, FOR CLARIFICATION ⊂ TO FILE), single-box note rule (REMARK_REQUIRED, clarify_reason nulled on new writes, legacy round-trip preserved); tests updated + 4 new (256/256 dual-TZ)
- [x] T120 Frontend: clarification display block to frame pixels (red-50/red-500 badge 25px + red-bordered 288px wrapping note, no truncation), single-box editor (reason field removed, text red-500 while flagged, 10px checkbox row), REQUEST_SEGMENTS predicate filters (To File card includes flagged-unfiled), `noteText()` legacy fallback in display/search/editor; verified live incl. filed+flagged fallthrough

---

## Phase 13e — Requests sorting + filed Year/Month columns (~0.5d) — added 2026-08-14 (JP "run workflow"; owls #16–#18, Rex-verified on 470:21145 / 452:24773 / 470:21178 / 470:21206 + refreshed 452:23561 interaction annotation)

- [x] T121 Frontend: YEAR + MONTH leading columns (filed year/month; 4-digit int, MMM via `monthShort` canonicalization — name/number/Sep/Sept all map, used in cell + dropdown labels + filter matching + calendar order); header sort asc→desc→clear on all columns except Brief/Frost Notes (default = newest-filed, nulls last, page-1 reset, project-switch reset, aria-sort + keyboard); client-side sort over the full filtered set (= the annotation's "server-side over the full dataset" semantic — client holds all rows); badge tokens per product ruling (For Clarification red-500, In Pipeline green-50/500); min-width 1660; row-click + cell links CLOSED as none (owl #18)
- [x] T122 Quality: 256/256 dual-TZ held (frontend-only), 209-assertion comparator sweep + 23-case month-encoding harness, two Fable lenses zero actionable findings; verified live (sort cycle, natural MC order, cleared state, token colors)

---

## Phase 13f — Planner toolbar + capacity write + sync strip (~1d) — added 2026-08-14 (owls #20–#21, Rex-verified on 94:4828; §3.2 build ruled by product)

- [x] T123 Backend: `PATCH /api/projects/:projectId/capacity` — first write surface for `weekly_capacity` (Zod strict int 1..2000, audited `capacity.set`, project-scoped, NOT a registry entry — Sirius-internal planning data, same class as slotted_week/pins); 7 tests (263/263 dual-TZ)
- [x] T124 Frontend: schedules toolbar tokenized to frame 94:4828 (32px nav + exact-format range label — 2,922-case/3-TZ sweep; Cards/week slider bounded least..most w/ §5.4 five-band descriptor — 16,276-case sweep, degrades on null refs, serialized optimistic writes; black Suggest + slate Sprints + helper note; Accept/Discard preserved); Requests §3.2 sync strip (Trello mark + read-only rule + Manila sync status, honest degrade states); range-picker-on-click + slider snapping NOT built (product confirms pending); tab stays legacybg (only the toolbar tokenized this pass)

---

## Phase 13h — URL routing for the main tabs (~0.5d) — added 2026-08-15 (JP go; QoL/navigation, scheme approved by JP and not re-litigated)

## Phase 13g — Deliverables table + Gantt planner + calendar amendment (~2d) — backfilled 2026-08-15 (owl #22; JP calendar rulings same day)

- [x] T125 Deliverables table + Gantt planner (owl #22, node `95:5795`, 8 Rex-verified annotations; rulings R1–R11 in `gantt-frame-notes.md`): planner payload gains `phases[]` (sketch/review/render/renderOverdue from the verbatim forecast dates, day resolution) + `perWeek` (BR-6c weights, hardShare vs HARD_MIX 12.9%); view replaces the legacy week-board — pinned 999px columns + scrolling weekly timeline (`35-gantt.css`), Monday-owned month/wk labels, derived sprint blocks + outside-any-sprint + unscheduled (R5), capacity footer w/ warnings (R9), whole-row DnD via audited `/replot` (R7), violet ghost proposals + Sunday-key Accept guard (R8). 8-agent workflow, 13 findings fixed; 293/293 dual-TZ; deployed + live-verified (commits `9977f07`…`455a230`)
- [x] T126 Calendar amendment, constitution v4.2.0 (JP 2026-08-15): `lib/calendar.ts` TZ-safe — local-Monday week keys (un-breaks `/suggest` on Manila hosts), local-date `isHoliday`, injectable `setHolidays()`; **ARES working-day calendar canonical** (worker `calendarTick` derives from `/api/workload?mode=daily`, cross-checks `/api/portfolio/capacity`, persists `calendar_days`; server loads at boot + 15-min); migration `005-monday-slotted-week` (8 live rows, audited); golden tests rebuilt on a TZ-true reference, oracle parity kept where the oracle is correct; 305/305 under Manila + UTC + New York; deployed + live-verified (commits `ba94db4`…`fa0041a`)



- [x] T127 Routing core: `src/routing/paths.ts` — `isShellPath` whitelist (`/` · `/<code>` · `/<tab>` · `/<code>/<tab>`; segment `[a-z0-9-]+`, reserved first segments `api|auth|healthz|__test`, known tabs only), `safeReturnTo` (path-only, same-origin by construction — absolute/protocol-relative/backslash/CRLF/over-128 all refused), `injectBase`; `frontend/scripts/00-router.js` pure `parseRoute`/`buildPath`; drift guard pins the duplicated tab list to the server's (no module system on the frontend). 28 tests (15 whitelist/validator + 13 executing the shipped client source in a no-globals vm, which is itself the purity assertion)
- [x] T128 Server + client wiring: shell catch-all registered LAST as a plain middleware (Express 5 / path-to-regexp 8 — an inline param regex throws at registration) + serve-time `window.SIRIUS_BASE` stamp, mtime-keyed per-base cache, `express.static { index: false }` and an `/index.html` redirect closing both unstamped-shell doors; **`00-api.js` BASE fix** (was `location.pathname`-derived, wrong at any depth > 0 — every API call would have 404'd under a deep link); client initial-parse → apply-after-projects-load, one observer over `activeTab activeProjectId` → pushState, popstate restore under a depth-counter guard, `replaceState` normalization; `resetForProjectSwitch` extracted verbatim so back/forward across a project matches the switcher. Silent fallbacks throughout — unknown code, non-member, `/admin` for a non-admin (invariant 9 untouched; the data still 403s). 20 tests
- [x] T129 Deep-link return through sign-in: `returnTo` captured on `/auth/google` into the SESSION (the strategy has no state store), read **before** `req.logIn` — passport regenerates the session on login to guard against fixation, so a value read inside the callback is already gone — consumed once, whitelist-validated on both write and read; dev auto-login reads the query directly. 13 tests, refusal matrix run at both mounts (366/366 dual-TZ, from 305)

---

## Phase 13i — Capacity lock + suggest bar + legend + collapse (~1.5d) — added 2026-08-17 (owls #23–#26; nodes `262:34499` / `262:33342` Figma-verified; rulings R-a…R-e in `gantt-frame-notes.md`)

Two items were **excluded pending JP** and deliberately not built at batch 3: the FR-5.9 change (pins block Suggest only, allow manual drag) — still open — and the conflict-ack key gaining capacity (invariant 13 amendment / OD-4 slice), **ruled A by JP on 2026-08-17** and built as the T135 follow-up below. The broader OD-4 question (time / rule / other expiry dimensions) stays OPEN — only the capacity dimension was ruled.

- [x] T130 Capacity lock, Option B (owl #23; FR-5.x planning data, NOT a write-registry entry): `projects.capacity_locked` (default false, polarity the deliberate mirror of `writes_enabled` — absent = *unlocked*, so every read is `=== true`); `PATCH /capacity` answers **403 `CAPACITY_LOCKED` as the first statement of the handler**, before the Zod parse, so a locked project + malformed body is 403 not 400 (ruled; flagged) and the refusal writes **no** audit row (invariant 10 logs changes, not attempts); `capacity.locked` added to **both** emitters (`GET /deliverables` and the `PATCH` echo) so the client's wholesale re-seat cannot strip it. Admin toggle `PATCH /api/admin/projects/:projectId/capacity-lock` under `ensureAuthenticated, ensureAdmin` — **no `ensureProjectMember` by ruling**: the admin surface is global by construction and membership would lock out the admin who must unlock (flagged); audited `capacity.lock`/`capacity.unlock` with actor + before/after, no-op writes nothing. Migration `006-capacity-lock-rt837` locks that one project (its 120 is JP-held calibration vs the live 92), idempotent, audited; rt-test stays unlocked and no existing fixture is retro-locked. UI: the slider dims, refuses the cursor and states "Capacity locked — admin can unlock" on a padlock and on the input — the pin is **visible, not tribal knowledge** (Miles) — plus a second guard at the top of `writeCapacity` because a lock can flip in another tab; admin tab gains a per-project lock/unlock row on the shipped `.badge` recipes. 18 tests + a 28-check isolated-db probe
- [x] T131 Suggest-plan expanded bar (owl #25, node `262:34499`; rulings R-a…R-e): replaces the Suggest button **in its own toolbar slot** while a proposal is pending, so Sprints and the helper note keep theirs and the row never reflows. Three counts, **all client-side with zero server change** — proposed = `|plan|` (what Accept actually posts), flagged = `|plan ∩ notes|` (suggestPlan's own exception channel, intersected so the unit is *proposals*; `detectConflicts` is not reusable — it consumes forecast milestones for the *persisted* plan and would need a re-forecast the client must not run), hard-heavy = `|strain|` (the server's own hard-share verdict, so **12.9% is never retyped**). One computed `suggestBlockedWhy` drives both `disabled` and the tooltip, with the non-Monday tripwire taking precedence over the empty-plan reason (R-e); `acceptSuggest` re-guards both. R-d fixed: `resetForProjectSwitch` now clears `suggest` and `collapsedBlocks` (it cleared neither). Old green `.fnbtn.accept` deleted with its only use site (drift rule); one new token `--blue-300`. 19 tests (the four computeds executed out of the shipped source + the bar rendered through Ractive) + 2 server-contract tests pinning `plan`/`notes`/`strain` to the wire
- [x] T132 Work-phase legend to the node's facts (owl #26, node `262:33342`): 12px right-aligned strip, 4px inset, 16px between items, swatches 20×10 r4 at a 6px label gap, 12/400 slate-500, `flex-wrap` removed (a wrapped row cannot live in a 12px box); the client-deadline entry drops its `width: 3px` override so the **shared** `.gantt .gdl` 2px/slate-400 recipe reaches it — one deadline-marker recipe, never a second — and takes an 8px gap via a `.gleg-dl` modifier. The one phase→colour map (`.gseg` classes) is preserved and asserted to be declared exactly once per phase; the strip stays `aria-hidden`, non-interactive and outside `.pscroll`. 11 tests
- [x] T133 Sprint-block collapse + collapsible left pane (owl #24; closes half of R6 and product flag #1): a real `<button>` chevron per block header with `aria-expanded`/`aria-controls`, rows wrapped in `.gbrows` under an `{{#if}}` — **presentation only**, so `plannerGroups`, `schedRows` and `weekTotal`/`footText`/`footCls` are untouched and a hidden row still counts against capacity (the footer is data, not visibility); the Unscheduled bar keeps its unslot drop handlers while collapsed. Left pane collapses through the **one** `--gleft` variable, 999 → 417 (`c-mc` 155 + `c-scope` 262, asserted against the shipped column widths so a resize fails the test instead of mis-collapsing), hiding requestor/type/status/badges and taking ~5 → ~11 visible week columns at 1600px; `.gsheet { width: max-content }` recomputes the sheet, so **no JS width maths exists**. Both toggles refresh the timeline thumb. Known consequence: the pin/duplicate/note cluster lives in `c-status` and is unavailable while collapsed. 18 tests, rendered through Ractive's own `toHTML()` rather than grepped, plus a parse-tree scan for the `{{!`-comment leak (with a negative control) that `frontend/build.js` cannot catch
- [ ] T134 Live-browser pass in both pane states — drag-and-drop, `ArrowLeft`/`ArrowRight` reslot, the real horizontal thumb ratio, and the locked slider against the running app. Builder B measured geometry and computed styles in Chrome against a standalone harness built from the shipped CSS; the app-level pass is still owed. **Widened by phase 13j**: the drag reversal (T139) replaces the primary interaction, so the pass now also owes a real bar drag, a drop past the covered weeks, a drop on the Unscheduled header, a multi-select bar drag, the pinned refusal, and the arrival pulse + `scrollIntoView`. **Open**
- [x] T135 Capacity joins the conflict-acknowledgement key (owl #23 item 2 / Miles → OD-4 capacity slice → **JP ruling A, 2026-08-17**; constitution v4.3.0 invariant 13): the ack situation key becomes `week | rule | capacity | sorted card:phase pairs`, so a `weekly_capacity` change lapses the acknowledgement and the week re-surfaces. **Uniform across all three acknowledgeable rules** (`urgent-overlap`, `over-capacity`, `past-deadline`) — the amendment is written week-level, a rule-scoped key would need an `if` inside the composer (drift), and a capacity change is a re-plan signal for the whole week; the noise counter-argument is bounded by T130's Option-B lock, which makes a capacity edit a deliberate, audited act. Correction on the way in: **hard-mix is not a conflict rule** — it is the planner week flag `hardOver`/`hardWarn` (`src/services/pipeline.ts:201-202`) rendered as a Gantt footer tint, with no key, no ack button and no `conflict_acknowledgements` row, so it cannot carry a capacity component; a guard test pins it as flag-only. **One recipe, one place**: the three inline key templates in `src/services/conflicts.ts` collapse into an exported `conflictKey()` over a single private `compose()` — the pipe layout is now written exactly once in the repo, asserted by a drift guard. No schema field was added (the key *is* the carrier, per the amendment); no route composes or parses a key; the client holds no recipe at all and matching stays 100% server-side as one set-membership test. Invalidation is a **non-match, not a state change** — the superseded row stays put and writes **no** audit row (invariant 10), so reverting the capacity re-suppresses through the original ack: ruling A added a *situation* dimension, not an expiry, and nothing time-derived went near the key (OD-4 stays open). Migration `007-ack-capacity` lifts existing 3-component keys to their own project's CURRENT capacity — suppression state is preserved exactly, nothing silenced today un-silences on deploy — idempotent by key shape, audited per changed row (`ack.backfill-capacity`, actor `migration:007-ack-capacity`, before/after), cross-project isolated and orphan-safe. `test/acks.test.ts` goes 3 → 21 tests (10 new feature + 3 drift guards + 5 migration, on top of the three phase-8a lifecycle tests — one of which now also asserts a capacity change leaves the card-level late flags alone), plus a **strengthened** key-shape assertion in `test/schedule.test.ts` (4 components pinned, not 3) and a corrected sample key in `test/models.test.ts`; suite 445 → 463, dual-TZ. A negative control (capacity removed from the composer) fails 13 of them, so they bite. Also a 29-check isolated-db probe, `scripts/ackcap-probe.ts`: ack → suppressed → capacity change → re-surfaced → re-ack → suppressed → revert → suppressed through the original ack, with the audit trail and the locked-project and migration legs asserted. **Flagged to JP, not folded in:** (R1) `writeCapacity` never refetches `/deadlines` and `selectTab` never calls `loadAll`, so the re-surfaced banner is invisible in the running app until a reload — a pre-existing staleness defect this ruling makes user-visible; (R2) the ack tooltip at `frontend/templates/00-app.html:618` says "any change to the cards involved brings it back" and is now factually incomplete; (R3) the 2000-char `conflict_key` cap plus an un-caught `ackConflict` is a latent silent 400 on large rt-837 conflicts

## Phase 13j — Sprints modal + drag reversal + row action icons (~1.5d) — added 2026-08-17 (owls #27–#31; nodes `528:113433` / `322:30031` / `328:38162` / `328:38454` / `I262:33396;251:27040` / `262:33397` Figma-verified; rulings R-f-1…R-f-8, R-drag-a/b in `gantt-frame-notes.md`)

**Precedence override carried through the whole batch:** owls #27/#31 and two live Figma annotations still say *"a pin blocks Suggest, not deliberate action"*. **JP ruled B on 2026-08-17 — pins stay FULLY frozen (FR-5.9 unchanged)**, so pinned rows are not draggable by bar or by row and Calendar Remove is disabled on them. The supersession is flagged to Miles (jp→miles #18).

- [x] T136 Row action icons (owl #27, node `I262:33396;251:27040`): the emoji cluster becomes the design's own 55×13 group — three 13px sprites 8px apart in the frame's order **Copy · Pin · Calendar Remove**, added to `ICONS` from the library components' paths at `stroke-width 1.08333`, round caps/joins, `fill:none`, normalised to `currentColor` and tinted from `--slate-400` in CSS (no sprite-builder change). Every one is a real `<button>` with a `title` **and** an `aria-label`, revealed by the existing `opacity` rule so all three stay in the tab order at rest and stay visible while pinned. Calendar Remove routes to a new `unslotRow` handler that calls the **existing** `moveRows(id, null)` → `POST /replot` path — no new endpoint, no new audit action — and is **disabled in two states, for the same reason: it would change nothing.** Pinned (JP ruling B, `/replot` skips it server-side) and already-unslotted (a null → null move IS applied and audited by the route, and a non-change must not reach the immutable log — invariant 10); the handler re-guards both. **#27's divergence answered**: the build it replaces had pin / duplicate / **status-note**, and `editNote` is the app's only status-note edit surface anywhere, so it was preserved **outside** `badge-icons-container` rather than dropped — the `.gchips` "manual" chip becomes the edit button (same pixels, no new chrome) and a ghost pencil appears there when there is no note, leaving the cluster at exactly the three specified icons. **Placement flagged to Miles.**
- [x] T137 Sprints modal, four states (owls #28–#30; nodes `528:113433` empty / `322:30031` filled / `328:38162` alert / `328:38454` error): one 860-wide shell (r8, 24px padding, 16px stack, header ✕, verbatim explainer) over the retuned `.modal-back`/`.modal` recipe — **one use site, so it is retuned in place, never shadowed**. Empty state, 812-wide table (48 / 220 / 200 / 200 / flex columns declared **once** and shared by header and body), 78px rows, remove ✕ in column 1, derived read-only `LENGTH` reusing the existing counted-Mondays helper so the modal and the sprint block headers can never disagree. Batch semantics unchanged: one PUT of the whole draft on Save (the route is a full replace, audited as one `sprints.replace`), Cancel discards because `openSprints` re-copies from `sprints`. Three live computeds — `sprintDupNames`, `sprintOverlaps`, `sprintGaps` — drive **one `.sbanner` recipe with three modifiers**, duplicates leading and gaps/overlaps rendered between the two rows they name via an `after` draft index. **Gaps count WORKING days** against `getHolidays()` from the payload (R-f-8): a pure weekend, or a weekend plus a holiday, fires nothing. Neither banner carries a CTA (R-f-5) and the Alert Banner component's unused 1450px `List Item` / `items-container` slots never reach the markup (asserted). Deletion warning counts covered rows off `schedRows` with the same `slottedWeek ∈ [start,end]` test membership is derived from, and confirms before splicing the draft. START snaps to Monday and END to Friday on `change` (not `input`). **R7 resolved rather than shipped as a dead end**: Save is dead only for errors or a modal *opened* with no sprints — a table the user has **emptied** keeps Save live, so the last sprint can actually be removed (the route accepts an empty list and audits the replace like any other). Two new tokens `--space-12`, `--radius-input`
- [x] T138 Sprint-save server truth (owl #28; S1/S4): `PUT …/sprints` gains a route-owned `duplicateNameIssues()` beside the existing `sprintIssues()` call, **after the Zod parse and strictly before `Sprint.deleteMany`** — a rejected save leaves the collection byte-identical. Names compare trimmed + `toLocaleLowerCase()`, one issue per duplicated **name** (a triple reports once), travelling in the existing `422 { code:'SPRINT_CONFLICT', issues:[{id,kind,text}] }` envelope with copy verbatim identical to the modal's banner, so the client's `issues[0].text` fallback reads the same words as the live check. Uniqueness is **per project** by construction. It lives in `src/routes/schedule.ts`, **not** `lib/**` (invariant 5). Overlap needed no code — `lib/planner.ts sprintIssues()` already emits `overlap`/`inverted` and the route already 422s with nothing written (**invariant 12 answers #28.4**); the existing test was strengthened, not replaced. `GET /deliverables` now carries `holidays: getHolidays()` — the ARES-canonical active set, read-only, no new collection — which is what stops the client's working-day gap rule from growing a second calendar
- [x] T139 Drag reversal (owl #31; supersedes the row-drag for scheduled rows): the **bar** is the drag source. `draggable` + `on-dragstart` are emitted **only** on unscheduled rows (inside a conditional section, so a scheduled row emits neither — not even `draggable="false"`), and a new `.gbar` wrapper inside `.gtrack` carries them for scheduled ones. Per-week snap, `dropOnWeek`, `dropBlock` and `moveRows` are untouched, so BR-8 multi-select semantics and the `/replot` audit path are byte-identical and the row's relocation into another block is a **re-render of the derivation, never a second write**. `.gbar` spans the whole track with `pointer-events: none` and `auto` on its `.gseg` children (the `.gunsched` pattern), so the segments are the only hit area and the week cells stay droppable — a **deliberate deviation** from the contract's min→max box, because `.gseg` offsets are percentages *of the track* and a narrower wrapper would re-base every one of them and move the shipped bars. **R-drag-a**: unscheduled rows have no bar, so they keep row-drag. **R-drag-b**: the gutter grip renders only there — on a scheduled row it would drag nothing, the same trap R-f-6 deletes from the sprints table's column 1; the BR-8 checkbox stays on both kinds and the keyboard reslot is untouched. Pinned: `draggable="false"` + `cursor: not-allowed` + title. New `arrived` state drives a 1.2s pulse (with a `prefers-reduced-motion` fallback) plus `scrollIntoView({block:'nearest'})` on the moved rows after `loadAll()`, re-queried by a new `data-card` attribute inside a `requestAnimationFrame` because the reload changes node identity; pinned members are excluded because the server skipped them. The toolbar hint follows the interaction
- [x] T140 Batch-4 verification: `test/helpers/gantt-render.ts` gains `renderSprintModal()` (plus `UI_CSS` / `ICONS_JS` and an `arrived` stub), and two new files render the shipped template through Ractive's own `toHTML()` rather than grepping it — `test/sprints-modal.test.ts` (48) executes the three validators straight out of `frontend/scripts/01-app.js` (the R-f-8 working-day rule has no golden test behind it, `lib/**` being frozen) and renders all four states plus the deletion confirm, asserting the no-CTA ruling, the 1450px slot trap in markup **and** in the CSS declarations, a LENGTH cell that is never an input, and that the modal banner and the route's 422 say the same words; `test/gantt-rowactions.test.ts` (41) covers the cluster's order/labels/refusals, the sprite geometry, both drag kinds, the pinned double refusal and the arrival pulse, with the two Ractive **directives** (`on-dragstart`, `on-drop`) asserted against source and said so out loud because they never reach `toHTML()`. `scripts/batch4-probe.ts` — **54 checks** against an isolated in-memory mongod, no Trello or Sheets call by construction: save round-trip, duplicate reject, overlap + inverted reject, cross-project uniqueness, the deletion-warn count computed with the client's own expression against seeded rows, gap detection against an injected holiday calendar, the drag write path (slot + unslot + pinned skip with no audit row), the null → null audit that justifies the Calendar-Remove guard, and the empty-list save behind R7. Suite 463 → **557**, green on default, `TZ=UTC` and `TZ=Asia/Manila`; the probe passes under all three

## Phase 13k — Requests status fix + Pipeline row warning (~1d) — added 2026-08-17 (owls #34–#36; nodes `452:24800` / `452:24801` / `537:69131` / `537:69135` Figma-verified; rulings R-req-a in `requests-frame-notes.md`, R-warn-a…g in `pipeline-frame-notes.md`)

- [x] T141 Requests STATUS becomes two-valued, server side (owls #34/#35; FR-11.3, FR-11.4, AC-21): `STATUS` in `src/routes/requests.ts` drops the third entry and becomes `{ filed: 'In Pipeline', forFiling: 'For Filing' }` — the only place either literal is spelled in the route. The derivation collapses to `filedMcs.has(mc) ? filed : forFiling`, so the clarification flag no longer reaches it at all; `SegmentRow` gains the note and `SEGMENTS.clarification` re-keys to `r.status !== STATUS.filed && Boolean(r.note?.clarify)`, which is **set-identical** to the retired `status === STATUS.clarify` — owl #14 therefore survives *by construction* rather than by a precedence branch, and a filed+flagged row matches neither `clarification` nor `unfiled`. The counts code is byte-unchanged and every count is unchanged for every input; the `counts.toFile` KEY keeps its name because the tile is still labelled TO FILE (owl #35's deliberate card/badge asymmetry). Payload shape, `?filter=` vocabulary and the `PUT /note` route are all untouched. No Trello write, no Sheets path, no new audit row, no schema or migration, `project_id` filters intact
- [x] T142 Requests client + template to the two-value model (owls #34/#35): `STATUS_TO_FILE` and `STATUS_CLARIFY` are **deleted outright** rather than renamed — the client never needs to spell `'For Filing'` (the server sends `r.status`, the cell prints it) and an unused const fails `no-unused-vars`; `STATUS_FILED` survives as the one literal the badge branches on. ONE shared predicate `clarified(r)` at column 0 feeds **both** `REQUEST_SEGMENTS.clarification` and the template's Remarks branch, so the segment filter and the cell ask the same question. Badge map goes two-way (`pipe` / `file`); the optimistic note-save patch that rewrote `requests.N.status`, its `prev.status` capture and its rollback keypath are all removed, so a note save can only change the note — the badge and the cell re-derive from what the patch just wrote. CSS: the grouped `.rtable .sbadge.clar, .notewrap .clarpill` rule narrows to the pill alone; `.clarnote` needed no change (already red-600, R-req nit 24). `reqStats` labels untouched
- [x] T143 Pipeline per-row warning + popover, replacing the table banner (owl #36; FR-4.4): `.ipanel` deleted from template and stylesheet along with `visibleCorrections` / `showAllCorrections` / `toggleCorrections`; `corrections` stays on the wire and OPEN WORK (`kpi.open`) keeps counting it as the aggregate. ONE derived recipe `rowWarning(row)` over the `missing` array the server already ships (`src/services/pipeline.ts` — **no wire change**) returns `null` or `{ label, items[] }`, where `items[0]` is the row's OWN identity (the frame's `MC-821` is stale filler) and the rest are one per missing field with the rationale from a single `WARN_WHY` map keyed on the server's tokens. The label is a variable string in all three render sites (R-warn-c). The message is a real `<button>` under the card name (R-warn-d); the row's `.warn` class is presentation only and disables nothing. The popover **reuses `openOverlay`/`placeBox`** — no second positioner — and **joins** the `.selectmenu, .duepop, .warnpop` fixed-box recipe with the annotation's slate-100 stroke as its only fork (R-warn-e). Because its height is data-derived it is placed a **second time against the measured box** (R-warn-h), through the Requests select's own re-place path generalised to `placeMeasured()`. **Focus return added once in the shared close path** (R-warn-f): a module-scope `overlayTrigger` captured in `openOverlay`, replayed by `closeMenus()` on Escape and on any dismissal that unmounts the element holding focus — it did not exist on any of the four pre-existing overlays. The separator draws only when `Open Card` does, so a card with no `trelloUrl` gets no dangling rule; the dialog's accessible name is the same composed label + MC# + name its trigger carries. Row accent is the frame's stroke read as a 3px LEFT inset over amber-50 (R-warn-a); the 280px column and 113px row height are deliberately NOT hardcoded, and a blocked+warned row keeps its red fill (R-warn-g). `.i10` added beside `.i16`/`.i18`; `#i-warning` already existed in the sprite
- [x] T144 Batch-5 verification: `test/helpers/gantt-render.ts` gains `PIPELINE_CSS` / `REQUESTS_CSS`, a shared `decl()` extractor, and `renderPipelineTable()` / `renderRequestsTable()` — both rendering the SHIPPED template through Ractive's own `toHTML()`, with every iterated array stubbed (a missing stub renders nothing and passes on an empty string) and the recipe under test **never** stubbed. `test/pipeline-warning.test.ts` (25) executes `WARN_LABEL` / `WARN_WHY` / `rowWarning` straight out of `frontend/scripts/01-app.js` and feeds that same function to the renderer: null for a complete card, card identity first, server-ordered fields, every rationale traced to the one constant, an unknown token degrading to a blank rather than throwing; then the markup — `.warn` on the warned row and absent on the clean one, one `warnmsg` button with `aria-haspopup`/`aria-expanded`/an accessible name, the visible label sourced from the recipe with **no literal `Needs Info` in the template**, equal `disabled` counts on warned and clean rows, and with the popover open: `role="dialog"`, title, one `wpitem` per item, one separator ahead of the link (and **neither** when the row has no `trelloUrl`), `Open Card` carrying the row's own `trelloUrl` with `target="_blank" rel="noopener"`, and exactly one popover across two warned rows. Plus the CSS one-recipe assertions and the deletion guards (`ipanel` / `iwhy` / `toggleCorrections` gone from template, client and stylesheet; `kpi.open` and `corrections: pipeline.corrections` still present; `leakedMustacheText()` empty). `test/requests-render.test.ts` (15) renders all four filed×flagged rows through the shipped `clarified()` and `STATUS_FILED` — and **pins `STATUS_FILED` to the route's own `'In Pipeline'`**, the one literal each side must spell independently and the only assertion in the file that does not build itself from the constant: the badge class sequence is `pipe|file|file|pipe` and never `clar`, the flagged UNFILED row shows `For Filing` **and** the `With Clarification` pill with exactly ONE red badge on the row, the flagged FILED row keeps the plain remark (R-req-a), and the note-save handler names no status keypath. `test/requests-status.test.ts` (6) guards the server vocabulary over the whole payload. Both new render suites verified to bite by negative control (`warn` → `NEGCTL` on the row class fails 1; `clarified(r)` → `r.note.clarify` fails 2, including the R-req-a case). `scripts/batch5-probe.ts` — **49 checks** against an isolated in-memory mongod, no Trello or Sheets call by construction: the four combinations end to end, the note key on every row, counts and both cross-cutting invariants, a plain remark moving nothing, all three `?filter=` segments with the filed+flagged row excluded from clarification, the SHIPPED client predicate reproducing the server segment row-for-row, project isolation, a read that writes no audit row, and the warning map checked against the `missing` tokens a deliberately incomplete seeded card actually emits. Suite 564 → **610** (55 files), green on default and `TZ=UTC`
- [ ] T145 Live-browser pass for batch 5 — the warning popover's flip/clamp against a real viewport (`WARN_POP_H = 346` is only the pre-measure for the first flip decision; `placeMeasured` re-places against the rendered `offsetHeight`, so confirm a three-problem card low in the table lands fully on screen with `Open Card` clickable), Escape **and a scroll while focus is inside the popover** both returning focus to the message button, Tab reaching `Open Card`, the amber row against a 🛑 blocked row, and the Requests badge pair at 1600px. **Open**

## Phase 13k (cont.) — Sprints modal: Save gating + blank names (~0.5d) — added 2026-08-17 (owl #37 items 1–2; nodes `528:113433` / `322:30031` / `328:38162` / `328:38454`; rulings **R-f-10 / R-f-11** in `gantt-frame-notes.md`, superseding the R7 note). Item 3 (arrival pulse) Miles ruled **keep as built** — no work.

- [x] T146 Save gates on unsaved changes, client (owl #37 item 1, R-f-10; FR-5.14, FR-5.15, invariant 10): `sprintOpenedEmpty` is **deleted outright** — state key, the set in `openSprints`, and the template reference — rather than left as an unused key, and a test asserts the identifier appears nowhere in either shipped file. `openSprints` captures a new `sprintBaseline` beside the draft: a fresh copy of the three PERSISTED fields a save PUTs (`{name, start, end}`), mapped off `stored` so it can never be a reference an edit reaches. A `sprintDirty` computed compares draft to baseline on those three fields **in draft order with no trimming** (a name changed to `"Sprint 1 "` is an edit the user made; whether the route trims on store is a separate question, untouched), a length change counting as dirty. Save's `disabled` expression becomes `sprintDupNames.length || sprintOverlaps.length || sprintBlankNames.length || !sprintDirty`, so it is live iff **something to persist and nothing blocking**. All three cases fall out of the one rule: opened-empty dead, emptied-by-the-user live (so the last sprint can still be removed), and — the case the reframe buys, unreachable under the old flag — **edited and put back, dead again**. `saveSprints` gains a matching `if (!app.get('sprintDirty')) return;` beside the blocking lock: a no-op PUT would write a `sprints.replace` audit row for a non-change, the exact defect batch 4 fixed for Calendar Remove (invariant 10 logs changes, not attempts). Cancel's `dim` (`!sprintDraft.length`) left alone — it tracks the rendered empty branch, not Save's state, and dim-Cancel beside live-Save was already the shipped pairing; verified by render
- [x] T147 Blank sprint names reject, both sides (owl #37 item 2, R-f-11; FR-5.14, BR-5, invariant 12): blank is `String(name || '').trim() === ''` on both sides. Client gains `sprintBlankNames` beside `sprintDupNames`, following the **existing** `.sbanner` recipe — `variant: 'err'`, carrying the draft index of its row so placement stays data (R-f-4), no CTA (R-f-5) — with title **"Sprint name required"**, ONE banner per blank **ROW** (unlike duplicates, one per NAME: no shared name to collapse onto). Server gains `blankNameIssues()` beside `duplicateNameIssues()` in `src/routes/schedule.ts` — route layer, **never `lib/**`** (invariant 5) — in the same `SprintSaveIssue` shape and the same `422 SPRINT_CONFLICT` envelope, appended LAST so `issues[0]` is byte-unchanged for every pre-existing case, and returning before `deleteMany` so a rejection **writes nothing and audits nothing**. Copy is byte-identical on both sides — `A sprint starting 17 Aug 2026 has no name. Name every sprint to save.` — the route's `longDate` reproducing the client's `fmtLongIso` as pure string math, no `Date` (invariant 11) and no locale (en-GB emits "Sept"); a client-only fallback `This sprint has no name. …` covers a cleared start (`snapSprintStart` writes `''`), unreachable on the server where `start` is `DATE_ONLY`-required. **The two sides disagreed and the server was wrong**: `duplicateNameIssues` did not skip blanks, so two unnamed rows collided on the key `''` and reported as `Multiple sprints are named ""`; it now carries the client's guard, and a blank reports once, as a blank, never also as a duplicate. **One Zod constraint relaxed**: `name` loses `.min(1)`, keeps `.max(80)` — an empty name is a user mistake with a friendly fix, not a malformed body, and Zod's `400 INVALID_BODY` carries **no `issues[]` at all**, so the modal's `issues[0].text` fallback would have printed a raw developer string. No Trello write, no Sheets path, no schema or migration, `project_id` filters intact
- [x] T148 Batch-5b verification: `test/helpers/gantt-render.ts` `SprintModalState` swaps `sprintOpenedEmpty?` for `sprintDirty?` (defaulting `false` — the same dead-Save polarity the old `?? true` had, so the empty-state suite passes untouched) and `sprintBlankNames?` defaulting **`[]`**, because the template now `.concat`s it and an undefined stub would render no banner rather than throw — the silent pass the helper doc warns about. `test/sprints-modal.test.ts` grows both new computeds sliced **out of the shipped `frontend/scripts/01-app.js`** and executed, never re-implemented: `sprintDirty` false on a match, true on each of the three fields, **false again on revert**, true on a trailing space (no trimming), false on `[]` vs `[]`, true on emptied / added / reordered; `sprintBlankNames` on `''`, `'   '` and `'\t'` alike, one banner per row with the right `after` index, the dateless fallback with no double space, and two blanks raising **zero** duplicates. Then the same shipped validators drive the shipped markup through `renderSprintModal`: opened-empty Save dead, emptied-by-the-user live, **edit-and-revert dead**, a blank rendering `class="sbanner err"` + `role="alert"` immediately after its row with **no `<button>`** and no `Duplicate sprint names found`, two blanks rendering two banners and still zero CTAs, a real duplicate still rendering (the guard took nothing else), and a gap still advisory with Save live. Copy parity extended to the new class — the shared sentence counted once per interpolated form on each side, so no third copy can drift in — plus the deletion gate on `sprintOpenedEmpty` and `leakedMustacheText()` still empty. `test/schedule.test.ts` adds 7 cases: whitespace-only → 422 `blank-name` with zero writes and zero audits, `''` → **422 and explicitly NOT `INVALID_BODY`**, two blanks → two blank issues and zero duplicate ones, the same list named → 200, `'x'.repeat(81)` still 400 (`.max(80)` survived), an already-stored list left intact, and `1 Sep 2026` never rendering as "Sept" in any timezone; the four existing duplicate-path tests pass byte-unchanged. `scripts/batch4-probe.ts` retargets check 7c to the dirty rule and adds leg 7d against the isolated in-memory mongod. Suite 610 → **645** (55 files), green on default and `TZ=UTC`; probe **65 checks**, all passing (the suite total is real but not deterministic — a pre-existing transport flake in four older files, none of them batch-5b, is tracked as **T149**)
- [ ] T149 Suite non-determinism — pre-existing, NOT batch 5b, found while verifying this batch's gate. A 12-run loop (6 default TZ + 6 `TZ=UTC`) on the batch-5b tree went red **4 times**, each on a single test in a different long-standing file: `test/acks.test.ts` ×2, `test/admin.test.ts`, `test/basepath.test.ts`. No batch-5b test ever failed, and the same flake was already visible at **443** tests on 2026-08-17 15:52 (`acks` + `writes-disabled`, 2 red in 6), three batches earlier. **A fifth file joined the list during the batch-6 review (2026-08-17): `test/schedule.test.ts`, red once in setup — `POST /__test/login` answered `404 Not Found` (`test/schedule.test.ts:40`), i.e. the request reached a *different* file's Express app, the same cross-file transport signature — then green alone and green on three subsequent full runs including `TZ=UTC`. No server file changed in batch 6, so it is this flake and not a regression; recorded here so a future red on that file is not misread as one.** The errors are transport-layer, never assertion-layer: `socket hang up` / `ECONNRESET`; `Parse Error: Expected HTTP/, RTSP/ or ICE/` (`HPE_INVALID_CONSTANT` — the HTTP client read bytes that are not HTTP off the socket); and `expect(200)` answered `404 PROJECT_NOT_FOUND` (a request arriving at a *different* file's Express app, which has no such project). One mechanism explains all three: 55 files each start their own `mongod` and their own ephemeral supertest listeners, and under CPU contention connections cross. Both timezones fail at the same rate, so `lib/calendar.ts` is not implicated. Fix deliberately in its own change — the candidates are binding each test app to an explicitly reserved port instead of supertest's per-request ephemeral bind, or capping `maxWorkers` / sharing one `mongod` across files via a global setup — and **not** by adding `retry`, which would hide real regressions. Until then a single red run on those files is not evidence about the batch in flight. **Open**

## Phase 13k (cont.) — Requestor cell truncation (~0.25d) — added 2026-08-17 (owl #39 as corrected by #40; node `262:33394` Figma-verified). Frontend only: no server, no wire, no schema, no migration

- [x] T150 Requestor cell truncates with an ellipsis and shows the whole value on hover AND on keyboard focus (owl #39 as corrected by #40; **FR-5.1**, **NFR-9**, invariant 2 — frontend only, nothing near the write registry): `.gantt .gdetails .c-req` is a fixed 136px box whose `.pbadge` hugs its text, and the clip lived on the CELL (`.gcell { overflow: hidden }`), so a long requestor was cut **mid-character** AND ~8px of it painted **outside the badge's own 1px stroke** — two defects with one cause, `text-overflow` acting only on the box that owns the overflow. The clip **moves inward**: a reusable `.clipbadge` / `.cliptext` recipe beside `.pbadge` in `frontend/styles/20-pipeline.css` (it names no column and no pixel), and the single application `.gantt .gdetails .c-req { width: 136px; overflow: visible; }` — same specificity as the `.gcell` rule (0,3,0), declared later, so it wins on **source order with no new selector**. The badge's cap is `.pbadge`'s **existing** `max-width: 100%`, resolving against the cell's 128px content box, so **no pixel number is repeated anywhere in the fix** and nothing can drift from `.c-req`'s 136px. `min-width: 0` on `.cliptext` is load-bearing and silent when missing: the text is a flex item of the inline-flex badge and at `min-width: auto` refuses to shrink, so the ellipsis would never render. Tooltip is **ONE rule, two selectors** — `:hover::after` + `:focus-visible::after` — with `content: attr(aria-label)`, making the visible tooltip and the accessible name the same string by construction; `position: absolute` keeps it out of the badge's flex flow so it **cannot grow the row** (row heights are aligned to the timeline bars — ruling 4, no wrap), and `top: 100%` leaves no dead zone so it is hoverable and persistent. **Column NOT widened** (ruling 5): #40's correction is that production requestors are "Andy" / "Chev" and the clipped value in the frames is a fixture `@handle`. Truncation is a **MEASUREMENT, never a character count** (ruling 3): a new `refreshClips()` beside `refreshThumbs()` in `frontend/scripts/01-app.js`, mirroring its shape but comparing **strictly** — `scrollWidth > clientWidth`, *not* `updateThumb()`'s `+ 1` epsilon, which shipped in the first cut and a review caught in a real browser: `text-overflow: ellipsis` fires on **any** overflow, so `scrollWidth === clientWidth + 1` is a visibly truncated badge with no tooltip and no tab stop — this bug surviving in a ~1px band; the costs are asymmetric (a false positive is a harmless tooltip on a value that just fits, a false negative hides the identifying part of a name), and both metrics are integer-rounded anyway. The badge carries **`role="note"`** because a bare `<span>` is `role=generic`, on which ARIA 1.2 **prohibits** an author-supplied name — Chrome honours the `aria-label` regardless and the full value is also the text content, so nothing was lost, but the badge joins the tab order when clipped and `note` is the weakest role that permits naming. Writes `data-clipped` + `tabindex="0"` and **revoking both** every sweep — `{{#each g.rows}}` is unkeyed, so Ractive reuses badge nodes by index and a long→short row would keep a stale tab stop forever if the sweep only added; neither attribute is in the template, so Ractive never fights it. Both the tooltip and the tab stop hang off `data-clipped`, so **"no tab stop on a badge that fits" and "no tooltip when the value fits" are one mechanism**. Re-runs on the four **pre-existing** rAF seams — `loadAll()` (which is also the project switch: `resetForProjectSwitch()` and `popstate` both end there), `selectTab`, `toggleBlock`, `toggleLeftPane` — plus one new `document.fonts.ready.then(refreshClips)`, because `index.html` loads Google Sans Flex with `display=swap` and the first paint measures fallback metrics. **No observer added**; the codebase still has no `ResizeObserver`/`MutationObserver`/`IntersectionObserver`. Left-pane collapse needs no special case: `.c-req` is `display: none` there, both widths read 0, and a hidden cell correctly loses its tab stop. Badge carries `on-keydown="['noop']"` — the **existing** handler — because `.growr`'s `rowKey` reslots the deliverable a week on ArrowLeft/ArrowRight and keydown bubbles; this is the app's first *informational* focus target, where that side effect would be indefensible. **No `title`** (fails the keyboard half) and **no `tabindex`** in the template. Tokens only, no new colour, no new dependency, `lib/**` untouched
- [x] T151 Batch-6 verification: `test/gantt-requestor-clip.test.ts` (**37**), rendering the SHIPPED template through the existing `test/helpers/gantt-render.ts` harness with a suite-local long fixture (`@charlotte.hemsworth-fixture`) — the harness's own `ROWS[0].requestor` stays `'Ana'` so no other suite changes. Render half: the badge wears `.pbadge .clipbadge` with the value in a `.cliptext` child, the `aria-label` **equals** the visible text (asserted as an equality, not two `toContain`s — that is what proves tooltip and accessible name cannot drift), **no `tabindex` and no `title=` for any value length**, an absent requestor still renders `<span class="gdim">—</span>`, `clipbadge`/`cliptext` occur exactly twice across three rows and in **no other column**, the header cell is byte-unchanged, and `leakedMustacheText()` is empty (the edit sits in element-content position, so its explanation is an HTML comment — `{{! }}` would leak). CSS half: the ellipsis recipe declares `min-width: 0` (named in the test, because without it the ellipsis silently never renders), `text-overflow: ellipsis` **once** in the recipe and exactly twice in the file (the other being the pre-existing `.datefield .dpdate`) and **never** in the gantt sheet, `max-width: 100%` as the cap with no `width: \d+px` in either clip rule and no `136` anywhere in `20-pipeline.css`, the tooltip selector group extracted and asserted to be **exactly** `[':hover::after', ':focus-visible::after']` on `[data-clipped]`, `content: attr(aria-label)`, no bare `.clipbadge:hover::after` (a value that fits shows nothing), the house focus ring byte-identical to `.gantt .gact:focus-visible`, and **no hex in any new rule**. Application half: `.c-req` carries both `width: 136px` and `overflow: visible`, `.gcell` still `overflow: hidden`, the **source-order** argument asserted by index (a tidy-up that lifts the rule above `.gcell` restores the bug), the other four columns carry no `overflow` at all, and the `.gantt.lpc` hide list + `--gleft: 417px` re-asserted unchanged. Measurement half — **source assertions, said so in the test names**, because there is no vitest config and no jsdom so vitest runs in `node` and nothing has a layout: the sweep contains `scrollWidth`/`clientWidth`, compares **strictly** (asserted both ways — the comparison matches and `clientWidth +` does not appear, so a reinstated epsilon fails), and matches **no `.length >`**, grants **and revokes** both attributes, the template declares neither, all four rAF seams contain `refreshClips` (one assertion each, naming what each covers), `document.fonts.ready.then(refreshClips)` exists, no observer API appears, and the four seams share one `requestAnimationFrame(() => { refreshThumbs(); refreshClips(); })` shape. **One test-plan assertion relaxed with reason**: `expect(APP_JS).not.toContain('136')` fails on *pre-existing prose* — `01-app.js:17` has documented the column arithmetic since phase 13i and points AT the stylesheet rather than duplicating a constant — so the ban is enforced on **code** by a `widthInCode()` scanner that skips block and line comments, with its own negative control; `not.toMatch(/\b(999|417)\b/)` is re-asserted untouched. Suite **645 → 682** (56 files), green on default and `TZ=UTC`. Proved to bite by **ten** negative controls, each reverted: drop `min-width: 0`, restore the cell's clip, drop the `noop` guard, add a `tabindex` to every badge (2 red), swap the measurement for a character count, make the sweep add-only, remove the webfont re-measure, drift `aria-label` from the visible text, **reinstate the `+ 1` epsilon**, and **drop `role="note"`**. Flake honesty (T149): one `TZ=UTC` run of six went red with **3** supertest transport failures in server files this batch does not touch; every other run — three default, three `TZ=UTC` — was 682/682, and the new suite is 37/37 on both timezones in isolation
- [ ] T152 Live-browser pass for batch 6 — nothing below has a DOM in this runner and all of it is load-bearing: (a) a short name renders in full with no ellipsis, no focus ring and **no tab stop**; (b) the fixture `@handle` ellipsises, is reachable by Tab, and shows the full value on `:focus-visible` as well as on hover; (c) the tooltip is not clipped by the cell, does not cover the row-action cluster, and clears the sheet's bottom edge on the LAST row (it occupies y≈41–63 inside an 84px row and `.gfoot` sits below the last block in the same scroller — checked on paper only); (d) collapsing the left pane removes the tab stop and expanding restores it; (e) the verdict is correct **after** the webfont swaps, not only before; (f) an arrow key on a focused badge does **not** move the row; (g) the **boundary case** — a value whose text lands ~1px past the badge's inner box (the review measured `efnnbggtgubwkidx` into that band at the shipped geometry) shows the ellipsis **and** gets the tooltip and the tab stop, the case the withdrawn `+ 1` epsilon missed; (h) **Miles/JP rule on the two recorded gaps** — WCAG 1.4.13's Escape dismissal (a pure-CSS tooltip cannot answer it; NFR-9 names WCAG 2.1 AA, so this is a requirement, not polish) and touch having no reveal path at all. Folds naturally into the same session as T145. **Open**

## Phase 13k (cont.) — the Gantt bar cannot be dragged with a real mouse (~0.25d) — added 2026-08-18 (JP, against the live site; root-caused with real mouse input before any code was written). Frontend only: no server, no wire, no schema, no migration

- [x] T153 **The bar owns its own drop** — a drag source must stay hit-testable, in every state (**FR-5.4**, invariant 2 — `frontend/**` only, nothing near the write registry). JP reported the phase bar no longer drags on the live site; the app's handlers were never the problem, they were never reached. `.gbar` carries `draggable="true"` and its `.gseg` children carry the pixels, but Chrome begins a drag from the draggable **ancestor** and hit-tests *that*, so `.gantt .gbar { pointer-events: none }` (shipped since 13g/13j) made the drag get created and cancelled in the same tick. Real mouse input (CDP) produced exactly `mousedown:.gseg → mousemove → dragstart:.gbar → dragend:.gbar` with `dropEffect: "none"` — no `drag`, no `dragenter`, no `dragover`, no `drop` — while at `dragstart` everything looked right (`text/plain` set, `effectAllowed: "move"`, `.gantt.gdragging` applied, source node connected, nothing calling `preventDefault`). **Three controls close it**: (1) the unscheduled ROW drags fine with the same tool because `.growr` is `pointer-events: auto` — tooling and app both sound; (2) forcing `auto` onto `.gbar` makes the sequence live (`dragstart → drag → dragenter → dragover ×N → dragend`); (3) forcing `.gantt.gdragging .gbar { pointer-events: none }` returns the instant-cancel signature, so "hit-testable at mousedown only" is **not enough** — which is also what rules out moving `draggable` onto `.gseg`, since `.gantt.gdragging .gbar .gseg { pointer-events: none }` would then blank the SOURCE mid-drag. **Fix**: `pointer-events: auto` on `.gbar`, written out explicitly rather than merely omitted (the property **inherits**, so the stated value defends the source against a future ancestor rule, and it is the guard's positive assertion); `.gseg` deleted from the `.gdragging` rule, leaving `.gantt.gdragging .gdl { pointer-events: none; }` alone; and the bar takes `on-dragover="['dragOver']" on-drop="['dropOnBar']"` on a **third** attribute line so lines 1–2 of the tag stay byte-identical for the tests that pin them. The existing `dragOver` is reused (already exactly `preventDefault()`); no `on-dragenter`, mirroring the shipped `.gweek` recipe. **`ganttDragging` SURVIVES** — the brief allowed deleting it, recon proved it still load-bearing: `.gdl` is a later sibling of `.gbar` in the same `position: relative` `.gtrack`, both absolute at `z-index: auto`, so the tick paints OVER the bar and wins hit-testing at its 2px column; left solid it takes the `dragover`, carries no handler, and the drop is refused at exactly the deadline — and it cannot be permanently transparent because at rest it owns a real `title="deadline …"`. `moveRows`' defensive clear stays (a re-render that eats the source node swallows `dragend`; a stuck flag now costs every tick its tooltip). **New pure function** `weekAtX(clientX, rect, weeks)` at column 0 beside `dayIndex`/`clampUnits`/`unitPct`: named and pure so a test can execute *that exact source* out of the shipped file — no `document`, `window`, `app.get`, `WEEK_PX` or `WEEK_COUNT` — dividing the **measured** width by the count rather than the hard-coded 92px (zoom/DPR rounding then spreads evenly and a retune of `--gw` cannot break it), half-open so a pointer exactly on a boundary belongs to the RIGHT column, clamped both ends so a drop can never fall off the track, `null` only when there is nothing to measure. **One write recipe**: `dropOnBar` runs the same `moveRows` `dropOnWeek` runs — `01-app.js` still holds exactly the **two** pre-existing `POST …/replot` call sites (Accept-suggestions and `moveRows`) it held at `1e13088`, and BR-8 group resolution, the optimistic footer, the rollback banner and the arrival pulse are byte-unchanged. It measures `ctx.node.closest('.gtrack')`, **never** `ctx.event.target` (with `.gseg` hit-testable the event fires on a 26px segment and bubbles, and the target's rect is a fraction of a column), and takes the card id from `dataTransfer.getData('text/plain')`, **never** from the bar's own `row.cardId` — an unscheduled row dragged across a scheduled row now lands on that bar, and reading the landing row would move the wrong card. **Three intended consequences**: `.gweek` no longer sees the drag on a SCHEDULED row (its handlers stay live exactly where there is no bar — an unscheduled row renders `.gunsched`, `pointer-events: none`, instead; `.gweek` markup untouched); a **pinned** row keeps `draggable="false"` and its refusal title but **does** carry the drop handlers, outside any pinned conditional, because the pin freezes the pinned ROW not the column and suppressing them would carve a dead 1104px strip that silently refuses every drop; and `cursor: grab` plus the bar's `title` now spread across the full track, both true statements post-fix (moving `cursor: grab` to `.gseg` would restore pixel-identical hover at the cost of making the cursor lie — not taken). Untouched: `.gblockhead` unslot, the unscheduled row-drag and `.ghandle`, `rowKey`'s keyboard ±1-week reslot, `unslotRow`, the Deadlines `.entry` drag, the phase→colour map, segment geometry and the legend. Both falsified comment blocks in the CSS, the template and `01-app.js` rewritten to the proven mechanism; the template block stays a Ractive `{{! }}` and was verified to quote no `}}` before its terminator and to be stripped by `Ractive.parse()` (an HTML comment there would have broken `expect(renderGantt()).not.toContain('gdragging')`). No new dependency, `lib/**` untouched, write registry unchanged at W1 / W2 / W3
- [x] T154 Batch-7 verification: `test/drag-hittest.test.ts` (**44**, hardened to **49** by T156) plus three rewrites in `test/gantt-rowactions.test.ts` (net count unchanged) and a comment-only correction in `test/helpers/gantt-render.ts`. **The required guard** asserts no rule makes a DRAG SOURCE `pointer-events: none` in any state, and it is built to keep working: drag sources are enumerated **from the shipped template** — every element open tag carrying a `draggable` directive, with conditional class tokens kept (`growr sel pinned unsched arrived`, `gbar`, `entry late urgent`) — and the count pinned at 3, so a fourth source joins the guard automatically instead of slipping past a hard-coded list; the scan uses `\bdraggable=` because `.growr`'s directive is written `}}draggable=` with no leading space inside `{{#if !row.slottedWeek}}`, and a space-anchored match silently finds only 2 of 3, which is exactly how a guard like this quietly stops guarding. Every `pointer-events: none` rule in **all seven stylesheets** is flattened (read off disk via `readdirSync` so the bug cannot move to a new file; `@media`/`@supports` walked into, `@keyframes`/`@font-face` skipped, comments stripped), each selector's **rightmost compound** taken as the subject with pseudo-classes stripped, and the rule fails when its class-token set is a subset of any source's possible tokens. Beyond the plan: an **ancestor sweep** (`pointer-events` inherits — hand-kept as `gtrack`, `growr`, `gbrows`, `gblock`, `gsheet`, `gwrap`, `gantt`, `daycol`, `daygrid`, `weekcard`, `weekgrid` here, **derived from the template's own nesting as of T156**), a **class-less-subject** check (`*` or a bare tag names no class and would slip a subset test while disabling everything), and **parser self-tests**, because a guard whose parser silently matches nothing is worse than no guard. **Proved non-vacuous twice**: permanent in-file fixtures flag the shipped rule, the mid-drag `.gantt.gdragging .gbar` variant (control 3), a state-scoped `.growr.pinned` variant and an `:active` variant, with negative controls that must NOT fire (`.gseg`, `.ubadge.saving`, `.gghost`, a `pointer-events: auto`); and at integrate the real tree was reverted to `pointer-events: none` on `.gantt .gbar`, going red with ``35-gantt.css: `.gantt .gbar` makes the .gbar drag source un-hit-testable`` before the line was restored. **The mapping** is sliced out of the shipped `01-app.js` with the shared `decl()` and executed in a `new Function` harness, never retyped — sliced **lazily** on purpose, since `decl()` throws when absent and a module-scope throw would take the hit-test guard down with it: first column at its exact left edge and middle, last column at its last subpixel and middle, the half-open boundary (`1091.99` → col 0, exactly `1092` → col 1, named for the rule), left- and right-of-track clamps including `-5000`, a full 12-column walk, a 2× zoom rerun proving it divides the measured width rather than a hard-coded 92, three degenerate guards returning `null`, and purity. A companion suite **pins the equal-column premise in CSS** so a future variable-width layout fails loudly instead of skewing every drop (`--gw` declared once on `.gantt`, `.gweek` `width: var(--gw)` + `flex: none`, `.gtrack` `flex: none`, the `box-sizing: border-box` reset, `.gantt.lpc` touching `--gleft` only, the single `@media` being `prefers-reduced-motion`). **Wiring**: all five directives on the `.gbar` open tag (source assertions, said so in the test names — directives never reach `toHTML()`); `dropOnBar` contains no `api.send` and reaches the write only through `moveRows`; the id comes off the dataTransfer with no `row.cardId` in the body and no `cardId` parameter; `ctx.node.closest('.gtrack')` present and `event.target` absent; `preventDefault` called and the null-week bail placed **before** `moveRows`; `.gweek` and `.gblockhead` handlers verbatim; a pinned row still emits `draggable="false"` and its refusal title while **keeping** the drop directives outside any conditional (named so nobody "fixes" the landing strip into a dead column); the unscheduled row keeps its whole-row drag, grip, hint and absent bar. **One test-plan item corrected**: the plan asked for *exactly one* `POST …/replot` call site, but the `1e13088` baseline already has **two** (Accept-suggestions at `:2188`, `moveRows` at `:2469`) — asserting one would have failed on pre-existing, correct code, so the test asserts **exactly two** (no third was added) plus `dropOnBar` containing no `api.send` at all. Suite **682 → 726** (57 files), green on default and `TZ=UTC`; `test/writes-disabled.test.ts` and `test/gantt-collapse.test.ts` untouched and green. **Flake honesty (T149)**: eight full runs at integrate — 2 default (both 726/726) and 6 `TZ=UTC` (3 × 726/726, 3 red). Every red was a **single** test in a long-standing server file this batch does not touch (`test/admin.test.ts`, `test/acks.test.ts`, one not captured), never an assertion in a file this batch edits; both named files are 3/3 green in isolation, and the five suites this batch touches or neighbours (`drag-hittest`, `gantt-rowactions`, `gantt-collapse`, `planner-weeks`, `writes-disabled`) are **124/124 across five isolated `TZ=UTC` runs**. Same parallel-startup contention signature T149 records; nothing was retried, capped or otherwise adjusted to hide it. **Stated in the file header, the guard's `describe` name and the mapping's `describe` name**: none of this proves the drag works — there is no jsdom and no browser runner, a synthetic `DragEvent` never enters Chrome's drag machinery, and that blind spot is precisely why this bug shipped past every check from 13g/13j to batch 6
- [ ] T155 Live-browser pass for batch 7 — **the only thing that can close T153**, and non-negotiable: this batch's entire subject is a class of defect the suite is structurally blind to. With a real mouse on the deployed build: (a) dragging a bar produces `dragstart → drag → dragenter → dragover ×N → drop → dragend` with `dropEffect: "move"` and the row lands; (b) a **±1-week** move — the reslot the bar's own title advertises, and the case batch 4 could never do — lands rather than snapping back; (c) a drop **at the deadline tick's column** lands (the `.gdl` clause earning its keep); (d) an **unscheduled** row still drops onto a bare `.gweek` cell **and** onto another row's bar, moving the dragged card and not the landing row; (e) a **pinned** row still refuses the grab and shows its message while remaining a valid landing strip for someone else's drag; (f) the Unscheduled block header still unslots and the keyboard ±1-week reslot is unaffected; (g) BR-8 multi-select still moves the whole group through one `/replot` with one audit row; ~~(h) JP rules on the cosmetic spread of `cursor: grab` and the bar `title` across the full 1104px track — accept, or move `cursor: grab` to `.gseg` (one line).~~ **(h) RULED AND CLOSED 2026-08-18** — JP: the affordance belongs to the COLOURED BARS ONLY; built as T157, so nothing under (h) is owed a browser. Folds naturally into the same session as T145 and T152. **AMENDED 2026-08-18 by T158** — batch 8 moved the drag SOURCE off `.gbar` onto the new `.grun` box, so (a)-(g) must be re-run against that build rather than batch 7's, and five checks join them: **(i)** the drag starts ONLY from the coloured run — pressing empty track in the same row starts nothing; **(j)** a drop on empty track OUTSIDE the run still lands, through the `.gweek` cell and the same `moveRows` (this is what `.gbar`'s return to `pointer-events: none` buys, and it is the one behaviour batch 7 traded away); **(k)** a ONE-DAY phase is catchable at the 24px minimum, including one in the FINAL column, where the handle must not hang off the track; **(l)** a PINNED row still refuses the grab on its colour and still accepts someone else's drop, both on its run and on its empty track; **(m)** a before/after screenshot that is identical apart from the cursor — the automated proof of that goes to 0.083px (T159) but a string renderer cannot see a repaint. **Open on (a)-(g) and (i)-(m)**
- [x] T156 Batch-7 adversarial-review fix pass — two real findings, both about **honesty and reach**, no behaviour change (`frontend/**` comments + `test/**` only; the CSS rule, the template directives, `weekAtX` and `dropOnBar` are byte-identical to T153). **(1) A comment overclaimed.** T153 wrote, in `35-gantt.css`, `01-app.js` and both test files, that the segments left the `.gdragging` rule because "blanking them mid-drag blanks the source and aborts the drag". That is not true of the code as shipped: `draggable` is on `.gbar`, the wrapper is `pointer-events: auto`, so a transparent `.gseg` simply falls through to the solid bar underneath and the drag lives. The real reason is now written down — nothing NEEDS the segments transparent once the bar owns the drop, and the guard bans it anyway because blanking a segment becomes control 3's same-tick cancel the moment `draggable` moves down onto `.gseg`, the variant batch 7 rejected. The ban is unchanged and still stricter than strictly necessary, on purpose. **(2) The guard had three ways to be walked past.** It read `/pointer-events:\s*none/` (CSS is case-insensitive and allows whitespace before the colon, so `POINTER-EVENTS : NONE` was invisible to it) → now `/pointer-events\s*:\s*none/i`, with fixtures for the reformatted and `!important` spellings; its ancestor chains were an eleven-entry **hand-kept list** — the same staleness vector the drag sources are already protected from — → the template is now parsed as a **balanced tree** (open/close stack, void and self-closed elements handled, imbalance reported as data rather than thrown so a parse problem cannot take the whole guard down with it) and each source's ancestors come from its real nesting, which immediately picked up `view`, `pscrollwrap`, `pscroll` and `gscroll`, four wrappers the hand-kept list had **missed**; and it read stylesheets only → a `style="…"` attribute or a `node.style.pointerEvents` write would have reintroduced the bug where no CSS sweep could see it, so template inline styles and **all four** shipped scripts are now swept too (none exists today; this is what keeps it so). Added with them: a mid-drag sweep over every `.gdragging` rule for `visibility: hidden` and `display: none` as well as `pointer-events: none`, since all three take the source out of hit-testing and control 3 proved "hittable at mousedown only" is not enough — scoped to `.gdragging` on purpose, because at-rest hiding (a filtered row, a collapsed block) is ordinary layout work and flagging it everywhere would be noise. Non-vacuity re-proved against the **real pre-fix file**: `git show 1e13088:frontend/styles/35-gantt.css` dropped into the tree turns the suite red on three tests before it is restored. `test/drag-hittest.test.ts` **44 → 49**; suite **731/731** (57 files) on default TZ and on `TZ=UTC` (rerun; see T149). **Rejected, with reasons:** the `cursor: grab`/`title` spread across the full track stays as T155(h) for JP — batch 7 is a hit-testing fix and restyling it here would pre-empt the ruling; and the `TZ=UTC` red seen once in this pass is T149, not a regression — `test/webhook.test.ts` failed at **suite setup** with `Port "54463" already in use` from `mongodb-memory-server`, is untouched by this batch, and is 9/9 in isolation. That signature is sharper than the `socket hang up` / `/__test/login` 404 faces recorded so far and points at **mongod port allocation racing across 57 parallel suites**, not at supertest listeners — worth folding into T149's fix direction.
- [x] T157 **The drag affordance moves onto the coloured bars** — JP's ruling of 2026-08-18, which closes **T155(h)** (**FR-5.4**, invariant 2 — `frontend/**` + `test/**` only, nothing near the write registry). Batch 7 correctly made `.gbar` span the whole 1104px track and stay `pointer-events: auto`, and `cursor: grab` plus `title="Drag along the timeline to reslot"` spread with it, so empty track advertised a handle that is not drawn there. T153's consequence 3 called that honest because the wrapper *is* the drag source; it is honest about the mechanism and wrong about the user, and JP overruled it. **The drag MECHANICS do not change at all**: `pointer-events: auto` on `.gbar` and `.gseg`, the five directives on the open tag, `weekAtX`, `dropOnBar`, `moveRows`, geometry, colours, z-order and `.gantt.gdragging .gdl` are byte-identical — `pointer-events` was not touched anywhere, which is the one thing this change could not be allowed to break (a drag source Chrome cannot hit is a drag Chrome cancels in the same tick, and `test/drag-hittest.test.ts` still bans it). **Cursor**: `.gantt .gbar` `grab` → `default` (`default`, not `auto`, because `.growr` declares no cursor — the track now reads exactly like the rest of the row); `.gantt .gbar .gseg` `inherit` → `grab` (`inherit` is *how* it picked up the wrapper's `grab`; naming its own value stops the two re-merging); `:active` `grabbing` unchanged; and the pinned rule's subject moves off the wrapper, `.gantt .growr.pinned .gbar,` → `.gantt .growr.pinned .gbar .gseg,` with the `:active` clause kept — **both** are load-bearing (the resting one beats `.gseg`'s new `grab`, the `:active` one beats `grabbing`), and it is segment-scoped for the same reason `grab` was: a refusal is an affordance too, and a 1104px `not-allowed` would withhold a track that never offered anything. **Title**: deleted from `.gbar` in BOTH branches — no new tooltip mechanism, and nothing added to the segment titles, which name the phase and its end date and would go noisy restating the mechanic on every run. The reslot sentence survives as **standing text** in the `.fnnote` hint above the Gantt (*"Drag a bar along its row to reslot it — an unscheduled row drags onto a week. Pinned rows never move."*), which was already there, is not hover-gated, and is strictly the better home: a tooltip that appears only once the pointer is on the bar teaches nobody how to find the bar. The pinned sentence survives twice — `.growr` carries `title="Pinned — unpin to move"` and `title` is **inherited by a title-less descendant**, so the row's attribute covers the `.gtrack` and `.gbar` inside it (deleting the bar's copy removed a duplicate, not the message), and each `.gseg` still appends `· Pinned — unpin to move` to its phase title. **A pinned row, verified against the render**: empty track = `default` cursor + "Pinned — unpin to move"; a coloured segment = `not-allowed` at rest AND while pressed + "Sketch · Pinned — unpin to move"; `draggable="false"` unchanged, and its bar still carries the drop directives so it stays a valid landing strip for someone else's drag. An unpinned scheduled row: empty track `default` and **silent**, segment `grab`/`grabbing` with its phase title only. **Tests moved, not weakened** (`test/gantt-rowactions.test.ts`): the old `title="Drag along the timeline to reslot"` assertion is not softened, it is wrong now, and four assertions took its place — the wrapper emits no `title=` and the string is absent from the row; `.gbar` is `cursor: default` and **not** `grab` while `.gseg` is `grab`/`grabbing`; the instruction is present as standing template text; the segment titles still carry a title with no `reslot` in it. The pinned block gains the same plus a **negative subject guard** — no rule may name `.gantt .growr.pinned .gbar` as a subject again (matches `.gbar,` / `.gbar {`, deliberately not `.gbar .gseg`). Five of the six new CSS assertions flip against the pre-ruling stylesheet; the sixth (`.gseg:active` = `grabbing`) is byte-identical before and after and is labelled in the file as a preservation guard, not a regression proof. `test/drag-hittest.test.ts` keeps its pinned assertion with a comment saying it is no longer a claim about the bar. Suite **735/735** (57 files) on default TZ and `TZ=UTC` — 731 + the four net new tests. No new dependency, `lib/**` untouched, write registry unchanged at W1 / W2 / W3

## Phase 13k (cont.) — the drag handle becomes the coloured run (~0.5d) — added 2026-08-18 (JP's structural ruling, read off the live DOM himself). Frontend + tests only: no server, no wire, no schema, no migration

- [x] T158 **The coloured run gets its own draggable box** — JP's structural ruling of 2026-08-18 (**FR-5.4**, invariant 2 — `frontend/**` only, nothing near the write registry). JP, from the live DOM: *"gbar is a draggable element, inside that is the actual bars. The issue is gbar spans across the whole of the timeline so dragging it makes the whole thing draggable. What I would like to be draggable are only the bars inside the colored ones. However, for that to happen, those colored bars should be wrapped into a draggable container instead. Gbar stays, but the colored bars wrapped in a draggable container can now be dragged."* Batch 7 made the drag work by making the source hit-testable; T157 moved the *cursor* onto the segments; this moves the **source**. **Structure**: `.gtrack > .gbar > .grun > .gseg × N`, with `.gdl` and `.gghost` still direct children of `.gtrack` and untouched. `.grun` (new, grep-clean at `1c571f1`) carries all five directives — `draggable="{{ row.pinned ? 'false' : 'true' }}"`, `on-dragstart`, `on-dragend`, `on-dragover`, `on-drop` — plus its own inline `left`/`width`; `.gbar` keeps its element and its class and carries **none of them** and no `title`, and neither box carries a `title` (T155(h) applies to the invisible extension for the same reason it applied to the wrapper). **Two shape rulings, not re-litigated**: ONE box over the whole coloured run rather than one handle per segment (the segments touch, so it looks identical and the handle cannot flicker across a seam — and `test/drag-hittest.test.ts`'s source count of 3 is what would catch the alternative); and a minimum grab width whose extension is invisible. **`.gantt .gbar` goes back to `pointer-events: none` ON PURPOSE, and that is not the 13g/13j bug returning** — the bug was a *drag source* Chrome could not hit, and `.gbar` is no longer a drag source but the source's parent. At 1104px it swallows every drop landing on the track outside the coloured run and the `.gweek` columns beneath never see them; transparent, it hands those columns their drops back while `.grun` keeps its own, both ending in the **same** `moveRows`. It is legal only because `pointer-events` **inherits and a child's explicit value overrides an inherited one**, so `.gantt .grun { pointer-events: auto }` is written out rather than omitted — **the two rules ship together or not at all**, and the guard now asserts exactly that. **The geometry had to move, in one place.** `phaseBars(row)` emitted each segment as a percentage of the 60-workday window; inside a narrower box every one of them re-bases and the bars move on screen. `phaseBars` is **DELETED** and replaced by a column-0 `const phaseRun = (row) => …` returning the box **and** its re-based segments in one shape — no second helper, nothing to drift, and the template does **no arithmetic** (no `*`, no `+`, no `calc(`, no `{{#if}}` on geometry; every value arrives as a string already at 2dp). All of it in units with one rounding at the end: raw run `R0 = min(sL)`, `R1 = max(sL+sW)`; `W = max(R1−R0, MIN_GRAB_UNITS)`; **`L = max(0, min(R0, TOTAL_UNITS − W))`** — one expression, no branch: anchor at the run's left edge and grow the invisible part RIGHT, slide left only if the grown right edge would pass the end of the track, never let the left edge go negative (the left-edge mirror needs no special case, `R0=0` falls straight through). Segments restate as `left' = pctOf(sL − L, W)`, `width' = pctOf(sW, W)`. **The invariant that proves nothing moved**: `left + left'ᵢ·width/100 = 100·L/T + (100·(sLᵢ−L)/W)·(100·W/T)/100 = 100·sLᵢ/T`, exactly what `phaseBars` emitted — **W cancels**, which is why the minimum-width extension is free of visual consequence whatever its size, whichever clamp branch fired, and however many segments there are. `unitPct` redefined as `pctOf(u, TOTAL_UNITS)` so the file holds exactly ONE rounding rule; output byte-identical. **No forecast math changed**: `dayIndex`/`clampUnits` run on the same server-supplied `startIso`/`endIso`, in the same order, with the same zero-width drop rule — only the denominator of the final division moved. **The minimum is ARITHMETIC, never CSS, and that was the trap.** `MIN_GRAB_PX = 24` → `24 / (92/5) = 1.3043478…` units = **2.17% of the track**, exact because `--gw` is declared once and `.gtrack` is content-sized at 12 columns (1104px). A CSS `min-width: 24px` would look equivalent and be wrong: `.grun` is positioned in percent and its `.gseg` children resolve **their** percentages against its **rendered** box, so a CSS minimum widens the box after the arithmetic has run and every short bar visibly stretches — same for `padding`, `border`, `min-inline-size`. `.grun` is `top: 0; bottom: 0` (a purely horizontal box) so `.gantt .gtrack .gseg`'s `top: 50%; transform: translateY(-50%)` resolves against a containing block of exactly the height it does today — that rule is **byte-identical** and the segments provably cannot move vertically — and the grab target is the full 84px row instead of 26px, free because the box paints nothing. **`.grun` paints NOTHING**: no background, border, outline, radius, shadow, opacity, transition, z-index, `::before`/`::after`, no hover rule; `cursor: default` on the box and `grab`/`grabbing` on the segments is T157's affordance ruling applied at 24px instead of 1104px, so the ~6px extension shows the plain arrow while staying grabbable. Pinned specificity preserved exactly by the substitution: `.gantt .growr.pinned .grun .gseg` (0,5,0) still out-ranks `.gantt .grun .gseg` grab (0,3,0) and `:active` grabbing (0,4,0). **A row whose phases the window clips away renders `<div class="gbar"></div>`** — `phaseRun` returns `[]`, the same idiom `ghostBar` uses, so `{{#each}}` emits no box, no `draggable`, no segment; the absence falls out of the empty-array shape, not a second code branch. **Untouched, byte-for-byte**: `dragRow`, `dragEnd`, `dragOver`, `dropOnWeek`, `dropOnBar`, `dragOverBlock`/`dropBlock`, `moveRows`, `weekAtX`, `dayIndex`, `clampUnits`, `TOTAL_UNITS`, `deadlineTick`, `ghostBar`, `.gantt .gtrack .gseg`, `.gantt.gdragging .gdl` (`ganttDragging` survives — `.gdl` still paints over the track and carries no dragover handler), `.gdl`, `.gghost`, `.gunsched`, `--gw`, the four `.gseg.<phase>` colour rules (still exactly four) and both `.glegend` rules. Only prose changed in `dropOnBar`'s comment: `ctx.node` is the run box now, and `closest('.gtrack')` makes that a non-event because the measured rect is the TRACK's either way. The rewritten Ractive `{{! … }}` block was verified to quote no `}}` before its terminator. No new dependency, no token invented, `lib/**` and `src/**` untouched, write registry unchanged at W1 / W2 / W3
- [x] T159 Batch-8 verification — new `test/gantt-run-geometry.test.ts` plus the guard repointed in `test/drag-hittest.test.ts`, render/CSS assertions repointed in `test/gantt-rowactions.test.ts`, and the `phaseBars` stub replaced in `test/helpers/gantt-render.ts`. **The position invariant is the point of the new file**, and it is proved against a **frozen `barsAt1c571f1` oracle** — `phaseBars` transcribed into the test because the app no longer contains it, deliberately frozen as a fixed point rather than a mirror that tracks the source, built on the SLICED shipped `dayIndex`/`clampUnits`/`unitPct`/`fmtDate` so the two sides cannot disagree about what a date *means*, only about where a segment is *drawn*. `phaseRun` is a column-0 `const` specifically so a test can slice and execute it: `app.set('phaseBars', (row) => …)` was un-sliceable, which is why the old helper was **never once executed by a test** — that is the gap this batch closes. Ten fixtures (single · three touching · left-clipped · right-clipped · all-clipped both directions · final column · one-day mid-window · already-wider-than-the-minimum · missing/null/`[]`/non-array phases · a zero-width segment mixed with a real one), every fixture date asserted to land on the unit its comment claims so a wrong comment fails rather than lies, plus a sweep of **every** one-day run at units 0…59 proving `0 ≤ L` and `L + W ≤ TOTAL_UNITS` everywhere. **Measured at integrate by rendering both templates** (OLD at `1c571f1` + `phaseBars`, NEW + `phaseRun`, real Ractive, absolute positions read back out of the HTML, each fixture rendered pinned **and** unpinned): worst composed drift **0.0075 pp = 0.083px** on a 1104px track, at F2 (three touching segments); F6 the final column 0.0063 pp = 0.069px with its right edge at exactly 100.00%; the 60-unit sweep 0.0063 pp, **0** boxes off the track and **0** under 2.17%; segment count, `cls` and `title` identical to the old render in every case. **The build brief predicted F6 as the worst and was wrong** — three roundings feed the composed error, not two, and the omitted one is the largest: the `1c571f1` value being compared against was rounded too and does not cancel. A wide run lets the segment's own rounding through at full scale, which is why three touching segments beat the single-column case. The analytic bound is ≈0.02 pp, so the 0.02 pp tolerance **is** the bound rather than a round number; both the helper's comment in `01-app.js` and the tolerance comment in the test were corrected at integrate from "≈0.006 pp, F6" to the measured numbers. Mutation-probed off-disk: dropping the re-base costs **4.58 pp** (~600× the tolerance), and anchoring the box on the run's right edge, re-basing widths against the raw run, removing the minimum, and removing the right-edge clamp are each caught by a **named** assertion. **The guard moved with the source and did not shrink** (`test/drag-hittest.test.ts`, header says so): source count still **3** — `['entry','growr','grun']`, the source moved rather than multiplied — `byPrimary['grun'] === ['grun']`, and the ancestor chain now includes `gbar` explicitly. The ban **grew a rule it did not have**: `declaresAuto()` + `ancestorOffenders()` teach the ancestor sweep that an inherited `none` is cured by the source's own explicit `auto`, **per source** (`.gantt` and `.gbrows` are ancestors of both `.grun` and `.growr`, and `.growr` declares no `auto`, so a `none` up there is still caught — an exemption, not a hole), evaluated against the same rule set being swept so a fixture holding only a `none` still fires. Six fixtures pin it, including `.gbrows { none } + .grun { auto }` → still **1**; the SOURCE sweep takes **no** exemption in any state or spelling and says so; the `.gdragging` sweep takes none either, because `display: none` on an ancestor cannot be undone from inside it. One test beyond the plan: strip `.gantt .grun`'s `auto` out of the **real** rule set and the sweep must flag `.gantt .gbar` — proving the escape is load-bearing on the shipped sheet, not just on fixtures. The legitimate transparencies go 4 → **5**, `.gantt .gbar` added with its reason. The minimum width is asserted as arithmetic **and** as a CSS ban: `.gantt .grun` must declare no `min-width`/`min-inline-size`/`padding`/`border`/`box-sizing`/`height` and must paint nothing. **Unchanged and green, which is itself the check**: every `dropOnBar` body assertion (the drop path did not move), both `weekAtX` suites, the class-less-subject sweep, the inline-style vector, `draggingSubjects === ['gdl']`, and `test/gantt-legend.test.ts` + `test/planner-weeks.test.ts` **untouched** — the phase→colour map did not fork and `TOTAL_UNITS`/`dayIndex` survived verbatim. **Three contract deviations at build, all forced, none weakening**: the sorted source names are `['entry','growr','grun']` because `'o' < 'u'` (the contract was wrong about JS, not about the pin); `draggable` **adjacency is not assertable** because Ractive 1.4.4's `toHTML()` emits `style` before `draggable` whatever order the template writes them in — verified at integrate across three variants including a fully static one — so three render assertions read the extracted open tag `/<div class="grun"[^>]*>/` and then `.toContain('draggable="false"')`, same claim, order-independent, with the template-**source** assertions keeping the contract's exact form; and the ancestor sweep is a **de-duplicated union** of the non-exempt sources' chains rather than a per-source concatenation, which the contract's own fixture table requires (`.gscroll` is an ancestor of two sources and concatenation would report 2 where the table says 1) and which is what the pre-batch guard already did — `dedupe()` unchanged, the per-source filter added in front of it. Suite **735 → 783** (57 → **58** files; 780 at integrate, +3 from the fix pass below), green on default TZ and on `TZ=UTC`; `node frontend/build.js` 333,049 bytes, `npx tsc --noEmit` and `npx eslint .` clean. **Fix pass (post-integrate review), test-only, no frontend change**: (1) the `.gweek` cells got their own sweep — the T158 `auto` exemption is right for a drag SOURCE, but `.gtrack` is an ancestor of `.grun` and of nothing else that drags, so the chain carrying the week cells left the sweep the moment the source moved, and T158 is exactly what made those cells load-bearing (`.gbar` is transparent so a drop outside the run reaches a cell). `WEEK_CELLS` is derived from the `dropOnWeek` handler and swept with its ancestors under the same per-target cure, plus in the `.gdragging` sweep; mutation-probed on the real sheet — appending `.gantt .gtrack { pointer-events: none }` to `35-gantt.css` fails the new test and **nothing else**, which is the proof the old guard was blind to it. Scoped to the planner's cells; `.gblockhead`/`.daycol` untouched. (2) `declaresAuto()` no longer accepts a state-scoped (`.growr:hover`) or `@media`-scoped `auto` as a cure — either would exempt a whole ancestor chain on a cure that evaporates mid-drag; `CssRule` gained `conditional` and the subject compound must be state-free. The asymmetry is stated in the test: a BAN still counts `:active` and `@media`. (3) Byte count and the "six directives" miscount corrected here, in `STATE.md` and in `gantt-frame-notes.md`. **One real defect found at integrate, by an unrelated guard**: correcting the helper's comment in `01-app.js` to the measured drift turned `test/suggest-counts.test.ts` red on *"the client declares the hard-mix fallbacks exactly once and nowhere else"* — that guard strips lines beginning `/*`, `*` or `//` and then requires the `HARD_IDEAL` share to appear on exactly ONE line of the client, and this file's block comments are indented continuation lines with no leading `*`, so a prose pixel measurement survived the strip and read as a second copy of the constant. **The guard was right and the comment was changed**, not the guard: the pixel figure is spelled in words now with a note beside it saying why a bare decimal must not go back. The precise figure is quoted in `gantt-frame-notes.md` and `STATE.md`, which the guard does not read. **Flake honesty (T149)**: the first run went red on ONE test in `test/dayplan-api.test.ts` (`/__test/login` → 404), a file this batch does not touch — the same parallel-startup contention signature; rerun clean, and nothing was retried, capped or adjusted to hide it. **Stated in both new/updated file headers**: none of this proves a drag works — there is no jsdom and no browser runner, a synthetic `DragEvent` never enters Chrome's drag machinery, and **no synthetic DragEvents were added and none may be**. What it proves is that the conditions Chrome needs are present in the shipped files and cannot silently regress, and that the coloured bars did not move by a pixel



---

## Dependencies

```
Phase 0 ─► 1 ─► 2 ─► 3 ─►(GATE T026: JP)─┐
                │                        ├─► 6 ─►(GATE T045: JP+PM)─► 7 ─► 8a ─► 9
                └─► 4 ─────► 6           │
                └─► 5 (independent of 4)─┘
Phase 8: after 2 + 7 + duplicate-board confirmation (JP) — its own review
```

- Phase 5 is independent of phase 4 — parallelizable with it.
- Phases 3's gate (T026) blocks 6; 6's gate (T045) blocks 7. Gates do not parallelise.
- Phase 8 additionally requires the duplicate staging board (JP) — currently **BLOCKED**.

## Parallel opportunities

- Phase 0: T002, T003, T005 alongside T001/T004.
- Phases 4 + 5 concurrently (two developers compress the middle, per plan estimates).
- Within phase 7: T047, T049, T053, T056, T062 are parallel-safe against the tab they don't touch.
- Phase 9: T070–T072 concurrently after T069.

## MVP scope

First demonstrable value = **US1 Pipeline read-only** on real data: phases 0–2 + T027–T033 (ARES sync) + T048 (Pipeline tab shell — behind the gate discipline, forecast columns absent). Each later phase is an independent increment per the ladder.

## Day-count sanity check

0: ~4 · 1: ~3 · 2: ~4 · 3: ~4 · 4: ~6 · 5: ~4 · 6: ~4 · 7: ~15–18 · 8: ~2 · 8a: ~1 · 9: ~13 → **~60–63 days** — matches the amended plan bound; phase 7 is the biggest block, as expected. Phase 10 (added 2026-08-04) adds ~5–6 Sirius-side days; the ARES half is a separate build outside this bound. Phase 12 (added 2026-08-12) adds ~6 days.
