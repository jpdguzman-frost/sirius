# Pipeline frame — annotation extraction & build notes

**Source:** Figma `Frost-Sirius--Working-File` (abDRsIVDs1XjJKeR8xYOoF), frame `Pipeline/Default` (17:1015, 1600×1201), extracted 2026-08-12 via Figma MCP.
**Authority order (JP 2026-08-12):** frame annotations govern the Pipeline rebuild; where an annotation is stale against decided governance (marked ⚠ below), the constitution/v1.2 build spec wins and the drift goes back to the product team.

## Annotations, verbatim by component

| Node | Component | Category | Content (condensed) |
|---|---|---|---|
| 17:1438 | TopNav | Design | 1600×186, white bg; two 45px rows (breadcrumb+controls / tabs), 32px gap, 40px top pad; content 1472 wide, 64px inset |
| ↳ 19:920 | Breadcrumb | Design | `Frost: Sirius › {tab}` · `--text-title` 24/600 `--surface-foreground` · 22px chevron · `<nav aria-label="Breadcrumb">`, `aria-current="page"` |
| ↳ 19:927 | Project selector | Functionality | Bound to projects; switch swaps entire context; board id external link under `PROJECT` label; **board AND `trello_label`** rule; done-when: switch empties/restores |
| ↳ 19:940 | Last Synced | Functionality | `--text-caption` 400 muted; locale time; updates on successful sync |
| ↳ 19:941 | User chip | Functionality | Four auth checks; 28px initial avatar (foreground bg); name + Sign out; slate-50 box, 1px border, radius-md. ⚠ "Sign out clears imported data" is prototype-era — v1.2 corrected: server-side data, sign-out clears nothing |
| ↳ 19:947 | Tab group | Design | **Five tabs** icon+label; active = foreground text + 2px foreground underline; inactive = muted + `--border-border` underline; divider extends right; `--text-body` 600. ⚠ v1.2 says SIX (Admin, admin-only) — keep Admin in production, styled to match |
| ↳ 19:948/949 | Tab states | Interaction | `role="tab"`, `aria-selected`, `tabIndex=0`; hover darkens inactive text |
| 28:3666 | Metric strip | Functionality ×4 | MAIN CARDS (deliverables; done-when 269+209+20=498 on the verified board) · WORK CARDS (attach to MC group, never a parent link) · OPEN WORK (incomplete-field count, mirrors the §4.4 panel; "clicking could scroll to/filter panel" — optional) · URGENT (red, `Urgent`-label count) |
| 70:1358 | Alert Banner | Design + Func | §4.4 panel: amber-50 bg, amber-300 border, radius-md; row = `Row N` chip + bold MC + name + amber reason + right-aligned Open Card (new tab); consequence copy beneath; hide entirely when empty; count feeds OPEN WORK |
| 17:2057 | Search | Functionality | Placeholder "Search cards or MC#"; case-insensitive substring over MC #, name, type, client, status…; matches wrapped in `<mark>` amber; realtime filter |
| 70:1211 | Callout | Functionality | "Difficulty and Current List are read from Trello — can't be edited here" (dashed slate-50 box). **"Simulate Trello sync" button is dev-only — not in production** |
| 70:10024 | Row | Interaction | Expandable; chevron toggles the MC group's task cards; sub-id `MC-655.3` display; row focusable, **Enter toggles expand** (WCAG) |
| 169:26426 | Difficulty cell | Functionality | **"Read-only from Trello (§4.1). Render as coloured badge, never an input."** Missing difficulty → incomplete panel. (The chevron inside the badge is Badge-component chrome, not a select) |
| 169:26364 | Urgency (urgent) | Interaction | The write: optimistic then reconciled, restore+surface error on failure; urgent = destructive-light bg / destructive-strong text / red-300 border / ⚡; **saving state: opacity .5, label "saving…", pointer-events none** |
| 169:26074 | Urgency (non-urgent + menu) | Interaction | Click toggles urgent; creates label on board once; non-urgent = secondary bg, muted text, **dashed** slate-300 border. Open state renders a Select Group (white, slate-200 border, radius-sm, Shadow/xs; "Urgency" caption header; dot-indicator items) |
| 70:10030 | Status cell | Functionality | Trello list name **verbatim**, keyword-classified: pending warning-light / ongoing blue-100 / done emerald-100 |
| 251:7925 | Due cell | Interaction | Editable here; click opens date input with Set and clear. ⚠ Annotation still reads v1.1 ("precedence manual → Trello → sheet", dashed local override) — **superseded**: v1.2 §4.2 + errata confirm the edit WRITES the Trello due date (W2), no local layer. Implement write-through in the Date-Picker chrome shown |
| 70:10037 | Links cell | Functionality | Trello/Figma source icons, open new tab, never editable inline; missing Figma → incomplete panel |

## Rex verification pass (plugin-context sweep, 2026-08-12) — 32 total, 8 the MCP missed

Full-tree walk via Rex confirmed every MCP-extracted annotation **verbatim** and surfaced 8 more (mostly frame/container-level, dropped by the MCP's sparse-response mode):

| Node | Component | Category | Content (condensed) |
|---|---|---|---|
| 17:1015 | Pipeline/Default (frame) | Design | Full page 1600×1201; Top + Content sections; **success criterion: 1:1 at 1600px, all values bind to ARES variables** |
| 70:10522 | Top | Design | Shell header 1600×268 flex column: TopNav 186 + Stats 82; 64px inset (content 1472); `--font-sans` Google Sans Flex (annotation says self-hosted — **JP overrode: Google Fonts CDN**); raw hex/px is a defect; done-when: diff to zero at 1600px |
| 28:3683 | stats-container | Design | 82px row, four equal 368px columns (1472÷4), no gap, shared 1px `--border-border`; overline `--text-label` 600, value `--text-display` 600, 8px apart |
| 17:2063 | Content | Design | Stack: incomplete panel (conditional) → search 54px → callout 53px → table + scroll bar; 24px gaps |
| 70:10008 | Table | Functionality | §4.1: rows = deliverables (`Main Card`); expand reveals the MC group's tasks; identity `(project_id, trello_card_id)`; done-when 269+209+20=498 |
| 70:10009 | Header | Design | 56px, `--slate-100` bg; **definitive column order: MC #, Card Name, Type, Difficulty, Urgency, Status, Client, Due, Work Started, Work Done, Cycle Time, Weeks, Links**; header `--text-label` 600 uppercase muted; 1px `--slate-100` row separators, no vertical rules; horizontal scroll below min width |
| 251:23859 | Due (missing/overdue) | Design | Missing: outlined red "none" pill or amber warning border on the picker; format `17 Jan 2026`; tooltip names the source; overdue tints `--status-warning` |
| 251:6758 | horizontal-slider | Interaction | Custom scroll bar: track `--slate-300`, thumb on `--slate-100` container, chevron nudge buttons; **below 1600px the table scrolls horizontally — never column collapse** |

**Un-annotated elements** (build from visual + existing FRs): Type cell (badge — UI/Asset/Icon/Spot Illustration), Client cell (`@handle` badge), MC/Card-Name cells, Work Started / Work Done / Cycle Time / Weeks cells (Work Started renders in Date-Picker chrome but carries no annotation — **read-only display per FR-4.5**, cycle fields derive from Trello activity; Weeks = slotted-week display).

**Team corrections after build (2026-08-12, via JP):** content-stack gap is 16px with 0-margin elements (frame geometry confirms; the Content annotation's "24px" is the container's top padding only) · column labels **Started** / **Done** (renamed from Work Started / Work Done in the header annotation) · Card Name flexes to fill remaining width · scroll bar only when the table overflows · **Cycle Time + Weeks columns removed, Links 94px, header 44px** (frame re-measured after their edit) · **read-only callout strip removed** (simplification; the read-only rule survives in the difficulty tooltip — annotation 70:1211 superseded).

## Batch-2 cell specs (2026-08-13, owl #06–#10; Rex-verified 1:1 against the owls)

Five nodes: `415:54979` Due popover · `431:17015` start-date-cell · `431:17016` done-date-cell ·
`431:17017` Links · `432:17733` mc-cell (DATA FIX). All built 2026-08-13 (tasks T113–T115).
Decisions folded in: **per-card Started/Done** (the row card's own transitions — supersedes the
phase-13 group-derived assumption; JP "proceed as defined from Miles"); done = latest entry into
done, held only while the card sits there (our rule, told to product); **MC# decimal ruling**
(JP): the table shows the bare MC number, `display_id` survives internally, in search, and on
every other surface; commit-on-Apply on the Due popover; links off-state truly disabled at 30%.
Read-only Due field (observation mode) says "No Due Date" rather than "Select Date" — it is not
a selectable field there.

## Drift register (frame ↔ decided state) — for the product team

1. Due cell annotation carries v1.1 §4.2 (local override) — v1.2 + errata already corrected this; annotation needs the same fix.
2. Urgency annotations say "the only write" — registry has two.
3. User chip: "sign out clears imported data" — prototype-era.
4. Tab group: five tabs; v1.2 says six (Admin).
5. Project selector's rendered label uses the `Semantic/Button` token → **Inter 13.9px**, off-system vs §0 (Google Sans Flex only) — normalising to `--text-body`/600 Google Sans Flex unless the team objects.
6. **Due popover annotation (415:54979 Functionality) states display precedence backwards** — "deadlines-first: intake sheet then Trello". The constitution (invariant 14) and BRD BR-note say a Trello due date, where present, WINS (set deliberately), else sheet. The write-target description (Trello only) is correct. Flagged to Miles 2026-08-13; annotation needs the fix.
7. mc-cell frame (432:17733) draws the MC# column at 132px — superseded by JP's 150px fine-tune (2026-08-12 team correction).

## Difficulty editability — RESOLVED 2026-08-12 (W3 approved and built)

The frame's read-only annotations (169:26426 and the Callout) were the stale state: product
approved the difficulty writeback the same day (Miles, BRD-§9-A1, owl #01–#03) and appended an
**APPROVED** annotation to cell `415:54974` (parent frame `415:18672` Pipeline/Default/v.1.3,
verified); the stale GOVERNANCE BLOCKER note is theirs to clear. JP authorized the amendment
(constitution 4.1.0, write registry W3 — `contracts/trello-write.md`), and the dropdown is
built per Miles's #01 spec: badge-chip trigger + select panel (white, `--slate-200` border,
head + Easy/Medium/Hard with value dots), phase-13 `d-*` color recipes retained — Hard =
red-50/red-500, exactly the spec's `#fef2f2`/`#ef4444`; **Easy/Medium tokens supplied and applied** (Miles, owl #04:
Easy green-50 `#f0fdf4` / green-500 `#22c55e`, Medium amber-50 `#fffbeb` / amber-500
`#f59e0b` — 50 fill, 500 stroke+text, same shape as Hard). Write mechanics **confirmed label
swap** (owl #04, not a custom field); we keep add-first ordering over product's remove-first
suggestion — same last-known-good display guarantee, safer worst case (see
`contracts/trello-write.md` W3). Rows with no difficulty render a dashed unset chip and are settable (the
missing-difficulty fix path). On projects with `writes_enabled: false` (rt-837 observation
mode) the dropdown is disabled with the read-only tooltip.

## Batch 5 — the incomplete-card panel becomes a row state (owl #36, 2026-08-17)

Nodes: `537:69131` (warned row) and `537:69135` (popover); dark variant
`528:112097` noted but not built. The `70:1358` Alert Banner above (§4.4 panel)
is **deleted** — the three conditions it covered now speak on the row that owns
them, and the aggregate signal is the OPEN WORK metric (`28:3666`), which still
counts `corrections` off the same payload.

**Migration completeness — the answer owed to Miles (#36 item 1).** The Pipeline
tab had exactly ONE top-of-table notice: the incomplete-card panel, covering
missing **difficulty label** · missing **due date** · missing **Figma
attachment**, all three derived read-only from the ARES read. Everything else
already lives on the row (the 🛑 blocker badge) or is an app-level flash strip
(sync error / load failure / write rollback), which is not a table banner and
stays. So the row state expresses three conditions, one variable label covers
all three today, and the popover carries one list-item per missing field.

**Search interaction, stated deliberately:** a warned row the search hides stays
hidden. No special case — which is exactly why the OPEN WORK KPI keeps carrying
the total.

| ID | Ruling | Shipped |
|---|---|---|
| **R-warn-a** | The frame draws the amber-300 as a "stroke" around the row and grows the row to **113px** in a **280px** card-name column. Built as a **3px LEFT accent** (`box-shadow: inset 3px 0 0 0 var(--amber-300)` on the first cell) over an amber-50 fill, with the table's own geometry left alone. `.ptable .col-name` is fluid (`width: auto`, `min-width: 158px`) and row height is content-derived from the 16px cell padding — hardcoding either frame number would break the table. **Divergence recorded, asked of Miles.** | Yes — accent declared exactly once, asserted; a render test proves neither frame number was pinned |
| **R-warn-b** | The Figma-attachment rationale is **new copy**: *"Without the Figma attachment the deliverable cannot be opened from the plan."* The other two are the deleted banner's own wording, carried over verbatim. All three live in ONE constant keyed by the server's `missing` tokens. **Proposed, asked of Miles.** | Yes — the probe reads the tokens off a real incomplete card and asserts every one has a non-empty rationale, so a server token the map does not know can never render blank unnoticed |
| **R-warn-c** | The label is a **variable string** (`WARN_LABEL`, "Needs Info" today) in all three places it appears — the row message, its accessible name, and the popover title. Never a literal at a render site. | Yes — a test asserts the template contains no literal `Needs Info` |
| **R-warn-d** | The **message** is the click target, not the row: a real `<button>` with `aria-haspopup="dialog"`, `aria-expanded` and an accessible name of label + MC# + deliverable. The `.warn` class is presentation only and **no control is disabled** by the warning state. | Yes — a render test compares the `disabled` count of a warned row against a clean one |
| **R-warn-e** | The popover **joins the existing fixed-box recipe** (`.selectmenu, .duepop, .warnpop`) rather than forking it; the annotation's `#f1f5f9` stroke is the single forked value. There is **no dark popover recipe in this codebase** to share a base with — the brief's "already in use on Sprint Schedules" is a Figma statement, not a code one. | Yes — asserted as one selector group |
| **R-warn-f** | **Focus return** did not exist on any of the four pre-existing overlays. Added **once**, in the shared close path: a module-scope `overlayTrigger` captured in `openOverlay` and replayed by `closeMenus()`. It fires on Escape (`{ restoreFocus: true }`) and on **any** dismissal that unmounts the element currently holding focus — a scroll or trackpad nudge while the user is tabbed onto `Open Card` would otherwise drop them at `<body>`. Outside-click still does not restore (it would steal focus from whatever was clicked) and the toggle-off re-click does not need to (focus is already there). | Yes — one mechanism now serving all five overlays, not a per-caller fix |
| **R-warn-h** | The popover's height is **data-derived** — one wrapping list-item per missing field, ~202px for one problem and ~346px for all three — so `WARN_POP_H` can only ever be a pre-measure for the first flip decision, exactly as `REQ_MENU_H` is the Requests select's cap. `openWarnPop` therefore **measures the rendered box and places it a second time**, through the same `placeBox`; the Requests select's `placeReqMenu` was generalised to `placeMeasured(trigger, id, {key, posKey, sel})` so both share one recipe. Without it a three-problem card opened low in the table ran ~70–80px off the bottom of a 900px viewport, putting the separator and `Open Card` out of reach — `.warnpop` deliberately has no `max-height`/`overflow-y` (it must stay out of the scroll dismisser's self-scroll exemption, which only `.duepop`/`.selectmenu` hold). **Amended by R-warn-y (batch 11, Miles's ruling in owl #43 item D):** the no-overflow clause gains one last-resort exception; the rule itself stands everywhere else. | Yes — the live-browser pass (T145) is the only place a real `offsetHeight` exists |
| **R-warn-g** | A **blocked + warned** row keeps the red blocker fill. `.ptable tr.prow.warn > td` (0,3,2) outranks the pre-existing `.ptable tr.blocked td` (0,2,2), so the amber would have silently demoted the stronger signal; one added rule restores it and the amber left accent still draws, so the row reads "blocked, and also incomplete". Not specified anywhere — **flagged to Miles**, and one line to revert if amber should win. | Yes |

**Unresolved measurement:** the frame's `content › subtone-offset` gap between
the card name and the message carries no px value. Built at **6px** (matching
the popover's first-list-item gap). Recorded rather than ruled. — **MOOT as of
batch 10 (owl #41):** the element it measured is deleted. The equivalent
unmeasured token for the icon is R-warn-n below.

## Batch 10 — the row warning becomes an icon + hover card (owl #41, 2026-08-18)

Nodes: `578:56516` (row) · `I578:56516;484:27906` (MC# cell) · `537:69135`
(tooltip). A **revision** of what batch 5 shipped, not a new feature. Three
changes and nothing else: the underlined `Needs Info` message line under the
card name becomes a **14×14 alert icon immediately after the MC# label**; the
amber-50 full-row wash is **deleted**; the click popover becomes a **hover
card** that also opens on keyboard focus. Content, copy map, recipe and the
five-overlay machinery are untouched — `openWarnPop` is renamed `showWarnPop`
and gains the guards below, so R-warn-h's second measured placement reads under
the new name.

Consequences worth stating once: `.warnwrap` / `.warnmsg` / `.warnlabel` are
gone from the template (they had one stylesheet rule between them and it went
with them), `.i10` had no other user and is deleted, and the row returning to
~95px is an **outcome** of deleting the message line — R-warn-a's rule stands
and the guards assert the absence of the line, never the number.

| ID | Ruling | Shipped |
|---|---|---|
| **R-warn-i** | **Which corner is squared when the card flips ABOVE the icon.** The annotation specifies the anchored-below case only (top-left `0`, other three 8px). Default taken: **mirror it** — bottom-left `0`, other three 8px — so the card still reads as growing out of the icon rather than floating over it. Carried by `.warnpop.flip`, driven by `warnPopPos.up`. **CONFIRMED by Miles (owl #46 item 1)** — "better than what I specced"; the annotation records the mirroring. | Yes — `.warnpop` and `.warnpop.flip` each assert four explicit values with the square on the gap side |
| **R-warn-j** | **Close delay = 150ms.** Not specified anywhere. Long enough to cross the 4px gap, short enough not to feel sticky. Named constant `WARN_CLOSE_MS`, so tuning it is one line. **RULED kept-as-built (Miles, owl #46 item 2)** — tune only if real use complains. | Yes — a test refuses a literal at the call site |
| **R-warn-k** | **Dark variant not built.** The annotation says the dark variant is "in use on Sprint Schedules (#0f172a)". **There is no warning card on Sprint Schedules in this build** — the same class of Figma-side statement as R-warn-e's. Nothing invented, no unused dark recipe added. **CLOSED by Miles's own correction (owl #46 item 3): his note was wrong — he saw a dark *tooltip* on that screen and misattributed it. There is no dark variant of this card anywhere and none is planned; LIGHT ONLY, annotation corrected on his side.** | n/a — nothing built, deliberately — and now ruled, not just recorded |
| **R-warn-l** | **Touch.** No separate touch mode built. The icon is a real focusable `<button>`, so a tap focuses it and the focus path opens the card; an outside tap dismisses through the existing document click dismisser. What does **not** fall out: a second tap on the icon does not close it (the icon is in the click-ignore list and no new focus event fires), and after Escape the card stays closed until focus leaves the icon and returns — which is the WAI-ARIA tooltip pattern, not a defect. Carried alongside T152's open touch question for the requestor tooltip rather than answered here. | Partly — the tap-to-open path falls out; tap-to-close does not exist |
| **R-warn-m** | **The accessible name composes label + pluralised count + card identity.** The annotation's example (`'Needs info — 2 missing fields'`) omits identity; with 247 of 249 live rows warned, two icons on screen would be indistinguishable, and the trigger already carried MC# + name before this batch. Built as `` `${WARN_LABEL} — ${n} missing field(s) — ${mcLabel} ${name}` ``, composed in `rowWarning` as `warning.srLabel` — **the template does no arithmetic and never spells the label** (R-warn-c holds). **Flagged to Miles.** | Yes — the button's `aria-label` is asserted equal to the recipe's own output, character for character, and the pluralisation is tested at 1 and 3 |
| **R-warn-n** | **`content › icon-offset (utility-icons-small)` carries no px value** — the same unmeasured-token situation as batch 5's `subtone-offset`. Built as `--space-4` on `.mcid` plus the button's own `--space-4` pad, reading as the 8px the frame shows. Recorded rather than ruled. | Yes — the gap is a token, asserted as `var(--space-N)` rather than a number |
| **R-warn-o** | **The amber wall: ruled, not proposed.** Miles's answer to my #21 removes the amber-50 row wash entirely; the state is now the 3px left accent plus the icon, and nothing else. Consequence worth naming: `.ptable tr.prow.warn.blocked > td` was deleted **too**, because with the wash gone **no rule paints a warned row's background at all** — so R-warn-g ("a blocked+warned row keeps its red fill") now holds **by construction** rather than by an override, and the annotation confirms it ("PRECEDENCE UNCHANGED"). | Yes — asserted by SELECTOR SHAPE (`/\.prow\.warn[^{]*\{[^}]*background/` matches nothing), so a re-added wash under any spelling fails |
| **R-warn-p** | **The hover bridge, and its two known limits.** A transparent `::before` on `.warnpop`, exactly the gap tall, flipping `top`→`bottom` with `.flip`, and existing only while the card is rendered. Limits, both viewport/geometry and both for the live pass: (1) `placeBox` decides `up` **before** the on-screen clamp, so a clamped-but-unflipped card can sit closer than the gap — and, on a viewport too short for a flipped card, the clamp can slide it back *over* its own icon, the one case where the strip is not spanning a gap at all; (2) the bridge is absolutely positioned inside a **bordered** fixed box, so its containing block is that box's *padding* box, giving it 235px **less the two 1px borders** and a start 1px inside the gap rather than at its far edge. **Correction to the T166 integration report** (recorded 2026-08-18, verified in the vendored spec and measured in a browser by the interaction verifier): that report claimed the bridge "spans ~201px inset ~17px each side", confusing the padding box with the *content* box. The padding box **includes** the ancestor's padding, so the horizontal span is ~233px and the icon's 22px sits well inside it — **the direct pointer path is bridged, and the limit that was recorded for it does not exist**. Only the 1px vertical band survives. Compensating for what remains would cost a raw px in component CSS, which is a defect here. In practice the **150ms delay** is what carries the pointer across, and the bridge is the belt to its braces. | Yes as built; the geometry is an `it.todo` the live pass owes |
| **R-warn-q** | **Figma-side filler, no action.** *"REMAINING FILLER MISMATCH: the first list-item's #label still reads 'MC-821'"* — our build has always rendered `row.mcLabel`, and a test has asserted `items[0].label !== 'MC-821'` since batch 5. Theirs to clear. **Cleared on their side (owl #46): the filler now reads MC-837.** | n/a |
| **R-exp-l** | **The shared state LEADS WITH THE TASKS; the count trails as secondary detail** (Miles, owl #55, answering R-exp-h). Because it is 78% of task views it is not an error path handled gracefully — it is the normal way of seeing work on a reused MC number, and must read as *"here is the work on MC-837"*, not *"we couldn't do what you wanted"*. The task names are the content and are already the row's prominent `.cardname`; the explanation is a caption. **The count renders at EVERY N with no threshold rule** — it is diagnostic (a high N marks an MC number needing renumbering at source), it never tells a PM what to do, and "one treatment that degrades quietly beats two that need a boundary defended". Wording delegated to us; the chevron's accessible label carries the same framing so the two cannot describe the state differently. | Yes — order asserted (explanation before count), the quieter span asserted, and the count asserted at N=2 and N=3 |

| **R-row-a** | **Reveal rules, ruled as a SET** (Miles, owl #59; no new frame — the pass needed decisions, not drawings). **Always visible**: the checkbox (it is how a row is selected) and the note chip (it carries CONTENT, not an action — hiding it would mean hovering every row to find which ones have notes). **Hover-revealed**: the drag handle and the three-icon cluster. **Exception**: a PINNED row keeps its cluster visible permanently, because a pinned row behaves differently (Suggest will not move it) and that state must be legible at rest rather than discovered by trying to move it. | Yes — asserted as a set, since each rule is only correct relative to the others |
| **R-row-b** | **The drag-handle column stays RESERVED on rows that have no handle.** If it collapses, scheduled and unscheduled rows start their content at different x, which reads as broken rather than tidy in a table; a small consistent gap is the cheaper cost. Holds by construction — the 58px pad is on `.c-mc` and the handle is absolutely positioned inside it — not by a second rule that could be forgotten. | Yes |
| **R-row-c** | **The note chip's position STANDS as built** — Miles confirmed the placement rather than redesigning it, to be revisited only after real users have been watched using it. | n/a |
| **R-row-d** | **Keyboard access STAYS AS BUILT — the ruling was revised** (Miles, owl #61, superseding #59). The three row-action icons remain real `<button>`s in the tab order with focus rings and accessible labels. #59 had ruled them mouse-only; escalating rather than complying surfaced two facts product did not have when it ruled: the access already existed (so it was REMOVAL, not a declined addition), and labels-retained-plus-focus-removed is the worst of the three positions — actions announced on every row that nobody can activate. The underlying goal was already met anyway: **tab and arrows are different keys**, and reserving arrows for the bars is delivered by arrows stopping dead at the controls, which never depended on tab access. The frame annotation still carries the superseded ruling — owl #61 is authoritative until it catches up (their Figma plugin is down). | Yes — the existing tab-order and focus assertions stand unchanged |

| **R-exp-m** | **Work that belongs to no row must be VISIBLE** (Miles, owl #61). A work card whose MC number has no deliverable row hangs under nothing in the Pipeline AND weighs into nothing — BR-6c only weighs a task whose MC has a row — so it is real work that is invisible *and* silently absent from capacity, making planned load read lighter than reality with nothing on screen saying so. Live board 2026-08-20: **11 MC numbers, 35 cards**. **Not a data fix** — the cards need correcting at source; this stops the gap being silent. **Two surfaces, one server field** (`unattachedWork`, derived from the two maps already built — an MC with tasks and no row is exactly a `workCardsByMc` key `rowsByMc` does not know): an `UNATTACHED` tile on the Pipeline metrics strip, and a line beside the planner's capacity control, which is Miles's stated property — *someone looking at capacity can tell work is missing from it*. **Both hide at zero** (a permanent zero teaches people to stop reading it) and **neither sits in a week cell**, because these cards are in no week and a per-week home would be a lie about where they are. Amber, not red: a data-hygiene gap to notice, not a failure to alarm about. **Placement and wording are a proposal** — Miles rules on both (jp→miles #48). | Yes — count/naming/sorting, empty at zero, project scoping, the capacity gap it reports (orphans weigh nothing while the field says so), hides-at-zero, tooltip carries the CONSEQUENCE not just the number, and both plurals |
| **R-exp-n** | **`WORK CARDS` on the metrics strip has always counted these.** The server keys work cards by MC and an orphan gets a key like any other, so the strip has reported a total including cards no row could ever show. The new tile names a SUBSET rather than adding to a total — a quiet inaccuracy becomes a stated one. | Covered by the tile's tests |

| **R-warn-r** | **A passive hover must not destroy an active edit — in BOTH directions.** `showWarnPop` refuses to open while any non-`warnPop` overlay is open, because `openOverlay` nulls all of them and the due popover holds a staged date only Apply writes (W2). Before this batch that discard needed a deliberate click; hover made it an accidental mouse path. **Integration fix (T166):** the refusal to *open* was matched by an unconditional close on `mouseleave` — a pointer that merely grazed a warning icon armed a 150ms timer that then called `closeMenus()` and discarded that same staged date. `warnPopOut` now arms nothing unless a hover card is actually open. The guard is derived from `OVERLAY_KEYS`, so a sixth overlay is one entry and never a second roll-call. | Yes — both directions asserted, and the open-side guard is asserted to *derive* from the list rather than restate it |
| **R-warn-s** | **The icon opens on FOCUS, and the shared close path RETURNS focus to it.** Left alone those two facts fight: `closeMenus({ restoreFocus: true })` focuses the trigger, the trigger's `focus` opens the card, and Escape would close and instantly re-open — Escape would look broken. Held by one module-scope `restoringFocus` flag, set by the one close path across its synchronous `focus()` call and read by the one opener. The same flag covers every dismissal that unmounts the element holding focus (the scroll dismisser's `heldFocus` restore). Second half of the same rule: a card the **keyboard** opened is not the pointer's to close — the timer callback stands down if focus is still inside the open card's own `.warnhost` (scoped by R-warn-v), or a mouse nudge would strand a focused trigger with nothing open. **Not in the annotation; it is what the annotation's "opens on hover AND on focus" plus R-warn-f imply together.** | Yes — flag counted once, ordering asserted around the focus call; the behaviour itself is the live pass's |
| **R-warn-t** | **Ractive's event delegation simulates bubbling, and `mouseenter`/`mouseleave` do not bubble.** Inside an `{{#each}}`, Ractive registers ONE **capture-phase** listener on the block's parent (`<tbody>`) and then walks from `ev.target` upward looking for directives (`delegateHandler`, `ractive.mjs`). Capture is what makes these three non-bubbling events arrive at all — without it the directives would have been silently dead — but the upward walk also re-dispatches a **child's** `mouseleave` to an ancestor's handler, which native `mouseleave` never does. Uncorrected, moving off the 14px glyph onto the button's own 4px pad, or between two lines across the card's 16px padding, armed a close while the pointer had left nothing. **Integration fix (T166):** `warnPopOut` stands down when `event.relatedTarget` is still inside `ctx.node` — the same shape as `warnPopFocusOut`'s guard, for the same reason. Opening stays unguarded because it is idempotent. | Yes — guard and its ordering asserted; that the pointer path actually holds is the live pass's |
| **R-warn-u** | **The shared focus return must RETURN focus, never STEAL it** (verify fix, T166). Every overlay before this batch opened on a **click of its own `<button>`**, so `overlayTrigger` was also what the browser had just focused: `closeMenus({ restoreFocus: true })` either re-focused the already-focused trigger or stepped back out of the overlay. The hover card is the first an overlay a **pointer** opens, with focus left wherever the user actually is — so Escape typed in the Pipeline search field (with 247 of 249 rows warned, a pointer resting anywhere in the MC# column is enough) dragged the caret onto a warning icon and swallowed every keystroke after it. `restoringFocus` does not cover this: it suppresses the *re-open*, not the focus move. **Fix:** the restore now also requires focus to be *ours* — already on the trigger, inside the overlay being closed (`heldFocus`), or nowhere (`document.body`, i.e. an unmount or a browser that does not focus a clicked button). Every pre-batch path satisfies one of the three, so R-warn-f is unchanged for the other four overlays. | Yes — the three-way condition is asserted, and that it gates the same `t.focus()` the ordering guards already pin |
| **R-warn-v** | **"Is a card open" is not "is MY card open."** `.warnhost` renders on every warned row, so two guards written as global state reads stood down for a card they had nothing to do with. (a) `warnPopOut`'s timer callback returned whenever `document.activeElement.closest('.warnhost')` matched **any** host — Tab to row A's icon, then hover row B's and move away, and card B was stranded open with no pointer on it and no focus in it, dismissible only by Escape, an outside click or a scroll. Now scoped to the host that contains `overlayTrigger`, which is the open card's own icon. (b) `warnPopFocusOut` early-returned on `!app.get('warnPop')` — a global read — so a Tab off row A's icon dismissed a card the **pointer** had opened on row B and discarded a trigger this host never captured. Now `ctx.node.querySelector('.warnpop')`: the card is rendered inside its own host exactly when it is ours. Both comments already claimed the scoped rule; only the code did not. | Yes — each scoping asserted, and the global spelling banned so it cannot regress |
| **R-warn-w** | **A trigger with no click handler earns no unconditional click shield.** The document dismisser's ignore list exists so a click on a trigger is not read as an outside click — the trigger's own handler owns the toggle. `.warnbtn` has **no** `on-click`; hover and focus open it. Listed unconditionally, it became a hole: with a difficulty menu or a due popover open, a click on a warning icon neither opened the card (R-warn-r refuses over an active edit) nor dismissed what was actually open — a gesture with no effect at all, where before this batch the same click on `.warnmsg` dismissed the other menu through `openOverlay`. **Fix:** `.warnbtn` is shielded only while `warnPop` is set, i.e. while its own card is what a click would otherwise dismiss (which is also what keeps R-warn-l's tap-to-open from closing itself on the synthesised `click`). A deliberate click is not the passive mouse path R-warn-r guards; only the *hover* refusal protects the staged date. | Yes — the conditional shield asserted, with the unconditional spelling banned |

## Batch 11 — Miles's rulings land (owls #43 and #46, 2026-08-18)

Not a redesign: three ruled changes to the batch-10 card and icon, plus the
closures annotated in the tables above (R-warn-i confirmed, R-warn-j kept,
R-warn-k closed by Miles's own correction, R-warn-q cleared their side). Two
further rulings from owl #46 carry no code: the **occlusion** of the icons
beneath an open card is **intentional** (Miles looked and ruled it acceptable —
the card is transient and the pointer restores the view; recorded in the
annotation so nobody reopens it as a bug), and the batch-10 verify pass's
occlusion `it.todo` left the live checklist with it.

| ID | Ruling | Shipped |
|---|---|---|
| **R-warn-x** | **The closing line returns (Miles, owl #43 item B).** The deleted batch-5 banner's own sentence — *"Fix in Trello and it corrects on the next sync."* — restored **verbatim** as a closing line after the field list and above `Open Card`. Reading order becomes: what's wrong → why it matters → where to fix it → the link that takes you there; it is also what tells a PM that Sirius will not fix the card itself. It is MESSAGE copy, not the link's caption, so a card with no Trello URL still renders it; the separator keeps dividing details from link. `.wpfix` wears `.wpwhy`'s own recipe. `WARN_POP_H` pre-measure grew with it (346 → 390); the measured second placement still owns the truth. | Yes — verbatim copy, position (after last item, before separator), no-URL presence, and single occurrence all asserted |
| **R-warn-y** | **Last-resort internal scroll (Miles, owl #43 item D, amending R-warn-h).** No-internal-scrolling stays the rule; scrolling is allowed **only when the viewport genuinely cannot fit the card** (~350px and below for a three-problem card). Built as `warnPopPos.over` — decided in `placeBox` from the measured height against the viewport (`>=`, because the scroll state caps the box at exactly viewport-minus-margins and a strict compare would oscillate), spelled by the template as `.warnpop.scroll`, which caps at `calc(100vh - var(--space-8))` with `overflow-y: auto` and `overscroll-behavior: contain`. `.warnpop` therefore joined the scroll dismisser's self-scroll exemption — safe unconditionally, because on every normal viewport the card has no overflow and a scroll's target is never inside it. The wheel swallow stays duePopover-only. Known and accepted: in the one capped state the hover bridge is clipped by the overflow — the card fills the viewport, so there is no gap left to span. | Yes — the base recipe is asserted uncapped, the cap asserted on `.warnpop.scroll` alone, the decider and its `>=` asserted, the exemption asserted; the behaviour itself is the live pass's |
| **R-warn-z** | **Hit target grows to the documented threshold (Miles, owl #46).** The batch-10 box sat 2px under the target-size guideline; ruled up to the threshold since the fix is free and the row spacing already accommodates it. Built as `min-width`/`min-height` of `--space-24` on `.warnbtn` — the number lives in the token sheet and nowhere in component CSS; minimums, not fixed sizes. The 14px glyph and the `--space-4` pad are unchanged (R-warn-n's token gap holds). | Yes — both minimums asserted as the token, and the tokens-only guard covers the rule |

Related, same owls, different surface: the **Requestor tooltip ruling** (owl
#42) accepts the pure-CSS clip recipe **gaps and all** (Escape, touch, last-row
flip) — do not build the interactive version; and owl #43 item C extends that
same recipe to the planner's **Asset Type** cell (law and guards:
`test/gantt-requestor-clip.test.ts`, the recipe comment in
`frontend/styles/20-pipeline.css`).

## Batch 12 — the expanded MC row (owl #45, node 520:54192, 2026-08-18)

Expansion already shipped; this batch makes it the spec'd UI. The structural
call, promised to Miles in jp→miles #40: parent and child are `<tr>`s of the
SAME `<table>`, so both levels share one column model BY CONSTRUCTION — the
frame's 1610-vs-1570 container question cannot arise here, and the child's
empty first cell is simply an empty `<td>` in the parent's first column. Guards:
`test/pipeline-expanded.test.ts`.

| ID | Ruling / default | Shipped |
|---|---|---|
| **R-exp-a** | **The tint is the nesting cue, inverted from the earlier version**: parent stays the table's white ground, task rows are `--slate-50`, and NO rule paints a parent row's background (asserted by property walk, the amber-wall pattern). The frame's bordered inner container translates to the child cells' stronger `--slate-200` bottom border, with the indent cell's border transparent so the gutter under the MC# reads as one strip. | Yes |
| **R-exp-b** | **Children show no type / difficulty / urgency / requestor** — MC-level attributes; repeating them per task would imply a task can diverge from its MC (Miles's own note in #45). Status, Due, Started, Done and Links render with the parent table's own recipes (`.cardname`, `.pbadge s-*`, `.plaincell`, `.srclink`/`.off`). | Yes — the four cells asserted EMPTY |
| **R-exp-c** | **Childless MC: no expand affordance** (proposed in jp→miles #40, built as proposed pending Miles's confirm — one conditional to flip if he rules otherwise). Expanding a childless MC used to show nothing at all; now a glyph-box-wide spacer holds the column alignment and no chevron renders. | Yes — both halves asserted, spacer width pinned to the glyph box |
| **R-exp-d** | **SubTone `Main Card` renders ONLY in the expanded state.** The frame specs the expanded composite and the SubTone's job is the parent-vs-children distinction, which does not exist collapsed — and the collapsed table is not this spec's to change. **Default taken, flagged to Miles** (the retrieved render also did not show the element — the annotation did; the MCP output truncated before a possible third annotation, re-verify on his answer). | Yes — expanded-only asserted both ways |
| **R-exp-e** | **Task due = W2's task-card half** (JP 2026-08-18; `contracts/trello-write.md` §W2 scope). ONE route handler serves both kinds through the same `writeGuards` door — same Zod body, no-op guard, Trello-first rollback, board guard, `writes_enabled`, audit (+`entity: 'work_card'`) and sync-run rows; the client's `writeDeadline` stays the one door and routes a cardId `rows` does not know to the task half. A task due has no sheet fallback and no precedence part — its display field IS its Trello field, and Clear means cleared. The popover markup is duplicated in the task cell (two data shapes), but the CSS recipe, the calendar state keys and the handler set are ONE. | Yes — route halves cross-404 each other's kind; rollback, audit entity, time-of-day preservation all asserted server-side |
| **R-exp-f** | **Expansion is per-project view state** and now resets on project switch — the recon found it keyed on `mc_number` alone, which repeats across projects (invariant 3), so project A's expanded MC-655 arrived pre-expanded in project B. Multi-expand stays (per-MC map); refresh persistence stays out (per-tab URL sub-state is the parked home if wanted). | Yes — reset asserted in the shared block; multi-expand rendered |

**Review pass (2026-08-18, same evening — /simplify + /code-review over the
batch):** the amendments that bind. **R-exp-a**: the task list AND the SubTone
hang under the MC group's **first row only** (`firstOfMc`, stamped) —
`mc_number` repeats across sibling deliverable rows (invariant 3), and the
per-sibling render duplicated the whole list, popover included, once per row.
**R-exp-b**: task dues wear the parent's **full** dress — `overdue` rides the
wire and tints exactly as the parent recipe does (the omission was the review's
find, not a ruling). **R-exp-c**: the keyboard path refuses where the chevron
refuses — `pipeRowKey`'s Enter checks `hasTasks`. **R-exp-e**: the due
dispatch is EXPLICIT (the template passes `'task'`; kind is never re-derived
from set-membership), the task write's post-success reload is **correctness,
not precedence** (a concurrent pre-commit reload would strand the old value
with no client poll to heal it — do not remove it again), and the failure
revert re-finds by cardId at write-back time. `dateOnly` is the **Manila**
day on both W2 halves (invariant 11); the calendar lives once as the
`dueCalendar` partial, registered on the test renderer so its guards render
what ships.

## Batch 14 — a shared MC stops attributing (owl #52) + the card's shadow (owl #53, 2026-08-20)

| Rule | Ruling | Tested |
|---|---|---|
| **R-exp-g** | **Attribute a task list to a deliverable ONLY when its MC number has exactly one.** Above one, the tasks surface once at MC level under the group's first row, captioned `Shared across N deliverables — these tasks carry the MC number, not a link to one card`. This is invariant 4 applied, not amended: the probe below proves there is no edge to model, so being explicitly uncertain is the only honest option — a misattributed task is indistinguishable from a correct one, whereas a shared one announces itself. | Yes — caption renders once, names N, absent on a single-deliverable MC |
| **R-exp-h** | **THE AMBIGUOUS CASE IS THE NORM, NOT THE FALLBACK.** Live board, 2026-08-20: 19 of 37 MC numbers carry more than one main card, one carries 60, and **279 of 356 task cards (78.4%)** sit under an ambiguous MC. Miles's #52 called the attributable case "the majority" — true per MC number (18 of 37), inverted per task. Any future treatment must be designed for the shared state as the primary one. **Presentation is a flagged default** — Miles specced the rule, not the look (asked in jp→miles #43). | Board figures asserted in the probe, mirrored by the live DB (37 / 19 / 60) |
| **R-exp-i** | **NO STRUCTURAL TASK→DELIVERABLE EDGE EXISTS**, and this is now measured rather than assumed. Probed on `hLL7WW2V`, all 592 live cards: checklists **0 of 218** mains · description card-links **0** both ways · list position resolves **0 of 279** · members **36 of 279** (only 50 even have one) · best name segment **60 unique against 117 that match several mains and 102 that match none**. ARES *does* return `checklists`/`members`/`description` — our adapter drops them — so the fields were reachable and are simply empty. Name-matching is REJECTED as a fallback: it would resolve 1 task in 5 and silently mis-resolve 117. | The figures are the ruling; re-run the probe before revisiting |
| **R-exp-j** | **The chevron belongs to the MC**, so it renders on the group's first row only. It always controlled MC-level state (`expanded` is keyed on `mc_number`) and the list always rendered under the first row — so on a shared MC a sibling's chevron opened a list elsewhere on screen. Its accessible label names the shared case. Supersedes nothing in R-exp-c: a childless MC still gets the spacer. | Yes — one chevron per shared MC, spacer on the sibling, label asserted |
| **R-exp-k** | **Row weight needed NO change.** BR-6c already divides an MC's tasks across the group's deliverables, so sibling rows always weighed evenly — the bug was display-only. Owl #52's "one row looks enormous while its siblings look empty" does not describe what shipped, and the forecast never read oddly at row level. | Yes — weight asserted against the same count the wire now carries |
| **R-warn-p** | **The hover card's shadow is `--shadow-card: 0 4px 12px rgba(0,0,0,.15)`** (owl #53; Figma `Shadow/sm` on node 537:69135 — dy 4, radius 12, `#00000026`). Deliberately heavier than `--shadow-xs`, and **not to be normalised toward it**: the card overlaps the alert icons beneath it, which Miles accepts, and the shadow is what makes that layering read as depth rather than a rendering fault. Figma's codegen reports the SVG `stdDeviation` (6) where CSS wants the blur radius (12) — the annotation and the style token both say 12. | Yes — the fork asserted, the base's `--shadow-xs` asserted, the token's exact value pinned |
| **R-warn-q** | **A shadow's bleed is part of the clamp.** `placeBox` takes an optional per-overlay `bleed`; the warning card passes `WARN_SHADOW_BLEED = { x: 12, top: 8, bottom: 16 }` — a 12px blur reaches 12px every way, and the 4px drop takes 4 off the top and adds 4 to the bottom. Miles's "~12px each side and ~8px above" agrees but omits the bottom, which is the largest. Both placements carry it (the measured re-place would otherwise undo the first). Absent `bleed` every other overlay's arithmetic is byte-for-byte unchanged. `--warn-vclamp` is the CSS twin and a test DERIVES it from the JS constants. | Yes — twin derived, not retyped; horizontal spend asserted in `placeBox` |

| **R-warn-r** | **`Open Card` is NOT underlined** (JP, 2026-08-20, comparing the built card with the frame side by side). Node 537:69135 contradicts itself — the annotation TEXT says "UNDERLINED", the frame's render does not — and batch 10 had resolved it the other way, recording the choice in a CSS comment. **The frame wins: it is what JP and Miles both look at.** Asserted NEGATIVELY so putting the declaration back is deliberate, not drift. **SETTLED by owl #57**: Miles withdrew the instruction outright — `get_styled_text_segments` returns `textDecoration: "NONE"`, the instruction belonged to the RETIRED message-line design and lapsed at batch 10 without being retired, and the stale source frame is now deleted. **Nothing in this card is underlined, at rest or on hover/focus** — the hover case I deliberately did not invent is answered the same way. Second defect this week from deferring to prose over a measurement (cf. the 4px accent). | Yes — `text-decoration: none` asserted, `underline` asserted absent |
| **R-warn-s** | **Missing-field labels are sentence-cased at DISPLAY, and the server token is untouched.** The card read `due date` where the frame reads `Due date`. The tokens are lowercase on purpose — `srLabel` reads them mid-sentence, where a capital is wrong — while the frame shows them as list headings, where lowercase is. So a warning item now carries `field` (raw token; what `WARN_WHY` is keyed on, and it never reaches the DOM) beside `label` (display). `Figma attachment` is unharmed: upper-casing a capital is a no-op. | Yes — casing derived from the tokens so a new server token cannot slip through un-cased; the markup comparison narrowed to the display pair |

## Batch 14pf — Pipeline filter + sort (owl #62, nodes 592:56850 · 592:56913 · 592:56966 · 593:78434 · 593:74881, 2026-08-20)

| Rule | Ruling | Tested |
|---|---|---|
| **R-pf-a** | **The default order is the card's OWN filing date, and the field had to be built first.** `created_at` is the SIRIUS row's timestamp: on the live board it stamps 289 rows with the day it was onboarded, so ordering by it is a meaningless tie dressed as an order. ARES carries `createdAt` on every card (100/100 sampled, 13 distinct days) → schema `trello_created_at`, carried by the mapper, written by the sync on the UNCONDITIONAL half (a card's creation time cannot change, so no stale-read guard applies), indexed by **migration 008**. **No backfill script**: the value comes from ARES, so the first sync after deploy filled every active row — 217/217 verified live. Product asked us to confirm the source rather than infer it; that instinct is what caught this. | Yes — the default reads `filedAt` and provably not `created_at` |
| **R-pf-b** | **EMPTY SORTS LAST IN BOTH DIRECTIONS.** Most cards on the real board lack a due date, so a nulls-first *descending* order fills the top of the table with blanks and looks broken. The empty test happens before direction is applied, so it holds either way. `Non-Urgent` is a VALUE, not an absence — nothing falls to the bottom on the urgency sort. | Yes — asserted ascending AND descending, plus started/completed/hardest/name |
| **R-pf-c** | **Facet counts ignore their OWN axis** (jp→miles #49 — neither option the frame offered). Counting against every applied filter *including its own* drops each sibling value to zero the moment one is picked, so a second value can never be added without clearing first: accurate and unusable. Ignoring its own axis keeps counts honest as you narrow AND usable for widening. **Zero-count values are disabled**, not silently selectable. | Yes — the reachability property is asserted directly |
| **R-pf-d** | **OR within a category, AND across** — confirmed as the frame assumed, and it is the same decision as R-pf-c seen twice, since it is what makes the counts coherent. **No state axes**: blocked and missing-info were considered and declined; row state stays on the rows. Categories are DERIVED from board values, never a fixed list. | Yes |
| **R-pf-e** | ⚠️ **STATUS is alphabetical, NOT Trello's list order — a correction to my own #49.** I told product the group would keep the board's progression; the wire carries the list NAME but no list POSITION, so there is no progression to preserve. Alphabetical happens to match the frame's four sample values exactly. Restoring the true order is a change to what ARES gives us and needs its own decision. The group **scrolls inside itself** (cap ~6 rows) so the four short axes keep their place — scrolling the whole panel would let the one open-ended axis push the others out of view. | Yes — the in-group scroll asserted, and the panel asserted NOT to scroll |
| **R-pf-f** | **The two active states are deliberately different and must not be aligned.** SORT expands and names its one selection as `Group: Item` — the prefix does real work, since "Urgent first" alone is ambiguous about which axis is ordering. Capped at **240px**: the longest of the eight measures ~235px at 12px, so nothing truncates today and the cap exists so a FUTURE label cannot push the search field around. FILTER stays fixed-width, fill only, **no label and NO COUNT** — declined explicitly, not to be re-added "for consistency" with the counts inside. The applied count lives ONLY in its accessible name (`Filter, 3 applied`), which is the single route to it for someone who cannot see the fill change. | Yes — no count in the button's markup, the count asserted present in the accessible name |
| **R-pf-g** | **Both panels join `OVERLAY_KEYS`.** "Opening one closes the other", the outside click, the scroll dismisser and Escape-with-focus-return are what `openOverlay` already does to every key in that list — re-implementing any of them here would be a second rule that could disagree with the first. Group headings are `aria-hidden` labels with the group named on the wrapper, so they are never focusable. **Clear is disabled when there is nothing to clear**, in both panels. | Yes |
| **R-pf-h** | **"Reset pagination to page one" — the Pipeline HAS NO PAGINATION.** It renders every row inside a scroller; only Requests pages. The requirement's intent maps to returning the reader to the TOP when the set changes; inventing a page counter nobody reads would have satisfied the words and nothing else. **Filter and sort RESET on project switch** (default, flagged): a Requestor or Status carried into another project names values that project may not have, and would silently show an empty table — the same reasoning as R-exp-f. They persist across tab switches within a project. | Yes — the project-switch reset asserted out of the shipped source |
| **R-pf-i** | ⚠️ **OPEN — the "None" gap.** Cards with no type or no difficulty exist (a missing difficulty label is one of the three Needs Info conditions), so five categories of *values* cannot reach every row, and the unreachable ones are the incomplete rows most needing attention. Raised in #49/#50; **not invented**. Consequently the frame's "the five categories must reconcile to the row count — worth a test" is FALSE on a real board, and that test was deliberately NOT written: it would pin a falsehood. | n/a — deliberately untested |

## Build mechanics

- Tokens: extracted variable set (slate/red/amber/blue/green scales, text/caption 10 · label 12 · body 14 · title 24 · display 32, radius/xs 2 · sm 4, space/4 · 8 · 24, Shadow/xs) → CSS custom properties, names preserved (`--slate-50`, `--text-body`, `--radius-sm`…).
- Font: **Google Sans Flex via Google Fonts CDN** (JP call, verified available 400–700): `fonts.googleapis.com/css2?family=Google+Sans+Flex:wght@400..700`.
- Icons/assets: MCP export URLs expire in 7 days — download the needed SVGs (tab icons, chevrons, search, calendar, warning, Trello/Figma marks) into `frontend/assets/` at build time and inline/reference locally.
- Scope: THIS frame only (Pipeline tab + shell it carries: top nav, breadcrumb, tabs, metric strip). Other tabs keep the current design until their frames arrive; the shell restyle applies app-wide by construction.
- 1:1 target at 1600px per build spec §0; wire to live data (the frame's demo content — MC-05, Demo User, counts — is placeholder).
