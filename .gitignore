# OS
.DS_Store

# Dependencies
node_modules/

# Env & secrets — never committed (Invariant 15)
.env
.env.*
!.env.example
*.pem
*-key.json

# Build output
public/
dist/
coverage/

# Logs
*.log

# Regenerated by `npx tsx scripts/gate-t045.ts` and carries the real board id
docs/gate-t045-model-validation.*

# Local deploy wrapper — host/SSH details stay out of the repo
deploy.local.sh
# Defensive: the borrowed ARES deploy script (host/SSH details) lived here
# until 2026-08-18 and now lives outside the repo — if a copy ever lands
# back in docs/, it must not become committable.
docs/deploy.sh
docs/**/deploy.sh
